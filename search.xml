<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Sass 簡介</title>
    <url>/scss/20190924/ad533d91/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>Sass 是一種指令碼解析成 CSS 的手稿語言，即 SassScript。Sass 檔案本身本身不被瀏覽器所讀取，需要透過 Sass 直譯器將 SassScript 轉成 CSS 語法的 CSS 檔案。同時，SassScript 直譯器會同時對 Sass 檔案進行監視，即時更新 CSS 檔案。</p>
<span id="more"></span>

<h2 id="Sass-有什麼好處"><a href="#Sass-有什麼好處" class="headerlink" title="Sass 有什麼好處 ?"></a>Sass 有什麼好處 ?</h2><ul>
<li>將 CSS 模組化，拆分成多支檔案，方便管理與維護</li>
<li>重複的前綴詞可以透過巢狀的寫法而不需要重複撰寫</li>
<li>導入變數、函式等程式語言的概念，相同的 CSS 可以集中管理</li>
<li>常用到的 CSS 技巧可以利用模組化的方式儲存成一個 “工具包”，每次要用到時再透過 Sass 語法呼叫即可，不需要每次再從過去的資料找解法</li>
<li>許多 CSS framework 也是透過 Sass 完成，學會 Sass 可以更靈活運用在專案上</li>
</ul>
<h2 id="Sass-與-Scss"><a href="#Sass-與-Scss" class="headerlink" title="Sass 與 Scss"></a>Sass 與 Scss</h2><p>Sass 有兩種寫法，分別透過 <code>.sass</code> 和 <code>.scss</code> 兩種副檔名作為區分<br>其中 Scss 是 Sass 3 引入的新語法，其語法完全兼容 CSS 語法，也就是原來的 CSS 可以直接寫進 Scss 檔案，在寫法上也與 CSS 較為接近，容易快速上手</p>
<h3 id="一般-CSS-寫法"><a href="#一般-CSS-寫法" class="headerlink" title="一般 CSS 寫法"></a>一般 CSS 寫法</h3><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.menu</span> &#123;...&#125;</span><br><span class="line"><span class="selector-class">.menu</span> <span class="selector-tag">ul</span> &#123;...&#125;</span><br><span class="line"><span class="selector-class">.menu</span> <span class="selector-tag">li</span> &#123;...&#125;</span><br></pre></td></tr></table></figure>

<h3 id="SASS-寫法"><a href="#SASS-寫法" class="headerlink" title="SASS 寫法"></a>SASS 寫法</h3><p>完全省略 <code>&#123; &#125;</code> 與 <code>;</code> ，利用縮排(兩個空白或一個 Tab)來段行，格式非常簡潔，但縮排也容易看錯</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.menu </span><br><span class="line">  ...</span><br><span class="line">  ul ...</span><br><span class="line">  li ...</span><br></pre></td></tr></table></figure>

<h3 id="SCSS-寫法"><a href="#SCSS-寫法" class="headerlink" title="SCSS 寫法"></a>SCSS 寫法</h3><p>保留 SASS 運用巢狀的方式省略重複的前綴，也保留原 CSS 的 <code>&#123; &#125;</code> 與 <code>;</code>，從 CSS 轉換過來較為直觀，且兼容 CSS，因此也較受歡迎</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.menu</span> &#123;</span><br><span class="line">  ...</span><br><span class="line">  <span class="selector-tag">ul</span> &#123;...&#125;</span><br><span class="line">  <span class="selector-tag">li</span> &#123;...&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="巢狀結構"><a href="#巢狀結構" class="headerlink" title="巢狀結構"></a>巢狀結構</h2><p>從以上兩種寫法可以發現到 <code>.menu</code> 內層的 <code>ul</code> 和 <code>li</code> 都已經省略前面的 <code>.menu</code>，不僅可以避免重複撰寫相同的前綴，如果要更改 <code>.menu</code> 的名稱，就只需要改一個地方，也清楚的表示彼此的關係</p>
<h2 id="amp-連接符號"><a href="#amp-連接符號" class="headerlink" title="&amp; 連接符號"></a>&amp; 連接符號</h2><p>使用 &amp; 連接符號會自動連接上層元素，不需要重複撰寫</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in scss</span></span><br><span class="line"><span class="selector-tag">a</span> &#123;</span><br><span class="line">  ...</span><br><span class="line">  &amp;<span class="selector-pseudo">:hover</span> &#123;...&#125;</span><br><span class="line">  &amp;<span class="selector-pseudo">::before</span> &#123;...&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">a</span> &#123;...&#125;</span><br><span class="line"><span class="selector-tag">a</span><span class="selector-pseudo">:hover</span> &#123;...&#125;</span><br><span class="line"><span class="selector-tag">a</span><span class="selector-pseudo">::before</span> &#123;...&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Sass-編譯方式"><a href="#Sass-編譯方式" class="headerlink" title="Sass 編譯方式"></a>Sass 編譯方式</h2><p>Sass 檔案無法被瀏覽器解讀，需要編譯成 CSS 檔案，而編譯方式一共有三種，在此先介紹兩種</p>
<h3 id="軟體編譯-prepros"><a href="#軟體編譯-prepros" class="headerlink" title="軟體編譯 : prepros"></a>軟體編譯 : prepros</h3><p><a href="https://prepros.io/">prepros 官方網站</a></p>
<ol>
<li>從網站下載完後，開啟 prepros，將整個跟目錄資料夾拖曳到 prepros 內，也可以藉由左下角的 <code>+</code> 將檔案拉到 prepros 內</li>
</ol>
<p>  <img src="prepros-1.png"></p>
<ol start="2">
<li>拖曳到 prepros 後會自動產生一個名為 <code>prepros-6.config</code> 設定檔，就可以開始新增 <code>scss</code> 資料夾，並在裡面建立 <code>.scss</code> 檔案</li>
</ol>
<p>  <img src="prepros-2.png"></p>
<ol start="3">
<li>所有在裡面的檔案都會出現在 prepros 的 <code>Files</code> 欄位</li>
</ol>
<p>  <img src="prepros-3.png"></p>
<ol start="4">
<li>在檔案內撰寫存檔後，prepros 就會自動在根目錄幫你建立相對的 <code>css</code> 資料夾，同時在裡面也建立相對的 <code>.css</code> 檔案</li>
</ol>
<p>  <img src="prepros-4.png"></p>
<ol start="5">
<li>所有編輯的紀錄都會出現在 prepros 的 <code>log</code> 欄位，如果編輯失敗也會列出錯誤的位置</li>
</ol>
<p>  <img src="prepros-5.png"></p>
<h3 id="編輯器內建插件-VS-Code-內建的-Live-Sass-Compiler"><a href="#編輯器內建插件-VS-Code-內建的-Live-Sass-Compiler" class="headerlink" title="編輯器內建插件 :　VS Code 內建的 Live Sass Compiler"></a>編輯器內建插件 :　VS Code 內建的 Live Sass Compiler</h3><ol>
<li>如果是 <code>VS Code</code> 的使用者，可以下載名為 <code>Live Sass Compiler</code> 的插件</li>
</ol>
<p>  <img src="live-sass-1.png"></p>
<ol start="2">
<li>下載後，開啟檔案下方會出現 <code>Watch Sass</code> 按鈕，按下後就會開始監聽 <code>sass</code> 檔案，並建立相對的 <code>css</code> 檔案，且及時更新</li>
</ol>
<p>  <img src="live-sass-2.png"></p>
<ol start="3">
<li>如果 Sass 檔案編譯出現錯誤，會在下方的 輸出 欄位顯示 <code>Error</code>，並列出錯誤的位置</li>
</ol>
<p>  <img src="live-sass-3.png"></p>
<ol start="4">
<li>如果想要改變 css 檔案的路徑，可以在設定中 <code>setting.json</code>，新增以下程式碼</li>
</ol>
  <figure class="highlight"><table><tr><td class="code"><pre><span class="line">&quot;liveSassCompile.settings.formats&quot;:[</span><br><span class="line">  <span class="comment">// 預設為在 css 資料夾中產生相對的 .css 檔案</span></span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">&quot;format&quot;</span>: <span class="string">&quot;expanded&quot;</span>,</span><br><span class="line">    <span class="attr">&quot;extensionName&quot;</span>: <span class="string">&quot;.css&quot;</span>,</span><br><span class="line">    <span class="attr">&quot;savePath&quot;</span>: <span class="string">&quot;/css&quot;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">// 生成壓縮檔</span></span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">&quot;format&quot;</span>: <span class="string">&quot;compressed&quot;</span>,</span><br><span class="line">    <span class="attr">&quot;extensionName&quot;</span>: <span class="string">&quot;.min.css&quot;</span>,</span><br><span class="line">    <span class="attr">&quot;savePath&quot;</span>: <span class="string">&quot;/css&quot;</span></span><br><span class="line">  &#125;,</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<h3 id="前端任務打包工具-gulp、webpack-需用到-JavaScirpt，在此先不介紹"><a href="#前端任務打包工具-gulp、webpack-需用到-JavaScirpt，在此先不介紹" class="headerlink" title="前端任務打包工具 : gulp、webpack (需用到 JavaScirpt，在此先不介紹)"></a>前端任務打包工具 : gulp、webpack (需用到 JavaScirpt，在此先不介紹)</h3>]]></content>
      <categories>
        <category>Scss</category>
      </categories>
      <tags>
        <tag>Scss</tag>
      </tags>
  </entry>
  <entry>
    <title>Scss extend</title>
    <url>/scss/20190924/1a508081/</url>
    <content><![CDATA[<p>為了不要使 CSS 太過龐大，遇到相同樣式時，都會將其合併起來，但這會遇到一個問題，如果開發到後面程式碼太多，遇到相同樣式要合併時又得拉到前面找出來合併</p>
<span id="more"></span>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 第20行 */</span></span><br><span class="line"><span class="selector-class">.header</span> <span class="selector-tag">h1</span>, <span class="selector-class">.content</span> <span class="selector-tag">h1</span>, <span class="selector-class">.footer</span> <span class="selector-tag">h1</span> &#123;...&#125; </span><br><span class="line"><span class="comment">/* 第400行 */</span></span><br><span class="line"><span class="selector-class">.content</span> <span class="selector-tag">h1</span> &#123;...&#125;</span><br><span class="line"><span class="comment">/* 第4000行 */</span></span><br><span class="line"><span class="selector-class">.footer</span> <span class="selector-tag">h1</span> &#123;...&#125;</span><br></pre></td></tr></table></figure>

<h2 id="extend-基本運用"><a href="#extend-基本運用" class="headerlink" title="@extend 基本運用"></a>@extend 基本運用</h2><p>透過 <code>@extend</code> 可以將其他的 class 拉進來合併延伸使用</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* in scss */</span></span><br><span class="line"><span class="selector-class">.box1</span> &#123;<span class="attribute">color</span>: red&#125;</span><br><span class="line">.box2 &#123;</span><br><span class="line">  width: <span class="number">100px</span>;</span><br><span class="line">  <span class="keyword">@extend</span> .box1;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* in css */</span></span><br><span class="line"><span class="selector-class">.box1</span>, <span class="selector-class">.box2</span> &#123;<span class="attribute">color</span>: red&#125;</span><br><span class="line">.box2 &#123;width: <span class="number">100px</span>&#125;</span><br></pre></td></tr></table></figure>

<p>須注意合併樣式的先後順序，以下情況 <code>.box2</code> 的 <code>border-color</code> 樣式就會被覆蓋</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* in scss*/</span></span><br><span class="line"><span class="selector-class">.box2</span> &#123;</span><br><span class="line">  <span class="keyword">@extend</span> .box1 ;</span><br><span class="line">  <span class="attribute">border-color</span>: blue;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.box1</span> &#123;<span class="attribute">border</span>: <span class="number">1px</span> solid red&#125;</span><br><span class="line"></span><br><span class="line">/* in css */</span><br><span class="line">.box2 &#123;border-color: blue;&#125;</span><br><span class="line"><span class="selector-class">.box1</span>, <span class="selector-class">.box2</span> &#123;<span class="attribute">border</span>: <span class="number">1px</span> solid red&#125;</span><br></pre></td></tr></table></figure>

<h2 id="專門被呼叫的樣式"><a href="#專門被呼叫的樣式" class="headerlink" title="% 專門被呼叫的樣式"></a>% 專門被呼叫的樣式</h2><p>可以用 <code>%</code> 建立一個樣式，只有使用 <code>@extend</code> 呼叫時才會作用，本身並不會被 Sass 編譯成 CSS</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* in scss */</span></span><br><span class="line">%all-<span class="selector-tag">h1</span> &#123;<span class="attribute">color</span>: red&#125;</span><br><span class="line">.header &#123;</span><br><span class="line">  width: <span class="number">100px</span>;</span><br><span class="line">  <span class="keyword">@extend</span> %all-h1</span><br><span class="line">&#125;;</span><br><span class="line"><span class="selector-class">.footer</span> &#123;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">200px</span>;</span><br><span class="line">  <span class="keyword">@extend</span> %all-h1</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* in css */</span></span><br><span class="line"><span class="selector-class">.header</span>, <span class="selector-class">.footer</span> &#123;<span class="attribute">color</span>: red&#125;;</span><br><span class="line"><span class="selector-class">.header</span> &#123;<span class="attribute">width</span>: <span class="number">100px</span>&#125;;</span><br><span class="line"><span class="selector-class">.footer</span> &#123;<span class="attribute">width</span>: <span class="number">200px</span>&#125;</span><br></pre></td></tr></table></figure>

<h2 id="extend-與-mixin-的差異"><a href="#extend-與-mixin-的差異" class="headerlink" title="@extend 與 @mixin 的差異"></a>@extend 與 @mixin 的差異</h2><ul>
<li><h3 id="extend"><a href="#extend" class="headerlink" title="@extend"></a>@extend</h3><p>主要用於樣式的合併，無法帶入參數，如果樣式固定，使用 <code>@extend</code> 程式碼會較為簡潔</p>
</li>
<li><h3 id="mixin"><a href="#mixin" class="headerlink" title="@mixin"></a>@mixin</h3><p>將整包程式碼帶到對應的 <code>class</code> 內，可以帶入參數，適合用於多變數運算</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Scss</category>
      </categories>
      <tags>
        <tag>Scss</tag>
      </tags>
  </entry>
  <entry>
    <title>Scss function</title>
    <url>/scss/20190924/b33be25b/</url>
    <content><![CDATA[<p>如同一般程式語言中的函式 <code>function</code> ，Sass 中也提供了類似的方式，而 <code>@function</code> 與 <code>@mixin</code> 不同之處在於其本身就有已經設定好的函式供我們使用，稱為 “內置函式”</p>
<span id="more"></span>

<h2 id="function-回傳值"><a href="#function-回傳值" class="headerlink" title="@function 回傳值"></a>@function 回傳值</h2><p>藉由呼叫來使用函式，其本身代表的是一個 “值” ，而不是一個 CSS 樣式，所以無法單獨使用</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 直接使用為錯誤用法</span></span><br><span class="line">darken(#ff3366, <span class="number">50%</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 作為&quot;變量值&quot;帶入變數中</span></span><br><span class="line"><span class="variable">$redDark</span>: darken(<span class="number">#ff3366</span>, <span class="number">50%</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 作為&quot;屬性值&quot;帶入 CSS 屬性中</span></span><br><span class="line"><span class="selector-tag">p</span> &#123;<span class="attribute">color</span>: darken(<span class="number">#ff3366</span>, <span class="number">50%</span>);&#125;</span><br></pre></td></tr></table></figure>

<h2 id="內置函式"><a href="#內置函式" class="headerlink" title="內置函式"></a>內置函式</h2><p>Sass 本身就有提供許多已經寫好的函式供我們使用，以下為內置函式的分類</p>
<ul>
<li><h3 id="字串函式"><a href="#字串函式" class="headerlink" title="字串函式"></a>字串函式</h3><p><a href="https://yungshenglu.github.io/2018/04/30/SassNotesA3/">Sass 學習雜記 - A3. 字串函式</a></p>
</li>
<li><h3 id="數學函式"><a href="#數學函式" class="headerlink" title="數學函式"></a>數學函式</h3><p><a href="https://yungshenglu.github.io/2018/04/30/SassNotesA3/">Sass 學習雜記 - A1. 數學函式</a></p>
</li>
<li><h3 id="顏色函式"><a href="#顏色函式" class="headerlink" title="顏色函式"></a>顏色函式</h3><p>常見實用的顏色函式有提高亮度 <code>lighten</code>、降低亮度 <code>darken</code> 、提高飽和度 <code>saturate</code> 、降低飽和度 <code>desaturate</code> 和改變色相值 <code>adjust-hue</code>，可以透過以下連結調色，且不同項目的顏色函式可以疊加使用</p>
<p><a href="https://sassme.jim-nielsen.com/">Sass 顏色工具｜SassMe</a><br><a href="https://yungshenglu.github.io/2018/04/30/SassNotesA2/">Sass 學習雜記 - A2. 顏色函式</a></p>
</li>
</ul>
<h2 id="自訂函式"><a href="#自訂函式" class="headerlink" title="自訂函式"></a>自訂函式</h2><p>除了以上實用的內置函式，我們也可以自訂想要的函式，函式內部使用 <code>@return</code> 回傳結果到函式名稱，例如將 <code>px</code> 轉成 <code>rem</code> 的函式</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* in scss */</span></span><br><span class="line"><span class="keyword">@function</span> pxToRem(<span class="variable">$px</span>, <span class="variable">$base</span>: <span class="number">16</span>)&#123;</span><br><span class="line">  <span class="keyword">@return</span> (<span class="variable">$px</span> / <span class="variable">$base</span>) * <span class="number">1rem</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">p</span> &#123;<span class="attribute">font-size</span>: pxToRem(<span class="number">20</span>);&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* in css */</span></span><br><span class="line"><span class="selector-tag">p</span> &#123;<span class="attribute">font-size</span>: <span class="number">1.25rem</span>;&#125;</span><br></pre></td></tr></table></figure>

<h2 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h2><p>  <a href="https://yungshenglu.github.io/2018/04/30/SassNotesA3/">Sass 學習雜記 - A3. 字串函式</a><br>  <a href="https://yungshenglu.github.io/2018/04/30/SassNotesA3/">Sass 學習雜記 - A1. 數學函式</a><br>  <a href="https://yungshenglu.github.io/2018/04/30/SassNotesA2/">Sass 學習雜記 - A2. 顏色函式</a></p>
]]></content>
      <categories>
        <category>Scss</category>
      </categories>
      <tags>
        <tag>Scss</tag>
      </tags>
  </entry>
  <entry>
    <title>Scss import</title>
    <url>/scss/20190924/b8cb8afb/</url>
    <content><![CDATA[<p>透過 Sass 中的 <code>@import</code> 功能，可以將檔案拆分成許多支，最後再透過此功能彙整，以方便管理</p>
<span id="more"></span>

<h2 id="import-的用法"><a href="#import-的用法" class="headerlink" title="@import 的用法"></a>@import 的用法</h2><ul>
<li><p>使用 <code>@import</code> 可以將 “多支 Sass” 檔案彙整成 “一支 Sass” 檔案，再透過轉譯轉換成 CSS 檔案，因此載入時只要載一支 CSS 檔案即可</p>
</li>
<li><p>在 Sass 檔名前面加上下底線 <code>_</code>，此檔案就不會被編譯成 CSS 檔案，例如: <code>_reset.scss</code></p>
</li>
<li><p>將 CSS 拆分成多支 Sass 檔案，使 CSS 程式碼切分模組化，每支檔案的程式碼較少，使維護上更能精準的聚焦</p>
</li>
<li><p><a href="https://github.com/twbs/bootstrap/blob/master/scss/bootstrap.scss">Bootstrap</a> 就是用這種方式在管理眾多的 CSS 程式碼</p>
</li>
<li><p>如果我們要將變數檔案 <code>_variable.scss</code> 與首頁檔案 <code>index.scss</code> 載入到總檔案 <code>all.scss</code> 中，就可以透過以下方法</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in all.scss</span></span><br><span class="line"><span class="keyword">@import</span> <span class="string">&quot;variable&quot;</span>;</span><br><span class="line"><span class="keyword">@import</span> <span class="string">&quot;index&quot;</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>注意載入時不需要加最前面的下底線 <code>_</code> 和最後面的副檔名 <code>.scss</code>，並在每行結尾用分號 <code>;</code> 隔開</p>
</li>
<li><p>因為 Scss 檔案是 “由上而下” 讀取，所以會被其他檔案使用到的檔案要優先匯入，例如 <code>_variable.scss</code> 和 <code>_mixin.scss</code></p>
</li>
<li><p>通常都是將所有檔案匯入 <code>!import</code> 到一支名為 <code>all.scss</code> 的檔案管理，再將這支檔案編譯成瀏覽器可以讀取的 <code>all.css</code></p>
</li>
</ul>
<h2 id="CSS-reset"><a href="#CSS-reset" class="headerlink" title="CSS reset"></a>CSS reset</h2><ul>
<li><p>通常在寫網頁時，我們都會載入一支名為 _reset.scss 的檔案來淨空所有樣式</p>
</li>
<li><p>要完全靠自己完成 <code>_reset.scss</code>，會比較麻煩，所以通常都會載入網路上已寫好得程式碼，常見的有兩種，<code>meyerweb</code> 和 <code>Normalize</code></p>
<ul>
<li><a href="https://meyerweb.com/eric/tools/css/reset/">meyerweb</a> : 完全淨空所有樣式</li>
<li><a href="https://necolas.github.io/normalize.css/">Normalize</a> : 保留部分樣式，不會完全淨空所有樣式</li>
</ul>
</li>
<li><p>選擇性 : 如果是工程取向，建議使用 <code>meyerweb</code> 完全淨空，設計取向可以使用 <code>Normalize</code> 保留部分樣式，<code>Bootstrap</code> 即是使用 <code>Normalize</code>，在載入後仍然可以依照需求作修改</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Scss</category>
      </categories>
      <tags>
        <tag>Scss</tag>
      </tags>
  </entry>
  <entry>
    <title>Scss mixin</title>
    <url>/scss/20190924/4fef1f83/</url>
    <content><![CDATA[<p>透過 <code>@mixin</code> 可以省去重複撰寫相同的程式碼，並將 CSS 技巧儲存成工具包，快速取用，避免往後用到時還要從舊資料找方法</p>
<span id="more"></span>

<h2 id="mixin-用法"><a href="#mixin-用法" class="headerlink" title="@mixin 用法"></a>@mixin 用法</h2><p>以指令 <code>@mixin</code> 進行宣告，並以 <code>@include</code> 呼叫載入，以下為 “圖片取代文字” 的 CSS 技巧</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">//in Scss</span></span><br><span class="line"><span class="keyword">@mixin</span> text-hide &#123;</span><br><span class="line">  <span class="attribute">text-indent</span>: <span class="number">101%</span>;</span><br><span class="line">  <span class="attribute">overflow</span>: hidden;</span><br><span class="line">  <span class="attribute">white-space</span>: nowrap;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.brand</span> &#123;<span class="keyword">@include</span> text-hide;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// in css</span></span><br><span class="line"><span class="selector-class">.brand</span> &#123;</span><br><span class="line">  <span class="attribute">text-indent</span>: <span class="number">101%</span>;</span><br><span class="line">  <span class="attribute">overflow</span>: hidden;</span><br><span class="line">  <span class="attribute">white-space</span>: nowrap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="參數"><a href="#參數" class="headerlink" title="參數"></a>參數</h2><p><code>@mixin</code> 還可以導入參數並給予預設值，如果沒有給值就會自動導入預設值</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in scss</span></span><br><span class="line"><span class="keyword">@mixin</span> bg (<span class="variable">$bgc</span>:red, <span class="variable">$width</span>:<span class="number">200px</span>)&#123;</span><br><span class="line">  <span class="attribute">background-color</span>: <span class="variable">$bgc</span>;</span><br><span class="line">  <span class="attribute">width</span>: <span class="variable">$width</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.box</span> &#123;<span class="keyword">@include</span> bg&#125;</span><br><span class="line">.box2 &#123;<span class="keyword">@include</span> bg(blue, <span class="number">300px</span>)&#125;</span><br><span class="line"></span><br><span class="line">// in css</span><br><span class="line">.box &#123;</span><br><span class="line">  <span class="attribute">background-color</span>: red;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">200px</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.box2</span> &#123;</span><br><span class="line">  <span class="attribute">background-color</span>: blue;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">300px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="字串內插"><a href="#字串內插" class="headerlink" title="#{ } 字串內插"></a>#{ } 字串內插</h2><ul>
<li><p>Sass 的字串內插是將一個字串型態的變數放在兩個字串之間</p>
</li>
<li><p>對於 Sass 的組合來說，字串內插使我們能在選取項中重複使用 相同的 “字串變數” 和 “檔案名稱”</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in scss</span></span><br><span class="line"><span class="keyword">@mixin</span> bgi (<span class="variable">$file</span>)&#123;<span class="attribute">background-image</span>: url(#(<span class="variable">$file</span>).png)&#125;;</span><br><span class="line"><span class="selector-class">.box</span> &#123;<span class="keyword">@include</span> bgi(test)&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// in css</span></span><br><span class="line"><span class="selector-class">.box</span> &#123;<span class="attribute">background-image</span>:url(test.png)&#125;;</span><br></pre></td></tr></table></figure></li>
<li><p>如果要將變數帶入 <code>class</code> 也必須使用此符號包住變數</p>
</li>
</ul>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* in scss */</span></span><br><span class="line"><span class="variable">$name</span> :primary;</span><br><span class="line"><span class="selector-class">.bg-</span>#&#123;<span class="variable">$name</span>&#125; &#123;...&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* in css */</span></span><br><span class="line"><span class="selector-class">.bg-primary</span> &#123;...&#125;</span><br></pre></td></tr></table></figure>

<h2 id="content-傳遞內容"><a href="#content-傳遞內容" class="headerlink" title="@content 傳遞內容"></a>@content 傳遞內容</h2><p><code>@content</code> 的功能在於傳遞內容到 <code>@mixin</code> 中</p>
<ul>
<li><p>用在 RWD 斷點設計中，為每個斷點傳遞不同的內容</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in Scss</span></span><br><span class="line"><span class="keyword">@mixin</span> mobile &#123;</span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">max-width</span>: <span class="number">768px</span>)&#123;</span><br><span class="line">    <span class="keyword">@content</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="selector-class">.header</span> &#123;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">500px</span>;</span><br><span class="line">  <span class="keyword">@mixin</span> mobile &#123;</span><br><span class="line">    <span class="attribute">width</span>: <span class="number">300px</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// in css</span></span><br><span class="line"><span class="selector-class">.header</span> &#123;<span class="attribute">width</span>: <span class="number">500px</span>&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@media</span> (<span class="attribute">max-width</span>: <span class="number">768px</span>)&#123;</span><br><span class="line">  <span class="selector-class">.header</span> &#123;<span class="attribute">width</span>: <span class="number">300px</span>&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
<li><p>用在狀態樣式的表示</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in scss</span></span><br><span class="line"><span class="comment">// 連結樣式</span></span><br><span class="line"><span class="keyword">@mixin</span> link &#123;</span><br><span class="line">  &amp;<span class="selector-pseudo">:link</span>, &amp;<span class="selector-pseudo">:visited</span> &#123;</span><br><span class="line">    <span class="keyword">@content</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 被點擊後樣式</span></span><br><span class="line"><span class="keyword">@mixin</span> link-hover &#123;</span><br><span class="line">  &amp;<span class="selector-pseudo">:hover</span>, &amp;focus, &amp;<span class="selector-pseudo">:active</span>, &amp;<span class="selector-class">.active</span> &#123;</span><br><span class="line">    <span class="keyword">@content</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.box</span> &#123;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">50px</span>;</span><br><span class="line">  <span class="keyword">@include</span> link &#123;<span class="attribute">color</span>:red&#125;;</span><br><span class="line">  <span class="keyword">@include</span> link-hover &#123;<span class="attribute">color</span>: blue&#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// in css</span></span><br><span class="line"><span class="selector-class">.box</span> &#123;<span class="attribute">width</span>: <span class="number">50px</span>&#125;;</span><br><span class="line"><span class="selector-class">.box</span><span class="selector-pseudo">:link</span>, <span class="selector-class">.box</span><span class="selector-pseudo">:visited</span> &#123;<span class="attribute">color</span>: red&#125;;</span><br><span class="line"><span class="selector-class">.box</span><span class="selector-pseudo">:hover</span>, <span class="selector-class">.box</span><span class="selector-pseudo">:focus</span>, <span class="selector-class">.box</span><span class="selector-pseudo">:active</span>, <span class="selector-class">.box</span><span class="selector-class">.active</span> &#123;<span class="attribute">color</span>: blue&#125;;</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>Scss</category>
      </categories>
      <tags>
        <tag>Scss</tag>
      </tags>
  </entry>
  <entry>
    <title>Scss 變數</title>
    <url>/scss/20190924/4740e2f1/</url>
    <content><![CDATA[<h2 id="為什麼需要變數"><a href="#為什麼需要變數" class="headerlink" title="為什麼需要變數 ?"></a>為什麼需要變數 ?</h2><p>變數可以設定全域樣式，如果我們要改變專案中所有連結的顏色，純 CSS 只能一一去做更改，但將所有連結顏色的值導入變數後，只需要改變變數的值所有連結顏色就會一起更改</p>
<span id="more"></span>

<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="variable">$link-color</span>: red;</span><br><span class="line"><span class="selector-class">.header</span> <span class="selector-tag">a</span> &#123;<span class="attribute">color</span>: <span class="variable">$link-color</span>;&#125;</span><br><span class="line"><span class="selector-class">.menu</span> <span class="selector-tag">a</span> &#123;<span class="attribute">color</span>: <span class="variable">$link-color</span>;&#125;</span><br></pre></td></tr></table></figure>

<h2 id="變數的格式"><a href="#變數的格式" class="headerlink" title="變數的格式"></a>變數的格式</h2><p>變數可以是任何 CSS 屬性的值，只要在最前面加上 <code>$</code>，接著再加上命名的名稱，最後以 <code>;</code> 結束</p>
<h3 id="數值"><a href="#數值" class="headerlink" title="數值"></a>數值</h3><p>可以包括單位和小數，其中單位可以是絕對單位 (px)</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="variable">$z-index-l</span>: <span class="number">10</span>;</span><br><span class="line"><span class="variable">$font-size-base</span>: <span class="number">16px</span>;</span><br></pre></td></tr></table></figure>

<h3 id="字串"><a href="#字串" class="headerlink" title="字串"></a>字串</h3><p>可以用 雙引號 <code>&quot;&quot;</code>、單引號 <code>&#39;&#39;</code> 或沒有引號，是一個字串列時，可以用 <code>,</code> 或空白隔開</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="variable">$font-family</span>: <span class="string">&quot;Noto Sans TC&quot;</span>,sans-serif;</span><br><span class="line"><span class="variable">$brand-img</span>: <span class="string">&#x27;../img/brand.png&#x27;</span>;</span><br></pre></td></tr></table></figure>

<h3 id="顏色"><a href="#顏色" class="headerlink" title="顏色"></a>顏色</h3><p>可以是各種代碼表現</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="variable">$white</span>: white;</span><br><span class="line"><span class="variable">$primary</span>: <span class="number">#fff</span>;</span><br><span class="line"><span class="variable">$secondary</span>: rgb(<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>, .<span class="number">5</span>)</span><br></pre></td></tr></table></figure>

<h3 id="布林值"><a href="#布林值" class="headerlink" title="布林值"></a>布林值</h3><p><code>true</code>, <code>false</code></p>
<h3 id="空值"><a href="#空值" class="headerlink" title="空值"></a>空值</h3><p><code>null</code></p>
<h2 id="變數的運算"><a href="#變數的運算" class="headerlink" title="變數的運算"></a>變數的運算</h2><p>Sass 支援基本的數學運算，加 <code>+</code>、減 <code>-</code>、乘 <code>*</code>、除 <code>/</code></p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="variable">$font-size-base</span>: <span class="number">16px</span>;</span><br><span class="line"><span class="variable">$font-l</span>: <span class="variable">$font-size-base</span> * <span class="number">1.2</span>;</span><br><span class="line"><span class="variable">$font-s</span>: <span class="variable">$font-size-base</span> * <span class="number">0.8</span>;</span><br></pre></td></tr></table></figure>

<p>如果遇到除不盡的的小數會運算網頁解析度可以容許的範圍，雖然有很微小得留白，但肉眼幾乎無法察覺</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">//in .scss</span></span><br><span class="line"><span class="variable">$width</span>: <span class="number">960px</span>;</span><br><span class="line"><span class="selector-class">.menu</span> &#123;<span class="attribute">width</span>: <span class="variable">$width</span> / <span class="number">7</span>&#125;</span><br><span class="line">// in .css</span><br><span class="line">.menu &#123;width: <span class="number">137.14286px</span>;&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="陣列-List"><a href="#陣列-List" class="headerlink" title="陣列 (List)"></a>陣列 (List)</h2><ul>
<li><p>陣列的英文是 <code>Array</code>，而 Sass 只有 <code>List</code>，但用法上相同</p>
</li>
<li><p>陣列是用來存放 “沒有鍵名的變數”，表示只能透過 “索引” (1,2,3,4)取值，而不是 “鍵名”(hover,active,…) ，而一般程式語言都從 <code>0</code> 開始，但 List 從 <code>1</code> 開始，透過 nth() 使用 List</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="variable">$color</span>: red, blue, green;</span><br><span class="line"><span class="comment">// in scss</span></span><br><span class="line"><span class="selector-tag">p</span> &#123;<span class="attribute">color</span>: nth(<span class="variable">$color</span>, <span class="number">2</span>)&#125;</span><br><span class="line">// in css</span><br><span class="line">p &#123;color: blue&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>當然也可以位索引導入變數</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="variable">$color</span>: red, blue, green;</span><br><span class="line"><span class="comment">// in scss</span></span><br><span class="line"><span class="variable">$index</span>: <span class="number">2</span>;</span><br><span class="line"><span class="selector-tag">p</span> &#123;<span class="attribute">color</span>: nth(<span class="variable">$color</span>, <span class="variable">$index</span>)&#125;</span><br><span class="line">// in css</span><br><span class="line">p &#123;color: blue&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="物件-Map"><a href="#物件-Map" class="headerlink" title="物件 (Map)"></a>物件 (Map)</h2><ul>
<li><p>陣列的英文是 <code>Object</code>，而在 Sass 中只有 <code>Map</code>，但用法相同</p>
</li>
<li><p><code>Map</code> 與 <code>List</code> 不同之處在於可以 “自訂鍵名”，也就是每個值的名稱</p>
</li>
<li><p>Map 使用小括號 <code>( )</code> 包住資料，內層使用逗號 <code>,</code> 隔開資料，每筆資料都有獨立的鍵名 <code>key</code> 與值 <code>value</code>，每個鍵名需用引號 <code>&quot;&quot;</code>、<code>&#39;&#39;</code> 包住</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="variable">$map</span>: (</span><br><span class="line">  <span class="string">&#x27;key1&#x27;</span>: value1,</span><br><span class="line">  <span class="string">&#x27;key2&#x27;</span>: value2</span><br><span class="line">)</span><br></pre></td></tr></table></figure></li>
<li><p><code>使用 map-get(map, key)</code> 將值取出</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in scss</span></span><br><span class="line"><span class="variable">$theme</span>: (</span><br><span class="line">  <span class="string">&#x27;primary&#x27;</span>: blue,</span><br><span class="line">    <span class="string">&#x27;secondary&#x27;</span>: red,</span><br><span class="line">)</span><br><span class="line">p &#123;color: map-get(<span class="variable">$theme</span>, primary)&#125;</span><br><span class="line"></span><br><span class="line">// in css</span><br><span class="line">p &#123;color: blue&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>Map 指令</p>
<ul>
<li><p><code>map-get(map, key)</code> : 取出指定的值</p>
</li>
<li><p><code>map-merge(map1, map2)</code> : 合併 map</p>
</li>
<li><p><code>map-remove(map, key)</code> : 移除指定的值</p>
</li>
<li><p><code>map-keys(map)</code> : 列出 map 全部的 key</p>
</li>
<li><p><code>map-values(map)</code> : 列出 map 全部的 value</p>
</li>
<li><p><code>map-has-key(map, key)</code> : 確認 map 中是否有指定 key</p>
</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>Scss</category>
      </categories>
      <tags>
        <tag>Scss</tag>
      </tags>
  </entry>
  <entry>
    <title>Bootstrap 4 Grid System</title>
    <url>/boostrap/20190925/99a4840c/</url>
    <content><![CDATA[<p>在 Bootstrap 中，設計了相當完善的響應式格線系統，即使沒有要使用其中的模組化框架，也可以單獨使用格線系統，而且只要修改設定檔，就可以輕鬆做出客製化的響應式格線系統 </p>
<span id="more"></span>

<h2 id="響應式斷點"><a href="#響應式斷點" class="headerlink" title="響應式斷點"></a>響應式斷點</h2><p>Bootstrap 是設計成行動 (手機) 優先的框架，並透過 <code>media queries</code> 來設立中斷點</p>
<h3 id="預設斷點"><a href="#預設斷點" class="headerlink" title="預設斷點"></a>預設斷點</h3><p>Bootstrap 的預設斷點</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Small devices (landscape phones, 576px and up) */</span></span><br><span class="line"><span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="number">576px</span>) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Medium devices (tablets, 768px and up) */</span></span><br><span class="line"><span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="number">768px</span>) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Large devices (desktops, 992px and up) */</span></span><br><span class="line"><span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="number">992px</span>) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Extra large devices (large desktops, 1200px and up) */</span></span><br><span class="line"><span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="number">1200px</span>) &#123; ... &#125;</span><br></pre></td></tr></table></figure>

<h3 id="斷點變數"><a href="#斷點變數" class="headerlink" title="斷點變數"></a>斷點變數</h3><p>Bootstrap 預設的斷點變數如下，我們可以透過修改<code>_variable.scss</code> 中的變數來新增或刪除斷點</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="variable">$grid-breakpoints</span>: (</span><br><span class="line">  xs: <span class="number">0</span>,</span><br><span class="line">  sm: <span class="number">576px</span>,</span><br><span class="line">  md: <span class="number">768px</span>,</span><br><span class="line">  lg: <span class="number">992px</span>,</span><br><span class="line">  xl: <span class="number">1200px</span></span><br><span class="line">) !default;</span><br></pre></td></tr></table></figure>

<h3 id="mixin"><a href="#mixin" class="headerlink" title="@mixin"></a>@mixin</h3><p>我們也可以透過 Bootstrap 寫好的 <code>@mixin</code> 使用最大、最小或特定螢幕尺寸的其中一段</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* @media (max-width: 575.98px) */</span></span><br><span class="line"><span class="keyword">@include</span> media-breakpoint-down(xs) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* @media (min-width: 576px) */</span></span><br><span class="line"><span class="keyword">@include</span> media-breakpoint-up(sm) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* @media (min-width: 576px) and (max-width: 1199.98px) */</span></span><br><span class="line"><span class="keyword">@include</span> media-breakpoint-between(sm,xl) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* @media (min-width: 576px) */</span></span><br><span class="line"><span class="keyword">@include</span> media-breakpoint-between(sm,lg) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* @media (min-width: 576px) and (max-width: 767.98px) */</span></span><br><span class="line"><span class="keyword">@include</span> media-breakpoint-only(sm) &#123; ... &#125;</span><br></pre></td></tr></table></figure>

<h2 id="Grid-System"><a href="#Grid-System" class="headerlink" title="Grid System"></a>Grid System</h2><p>Bootstrap 中的格線系統</p>
<h3 id="基本架構"><a href="#基本架構" class="headerlink" title="基本架構"></a>基本架構</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;container&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;row&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;col&quot;</span>&gt;</span></span><br><span class="line">      <span class="comment">&lt;!-- 內容 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>透過最外層的 <code>.container</code>，為每個斷點提供固定的容器寬度 ; 如果想要滿版寬度，在這層可以使用 <code>.container-fluid</code></li>
<li>第二層的 <code>.row</code> 為橫列，用來包住內層的欄 <code>.col</code>，確保每個欄位排列適當</li>
<li>所有內容都放在第三層欄 <code>.col</code> 的 內層，而不是直接撰寫樣式在 <code>.col</code>上，<code>.col</code> 也只能是 <code>.row</code> 的直接子類</li>
</ul>
<h3 id="格線變數"><a href="#格線變數" class="headerlink" title="格線變數"></a>格線變數</h3><ul>
<li><p>預設的欄位數 <code>12</code> 與欄間距 <code>30px</code>，我們可以在 <code>._variable.scss</code> 找到以下相關變數來修改成想要的格式</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="variable">$grid-columns</span>: <span class="number">12</span> !default;</span><br><span class="line"><span class="variable">$grid-gutter-width</span>: <span class="number">30px</span> !default;</span><br></pre></td></tr></table></figure></li>
<li><p><code>.container</code> 在每個斷點都有預設寬度，同樣的我們也可以在<br><code>._variable.scss</code> 中找到相關變數來修改成想要的寬度</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="variable">$container-max-width</span>: (</span><br><span class="line">  sm: <span class="number">540px</span>,</span><br><span class="line">  md: <span class="number">720px</span>,</span><br><span class="line">  lg: <span class="number">960px</span>,</span><br><span class="line">  xl: <span class="number">1140px</span></span><br><span class="line">) !default;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="container-容器"><a href="#container-容器" class="headerlink" title="container 容器"></a>container 容器</h3><p>以下為 <code>.container</code> 的預設樣式</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="attribute">margin-right</span>: auto;</span><br><span class="line">  <span class="attribute">margin-left</span>: auto;</span><br><span class="line">  <span class="attribute">padding-right</span>: <span class="number">15px</span>; <span class="comment">/* $grid-gutter-width */</span></span><br><span class="line">  <span class="attribute">padding-left</span>: <span class="number">15px</span>;  <span class="comment">/* $grid-gutter-width */</span></span><br><span class="line">  <span class="attribute">width</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="attribute">max-width</span>: <span class="number">1140px</span>;　<span class="comment">/* 隨螢幕尺寸而變 */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><code>margin-right: auto</code> 和 <code>margin-left: auto</code> 為水平置中</li>
<li>因為有 <code>padding</code>，所以 <code>.container</code> 的實際寬度為 <code>max-width</code> 減 <code>$grid-gutter-width</code>，這個斷點下為 <code>1110px</code></li>
<li>如果是 <code>.container-fluid</code> 就沒有設定 <code>max-width</code>，所以會自適應延伸滿版寬</li>
</ul>
<h3 id="row-列"><a href="#row-列" class="headerlink" title="row 列"></a>row 列</h3><p>以下為<code> .row</code> 的預設樣式</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.row</span> &#123;</span><br><span class="line">  <span class="attribute">display</span>: flex;</span><br><span class="line">  <span class="attribute">flex-wrap</span>: wrap;</span><br><span class="line">  <span class="attribute">margin-right</span>: -<span class="number">15px</span>; <span class="comment">/* $grid-gutter-width */</span></span><br><span class="line">  <span class="attribute">margin-left</span>: -<span class="number">15px</span>; <span class="comment">/* $grid-gutter-width */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p><code>.row</code> 使用 <code>flex</code> 來排版，而 <code>flex</code> 的預設為水平排列</p>
</li>
<li><p>負值的 <code>margin</code> 將 <code>.container</code> 的 <code>padding</code> 外推，所以 <code>.container</code> 的內容寬度在此段點變回 1140px</p>
</li>
<li><p>因為 <code>.row</code> 是使用 <code>flex</code> 來排版，所以可以直接使用 <code>flexbox</code> 來操作內容，Bootstrap 已經有寫好的 class</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;row justify-content-center align-items-end&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;col&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;col&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="col-欄"><a href="#col-欄" class="headerlink" title="col 欄"></a>col 欄</h3><p>以下為 <code>.col</code> 的預設樣式</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.col</span> &#123;</span><br><span class="line">  <span class="attribute">position</span>: relative;</span><br><span class="line">  <span class="attribute">flex-basis</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">flex-grow</span>: <span class="number">1</span>;</span><br><span class="line">  <span class="attribute">max-width</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">1px</span>;</span><br><span class="line">  <span class="attribute">padding-right</span>: <span class="number">15px</span>;</span><br><span class="line">  <span class="attribute">padding-left</span>: <span class="number">15</span>xp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>因為有 <code>padding</code>，所以此斷點的內容寬度又變回 <code>1110px</code></p>
</li>
<li><p>因為是使用 <code>flex</code> 來排版，所以預設每個欄位是等寬的</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;row&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;col&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;col&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;col&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure></li>
<li><p>當然也可以指定 <code>.col</code> 的寬度，總和超過 <code>12</code> 就會自動換行</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;row&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;col-4&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;col-7&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;col-3&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="在不同螢幕尺寸下設定不同的寬度"><a href="#在不同螢幕尺寸下設定不同的寬度" class="headerlink" title="在不同螢幕尺寸下設定不同的寬度"></a>在不同螢幕尺寸下設定不同的寬度</h4><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.col-</span>&quot;螢幕尺寸&quot;-&quot;欄位數&quot;</span><br></pre></td></tr></table></figure>

<ul>
<li>Extra small ( <code>&lt; 576px</code> ) : <code>.col-</code></li>
<li>Small ( <code>≥ 576px</code> ) : <code>.col-sm-</code></li>
<li>Extra small ( <code>≥ 768px</code> ) : <code>.col-md-</code></li>
<li>Large ( <code>≥ 992px</code> ) : <code>.col-lg-</code></li>
<li>Extra large ( <code>≥ 1200px</code> ) : <code>.col-xl-</code></li>
</ul>
<p>Bootstrap 的響應式是 “行動版優先”，也就是 “由小到大”，以下為範例說明</p>
  <figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;row&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;col-12 col-sm-6 col-md-3&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>欄位預設為 <code>12</code></li>
<li>螢幕尺寸 <code>≥ 576px</code> 時，欄位為 <code>6</code></li>
<li>螢幕尺寸 <code>≥ 768px</code> 時，欄位為 <code>3</code></li>
</ul>
<h3 id="offset-欄的推移"><a href="#offset-欄的推移" class="headerlink" title="offset 欄的推移"></a>offset 欄的推移</h3><p>如果欄的位移空間與欄間隔相符合，我們就可以使用 <code>offset</code> 向左推移出空欄位</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.offset-</span>&quot;螢幕尺寸&quot;-&quot;推移欄位數&quot;</span><br></pre></td></tr></table></figure>

<p>以下範例為在螢幕尺寸 <code>≥ 768px</code> 時，向左推移 <code>4</code> 欄位</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;row&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;col-3 offset-md-4&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="Utilities-通用類別"><a href="#Utilities-通用類別" class="headerlink" title="Utilities 通用類別"></a>Utilities 通用類別</h2><p>在 Bootstrap 中，所有共用的變數都會放在 Utilites 這個分類中，以下介紹幾個常見的類別</p>
<h3 id="spacing-間隔"><a href="#spacing-間隔" class="headerlink" title="spacing 間隔"></a>spacing 間隔</h3><ul>
<li><h4 id="變數"><a href="#變數" class="headerlink" title="變數"></a>變數</h4><p>我們可以透過 <code>_variable.scss</code> 來修改間距數值，除了新增和刪除，也可以修改 “單位”</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="variable">$spacer</span>: <span class="number">1rem</span> !default;</span><br><span class="line"><span class="variable">$spacers</span>: () !default;</span><br><span class="line"></span><br><span class="line"><span class="variable">$spacers</span>: map-merge (</span><br><span class="line">  (</span><br><span class="line">    <span class="number">0</span>: <span class="number">0</span>;</span><br><span class="line">    1: (<span class="variable">$spacer</span> * .<span class="number">25</span>),</span><br><span class="line">    <span class="number">2</span>: (<span class="variable">$spacer</span> * .<span class="number">5</span>),</span><br><span class="line">    <span class="number">3</span>: <span class="variable">$spacer</span>,</span><br><span class="line">    <span class="number">4</span>: (<span class="variable">$spacer</span> * <span class="number">1.5</span>),</span><br><span class="line">    <span class="number">5</span>: (<span class="variable">$spacer</span> * <span class="number">3</span>)</span><br><span class="line">  ),<span class="variable">$spacers</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure></li>
<li><h4 id="符號"><a href="#符號" class="headerlink" title="符號"></a>符號</h4><figure class="highlight cs"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;屬性&quot;</span><span class="string">&quot;方位&quot;</span>-<span class="string">&quot;螢幕尺寸&quot;</span>-<span class="string">&quot;數值&quot;</span></span><br></pre></td></tr></table></figure>

<ul>
<li><p>屬性 :<br><code>.m</code> 代表 <code>margin</code><br><code>.p </code>代表 <code>padding</code></p>
</li>
<li><p>方位 :<br><code>t</code> 代表 <code>top</code><br><code>r</code> 代表 <code>right</code><br><code>b</code> 代表 <code>bottm</code><br><code>l</code> 代表 <code>left</code><br><code>x</code> 代表 <code>left</code> 和 <code>right</code><br><code>y</code> 代表 <code>top</code> 和 <code>bottom</code><br>沒有給值則是四邊全方位</p>
</li>
<li><p><a href="#%E5%9C%A8%E4%B8%8D%E5%90%8C%E8%9E%A2%E5%B9%95%E5%B0%BA%E5%AF%B8%E4%B8%8B%E8%A8%AD%E5%AE%9A%E4%B8%8D%E5%90%8C%E7%9A%84%E5%AF%AC%E5%BA%A6">螢幕尺寸:</a><br>上方已提過，點選回去查看</p>
</li>
<li><p>數值 :<br><code>0</code> 代表 <code>歸零</code><br><code>auto</code> 代表 <code>auto</code><br>其餘數值由上方變數定義，預設為 <code>1 ~ 5</code></p>
</li>
</ul>
</li>
</ul>
<h3 id="flex"><a href="#flex" class="headerlink" title="flex"></a>flex</h3><p>Bootstrap 4 使用 <code>flex</code> 來排版，並將所有樣式寫成 class 名稱，載入 <code>_utilities.scss</code> 並了解其命名規則就可以靈活運用，詳細參考以下連結</p>
<p><a href="http://bootstrap.hexschool.com/docs/4.2/utilities/flex/">通用類別 - Flex</a></p>
<h2 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h2><p><a href="https://getbootstrap.com/">Bootstrap</a><br><a href="https://bootstrap.hexschool.com/">Bootstrap 中文版</a><br><a href="https://github.com/twbs/bootstrap">Bootstrap on Github</a></p>
]]></content>
      <categories>
        <category>Boostrap</category>
      </categories>
      <tags>
        <tag>Bootstrap</tag>
      </tags>
  </entry>
  <entry>
    <title>Scss 條件與迴圈</title>
    <url>/scss/20190925/ff62e7a9/</url>
    <content><![CDATA[<p>在 Sass 中，也新增了與一般程式語言類似的條件判斷與迴圈，使 CSS 的運用更加靈活</p>
<span id="more"></span>

<h2 id="Conditions-條件運算"><a href="#Conditions-條件運算" class="headerlink" title="Conditions 條件運算"></a>Conditions 條件運算</h2><ul>
<li><p>Sass 的條件運算與一般程式語言的概念相同，時常會搭配 <code>@function</code> 與 <code>@mixin</code> 組合使用</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@if</span>(條件)&#123;</span><br><span class="line">...</span><br><span class="line">&#125;<span class="keyword">@else</span> if(條件)&#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;<span class="keyword">@else</span> (條件)&#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>以下為結合 <code>@mixin</code> 做出三角形的作法，使用條件運算產出不同方位的三角形，<code>$type:1</code> 為等腰三角形，<code>$type:0.86666</code> 為正三角形</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* in scss */</span></span><br><span class="line"><span class="keyword">@mixin</span> triangle (<span class="variable">$size</span>, <span class="variable">$color</span>, <span class="variable">$align</span>,<span class="variable">$type</span>:<span class="number">1</span>)&#123;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">@if</span>  (<span class="variable">$align</span> == top) &#123;</span><br><span class="line">    <span class="attribute">border-bottom</span>: (<span class="variable">$size</span>*<span class="variable">$type</span>) solid <span class="variable">$color</span>;</span><br><span class="line">    <span class="attribute">border-left</span>: (<span class="variable">$size</span>/<span class="number">2</span>) solid transparent;</span><br><span class="line">    <span class="attribute">border-right</span>: (<span class="variable">$size</span>/<span class="number">2</span>) solid transparent;</span><br><span class="line">  &#125;<span class="keyword">@else</span> if  (<span class="variable">$align</span> == right) &#123;</span><br><span class="line">    <span class="attribute">border-left</span>: (<span class="variable">$size</span>*<span class="variable">$type</span>) solid <span class="variable">$color</span>;</span><br><span class="line">    <span class="attribute">border-top</span>: (<span class="variable">$size</span>/<span class="number">2</span>) solid transparent;</span><br><span class="line">    <span class="attribute">border-bottom</span>: (<span class="variable">$size</span>/<span class="number">2</span>) solid transparent;</span><br><span class="line">  &#125;<span class="keyword">@else</span> if  (<span class="variable">$align</span> == bottom)&#123;</span><br><span class="line">    <span class="attribute">border-top</span>: (<span class="variable">$size</span>*<span class="variable">$type</span>) solid <span class="variable">$color</span>;</span><br><span class="line">    <span class="attribute">border-left</span>: (<span class="variable">$size</span>/<span class="number">2</span>) solid transparent;</span><br><span class="line">    <span class="attribute">border-right</span>: (<span class="variable">$size</span>/<span class="number">2</span>) solid transparent;</span><br><span class="line">  &#125;<span class="keyword">@else</span> if  (<span class="variable">$align</span> == left)&#123;</span><br><span class="line">    <span class="attribute">border-right</span>: (<span class="variable">$size</span>*<span class="variable">$type</span>) solid <span class="variable">$color</span>;</span><br><span class="line">    <span class="attribute">border-top</span>: (<span class="variable">$size</span>/<span class="number">2</span>) solid transparent;</span><br><span class="line">    <span class="attribute">border-bottom</span>: (<span class="variable">$size</span>/<span class="number">2</span>) solid transparent;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-class">.box</span> &#123;<span class="keyword">@include</span> triangle(<span class="number">50px</span>, red, top)&#125;</span><br><span class="line"></span><br><span class="line">/* in css */</span><br><span class="line">.box &#123;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">border-left</span>: <span class="number">50px</span> solid red;</span><br><span class="line">  <span class="attribute">border-top</span>: <span class="number">25px</span> solid transparent;</span><br><span class="line">  <span class="attribute">border-bottom</span>: <span class="number">25px</span> solid transparent;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>現在 CSS 也支援條件運算</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.parent</span> &#123;<span class="attribute">width</span>: <span class="built_in">if</span>(條件, 條件成立帶入的值, 條件不成立帶入的值);&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="Loops-迴圈運算"><a href="#Loops-迴圈運算" class="headerlink" title="Loops 迴圈運算"></a>Loops 迴圈運算</h2><h3 id="each-迴圈"><a href="#each-迴圈" class="headerlink" title="@each 迴圈"></a>@each 迴圈</h3><p><code>@each</code> 可以針對陣列 <code>list</code> 和物件 <code>map</code> 中的每一項進行運算，藉由變數 <code>$item</code> 動態帶入每一項進入迴圈</p>
<ul>
<li><p>@each 語法</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@each</span> <span class="variable">$var</span> in <span class="string">&quot;map or list&quot;</span> &#123;...&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>list 範例</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* in scss */</span></span><br><span class="line"><span class="variable">$list</span>: (red, blue, green);</span><br><span class="line"></span><br><span class="line"><span class="keyword">@each</span> <span class="variable">$item</span> in <span class="variable">$list</span> &#123;</span><br><span class="line">  .#($item)&#123;</span><br><span class="line">    <span class="attribute">background</span>: <span class="variable">$item</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* in css */</span></span><br><span class="line"><span class="selector-class">.red</span> &#123;<span class="attribute">background</span>: red&#125;;</span><br><span class="line"><span class="selector-class">.blue</span> &#123;<span class="attribute">background</span>: blue&#125;;</span><br><span class="line"><span class="selector-class">.green</span> &#123;<span class="attribute">background</span>:green&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>map 範例，此方式也是 Bootstrap 在管理變數的方法</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* in scss */</span></span><br><span class="line"><span class="variable">$theme-colors</span>: (</span><br><span class="line">  <span class="string">&#x27;primary&#x27;</span>: blue,</span><br><span class="line">  <span class="string">&#x27;secondary&#x27;</span>: <span class="number">#6c757d</span>;</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">@each</span> <span class="variable">$name</span>, <span class="variable">$value</span> in <span class="variable">$theme-colors</span> &#123;</span><br><span class="line">  <span class="selector-class">.bg-</span>#&#123;<span class="variable">$name</span>&#125; &#123;</span><br><span class="line">    <span class="attribute">background</span>: <span class="variable">$value</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* in css */</span></span><br><span class="line"><span class="selector-class">.bg-primary</span> &#123;<span class="attribute">background</span>: blue;&#125;</span><br><span class="line"><span class="selector-class">.bg-secondary</span> &#123;<span class="attribute">background</span>: <span class="number">#6c757d</span>;&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="for-迴圈"><a href="#for-迴圈" class="headerlink" title="@for 迴圈"></a>@for 迴圈</h3><p>Sass 的 <code>@for</code> 迴圈運算與一般程式語言的 <code>for</code> 概念相同，須給定 “起始值” 和 “終止值”</p>
<ul>
<li><p><code>@for</code> 語法</p>
<ul>
<li><p><code>$i</code> 變數代表 <code>map</code> 和 <code>list</code> 項目的編號</p>
</li>
<li><p><code>$begin</code> 和 <code>$end</code> 代表起始值與終止值</p>
</li>
<li><p>關鍵字 <code>through</code> 代表從包含起始值到終止值，使用 <code>to</code> 的話則從 “起始值” 開始到 “終止值前一位”</p>
</li>
</ul>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@for</span> <span class="variable">$i</span> from <span class="variable">$begin</span> through <span class="variable">$end</span> &#123;...&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>@for 範例: 產生顏色漸變</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* in scss */</span></span><br><span class="line"><span class="keyword">@for</span> <span class="variable">$i</span> from <span class="number">1</span> through <span class="number">3</span>&#123;</span><br><span class="line">  <span class="selector-class">.box-</span>#&#123;<span class="variable">$i</span>&#125;&#123;</span><br><span class="line">    <span class="attribute">background-color</span>: darken(white, <span class="variable">$i</span> * <span class="number">10%</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* in css */</span></span><br><span class="line"><span class="selector-class">.box-1</span> &#123;<span class="attribute">background-color</span>: <span class="number">#e6e6e6</span>;&#125;</span><br><span class="line"><span class="selector-class">.box-2</span> &#123;<span class="attribute">background-color</span>: <span class="number">#cccccc</span>;&#125;</span><br><span class="line"><span class="selector-class">.box-3</span> &#123;<span class="attribute">background-color</span>: <span class="number">#b3b3b3</span>;&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>Scss</category>
      </categories>
      <tags>
        <tag>Scss</tag>
      </tags>
  </entry>
  <entry>
    <title>錯誤的CSS設計方式</title>
    <url>/css/20190925/1185305d/</url>
    <content><![CDATA[<p>在這篇文章中紀錄一下在撰寫 CSS 時，常見的錯誤方式與不利於後續開發的壞習慣</p>
<span id="more"></span>

<h2 id="CSS-範圍性常見錯誤"><a href="#CSS-範圍性常見錯誤" class="headerlink" title="CSS 範圍性常見錯誤"></a>CSS 範圍性常見錯誤</h2><p>如果是滿足大部分的樣式盡量寫在外層，其餘的差異再由內層覆蓋，避免重複撰寫相同樣式</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="css">    <span class="comment">/* 滿足大部分樣式 */</span></span></span><br><span class="line"><span class="css">    <span class="selector-tag">body</span> &#123;<span class="attribute">font-family</span>: serif&#125;</span></span><br><span class="line"><span class="css">    <span class="selector-class">.wrap</span> &#123;<span class="attribute">font-size</span>: <span class="number">16px</span>&#125;</span></span><br><span class="line"><span class="css">    <span class="comment">/* 其餘差異 */</span></span></span><br><span class="line"><span class="css">    <span class="selector-class">.footer</span> &#123;<span class="attribute">font-size</span>: <span class="number">12px</span>&#125;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;wrap&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;header&quot;</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">p</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;footer&quot;</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">p</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="請勿將高度寫死"><a href="#請勿將高度寫死" class="headerlink" title="請勿將高度寫死"></a>請勿將高度寫死</h2><p>如果將外層高度寫死，當內層高度超過時就無法自適應延伸而破圖，此問題在 <code>RWD</code> 尤為明顯，盡量使用 <code>margin</code> 和 <code>padding</code> 產生相對空間</p>
<h2 id="請勿使用-ID-綁定網頁樣式"><a href="#請勿使用-ID-綁定網頁樣式" class="headerlink" title="請勿使用 ID 綁定網頁樣式"></a>請勿使用 ID 綁定網頁樣式</h2><p>ID 具備 “錨點功能” ，可以定位網頁位置，也具備 “唯一性” ，如果使用 ID 模組化，後續開發就無法重複使用，且權重較高，新增樣式時難以覆蓋</p>
<h2 id="解決-JS-與-CSS-功能互相干擾"><a href="#解決-JS-與-CSS-功能互相干擾" class="headerlink" title="解決 JS 與 CSS 功能互相干擾"></a>解決 JS 與 CSS 功能互相干擾</h2><p>將 JS 效果的 class 與樣式分離，好處在於修改 CSS 樣式時不用連同 JS 一併更改，瀏覽 HTML 時，也可以清楚找到有 JS 效果的位置，可以使用前贅詞 <code>js-</code> 區分</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;button&quot;</span> <span class="attr">class</span>=<span class="string">&quot;btn btn-primary js-hello&quot;</span> <span class="attr">value</span>=<span class="string">&quot;hello&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>CSS</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>Git 入門</title>
    <url>/git/20190926/a121fcd1/</url>
    <content><![CDATA[<p>Git 為分散式版本控制系統，最初目的是為更好地管理Linux核心開發而設計</p>
<span id="more"></span>

<h2 id="Git-簡介"><a href="#Git-簡介" class="headerlink" title="Git 簡介"></a>Git 簡介</h2><h3 id="什麼是「分散式」"><a href="#什麼是「分散式」" class="headerlink" title="什麼是「分散式」?"></a>什麼是「分散式」?</h3><p>「分散式」版本控制系統也需要一台專用的伺服器，但即使沒有伺服器或網路的情況下也可以在本地端進行版本更新，等待網路或伺服器恢復再上傳更新，不會受到影響，而 Git 大部分也可以在自己電腦完成</p>
<h3 id="什麼是「版本控制系統」"><a href="#什麼是「版本控制系統」" class="headerlink" title="什麼是「版本控制系統」 ?"></a>什麼是「版本控制系統」 ?</h3><p>「版本控制系統」指的是會幫你紀錄每次檔案的新增、刪除與修改的狀態變化，並同時記錄修改者的名稱與修改時間，因此檔案出錯時可以輕易找出原因，如果檔案損毀也可以回到上一個紀錄重新來過</p>
<h3 id="速度快、檔案體積小"><a href="#速度快、檔案體積小" class="headerlink" title="速度快、檔案體積小"></a>速度快、檔案體積小</h3><p>過去要記錄每次檔案的版本往往是複製整個新的檔案或是只複製每個版本的差異，因此檔案會隨著不斷的修改與更新而使檔案大小日益增加</p>
<p>Git 在版本控制上不同的地方在於使用「快照」（snapshot），如同相機拍照般拍下每式的版本內容，在開啟某個版本時，並非拼湊每次的更新差異，而是各個檔案自動搜尋選擇的版本以前各自最新的檔案版本，因此速度上也較快</p>
<h2 id="常用的終端機指令"><a href="#常用的終端機指令" class="headerlink" title="常用的終端機指令"></a>常用的終端機指令</h2><h3 id="目錄切換"><a href="#目錄切換" class="headerlink" title="目錄切換"></a>目錄切換</h3><p>輸入 <code>cd</code> 加一個空格後，目錄位置可以複製按右鍵貼上，或是直接將目錄拖曳進終端機</p>
<p>切換到目錄 (絕對路徑)</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">cd</span> /目錄</span><br></pre></td></tr></table></figure>

<p>切換到目錄 (相對路徑)</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">cd</span> 目錄</span><br></pre></td></tr></table></figure>

<p>往上一層目錄移動</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">cd</span> ..</span><br></pre></td></tr></table></figure>

<p>切換到使用者的 <code>home</code> 目錄中的 <code>project</code> 裡的 <code>cards</code> 目錄，”~” 表示 <code>home</code> 目錄</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">cd</span> ~/project/cards</span><br></pre></td></tr></table></figure>

<p>展開列表</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ls</span><br></pre></td></tr></table></figure>

<h3 id="檔案與目錄"><a href="#檔案與目錄" class="headerlink" title="檔案與目錄"></a>檔案與目錄</h3><p>建立目錄</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ mkdir <span class="string">&quot;目錄名稱&quot;</span></span><br></pre></td></tr></table></figure>

<p>建立檔案</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ touch <span class="string">&quot;檔案名稱&quot;</span></span><br></pre></td></tr></table></figure>

<p>移除檔案名稱</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ rm <span class="string">&quot;檔案名稱&quot;</span></span><br></pre></td></tr></table></figure>

<p>刪除所有的 html 檔案</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ rm *.html </span><br></pre></td></tr></table></figure>

<p>把檔案 a.html 複製一份成 b.html</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ cp a.html b.html</span><br></pre></td></tr></table></figure>

<p>把檔案 a.html 更名成 b.html</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ mv a.html b.html</span><br></pre></td></tr></table></figure>

<h2 id="Git-環境設定"><a href="#Git-環境設定" class="headerlink" title="Git 環境設定"></a>Git 環境設定</h2><h3 id="使用者設定"><a href="#使用者設定" class="headerlink" title="使用者設定"></a>使用者設定</h3><p>設定使用者的名稱與信箱，每台電腦只有第一次需要設定</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git config --global user.email <span class="string">&quot;your email&quot;</span></span><br><span class="line">$ git config --global user.name <span class="string">&quot;your name&quot;</span></span><br></pre></td></tr></table></figure>

<p>查詢使用者設定列表</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git config --list</span><br></pre></td></tr></table></figure>

<p>輸入以上指令後會出現許多資訊，如要確認使用者資訊，只須尋找以下兩點</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">user.name = <span class="string">&quot;your name&quot;</span></span><br><span class="line">user.email = <span class="string">&quot;your email&quot;</span></span><br></pre></td></tr></table></figure>

<h3 id="每個專案設定不同作者"><a href="#每個專案設定不同作者" class="headerlink" title="每個專案設定不同作者"></a>每個專案設定不同作者</h3><p>前面新增的 <code>--global</code> 參數為全域設定，如需將特定專案設定不同作者，在專案開始前的 <code>git</code> 設定，加上 <code>--local</code> 參數</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git config --<span class="built_in">local</span> user.email <span class="string">&quot;your email&quot;</span></span><br><span class="line">$ git config --<span class="built_in">local</span> user.name <span class="string">&quot;your name&quot;</span></span><br></pre></td></tr></table></figure>

<p>查詢 Git 版本</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git version</span><br></pre></td></tr></table></figure>

<h2 id="Git-流程圖"><a href="#Git-流程圖" class="headerlink" title="Git 流程圖"></a>Git 流程圖</h2><p>基礎的操作方式為先將要追蹤的檔案使用 <code>add</code> 新增索引，接著使用 <code>commit</code> 提交更新紀錄，最後再將更新的紀錄使用 <code>push</code> 上傳至 “遠端數據庫”，更進階的指令後面會再一一介紹</p>
<p><img src="git-progress.png"></p>
<h2 id="Git-基礎操作"><a href="#Git-基礎操作" class="headerlink" title="Git 基礎操作"></a>Git 基礎操作</h2><h3 id="安裝遠端-本地端數據庫"><a href="#安裝遠端-本地端數據庫" class="headerlink" title="安裝遠端/本地端數據庫"></a>安裝遠端/本地端數據庫</h3><p>安裝本地端數據庫 : 先使用 <code>cd</code> 指令到要建立數據庫的目錄，再建立數據庫</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">cd</span> </span><br><span class="line">$ git init</span><br></pre></td></tr></table></figure>

<p>安裝遠端數據庫 : 先使用 <code>cd</code> 指令到要建立數據庫的目錄，再建立數據庫</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">cd</span> </span><br><span class="line">$ git <span class="built_in">clone</span> <span class="string">&quot;遠端數據庫網址&quot;</span></span><br></pre></td></tr></table></figure>

<p>建立數據庫後會出現 <code>master</code> (版本紀錄的主軸)</p>
<h3 id="基礎操作流程"><a href="#基礎操作流程" class="headerlink" title="基礎操作流程"></a>基礎操作流程</h3><p>將所有檔案加入索引(暫存區)</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git add . </span><br><span class="line">$ git add --all </span><br></pre></td></tr></table></figure>

<p><strong>以上兩者的差異為何 ?</strong><br><code>git add .</code> 只會將當下這個目錄與其子目錄加入索引，但這個目錄以外的檔案不會，而 <code>git add--all</code> 就沒有這個問題了，不管在哪執行都會將所有改變加入索引</p>
<p>將單一檔案加入索引</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git add <span class="string">&quot;檔案名稱&quot;</span> </span><br></pre></td></tr></table></figure>

<p>查詢狀態，包含新增、刪除與修改</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git status </span><br></pre></td></tr></table></figure>

<p>將加入索引的檔案提交更新紀錄</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git commit -m <span class="string">&quot;更新紀錄名稱&quot;</span> </span><br></pre></td></tr></table></figure>

<p>更新紀錄查詢</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">log</span> </span><br></pre></td></tr></table></figure>

<p>推送數據到遠端數據庫</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git push </span><br></pre></td></tr></table></figure>

<h3 id="gitignore-忽略檔案"><a href="#gitignore-忽略檔案" class="headerlink" title=".gitignore 忽略檔案"></a>.gitignore 忽略檔案</h3><p>新增 .gitignore 檔案</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ mkdir .gitginore </span><br></pre></td></tr></table></figure>

<p><strong>開啟 <code>.gitignore</code> 檔案，輸入要忽略的檔案</strong><br>忽略 index.html</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">index.html</span><br></pre></td></tr></table></figure>

<p>忽略所有 html</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">*.html</span><br></pre></td></tr></table></figure>

<p>忽略整個 css 資料夾</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">css/</span><br></pre></td></tr></table></figure>

<h3 id="工作狀態還原"><a href="#工作狀態還原" class="headerlink" title="工作狀態還原"></a>工作狀態還原</h3><p>取消所有檔案的索引</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git reset HEAD</span><br></pre></td></tr></table></figure>

<p>取消單一檔案的索引</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git reset HEAD <span class="string">&quot;檔案名稱&quot;</span></span><br></pre></td></tr></table></figure>

<p>還原所有檔案至最新的 commit 狀態</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git reset --hard</span><br></pre></td></tr></table></figure>

<p>還原單一檔案至最新的 commit 狀態</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git checkout <span class="string">&quot;檔案名稱&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="Git-分支-branch"><a href="#Git-分支-branch" class="headerlink" title="Git 分支(branch)"></a>Git 分支(branch)</h2><h3 id="HEAD"><a href="#HEAD" class="headerlink" title="HEAD"></a>HEAD</h3><p><code>HEAD</code> 代表目前所在的位置，預設的位置為 <code>master</code>，藉由切換 <code>HEAD</code>，可以瀏覽其他的 <code>commit</code> 紀錄</p>
<p>每個 <code>commit</code> 都有自己的代碼，藉由輸入代碼的前四碼或以上可以切換到該 <code>commit</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git checkout <span class="string">&quot;前四碼&quot;</span></span><br></pre></td></tr></table></figure>

<p>切換 <code>HEAD</code> 回到 <code>master</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git checkout master</span><br></pre></td></tr></table></figure>

<h3 id="建立分支"><a href="#建立分支" class="headerlink" title="建立分支"></a>建立分支</h3><p>在多人協作或已上線程式時，為避免更新時將 <code>master</code> 改壞，所以會先新增 <code>branch</code> 進行版本更新，確認更新內容無誤後再將 <code>branch</code> 合併回 <code>master</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git branch <span class="string">&quot;分支名稱&quot;</span></span><br></pre></td></tr></table></figure>

<p>分支查詢 : 如果新增一支名為 <code>feature</code> 的分支，使用分支查詢後就會出現 <code>feature</code> 與 <code>master</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git branch</span><br><span class="line">  master</span><br><span class="line">  feature</span><br></pre></td></tr></table></figure>

<h3 id="合併分支"><a href="#合併分支" class="headerlink" title="合併分支"></a>合併分支</h3><p>先回到 <code>master</code> ，再合併分支</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git merge <span class="string">&quot;分支名稱&quot;</span></span><br></pre></td></tr></table></figure>

<p>如果 <code>master</code> 與分支都有更新，在合併時會出現以下訊息，輸入 <code>:wq!</code> ，即可離開並自動合併，但分支仍保留原先檔案狀態</p>
<p><img src="auto-merge.png"></p>
<h3 id="解決合併分支衝突"><a href="#解決合併分支衝突" class="headerlink" title="解決合併分支衝突"></a>解決合併分支衝突</h3><p>如果再合併分支時，<code>master</code> 與分支在同一行出現不同資料時，就會出現衝突而無法自動合併，這時候只能使用手動合併的方式選擇要保留那些資料，以下是 VS code 出現衝突時的介面</p>
<p><img src="conflict-merge.png"></p>
<h2 id="標籤-tag"><a href="#標籤-tag" class="headerlink" title="標籤 (tag)"></a>標籤 (tag)</h2><p>為特定的 <code>commit</code> 加上標籤，例如重要的版本，可以使切換版本上更為輕鬆</p>
<p>新增輕量標籤</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git tag <span class="string">&quot;標籤名稱&quot;</span></span><br></pre></td></tr></table></figure>

<p>新增標示標籤，備註內容外的 “” 需要保留</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git tag -am <span class="string">&quot;備註內容&quot;</span> <span class="string">&quot;標籤名稱&quot;</span></span><br></pre></td></tr></table></figure>

<p>查詢標籤</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git tag</span><br></pre></td></tr></table></figure>

<p>查詢詳細標籤</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git tag -n</span><br></pre></td></tr></table></figure>

<p>切換到標籤的 commit</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git checkout <span class="string">&quot;標籤名稱&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="暫存-stash"><a href="#暫存-stash" class="headerlink" title="暫存 (stash)"></a>暫存 (stash)</h2><p>如遇突發狀況時，暫存可將修改的內容先記錄起來，再切換到其他分支作業，作業完後切換回來並還原暫存即可繼續原來的工作</p>
<p>暫時儲存當前目錄</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git stash</span><br></pre></td></tr></table></figure>

<p>還原暫存</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git stash pop</span><br></pre></td></tr></table></figure>

<p>清除最新暫存</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git stash drop</span><br></pre></td></tr></table></figure>

<p>清除全部暫存</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git stash clear</span><br></pre></td></tr></table></figure>

<p>瀏覽暫存列表</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git stash list</span><br></pre></td></tr></table></figure>

<p>如果有多筆暫存資料，輸入瀏覽指令後會出現以下列表</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">stash@&#123;0&#125;: WIP on master: 710851f update</span><br><span class="line">stash@&#123;1&#125;: WIP on master: 710851f update</span><br></pre></td></tr></table></figure>

<p>還原最近的指令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git stash apply</span><br></pre></td></tr></table></figure>

<p>還原第 n 筆暫存內容，n 從 0 開始</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git stash apply stash@&#123;n&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Git、Github-團隊協作"><a href="#Git、Github-團隊協作" class="headerlink" title="Git、Github 團隊協作"></a>Git、Github 團隊協作</h2><h3 id="9-1-推送新的分支到遠端數據庫"><a href="#9-1-推送新的分支到遠端數據庫" class="headerlink" title="9-1 推送新的分支到遠端數據庫"></a>9-1 推送新的分支到遠端數據庫</h3><p>查詢遠端數據庫名稱，預設的名稱為 <code>origin</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git remote</span><br><span class="line">  origin</span><br></pre></td></tr></table></figure>

<p>修改遠端數據庫名稱</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git remote rename <span class="string">&quot;原名稱(origin)&quot;</span> <span class="string">&quot;新名稱&quot;</span></span><br></pre></td></tr></table></figure>

<p>推送分支到遠端數據庫</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git push <span class="string">&quot;遠端數據庫名稱(預設為 origin)&quot;</span> <span class="string">&quot;分支名稱&quot;</span></span><br></pre></td></tr></table></figure>

<p><strong>為什麼不能直接使用 git push ?</strong><br>因為如果沒有指定遠端主機與要推送的分支，電腦無法確定要推送到遠端主機 (github)、正式主機 (terminal)或測試主機 (本地端)，且預設推送的分支為 <code>master</code>，所以要指定分支名稱</p>
<h3 id="下載遠端數據庫"><a href="#下載遠端數據庫" class="headerlink" title="下載遠端數據庫"></a>下載遠端數據庫</h3><p>如果沒有先從遠端數據庫載下最新版本就開始作業，上傳遠端數據庫時會發生衝突，必須先下載最新本進行合併</p>
<p>首先介紹 <code>fetch</code> ，<code>fetch</code> 就是當遠端數據庫進度超前本地端進度時，使用這個指令會從線上將本地端沒有的內容抓下來，一樣也有 <code>HEAD</code> 和 <code>master</code>，但會在前面加上 <code>origin</code>(遠端數據庫預設名稱)，變成 <code>origin/HEAD</code> 和 <code>origin/master</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git fetch</span><br></pre></td></tr></table></figure>

<p>接著將抓下來的超前內容合併到本地端分支</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git merge</span><br></pre></td></tr></table></figure>

<p><strong>git pull = git fetch + git merge</strong><br>下載遠端數據庫合併到本地端數據庫</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git pull</span><br></pre></td></tr></table></figure>

<h2 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h2><p><a href="https://nvie.com/posts/a-successful-git-branching-model/">A successful Git branching model</a><br><a href="https://backlog.com/git-tutorial/tw/intro/intro1_1.html">連猴子都能懂的 Git 入門指南</a><br><a href="https://gitbook.tw/chapters/introduction/about-this-book.html">為你自己學 Git</a></p>
]]></content>
      <categories>
        <category>Git</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript入門篇 - JavaScript簡介與環境</title>
    <url>/javascript/20190926/e0d364c/</url>
    <content><![CDATA[<p>作為前端領域最強勢的語言，<code>JavaScript</code> 隨著新版本演進，功能日趨完善，也不斷出現如 <code>jQuery</code> 與 <code>Vue.js</code> 等開源框架，讓 <code>JavaScript</code> 成為學習前端不可或缺的技能 ; 在這篇文章中，將透過 <code>VSCode</code> 來做為開發的編輯器，並部屬基礎環境設定</p>
<span id="more"></span>

<h2 id="JavaScript-環境設定"><a href="#JavaScript-環境設定" class="headerlink" title="JavaScript 環境設定"></a>JavaScript 環境設定</h2><p>有時候我們會利用 JavaScript 新增 HTML 樣板到 HTML 檔案中，而 VSCode 預設在 JavaScript 檔案中是無法使用 <code>Emmet</code> 的，因此我們要在 VSCode 的設定中的 <code>setting.json</code> 加上以下文字</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in setting.json </span></span><br><span class="line">&quot;emmet.includeLanguages&quot;: &#123;</span><br><span class="line">  &quot;javascript&quot;: &quot;html&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="建構一個-JavaScript-環境"><a href="#建構一個-JavaScript-環境" class="headerlink" title="建構一個 JavaScript 環境"></a>建構一個 JavaScript 環境</h2><p>如同 CSS 使用 <code>&lt;link&gt;</code> 載入 <code>.css</code> 檔案或使用 <code>&lt;style&gt;&lt;/style&gt;</code> 在內部直接撰寫 CSS，JavaScript 是使用 <code>&lt;script&gt;&lt;/script&gt;</code>，而又因為 HTML 是 “由上而下” 讀取，JS 時常會操作當中的 <code>DOM 元素</code>，所以 JS 要放在 “網頁內容之後” 以避免出現目標元素還未渲染而造成讀取錯誤的情況</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;content&quot;</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 網頁內容 --&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">          </span><br><span class="line">  <span class="comment">&lt;!-- 載入 .js 檔案 --&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;js/all.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">&lt;!-- 直接撰寫 JS --&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">    alert(<span class="string">&#x27;Hello World!&#x27;</span>);</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="HTML、CSS、JavaScript-的關聯性"><a href="#HTML、CSS、JavaScript-的關聯性" class="headerlink" title="HTML、CSS、JavaScript 的關聯性"></a>HTML、CSS、JavaScript 的關聯性</h2><ul>
<li><code>HTML</code> - 內容 : HTML 負責網頁的內容，為網頁的骨架</li>
<li><code>CSS</code> - 樣式 : CSS 負責網頁的樣式，替網站添加裝飾</li>
<li><code>JavaScript</code> - 行為 : JavaScript 負責網頁的行為，可以動態操作 HTML、CSS，也可以連接外部的伺服器與資料庫，與使用者互動</li>
</ul>
<h2 id="ID-與-textContent"><a href="#ID-與-textContent" class="headerlink" title="ID 與 textContent"></a>ID 與 textContent</h2><h3 id="ID"><a href="#ID" class="headerlink" title="ID"></a>ID</h3><ul>
<li><p><code>ID 選擇器</code> 具備 “錨點功能”，也就是能定位在網頁上，如果指定到該錨點，會在網址後方加上該 <code>ID</code> 名稱</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;* 選定前網址 *&#x2F; </span><br><span class="line">https:&#x2F;&#x2F;jerry-yeh.github.io&#x2F;website&#x2F;html&#x2F;JavaScript&#x2F;JavaScript-入門篇.html</span><br><span class="line"></span><br><span class="line">&#x2F;* 選定後網址 *&#x2F; </span><br><span class="line">https:&#x2F;&#x2F;jerry-yeh.github.io&#x2F;website&#x2F;html&#x2F;JavaScript&#x2F;JavaScript-入門篇.html#ID</span><br></pre></td></tr></table></figure></li>
<li><p>在 JS 中可以透過以下語法選取 ID</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.getElementById(<span class="string">&#x27;ID 名稱&#x27;</span>).textContent(<span class="string">&#x27;修改後的內容&#x27;</span>);</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="textContent"><a href="#textContent" class="headerlink" title="textContent"></a>textContent</h3><p>在 JS 中可以透過此語法選取 <code>DOM 元素</code> 的內容</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.getElementById(<span class="string">&#x27;ID 名稱&#x27;</span>).textContent(<span class="string">&#x27;修改後的內容&#x27;</span>);</span><br></pre></td></tr></table></figure>

<h2 id="直譯器流程介紹"><a href="#直譯器流程介紹" class="headerlink" title="直譯器流程介紹"></a>直譯器流程介紹</h2><p>瀏覽器接收到網頁後，就會由上而下開始解析網頁內容，當解析到 JS <code>&lt;script&gt;&lt;/script&gt;</code> 的部份時，會透過瀏覽器的引擎來將我們寫的 JS 程式語言轉換成電腦認識的語言，而這中介的引擎稱為直譯器 <code>interpreter</code>，如 <code>Chrome</code> 的 <code>V8</code> 引擎，而解析完後瀏覽器才會繼續渲染網頁內容</p>
<h2 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h2><p><a href="https://www.udemy.com/javascript-learning/">六角課程 - JavaScript 入門篇 - 學徒的試煉</a><br><a href="https://zh.wikipedia.org/wiki/JavaScript">JavaScript Wiki</a></p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript入門篇 - function</title>
    <url>/javascript/20190926/b767e9f3/</url>
    <content><![CDATA[<p>函式 <code>function</code> 指的是將一段程式指令包裝起來，可以重複使用且維護容易，也不會互相干擾</p>
<span id="more"></span>

<h2 id="定義函式的方式"><a href="#定義函式的方式" class="headerlink" title="定義函式的方式"></a>定義函式的方式</h2><h3 id="函式宣告（Function-Declaration"><a href="#函式宣告（Function-Declaration" class="headerlink" title="函式宣告（Function Declaration)"></a>函式宣告（Function Declaration)</h3><p>函式宣告為最常見的寫法</p>
<ul>
<li><p>函式最面前使用 <code>function</code> 進行宣告</p>
</li>
<li><p>接著是函式的名稱 ，但也有可能沒有名稱</p>
</li>
<li><p>再來是小括號 <code>( )</code> 的部分，稱為參數 <code>arguments</code>，參數可以不只一個，參數之間使用逗號 , 隔開</p>
</li>
<li><p>最後是大括號 <code>&#123; &#125;</code> 的部分，內含需要重複執行的內容，是函式的主要功能區塊</p>
</li>
<li><p>透過呼叫函式名稱來使用函式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> 函式名稱 (<span class="params">參數<span class="number">1</span>, 參數<span class="number">2</span></span>)</span>&#123;</span><br><span class="line">  重複執行的內容</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 呼叫函式</span></span><br><span class="line">函式名稱 (參數<span class="number">1</span>, 參數<span class="number">2</span>);</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="函式表達式-Function-Expressions"><a href="#函式表達式-Function-Expressions" class="headerlink" title="函式表達式 (Function Expressions)"></a>函式表達式 (Function Expressions)</h3><p>函式運算式是透過等號 <code>=</code> 將函式指定給某個變數</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> 變數名稱 = <span class="function"><span class="keyword">function</span> (<span class="params">參數</span>)</span>&#123; 重複執行的內容 &#125;;</span><br></pre></td></tr></table></figure>

<p>這裡可以發現在等號 <code>=</code> 後面的函式 “沒有函式名稱”，因為這裡的函式名稱只有在自己 “函式內的區塊” 有效，脫離此函式後，函式名稱就不存在，使用此函式則是直接呼叫變數名稱</p>
<h2 id="變數的有效範圍-Scope"><a href="#變數的有效範圍-Scope" class="headerlink" title="變數的有效範圍 Scope"></a>變數的有效範圍 <code>Scope</code></h2><p>變數分為 “全域變數” 和 “區域變數”，切分變數有效範圍的最小單位是 <code>function</code>，我們透過以下程式碼來解釋他們的差異</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 全域變數</span></span><br><span class="line"><span class="keyword">var</span> x = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">count</span>(<span class="params">y</span>)</span>&#123;</span><br><span class="line">  <span class="comment">// 區域變數</span></span><br><span class="line">  <span class="keyword">var</span> x = <span class="number">100</span>;</span><br><span class="line">  <span class="keyword">return</span> x + y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( count(<span class="number">50</span>) );   <span class="comment">// 150</span></span><br><span class="line"><span class="built_in">console</span>.log( x );   <span class="comment">// 1</span></span><br></pre></td></tr></table></figure>

<ul>
<li>在函式內宣告的變數稱為 區域變數，區域變數只有在函式執行的時候存在</li>
<li>在函式外宣告的變數稱為 全域變數，因為區域變數只屬於函式內，所以最後的 console.log( x ); 自然是全域變數的 1</li>
<li>所以函式內外兩個變數 x 其實是不同的變數</li>
</ul>
<h3 id="如何取得函式內的值"><a href="#如何取得函式內的值" class="headerlink" title="如何取得函式內的值 ?"></a>如何取得函式內的值 ?</h3><p>透過在函式外宣告變數，在函式內賦值，在呼叫函式後，我們就可以取得函式內的值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 全域變數</span></span><br><span class="line"><span class="keyword">var</span> total;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">count</span>(<span class="params">numOne, numTwo</span>)</span>&#123;</span><br><span class="line">  total = numOne + numTwo;</span><br><span class="line">  <span class="built_in">console</span>.log(total + <span class="string">&quot;元&quot;</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">count(<span class="number">8</span>, <span class="number">10</span>);   <span class="comment">// 18元</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(total);   <span class="comment">// 18</span></span><br></pre></td></tr></table></figure>

<h3 id="沒有使用-var-宣告的變數很危險"><a href="#沒有使用-var-宣告的變數很危險" class="headerlink" title="沒有使用 var 宣告的變數很危險"></a>沒有使用 <code>var</code> 宣告的變數很危險</h3><p>如果全域變數已經有賦值，函式內的變數沒有使用 <code>var</code> 宣告，就會向外層尋找同名的變數，直到最外層的 “全域變數”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 全域變數</span></span><br><span class="line"><span class="keyword">var</span> x = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">doSomeThing</span> (<span class="params">y</span>)</span>&#123;</span><br><span class="line">  x = <span class="number">100</span>;</span><br><span class="line">  <span class="keyword">return</span> x + y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( doSomeThing(<span class="number">50</span>) );   <span class="comment">// 150</span></span><br><span class="line"><span class="built_in">console</span>.log( x );   <span class="comment">// 100</span></span><br></pre></td></tr></table></figure>

<h2 id="全域物件-window"><a href="#全域物件-window" class="headerlink" title="全域物件 window"></a>全域物件 window</h2><p>所有宣告的全域變數其實都是 “全域物件(頂級物件) window “的屬性，在瀏覽器的 <code>console</code> 輸入 <code>window</code>，我們可以看到所有功能和已經宣告的全域變數</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="built_in">console</span>.log( <span class="built_in">window</span>.a );   <span class="comment">// 10 </span></span><br></pre></td></tr></table></figure>

<p><img src="window.png"></p>
<h2 id="提升-hoisting"><a href="#提升-hoisting" class="headerlink" title="提升 hoisting"></a>提升 <code>hoisting</code></h2><h3 id="變數的提升"><a href="#變數的提升" class="headerlink" title="變數的提升"></a>變數的提升</h3><p>對於已經選告的變數，JS 會自動將其提升至最上方，但要注意的是，提升的只有 “變數的宣告” ，”不包括變數的值”，所以變數還是盡量都在最上面宣告完後再開始使用</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in .js </span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">doSomeThing</span> (<span class="params">y</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log( x );   <span class="comment">// undefined</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">var</span> x = <span class="number">100</span>;</span><br><span class="line">  <span class="keyword">return</span> x + y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 瀏覽器解讀</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">doSomeThing</span> (<span class="params">y</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> x;   <span class="comment">// 變數宣告提升至 scope 最上方</span></span><br><span class="line">  <span class="built_in">console</span>.log ( x );  <span class="comment">// undefined</span></span><br><span class="line">  </span><br><span class="line">  x = <span class="number">100</span>;</span><br><span class="line">  <span class="keyword">return</span> x + y;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="函式的提升"><a href="#函式的提升" class="headerlink" title="函式的提升"></a>函式的提升</h3><h4 id="函式宣告"><a href="#函式宣告" class="headerlink" title="函式宣告"></a>函式宣告</h4><p>與變數不同的地方是使用 函式宣告 定義的函式會連同內容完全提升，所以可以在宣告前呼叫使用</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">square(<span class="number">2</span>);   <span class="comment">// 4</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">square</span>(<span class="params">num</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> num * num;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="函式運算式"><a href="#函式運算式" class="headerlink" title="函式運算式"></a>函式運算式</h4><p>但透過 “函式運算式” 定義的函式則會出現 “錯誤”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">square(<span class="number">2</span>);   <span class="comment">// TypeError: square is not a function</span></span><br><span class="line">              </span><br><span class="line"><span class="keyword">var</span> square = <span class="function"><span class="keyword">function</span> (<span class="params">num</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> num * num;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="回傳-return"><a href="#回傳-return" class="headerlink" title="回傳 return"></a>回傳 <code>return</code></h2><p>透過 <code>return</code> 可以把值回傳給函式，再透過呼叫函式取得值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getHamPrice</span>(<span class="params">num</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> hamPrice = <span class="number">50</span>;</span><br><span class="line">  <span class="keyword">var</span> total = hamPrice * num;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> total;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> Tom = getHamPrice(<span class="number">20</span>);   <span class="comment">// 1000</span></span><br><span class="line"><span class="keyword">var</span> John = getHamPrice(<span class="number">50</span>);   <span class="comment">// 2500</span></span><br></pre></td></tr></table></figure>

<h2 id="function-計算機案例"><a href="#function-計算機案例" class="headerlink" title="function 計算機案例"></a><code>function</code> 計算機案例</h2><p><a href="https://codepen.io/Jerry-Yeh/pen/JQBxmr?editors=0010">範例程式碼</a></p>
<ul>
<li>首先透過 <code>.value</code> 取得內部的值，取得的值為字串 <code>string</code></li>
<li>再透過 <code>.parseInt</code> 將取得的值由字串 <code>string</code> 轉為數字 <code>number</code></li>
<li>最後將數字運算完後透過 <code>.textContent</code> 帶入</li>
</ul>
<h2 id="結論"><a href="#結論" class="headerlink" title="結論"></a>結論</h2><p>變數有效範圍 <code>scope</code> 的最小單位是 <code>function</code><br>即使是在 <code>function</code> 內，沒有使用 <code>var</code> 宣告的變數仍會變成 “全域變數”<br>“全域變數” 指的是全域物件(頂層物件) <code>window</code> 的 “屬性”</p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JaveScript入門篇 - 變數</title>
    <url>/javascript/20190926/b2bfbc85/</url>
    <content><![CDATA[<p>變數為 JS 用來儲存資料和進行運算的基本單位，可以視為一個存放資料的盒子，透過 <code>var</code> 來宣告，<code>ES6</code> 開始可以使用 <code>let</code> 和 <code>const</code> 來宣告，關於 <code>ES6</code> 於之後的文章再作介紹</p>
<span id="more"></span>

<h2 id="變數的規則"><a href="#變數的規則" class="headerlink" title="變數的規則"></a>變數的規則</h2><ul>
<li><p>開頭不能是數字</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> 3k = <span class="number">30</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>不能使用中線 <code>-</code> 和點 <code>.</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> box-number = <span class="number">30</span>;</span><br><span class="line"><span class="keyword">var</span> box.number = <span class="number">30</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>不能使用關鍵字</p>
<p><a href="https://www.w3schools.com/js/js_reserved.asp">JS 關鍵字列表</a></p>
</li>
<li><p>大小寫有區分，變數 <code>app</code> 不等於 <code>App</code></p>
</li>
<li><p>盡量使用語意化命名，能透過名稱了解功能</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> price = <span class="number">30</span>;</span><br><span class="line"><span class="keyword">var</span> total = <span class="number">100</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>變數名稱是可以使用非英文(中文)的，但在其他語言系統可能會出現亂碼，所以還是盡量避免</p>
</li>
</ul>
<h2 id="變數的資料型別"><a href="#變數的資料型別" class="headerlink" title="變數的資料型別"></a>變數的資料型別</h2><p>變數本身沒有型別，變數的值才有，我們可以使用 <code>typeof</code> 運算子來判斷型別，型別分成基本型別 <code>string</code>、<code>number</code>、<code>boolean</code>、<code>null</code>、<code>undefined</code>，除了以上幾種型別，其他可以歸類為物件型別 <code>object</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typeof</span>(<span class="literal">true</span>);   <span class="comment">// boolean</span></span><br><span class="line"><span class="keyword">typeof</span>(<span class="string">&#x27;name&#x27;</span>);   <span class="comment">// string</span></span><br><span class="line"><span class="keyword">typeof</span>(<span class="number">123</span>);   <span class="comment">// number</span></span><br><span class="line"><span class="keyword">typeof</span>(&#123; &#125;);   <span class="comment">// object</span></span><br><span class="line"><span class="keyword">typeof</span>([ ]);   <span class="comment">// object</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 特例</span></span><br><span class="line"><span class="keyword">typeof</span>(<span class="built_in">window</span>.alert);   <span class="comment">// function</span></span><br><span class="line"><span class="keyword">typeof</span>(<span class="literal">null</span>);   <span class="comment">//object</span></span><br></pre></td></tr></table></figure>

<p>由以上結果可以看到 <code>function</code> 這個型別，但 <code>function</code> 其實不是一種獨立的型別，<code>function</code> 仍然是屬於 <code>object</code><br>而 <code>null</code> 反而是不屬於 <code>object</code>，而是獨立的 <code>null</code> 型別，詳細可以參考以下資料</p>
<p><a href="https://javascriptrefined.io/null-and-typeof-9330e475d272">Null and typeof</a><br><a href="https://2ality.com/2013/10/typeof-null.html">The history of “typeof null”</a></p>
<ul>
<li><h3 id="數字-number"><a href="#數字-number" class="headerlink" title="數字 number"></a>數字 number</h3><p>分為整數和浮點數(有小數)，可以透過加 <code>+</code> 、減 <code>-</code>、乘 <code>*</code>、除 <code>/</code> 來做運算</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">var</span> b = <span class="number">10.32</span>;</span><br><span class="line"><span class="keyword">var</span> c = <span class="number">3</span> + <span class="number">4</span>;</span><br></pre></td></tr></table></figure></li>
<li><h3 id="字串-string"><a href="#字串-string" class="headerlink" title="字串 string"></a>字串 string</h3><ul>
<li><p>字串必須用單引號 <code>&#39;&#39;</code> 或雙引號 <code>&quot;&quot;</code> 將其包住，而兩者不可混用</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str1 = <span class="string">&quot;字串1&quot;</span>;</span><br><span class="line"><span class="keyword">var</span> str2 = <span class="string">&quot;字串2&quot;</span>;</span><br><span class="line"><span class="keyword">var</span> str3 = <span class="string">&quot;字串3&#x27;;  // error</span></span><br></pre></td></tr></table></figure></li>
<li><p>如果內容出現單引號 ‘，就只能用雙引號 <code>&quot;&quot;</code> 包住，若非用不可，可以使用反斜線 <code>\</code> 跳脫字元 (escape character)</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str = <span class="string">&#x27;Let&#x27;</span>s go!<span class="string">&#x27;;  // error</span></span><br><span class="line"><span class="string">var str = &quot;Let&#x27;</span>s go!<span class="string">&quot;; // OK</span></span><br><span class="line"><span class="string">var str = &#x27;Let\&#x27;s go!&#x27;;  // OK</span></span><br></pre></td></tr></table></figure></li>
<li><p>可以使用加號 <code>+</code> 來組合字串</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str = <span class="string">&#x27;Hello&#x27;</span>+<span class="string">&#x27;World&#x27;</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>多行字串可以透過反斜線 <code>\</code> 來換行繼續，注意 每行的反斜線 <code>\</code> 之後不能有任何東西，包括 <code>空白</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str = <span class="string">&#x27;第一行 \</span></span><br><span class="line"><span class="string">第二行 \</span></span><br><span class="line"><span class="string">第三行&#x27;</span>;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><h3 id="布林值-boolean"><a href="#布林值-boolean" class="headerlink" title="布林值 boolean"></a>布林值 <code>boolean</code></h3><p>分為 true 和 false 兩種，通常用在判斷式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="literal">true</span>;</span><br><span class="line"><span class="keyword">var</span> b = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> c = (<span class="number">100</span> &gt; <span class="number">10</span>);  <span class="comment">//true </span></span><br></pre></td></tr></table></figure></li>
<li><h3 id="空值-null"><a href="#空值-null" class="headerlink" title="空值 null"></a>空值 <code>null</code></h3><p>代表曾經有值或沒有值，但現在此變數沒有值</p>
</li>
<li><h3 id="未定義-undefined"><a href="#未定義-undefined" class="headerlink" title="未定義 undefined"></a>未定義 <code>undefined</code></h3><p>變數的宣告分成兩個步驟，”宣告” 與 “賦值”，當已經宣告變數但還未賦值時，就會出現 <code>undefined</code></p>
</li>
<li><h3 id="物件-object"><a href="#物件-object" class="headerlink" title="物件 object"></a>物件 <code>object</code></h3><p>除了以上型別，其他都可以歸類為物件型別，關於物件型別後續會在介紹</p>
</li>
</ul>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JaveScript入門篇 - 陣列(Array)與物件(Object)</title>
    <url>/javascript/20190926/63428deb/</url>
    <content><![CDATA[<p>在前兩篇關於變數的文章中有提到，在 JavaScript 中，除了基本型別以外都可以歸類為物件型別，在這篇文章中就來介紹最基本的物件型別，陣列 <code>Array</code> 與物件 <code>Object</code></p>
<span id="more"></span>

<h2 id="物件-Object"><a href="#物件-Object" class="headerlink" title="物件 Object"></a>物件 <code>Object</code></h2><ul>
<li><p>物件可以是零至多個屬性 <code>property</code> 的集合</p>
</li>
<li><p>每個屬性 <code>property</code> 都有自己的值 <code>value</code></p>
</li>
<li><p>值可以是基本型別、另一個物件 <code>object</code> 、陣列 <code>array</code> 或函數 <code>function</code></p>
</li>
<li><p>使用大括號 <code>&#123; &#125;</code> 建立起新的物件</p>
</li>
<li><p>屬性 property 使用冒號 <code>:</code> 隔開值 <code>value</code>，而值 <code>value</code> 結尾使用逗號 <code>,</code> 連接下一個屬性 <code>property</code></p>
</li>
<li><p>屬性 <code>property</code> 可以透過 “點” <code>.</code> 來存取</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> farm = &#123;</span><br><span class="line">  farmer: <span class="string">&quot;卡斯伯&quot;</span>,   <span class="comment">// string</span></span><br><span class="line">  dogs: [<span class="string">&#x27;張姆士&#x27;</span>, <span class="string">&#x27;龐的&#x27;</span>],   <span class="comment">// array(object)</span></span><br><span class="line">  chicken: <span class="number">15</span>,   <span class="comment">// number</span></span><br><span class="line">  duck: <span class="number">3</span>,</span><br><span class="line">  poultryTotal: <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> num = farm.chicken + farm.duck;</span><br><span class="line">    <span class="built_in">console</span>.log(num);</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">&quot;001&quot;</span>: <span class="string">&quot;Hello&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(farm.farmer);   <span class="comment">// 卡斯伯</span></span><br><span class="line">farm.poultryTotal();   <span class="comment">// 18</span></span><br></pre></td></tr></table></figure></li>
<li><p>屬性也可以透過中括號 <code>[ ]</code> 來存取，好處是遇到不合法的 JavaScript 的識別字 (如帶有空白的字串或是數字) 時，不會出現錯誤</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">farm<span class="number">.001</span>;   <span class="comment">// SyntaxError: Unexpected number</span></span><br><span class="line">farm[<span class="string">&quot;001&quot;</span>];   <span class="comment">// &quot;Hello&quot;</span></span><br></pre></td></tr></table></figure></li>
<li><p>屬性 <code>property</code> 可以透過 <code>=</code> “新增” 與 <code>delete</code> “刪除”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123; &#125;;</span><br><span class="line"></span><br><span class="line">obj.name = <span class="string">&#x27;123&#x27;</span>;   <span class="comment">// 新增</span></span><br><span class="line"></span><br><span class="line">obj.name;   <span class="comment">// &#x27;123&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">delete</span> obj.name;   <span class="comment">// 刪除</span></span><br><span class="line"></span><br><span class="line">obj.name;   <span class="comment">// 刪除屬性後變成 undefined</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="判斷屬性是否存在"><a href="#判斷屬性是否存在" class="headerlink" title="判斷屬性是否存在"></a>判斷屬性是否存在</h3><p>判斷屬性是存在最簡單的方式就是檢查該屬性是不是 <code>undefined</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;&#125;;</span><br><span class="line">          </span><br><span class="line"><span class="built_in">console</span>.log( obj.name );   <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>

<p>但如果該屬性的值剛好是 <code>undefined</code>，此方法就無效，除了此方法，還有 <code>in</code> 運算子和 <code>hasOwnProperty</code> 方法</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  name: <span class="string">&#x27;Object&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 透過 in 檢查屬性</span></span><br><span class="line"><span class="built_in">console</span>.log( <span class="string">&#x27;name&#x27;</span> <span class="keyword">in</span> obj );     <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log( <span class="string">&#x27;value&#x27;</span> <span class="keyword">in</span> obj );    <span class="comment">// false</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 透過 hasOwnProperty() 方法檢查</span></span><br><span class="line">obj.hasOwnProperty(<span class="string">&#x27;name&#x27;</span>);       <span class="comment">// true</span></span><br><span class="line">obj.hasOwnProperty(<span class="string">&#x27;value&#x27;</span>);      <span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<h3 id="列舉物件-for-in"><a href="#列舉物件-for-in" class="headerlink" title="列舉物件 (for ... in)"></a>列舉物件 <code>(for ... in)</code></h3><p>另一個判斷物件內有什麼屬性的方法就是使用列舉 <code>(for ... in)</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Person = &#123;</span><br><span class="line">  name: <span class="string">&#x27;Jerry&#x27;</span>,</span><br><span class="line">  age: <span class="number">18</span>,</span><br><span class="line">  bwh: &#123;</span><br><span class="line">    strength: <span class="number">30</span>,</span><br><span class="line">    agility: <span class="number">31</span>,</span><br><span class="line">    intelligence: <span class="number">32</span></span><br><span class="line">  &#125;,</span><br><span class="line">  single: <span class="literal">true</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (prop <span class="keyword">in</span> Person)&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(Person[prop]);   <span class="comment">// Jerry 18 &#123;strength: 30, agility: 31, intelligence: 32&#125; true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="陣列-Array"><a href="#陣列-Array" class="headerlink" title="陣列 Array"></a>陣列 <code>Array</code></h2><ul>
<li><p>陣列可以是零至多個元素的集合</p>
</li>
<li><p>陣列內可以是 原始的資料類型、其他陣列 <code>array</code>、函式<code>function</code> 或物件 <code>object</code></p>
</li>
<li><p>使用中括號 <code>[ ]</code> 建立起新的陣列</p>
</li>
<li><p>每個元素以逗號 <code>,</code> 隔開</p>
</li>
<li><p>可以透過 <code>.length</code> 來取得陣列長度，而陣列長度是 可以被覆寫 的，如下原來的陣列長度為 3，透過 <code>a.length = 1</code> 將陣列長度改成 1 後，後面的元素就被移除了，之後再透過 <code>a.length = 3</code> 將陣列長度改回 3，後面被移除的元素也只會使用 <code>undefined</code> 來填補</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = [<span class="string">&quot;apple&quot;</span>, <span class="string">&quot;boy&quot;</span>, <span class="string">&quot;cat&quot;</span>];</span><br><span class="line">a.length;    <span class="comment">// 3</span></span><br><span class="line"></span><br><span class="line">a.length = <span class="number">1</span>;</span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// [&quot;apple&quot;]</span></span><br><span class="line"></span><br><span class="line">a.length = <span class="number">3</span>;</span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// [&quot;apple&quot;, undefined, undefined]</span></span><br></pre></td></tr></table></figure></li>
<li><p>陣列的索引是從 <code>0</code> 開始計算的</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">array = [<span class="string">&quot;a&quot;</span>, <span class="string">&quot;b&quot;</span>, <span class="string">&quot;c&quot;</span>];</span><br><span class="line">array[<span class="number">0</span>] = <span class="string">&quot;a&quot;</span>;</span><br></pre></td></tr></table></figure></li>
<li><p><code>.push(&#39;要新增的資料&#39;)</code> 新增資料</p>
<p>透過 <code>.push()</code> 可以在陣列末端新增新元素</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> array = [<span class="string">&#x27;a&#x27;</span>, <span class="string">&#x27;b&#x27;</span>, <span class="string">&#x27;c&#x27;</span>];</span><br><span class="line"></span><br><span class="line">array.push(<span class="string">&#x27;d&#x27;</span>);</span><br><span class="line"><span class="built_in">console</span>.log(array);   <span class="comment">// [&#x27;a&#x27;, &#x27;b&#x27;, &#x27;c&#x27;, &#x27;d&#x27;]</span></span><br></pre></td></tr></table></figure></li>
<li><p><code>.splice(&#39;從第幾筆資料開始&#39;,&#39;要刪除幾筆資料&#39;)</code> 刪除資料</p>
<p>透過 <code>.splice()</code> 可以刪除陣列中指定資料以後的資料</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> array = [<span class="string">&#x27;a&#x27;</span>, <span class="string">&#x27;b&#x27;</span>, <span class="string">&#x27;c&#x27;</span>]</span><br><span class="line"></span><br><span class="line">array.splice(<span class="string">&#x27;1&#x27;</span>,<span class="string">&#x27;2&#x27;</span>);   <span class="comment">// [&#x27;b&#x27;, &#x27;c&#x27;]</span></span><br><span class="line"><span class="built_in">console</span>.log(array);   <span class="comment">// [&#x27;a&#x27;]</span></span><br></pre></td></tr></table></figure></li>
<li><p>可以直接指定改變元素</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> array = [<span class="string">&#x27;a&#x27;</span>, <span class="string">&#x27;b&#x27;</span>, <span class="string">&#x27;c&#x27;</span>];</span><br><span class="line"></span><br><span class="line">array[<span class="number">1</span>] = <span class="string">&#x27;B&#x27;</span>;</span><br><span class="line"><span class="built_in">console</span>.log(array);   <span class="comment">// [&#x27;a&#x27;, &#x27;B&#x27;, &#x27;c&#x27;]</span></span><br></pre></td></tr></table></figure></li>
<li><p>除了 <code>.push</code> 外，陣列還有許多語法，可以參考以下連結</p>
<p><a href="https://developer.mozilla.org/zh-TW/docs/Web/JavaScript/Reference/Global_Objects/Array">Array 語法</a></p>
</li>
<li><p>使用 <code>typeof</code> 來判斷陣列和物件都會顯示 <code>object</code>，我們可以使用 <code>.inArray</code> 來判斷此變數是陣列而非物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Array</span>.isArray([]);            <span class="comment">// true</span></span><br><span class="line"><span class="built_in">Array</span>.isArray([<span class="number">1</span>]);           <span class="comment">// true</span></span><br><span class="line"><span class="built_in">Array</span>.isArray(<span class="keyword">new</span> <span class="built_in">Array</span>());   <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">Array</span>.isArray();              <span class="comment">// false</span></span><br><span class="line"><span class="built_in">Array</span>.isArray(&#123;&#125;);            <span class="comment">// false</span></span><br><span class="line"><span class="built_in">Array</span>.isArray(<span class="literal">null</span>);          <span class="comment">// false</span></span><br><span class="line"><span class="built_in">Array</span>.isArray(<span class="literal">undefined</span>);     <span class="comment">// false</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="陣列-物件"><a href="#陣列-物件" class="headerlink" title="陣列 + 物件"></a>陣列 + 物件</h2><p>陣列和物件時常會混和著使用，陣列透過中括號 <code>[ ]</code>，而物件透過點 <code>.</code> 來撈出資料</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> farms = [</span><br><span class="line">  &#123;</span><br><span class="line">    dogs: [<span class="string">&#x27;張姆士&#x27;</span>, <span class="string">&#x27;龐的&#x27;</span>],</span><br><span class="line">    chick: <span class="number">15</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    dogs: [<span class="string">&#x27;皮皮&#x27;</span>],</span><br><span class="line">    chick: <span class="number">30</span></span><br><span class="line">  &#125;</span><br><span class="line">];</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(farm[<span class="number">1</span>].dogs[<span class="number">0</span>]);   <span class="comment">// &#x27;皮皮&#x27;</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JSON概述</title>
    <url>/json/20190927/b2a33033/</url>
    <content><![CDATA[<p>JSON 常用來作為資料傳遞的檔案格式，這篇文章針對 JSON 做一個簡單的概述</p>
<span id="more"></span>

<h2 id="資料來源"><a href="#資料來源" class="headerlink" title="資料來源"></a>資料來源</h2><p><a href="https://zh.wikipedia.org/wiki/JSON">JSON Wiki</a><br><a href="https://ithelp.ithome.com.tw/articles/10159700">Day13– 前端小字典三十天【每日一字】– JSON</a><br><a href="https://data.kcg.gov.tw/">高雄市政府資料開放平台</a><br><a href="https://opendata.epa.gov.tw/Data/Contents/RainTenMin/">行政院環境保護署</a></p>
<h2 id="JSON-是什麼"><a href="#JSON-是什麼" class="headerlink" title="JSON 是什麼 ?"></a>JSON 是什麼 ?</h2><ul>
<li><code>JSON</code> 是一種輕量級的 資料交換語言，但其並非一種新的程式語言，而是一種 資料傳輸的格式</li>
<li>透過 JS 的物件 <code>Object &#123;&#125;</code> 和 陣列 <code>Array []</code> 所構成</li>
<li>目前很多程式語言都可以支援 <code>JSON</code> 的格式，其附檔名為 <code>.json</code></li>
</ul>
<h2 id="JSON-view-chrome-插件"><a href="#JSON-view-chrome-插件" class="headerlink" title="JSON view chrome 插件"></a>JSON view chrome 插件</h2><p>如果是 chrome 的使用者，可以下載 <code>JSON view</code> 這個插件，它能將 <code>JSON</code> 檔案整理成易讀的格式</p>
<p><a href="https://chrome.google.com/webstore/detail/jsonview/chklaanhfefbnpoihckbnefhakgolnmc?hl=zh-TW">JSON view</a></p>
<h2 id="JSON-的載入方式"><a href="#JSON-的載入方式" class="headerlink" title="JSON 的載入方式"></a>JSON 的載入方式</h2><p>將未經 <code>JSON view</code> 處理過的格式複製下來( 未經插件處理過的資料在載入 JS 中比較不會出問題 )，存入到一個變數中，就可以透過物件、陣列的擷取方式來擷取資料</p>
<p><a href="https://codepen.io/Jerry-Yeh/pen/ydRqNy?editors=0010">範例程式碼</a></p>
]]></content>
      <categories>
        <category>JSON</category>
      </categories>
      <tags>
        <tag>JSON</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS 設計模式</title>
    <url>/css/20190927/6900c966/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>隨著網站逐漸開發，CSS 的程式碼也會越來越繁雜，如果沒有一套方法來管理 CSS，不僅在維護上不方便，程式碼也會變得難以重複使用</p>
<span id="more"></span>

<h3 id="為什麼我們需要-CSS-設計模式"><a href="#為什麼我們需要-CSS-設計模式" class="headerlink" title="為什麼我們需要 CSS 設計模式 ?"></a>為什麼我們需要 CSS 設計模式 ?</h3><p>如同收納術一般，當我們生活用品越積越多時，我們就需要一些收納的方法來管理，以便在尋找需要的用品時更容易<br>同樣的，在開發一個網站時，CSS 也會越寫越多，甚至是多人一起偕同開發，這時候如果沒有一套共同的方法或標準來管理我們的 CSS，就會使的開發與維護上效率低落，合作上也不容易快速理解別人的程式，甚至是之後自己看自己的程式碼也無法快速的理解當初的思考邏輯</p>
<h3 id="CSS-設計模式的特色"><a href="#CSS-設計模式的特色" class="headerlink" title="CSS 設計模式的特色"></a>CSS 設計模式的特色</h3><p>常見的 CSS 設計模式有 SMACSS、OOCSS 和 BEM，其目的都是為了達到命名語意化、可重用性高、後期維護容易和加載渲染快等優點</p>
<p>但方法是死的，提高工作效率才是唯一目的，如同我們不會為了整理好一個桌面而運用到畢生所學的 20 種收納方法吧 ?同樣的在運用 css 設計模式時，也是視各個專案的狀況來選擇要使用的模式，而非一味遵守這些方法使開發效率未獲提升</p>
<h3 id="良好的-CSS-設計模式"><a href="#良好的-CSS-設計模式" class="headerlink" title="良好的 CSS 設計模式"></a>良好的 CSS 設計模式</h3><p>一個良好的 CSS 設計模式會具備以下特質</p>
<ul>
<li>預測性 - Predictable</li>
<li>複用性 - Reusable</li>
<li>維護性 - Maintainable</li>
<li>延展性 - Scalable</li>
</ul>
<h2 id="SMACSS-Scalable-and-Modular-Architecture-for-CSS"><a href="#SMACSS-Scalable-and-Modular-Architecture-for-CSS" class="headerlink" title="SMACSS (Scalable and Modular Architecture for CSS)"></a>SMACSS (Scalable and Modular Architecture for CSS)</h2><h3 id="SMACSS-介紹"><a href="#SMACSS-介紹" class="headerlink" title="SMACSS 介紹"></a>SMACSS 介紹</h3><p>SMACSS 顧名思義是將 CSS 模組化並提高可重用性，被模組化的 CSS 是獨立的，不會被父元素或是 ID 選擇器、標籤選擇器所綁定，能夠活用在頁面上各個地方，並將 CSS 結構分類</p>
<h3 id="SMACSS-分類"><a href="#SMACSS-分類" class="headerlink" title="SMACSS 分類"></a>SMACSS 分類</h3><p>SMACSS 分類會使用每個類別的前綴詞加以區分，例如 layout 裡面的 list 會寫成 <code>.l-list</code>，以提高辨識度快速搜尋相關 CSS</p>
<ul>
<li><h4 id="Base-全站設定"><a href="#Base-全站設定" class="headerlink" title="Base : 全站設定"></a>Base : 全站設定</h4><p>定義大部分的 CSS ，避免一直重複撰寫相同的 CSS 樣式，包含 CSS Reset、Reboot，以下為常見的全站設定<br>注意 Base 的 CSS 絕對不能使用 <code>!important</code> ，以免後續新增的 CSS 無法覆蓋</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">html&#123;font-family:sans-serif&#125;</span><br><span class="line">a &#123;<span class="attr">color</span>: red;&#125;</span><br><span class="line">...</span><br></pre></td></tr></table></figure></li>
<li><h4 id="Layout-網站布局"><a href="#Layout-網站布局" class="headerlink" title="Layout : 網站布局"></a>Layout : 網站布局</h4><p>所有頁面都會用到的內容，例如 header 與 footer，重複出現的內容以 “功能” 取代 “語意”，例如 書籍列表 與 DVD列表不會個別是 <code>.bookList</code> 與 <code>.dvdList</code>，而是統一為 <code>.list</code></p>
</li>
<li><h4 id="Module-模組化"><a href="#Module-模組化" class="headerlink" title="Module : 模組化"></a>Module : 模組化</h4><p>Module 是一個獨立的 CSS 組件，可以靈活運用在頁面的任何地方，不會綁定父元素、ID 選擇器和 HTML 標籤，直接將所有 CSS 寫在 class 上，不論要更動 HTML或新增 CSS 都會更有彈性<br>如下為一個名為 <code>.fld</code> 的模組，而兩個 <code>span</code> 隸屬於 <code>.fld</code> ，所以都以 <code>.fld-</code>做為開頭，在依據不同的需求各別寫入不同的 CSS，因為不是直接寫成 <code>.fld &gt; span</code> 所以後續新增的 <code>span</code> ，也不會受到影響而需要用更高的權重覆蓋</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;fld&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">&quot;fld-name&quot;</span>&gt;</span>Folder Name<span class="tag">&lt;/<span class="name">span</span>&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">&quot;fld-items&quot;</span>&gt;</span>(32 items)<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>為了方便 HTML 語意化調整，CSS 盡量不指定 HTML tag，以下為常見寫法</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.l-grid</span> &#123;</span><br><span class="line">  <span class="selector-class">.l-gird</span> <span class="selector-tag">h2</span> &#123;...&#125;</span><br><span class="line">  <span class="selector-class">.l-gird</span> <span class="selector-tag">ul</span> &#123;...&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以寫成</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.l-grid</span> &#123;</span><br><span class="line">  <span class="selector-class">.l-gird-title</span> &#123;...&#125;</span><br><span class="line">  <span class="selector-class">.l-gird-list</span> &#123;...&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>此做法也可以優化 SEO，如下 <code>article</code> 的權重會大於 <code>div</code></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">article</span> <span class="attr">class</span>=<span class="string">&quot;.l-gird&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">h2</span> <span class="attr">class</span>=<span class="string">&quot;l-gide-title&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">article</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;l-gird&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">h3</span> <span class="attr">class</span>=<span class="string">&quot;l-gide-title&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">h3</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure></li>
<li><h4 id="State-狀態"><a href="#State-狀態" class="headerlink" title="State : 狀態"></a>State : 狀態</h4><p>State 表達的是一個狀態，與 layout、 module 搭配，也常常利用 JavaScript 動態改變狀態，加上 <code>!important</code> 是因為有時候模組會有好幾層架構，防止 State 權重不足無法覆蓋</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.is-error</span>&#123;<span class="attribute">display</span>: block <span class="meta">!important</span>&#125;  </span><br><span class="line"><span class="selector-class">.is-hidden</span>&#123;<span class="attribute">display</span>:none <span class="meta">!important</span>&#125;</span><br></pre></td></tr></table></figure></li>
<li><h4 id="Theme-主題"><a href="#Theme-主題" class="headerlink" title="Theme : 主題"></a>Theme : 主題</h4><p>運用在網站結構相同，但有多種的主題樣式時，Theme 不會在核心 CSS(all.css) 內，通常都會將其抽離(theme.css)，常見的變動有 color、background-color、border …等</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">//in html</span><br><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">&quot;stylesheet&quot;</span> <span class="attr">href</span>=<span class="string">&quot;all.css&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">&quot;stylesheet&quot;</span> <span class="attr">href</span>=<span class="string">&quot;theme.css&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">//in all<span class="selector-class">.css</span></span><br><span class="line"><span class="selector-class">.mod</span> &#123;<span class="attribute">border</span>: <span class="number">1px</span> solid&#125;</span><br><span class="line"></span><br><span class="line">//in theme<span class="selector-class">.css</span></span><br><span class="line"><span class="selector-class">.mod</span> &#123;<span class="attribute">border</span>: red&#125;</span><br></pre></td></tr></table></figure></li>
<li><h4 id="Typography-文字排版"><a href="#Typography-文字排版" class="headerlink" title="Typography : 文字排版"></a>Typography : 文字排版</h4><p>建議不要超過 6 種文字大小，以免網站失焦，在建立全域變數(Base)，可以將文字納入規則</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">//in base</span></span><br><span class="line"><span class="variable">$font-size-base</span>: <span class="number">14px</span> !default;</span><br><span class="line"><span class="variable">$font-l</span>: (<span class="variable">$font-size-base</span>) * <span class="number">1.25</span>;</span><br><span class="line"><span class="variable">$font-m</span>: <span class="variable">$font-size-base</span>;</span><br><span class="line"><span class="variable">$font-s</span>: (<span class="variable">$font-size-base</span>) * <span class="number">0.75</span>;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="OOCSS-Object-Oriented-CSS"><a href="#OOCSS-Object-Oriented-CSS" class="headerlink" title="OOCSS (Object Oriented CSS)"></a>OOCSS (Object Oriented CSS)</h2><p>為鼓勵開發者使 CSS 複用性達到最高，用一套 CSS 即可持續開發，不需要隨著開發新增 CSS</p>
<h3 id="結構與樣式分離-Separate-Structure-and-Skin"><a href="#結構與樣式分離-Separate-Structure-and-Skin" class="headerlink" title="結構與樣式分離 (Separate Structure and Skin)"></a>結構與樣式分離 (Separate Structure and Skin)</h3><p>以 bootstrap 為例，<code>btn</code> 只包含結構，<code>btn-primary</code>提供顏色，<code>btn-lg</code> 增加大小，這樣的好處在於不用重複寫結構，只需要依據開發需求新增樣式即可</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;btn btn-primary btn-lg&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>或是常用到小技巧也可以使它成為獨立的樣式</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">.clearfix &#123;overflow: hidden;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="容器與內容分離-Separate-Container-and-Content"><a href="#容器與內容分離-Separate-Container-and-Content" class="headerlink" title="容器與內容分離 (Separate Container and Content)"></a>容器與內容分離 (Separate Container and Content)</h3><p>常見的容器有 <code>header</code>、<code>footer</code>和格線系統等，與內容分離的好處在於內容不只能使用在特定容器，可以提高內容的複用性</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;container&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;row&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;col&quot;</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;btn&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="結論"><a href="#結論" class="headerlink" title="結論"></a>結論</h3><ul>
<li>使用 OOCSS 時，盡量不要用後代選擇器 (.header ul)，以免無法活用到其他 <code>html tag</code></li>
<li>網站規模小時不建議使用，因為設立各個獨立功能會消耗許多時間，後續開發沒有重複用到便失去它的意義</li>
</ul>
<h2 id="BEM-Block-Element-Modifier"><a href="#BEM-Block-Element-Modifier" class="headerlink" title="BEM (Block Element Modifier)"></a>BEM (Block Element Modifier)</h2><p><code>BEM</code> 由 <code>Block</code> (區塊)、<code>Element</code> (元素)、<code>Modifier</code> (修飾符)三者所構成，主義的功能是瀏覽 <code>HTML CODE</code> 時，可以透過 <code>class</code> 直接了解 CSS 架構與彼此的關係，不用去細看 CSS 才能了解</p>
<h3 id="BEM結構"><a href="#BEM結構" class="headerlink" title="BEM結構"></a>BEM結構</h3><ul>
<li><h4 id="Block-區塊"><a href="#Block-區塊" class="headerlink" title="Block (區塊)"></a>Block (區塊)</h4><p>Block 是模組的名稱，位於模組的最外層，為模組的容器，在設計網站時，我們會先設計出幾個區塊，再對其命名對應的語意</p>
<ul>
<li>不能使用 html tag 和 ID 選擇器，否則難以複用到其他地方</li>
<li>Block 名稱能清楚表達含意，使用 “功能” 取代 “語意”</li>
<li>每個 Block 獨立運作，不能依附在其他的 Block</li>
<li>不要定義過多影響外部的樣式 (如 margin)，因為複用到其他地方時不一定有相同的外部樣式</li>
<li>Block 可以複製到頁面上任何位位置，甚至互相嵌套，但不互相依賴</li>
</ul>
<p><img src="block.png"></p>
</li>
<li><h4 id="Element-元素"><a href="#Element-元素" class="headerlink" title="Element (元素)"></a>Element (元素)</h4><p><code>Block</code> 內部會有許多 <code>Element</code> (元素)，在區塊的後方加入雙下底線 <code>__</code> 以辨識此為該去塊內的元素</p>
<p>基本原則 :</p>
<ul>
<li>如果無法拿到 <code>Block</code> 外單獨使用，就應該為 <code>Element</code>，且不能相依於其他的 <code>Block</code></li>
<li><code>Element</code> 能簡單的表達出其結構、布局和意義，在語意上與 <code>Block</code> 相呼應</li>
<li>同一個 <code>Block</code> 的內 <code>Element</code> 與 <code>Element</code> 可以互相嵌套，靈活運用</li>
</ul>
<p><img src="element.png"></p>
</li>
<li><h4 id="Modifier-修飾符"><a href="#Modifier-修飾符" class="headerlink" title="Modifier (修飾符)"></a>Modifier (修飾符)</h4><p><code>Modifier</code> (修飾符)的觀念和 <code>SMACSS</code> 的 <code>State</code>、<code>OOCSS</code> 的樣式很類似，在區塊或元素後方加上雙中線 <code>--</code> 以辨識此為該區塊或元素的修飾符，修飾符改變的可以是狀態或是樣式，圖中 Tab3 具有不同的 modifier</p>
<p>基本原則 :</p>
<ul>
<li>能直觀的表達出其所代表的外觀和狀態</li>
<li>無法脫離 <code>Block</code> 和 <code>Element</code> 單獨使用，因為本身不具備結構</li>
<li>改變的僅是外觀和狀態，並不是替換結構</li>
<li>不會同時使用一個以上不同值的相同 Modifier，例如: <code>--red</code> 和 <code>--green</code></li>
</ul>
<p><img src="modifier.png"></p>
<p>常見表狀態的用法，此用法為布林值(boolean)，拿掉即是原始狀態</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;btn btn--active&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>另一種常見表樣式的用法</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">// 顏色樣式</span><br><span class="line"><span class="selector-class">.btn--primary</span> &#123;...&#125;</span><br><span class="line"><span class="selector-class">.btn--secondary</span> &#123;...&#125;</span><br><span class="line">// 大小樣式</span><br><span class="line"><span class="selector-class">.btn-lg</span> &#123;...&#125;</span><br><span class="line"><span class="selector-class">.btn-m</span> &#123;...&#125;</span><br></pre></td></tr></table></figure>

<p>不同值的樣式可以同時使用</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;btn btn-primary btn-lg&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="連接符"><a href="#連接符" class="headerlink" title="連接符"></a>連接符</h3><ul>
<li><code>-</code> 中線 : 僅連接區塊的多單詞</li>
<li><code>__</code> 雙底線 : 用來連接 <code>Block</code> (區塊)與 <code>Element</code> (元素)</li>
<li><code>--</code> 雙中線 : 用來描述 <code>Block</code> (區塊)或 <code>Element</code> (元素)的狀態 (Modifier)</li>
</ul>
<h3 id="BEM-常見的問題"><a href="#BEM-常見的問題" class="headerlink" title="BEM 常見的問題"></a>BEM 常見的問題</h3><ul>
<li><p>可以單獨使用到的功能就不需要 BEM，例如:</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">//清除浮動</span><br><span class="line"><span class="selector-class">.clearfix</span> &#123;over-flow: hidden&#125;</span><br><span class="line">//隱藏文字</span><br><span class="line"><span class="selector-class">.text-hide</span> &#123;</span><br><span class="line">  <span class="attribute">text-indent</span>: <span class="number">101%</span>;</span><br><span class="line">  over-flow: hidden;</span><br><span class="line">  text-trap: no-wrap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>如果沒有直接的關係，Element 可以不用重複嵌套，盡量避免超過四層，以免降低網頁渲染速度，例如:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">nav</span> <span class="attr">class</span>=<span class="string">&quot;menu&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">ul</span> <span class="attr">class</span>=<span class="string">&quot;menu__list&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span> <span class="attr">class</span>=<span class="string">&quot;menu__list__item&quot;</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">a</span> <span class="attr">class</span>=<span class="string">&quot;menu__list__item__link&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">nav</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>可以寫成</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">nav</span> <span class="attr">class</span>=<span class="string">&quot;menu&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">ul</span> <span class="attr">class</span>=<span class="string">&quot;menu__list&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span> <span class="attr">class</span>=<span class="string">&quot;menu__item&quot;</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">a</span> <span class="attr">class</span>=<span class="string">&quot;menu__link&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">nav</span>&gt;</span></span><br></pre></td></tr></table></figure></li>
<li><p>沒有直接關係且會複用到頁面其他地方的 Block 應該獨立出來</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;content&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;content__menu&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>可以獨立成</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;content&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;menu&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure></li>
<li><p>增加 <code>Element</code> 和 <code>Modifier</code> 時需要重複撰寫前面的 <code>Block</code> 與 <code>Element</code> 很麻煩 ?</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.menu</span> &#123;...&#125;</span><br><span class="line"><span class="selector-class">.menu__list</span> &#123;...&#125;</span><br><span class="line"><span class="selector-class">.menu__item--active</span> &#123;...&#125;</span><br></pre></td></tr></table></figure>

<p>Sass3 開始支援 <code>&amp;</code> 連接字串可以解決這個問題</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.menu</span> &#123;</span><br><span class="line">&amp;__list &#123;...&#125;</span><br><span class="line">  &amp;__item &#123;</span><br><span class="line">    &amp;--active &#123;...&#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="guideline-指南"><a href="#guideline-指南" class="headerlink" title="guideline (指南)"></a>guideline (指南)</h2><p>當網站架構越來越大或多人協作時，我們就需要一套規範指南來使大家的程式碼具有一致性，以減少不必要的溝通與衝突，提高開發效率</p>
<h3 id="常見的設計規範"><a href="#常見的設計規範" class="headerlink" title="常見的設計規範"></a>常見的設計規範</h3><p>並非每次專案都要遵守所有設計規範，回歸設計規範的目的在於提升加載渲染、開發與維護效率，所以每次的 <code>guideline</code> 都不盡相同，而是視各個專案的複雜程度與參與的開發人員互相溝通而訂</p>
<ul>
<li><p>類別階層不超過 “四” 層為原則</p>
</li>
<li><p>不使用 <code>ID</code>，一律使用 <code>class</code></p>
</li>
<li><p>使用 <code>OOCSS</code> 建立類別工具庫</p>
</li>
<li><p>使用 <code>SMACSS</code> 來建立 Sass 結構</p>
</li>
<li><p>排版結構以 <code>BEM</code> 來開發</p>
</li>
<li><p>導入 BS4 的 <code>Grid System</code></p>
</li>
<li><p>使用 <code>Sass for</code> 來管理擴充元件</p>
</li>
<li><p>一率採用駝峰式命名，利用大小寫區分單詞</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">// 小駝峰式 (開頭小寫)</span><br><span class="line"><span class="selector-class">.listTitle</span> &#123;...&#125;</span><br><span class="line">// 大駝峰式(開頭大寫)</span><br><span class="line"><span class="selector-class">.ListTitle</span> &#123;...&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>導入第三方插件在 <code>Sass import vendor</code></p>
</li>
<li><p>使用 <code>REM</code> 單位</p>
</li>
<li><p>佈局使用 <code>flexbox</code> 進行元件設計</p>
</li>
</ul>
<h3 id="sass-7-1-設計模式"><a href="#sass-7-1-設計模式" class="headerlink" title="sass 7+1 設計模式"></a>sass 7+1 設計模式</h3><ul>
<li><p>base : 全站設定</p>
<p>定義大部分的 CSS ，避免一直重複撰寫相同的 CSS 樣式，概念同於 SMACSS 的 Base<br>注意 base 的 CSS 絕對不能使用 <code>!important</code> ，以免後續新增的 CSS 無法覆蓋</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">_reset.scss      # Reset&#x2F;normalize 樣式重製</span><br><span class="line">_reboot.scss     # 重製部分樣式</span><br><span class="line">_typography.scss # 文字規範</span><br></pre></td></tr></table></figure></li>
<li><p>components : 組件</p>
<p>為獨立的組件，放到頁面上任何位置、容器都可以自適應延伸，概念同於 bootstrap 中的 component</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">_button.scss  # 按鈕</span><br><span class="line">_menu.scss    # 選單</span><br></pre></td></tr></table></figure></li>
<li><p>layout : 排版</p>
<p>控制頁面的版型，彼此具備獨立性</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">_grid.scss   # 格線系統</span><br><span class="line">_header.scss # 表頭</span><br><span class="line">_footer.scss # 表尾</span><br></pre></td></tr></table></figure></li>
<li><p>pages : 個頁設定</p>
<p>當個別頁面出現唯一且複雜的內容時，就可以單獨切分出來管理</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">_index.scss # 首頁</span><br><span class="line">_cart.scss  # 購物車頁</span><br></pre></td></tr></table></figure></li>
<li><p>themes : 主題</p>
<p>工具型的 CSS，例如，<code>.clearfix</code> (清除浮動)</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">_variables.scss  # Sass variables</span><br><span class="line">_functions.scss  # Sass function</span><br><span class="line">_minin.scss      # Sass mixin</span><br><span class="line">_helper.scss     # 工具型的 CSS</span><br></pre></td></tr></table></figure></li>
<li><p>vendors : 套件</p>
<p>如果網站有載入其他外掛套件，可以統一放到這個資料夾</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">_bootstrap.scss  # Bootstrap</span><br><span class="line">_jquery-ui.scss   # jQuery 套件</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="收納"><a href="#收納" class="headerlink" title="收納"></a>收納</h3><p>當網站的 CSS 眾多且複雜，我們使用 Sass 7+1 進行管理時，可以將每個分類建立一個資料夾，將檔案個別放入，並建立一個名為 <code>all.scss</code> 的檔案載入其他所有檔案</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="comment">//in all.scss</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// utils</span></span><br><span class="line"><span class="keyword">@import</span> <span class="string">&quot;utils/_variables.scss&quot;</span>;</span><br><span class="line"><span class="keyword">@import</span> <span class="string">&quot;utils/_mixin.scss&quot;</span>;</span><br><span class="line"><span class="comment">// base</span></span><br><span class="line"><span class="keyword">@import</span> <span class="string">&quot;base/_reset.scss&quot;</span>;</span><br><span class="line"><span class="keyword">@import</span> <span class="string">&quot;base/_typography.scss&quot;</span>;</span><br></pre></td></tr></table></figure>

<h2 id="總結"><a href="#總結" class="headerlink" title="總結"></a>總結</h2><p>所有規範都不是構成網站的必要條件，設計規範的目的終究還是為了提高開發與維護的效率，並讓其他共同開發者能對彼此的程式碼一目了然，如果專案使用到 <code>Sass 7+1</code> ，甚至依照需求可以對分類自行新增或修改，同理其他的規範如 <code>SMACSS</code>、<code>OOCSS</code> 或 <code>BEM</code>，都可以只擷取部分概念使用，再次強調規範是死的，效率才是最終目的 !</p>
]]></content>
      <categories>
        <category>CSS</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript入門篇 - 流程判斷</title>
    <url>/javascript/20190927/2be5d539/</url>
    <content><![CDATA[<p>JS 用來控制流程的條件語法指的是，當指定條件為 <code>true</code>，就會執行後續指令，JS 條件語法分為以兩種 : <code>if...else</code> 和 <code>switch</code></p>
<span id="more"></span>

<h2 id="if…else"><a href="#if…else" class="headerlink" title="if…else"></a>if…else</h2><p>顧名思義，如果怎樣，就執行某事，否則就執行另一件事，而其中的條件會透過 <code>ToBoolean</code> 轉換成 <code>true</code> 或 <code>false</code></p>
<ul>
<li>使用 小括號 <code>()</code> 包住 條件</li>
<li>使用 大括號 <code>&#123;&#125;</code> 包住 條件成立時要執行的事</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="string">&quot;條件&quot;</span>)&#123;</span><br><span class="line">  <span class="comment">// 條件成立執行這件事</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">  <span class="comment">// 否則執行這件事</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>除了 <code>if</code> 和 <code>else</code>，還可以透過 <code>else if</code> 增加條件，增加條件可以不只一個，但為了維護與可讀性，建議不要過度增加條件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="string">&quot;條件1&quot;</span>)&#123;</span><br><span class="line">  <span class="comment">// 條件成立執行這件事</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;條件2&quot;</span>)&#123;</span><br><span class="line">  <span class="comment">// 以上條件都不成立就執行這件事</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;條件2&quot;</span>)&#123;</span><br><span class="line">  <span class="comment">// 以上條件都不成立就執行這件事</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">  <span class="comment">// 以上條件都不成立就執行這件事</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="switch"><a href="#switch" class="headerlink" title="switch"></a>switch</h2><p><code>switch</code> 和 <code>if..else</code> 具有相同的功能，但當條件過多時，使用 <code>if...else</code> 會因為需要跑完每個條件而造成維護不易且效能較差</p>
<ul>
<li>同樣使用 小括號 <code>()</code> 包住 條件 但不比較</li>
<li><code>case</code> 後方放要 與條件進行判斷的值</li>
<li>值後使用 冒號 :隔開 條件成立後要執行的指令</li>
<li>每個 <code>case</code> 最後 使用 <code>break;</code> 隔開其他的 <code>case</code>，如果沒有使用 b<code>reak;</code> 隔開，就會執行所有 <code>case</code> 的指令</li>
<li>如果以上所有 <code>case</code> 都不成立，就執行 <code>default</code> 中的指令</li>
</ul>
<h2 id="if…else-與-switch-比較"><a href="#if…else-與-switch-比較" class="headerlink" title="if…else 與 switch 比較"></a>if…else 與 switch 比較</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> month = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// if...else</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>( <span class="built_in">Math</span>.ceil( month/<span class="number">3</span> ) === <span class="number">1</span> )&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;春天&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span>( <span class="built_in">Math</span>.ceil( month/<span class="number">3</span> ) === <span class="number">2</span> )&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;夏天&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span>( <span class="built_in">Math</span>.ceil( month/<span class="number">3</span> ) === <span class="number">3</span> )&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;秋天&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span>( <span class="built_in">Math</span>.ceil( month/<span class="number">3</span> ) === <span class="number">4</span> )&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;冬天&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;月份錯誤&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// switch</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">switch</span> ( <span class="built_in">Math</span>.ceil(month / <span class="number">3</span>))&#123;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&quot;春天&quot;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">2</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&quot;夏天&quot;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">3</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&quot;秋天&quot;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">4</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&quot;冬天&quot;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">default</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&quot;月份錯誤&quot;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果沒有 <code>break</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> month = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">switch</span> ( <span class="built_in">Math</span>.ceil(month/<span class="number">3</span>) )&#123;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;春天&#x27;</span>);</span><br><span class="line">  <span class="keyword">case</span> <span class="number">2</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;夏天&#x27;</span>);</span><br><span class="line">  <span class="keyword">case</span> <span class="number">3</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;秋天&#x27;</span>);</span><br><span class="line">  <span class="keyword">case</span> <span class="number">4</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;冬天&#x27;</span>);</span><br><span class="line">  <span class="keyword">default</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;月份錯誤&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// in console</span></span><br><span class="line"><span class="string">&#x27;春天&#x27;</span></span><br><span class="line"><span class="string">&#x27;夏天&#x27;</span></span><br><span class="line"><span class="string">&#x27;秋天&#x27;</span></span><br><span class="line"><span class="string">&#x27;冬天&#x27;</span></span><br><span class="line"><span class="string">&#x27;月份錯誤&#x27;</span></span><br></pre></td></tr></table></figure>

<p>適當的使用 <code>break</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> month = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">switch</span> (month)&#123;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">  <span class="keyword">case</span> <span class="number">2</span>:</span><br><span class="line">  <span class="keyword">case</span> <span class="number">3</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;春天&#x27;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">4</span>:</span><br><span class="line">  <span class="keyword">case</span> <span class="number">5</span>:</span><br><span class="line">  <span class="keyword">case</span> <span class="number">6</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;夏天&#x27;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">7</span>:</span><br><span class="line">  <span class="keyword">case</span> <span class="number">8</span>:</span><br><span class="line">  <span class="keyword">case</span> <span class="number">9</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;秋天&#x27;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="number">10</span>:</span><br><span class="line">  <span class="keyword">case</span> <span class="number">11</span>:</span><br><span class="line">  <span class="keyword">case</span> <span class="number">12</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;冬天&#x27;</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">default</span>:</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;月份錯誤&#x27;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript入門篇 - 迴圈</title>
    <url>/javascript/20190927/25fb3458/</url>
    <content><![CDATA[<p>迴圈指的是透過 “遞增” 或 “遞減” 來完成退出的條件，使我們不用反覆撰寫相同的程式，在 JS 中，常見的迴圈語法有 <code>for</code> 和 <code>while</code> 兩種</p>
<span id="more"></span>

<h2 id="for-迴圈"><a href="#for-迴圈" class="headerlink" title="for 迴圈"></a>for 迴圈</h2><h3 id="for-基本寫法"><a href="#for-基本寫法" class="headerlink" title="for 基本寫法"></a>for 基本寫法</h3><ul>
<li>使用 <code>for</code> 開頭</li>
<li>接著小括號 <code>()</code> 的內容分為三個部分， 初始值、條件、更新內容</li>
<li>當滿足條件，就會執行 大括號 <code>&#123;&#125;</code> 內的程式碼</li>
<li>當執行完 大括號 <code>&#123;&#125;</code> 內的程式碼，就會 更新值，如果符合條件，就會再次執行 大括號 <code>&#123;&#125;</code> 內的程式碼，如果不符合，就會結束並 跳出迴圈</li>
</ul>
<h3 id="for-與-array-的寫法"><a href="#for-與-array-的寫法" class="headerlink" title="for 與 array 的寫法"></a>for 與 array 的寫法</h3><p>在使用 <code>for</code> 取的陣列中的資料時，可以透過 <code>.length</code> 來取的陣列的長度，以此作為 <code>for</code> 迴圈執行次數的條件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 撈出每個農場的農夫名字</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> farms = [</span><br><span class="line">  &#123;</span><br><span class="line">    farmer: <span class="string">&#x27;卡斯柏&#x27;</span>,</span><br><span class="line">    field: <span class="number">6</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    farmer: <span class="string">&#x27;查理&#x27;</span>,</span><br><span class="line">    field: <span class="number">10</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">var</span> farmsTotal = farms.length;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i =<span class="number">0</span>; i &lt; farmsTotal; i++)&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;第&#x27;</span> + (i+<span class="number">1</span>) + <span class="string">&#x27;個農場主人是&#x27;</span> + farms[i].farmer)</span><br><span class="line">  &#125;;</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="comment">// in console</span></span><br><span class="line">第 <span class="number">1</span> 個農場主人是 卡斯柏</span><br><span class="line">地 <span class="number">2</span> 個農場主人是 查理</span><br></pre></td></tr></table></figure>

<h3 id="for-與-if-的寫法"><a href="#for-與-if-的寫法" class="headerlink" title="for 與 if 的寫法"></a>for 與 if 的寫法</h3><p>透過在 <code>for</code> 中加入 <code>if</code> 可以為撈出的資料設定條件，而非撈出所有資料</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 撈出小雞數量超過 100 隻的農夫</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> farms = [</span><br><span class="line">  &#123;</span><br><span class="line">    farmer: <span class="string">&#x27;卡斯柏&#x27;</span>,</span><br><span class="line">    chick: <span class="number">50</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    famrer: <span class="string">&#x27;約翰&#x27;</span>,</span><br><span class="line">    chick: <span class="number">120</span></span><br><span class="line">  &#125;</span><br><span class="line">];</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> farmsTotal = farms.length;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; farmsTotal; i++)&#123;</span><br><span class="line">  <span class="keyword">if</span> (farms[i].chick &gt; <span class="number">100</span>)&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(farms[i].farmer + <span class="string">&#x27;的小雞超過 150 隻&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// in console</span></span><br><span class="line">約翰 的小雞超過 <span class="number">150</span> 隻</span><br></pre></td></tr></table></figure>

<h3 id="for-加總"><a href="#for-加總" class="headerlink" title="for 加總"></a><code>for</code> 加總</h3><p>透過 <code>+=</code> 我們可以不斷地將前面的項目累加上去</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 將所有香蕉數量加總</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> farms = [</span><br><span class="line">  &#123;</span><br><span class="line">    farmer: <span class="string">&#x27;卡斯柏&#x27;</span>,</span><br><span class="line">    banana: <span class="number">5000</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    farmer: <span class="string">&#x27;查理&#x27;</span>,</span><br><span class="line">    banana: <span class="number">1000</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    farmer: <span class="string">&#x27;約翰&#x27;</span>,</span><br><span class="line">    banana: <span class="number">3215</span></span><br><span class="line">  &#125;</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> farmsTotal = farms.length;</span><br><span class="line"><span class="keyword">var</span> bananaTotal = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; farmsTotal; i++)&#123;</span><br><span class="line">  bananaTotal += farms[i].banana;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&#x27;今年的香蕉採收量為&#x27;</span> + bananaTotal);</span><br><span class="line"></span><br><span class="line"><span class="comment">// in console</span></span><br><span class="line">今年的香蕉採收量為 <span class="number">9215</span></span><br></pre></td></tr></table></figure>

<h2 id="while-迴圈"><a href="#while-迴圈" class="headerlink" title="while 迴圈"></a>while 迴圈</h2><p><code>while</code> 和 <code>for</code> 在原理上大同小異，只是在寫法上有點不同</p>
<ul>
<li>在外層宣告 “初始值”</li>
<li>使用 <code>while</code> 開頭</li>
<li>接著 小括號 <code>()</code> 包住 條件</li>
<li>再接著 大括號 <code>&#123;&#125;</code> 包住 符合條件時執行的內容</li>
<li>符合條件時執行的內容 後才寫 “更新值”</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;初始值&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> ( <span class="string">&quot;條件&quot;</span> )&#123;</span><br><span class="line">  <span class="comment">// 符合條件時執行的內容</span></span><br><span class="line">  <span class="string">&quot;更新值&quot;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="for-與-while-的差異"><a href="#for-與-while-的差異" class="headerlink" title="for 與 while 的差異"></a>for 與 while 的差異</h2><h3 id="適合-for-的情境"><a href="#適合-for-的情境" class="headerlink" title="適合 for 的情境"></a>適合 <code>for</code> 的情境</h3><p>因為 <code>for</code> 包含 <code>初始值</code>、<code>條件</code>、 <code>更新值</code> 三部分，能夠一目了然執行次數，所以更適合用在迴圈執行次數 “明確” 的狀態</p>
<h3 id="適合-while-的情境"><a href="#適合-while-的情境" class="headerlink" title="適合 while 的情境"></a>適合 <code>while</code> 的情境</h3><p>因為 <code>while</code> 只包含 <code>條件</code>，所以較適合用在迴圈執行次數 不明確 的狀態</p>
<h2 id="無窮迴圈"><a href="#無窮迴圈" class="headerlink" title="無窮迴圈"></a>無窮迴圈</h2><p>當條件設定錯誤，不管如何都會符合條件時，就會無限執行內部程式碼，無法跳出迴圈，稱為 “無窮迴圈”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 當 i 不管如何都會大於等於 0</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// for</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &gt;= <span class="number">0</span>; i++)&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(i);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//while</span></span><br><span class="line"><span class="keyword">var</span> i = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">while</span> ( i &gt;= <span class="number">0</span> )&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(i);</span><br><span class="line">  i++;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="break-與-continue"><a href="#break-與-continue" class="headerlink" title="break 與 continue"></a>break 與 continue</h2><h3 id="break-會直接跳出迴圈"><a href="#break-會直接跳出迴圈" class="headerlink" title="break : 會直接跳出迴圈"></a>break : 會直接跳出迴圈</h3><p>找到陣列中第一個 <code>非 0</code> 的數字並印出</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> array = [<span class="number">0</span>,<span class="number">0</span>,<span class="number">7</span>,<span class="number">0</span>,<span class="number">9</span>];</span><br><span class="line"><span class="keyword">var</span> i = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> ( i &lt; array.length )&#123;</span><br><span class="line">  <span class="keyword">if</span> ( array[i] !== <span class="number">0</span> )&#123;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  &#125;;</span><br><span class="line">  i++;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="continue-只會跳過這次，然後繼續執行下一次迴圈"><a href="#continue-只會跳過這次，然後繼續執行下一次迴圈" class="headerlink" title="continue: 只會跳過這次，然後繼續執行下一次迴圈"></a>continue: 只會跳過這次，然後繼續執行下一次迴圈</h3><p>印出 1 ~ 10 的數字但跳過 3 的倍數</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">1</span>; i &lt;= <span class="number">10</span>; i++)&#123;</span><br><span class="line">  <span class="keyword">if</span> ( i % <span class="number">3</span> === <span class="number">0</span> )&#123;</span><br><span class="line">    <span class="keyword">continue</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">console</span>.log(i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript入門篇 - 運算式(Expression)與運算子(Operator)</title>
    <url>/javascript/20190927/d8cdf04e/</url>
    <content><![CDATA[<p>在 JS 語法可以分為兩大類，敘述句 <code>Statement</code> 與 運算式 <code>Expression</code></p>
<span id="more"></span>

<ul>
<li><p>敘述句 <code>Statement</code> :</p>
<p>簡單來說，敘述句就是執行某個動作，如變數的宣告、賦值、迴圈或 <code>if</code> 判斷式等，都可以被歸類於此</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> foo;</span><br></pre></td></tr></table></figure></li>
<li><p>運算式 <code>Expression</code> :</p>
<p>運算式最大的特性是會產生一個 值，如 <code>function</code> 的參數 <code>arguments</code>，或是透過等號 <code>=</code> 賦值時，在等號 <code>=</code> 右側的部分都屬於運算式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span> * <span class="number">10</span>;</span><br></pre></td></tr></table></figure>

<p>如上，在等號 = 右側的 10 * 10 就是屬於運算式</p>
</li>
</ul>
<h2 id="運算子的分類"><a href="#運算子的分類" class="headerlink" title="運算子的分類"></a>運算子的分類</h2><p>運算子的類型很多，常見的可以分為以下幾類，其餘可以操考 <a href="https://developer.mozilla.org/zh-TW/docs/Web/JavaScript/Guide/Expressions_and_Operators">MDN 運算式與運算子</a></p>
<ul>
<li>算術運算子 (Arithmetic Operator)</li>
<li>賦值運算子 (Assignment Operator)</li>
<li>比較運算子 (Comparison Operator)</li>
<li>邏輯運算子 (Logical Operator)</li>
<li>逗號運算子 (Comma Operator)</li>
<li>三元(條件)運算子</li>
</ul>
<h2 id="算術運算子-Arithmetic-Operator"><a href="#算術運算子-Arithmetic-Operator" class="headerlink" title="算術運算子 (Arithmetic Operator)"></a>算術運算子 (Arithmetic Operator)</h2><p>算術運算子包括大家熟知的 四則運算(加、減、乘、除)，但運算後的結果常常會出現一些特例，以下一一介紹</p>
<p><a href="#%E5%8A%A0%E8%99%9F">加號 <code>+</code></a><br><a href="#%E6%B8%9B%E8%99%9F">減號 <code>-</code></a><br><a href="#%E4%B9%98%E8%99%9F">乘號 <code>*</code></a><br><a href="#%E9%99%A4%E8%99%9F">除號 <code>/</code></a><br><a href="#%E5%8F%96%E9%A4%98%E6%95%B8">取餘數 <code>%</code></a><br><a href="#%E9%81%9E%E5%A2%9E-%E8%88%87-%E9%81%9E%E6%B8%9B">遞增 <code>++</code> 與 遞減 <code>--</code></a></p>
<h3 id="加號"><a href="#加號" class="headerlink" title="加號 +"></a>加號 <code>+</code></h3><ul>
<li><p>單純數字的相加</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span> + <span class="number">2</span>;</span><br><span class="line"><span class="built_in">console</span>.log(a);    <span class="comment">// 3</span></span><br></pre></td></tr></table></figure></li>
<li><p>特殊字無限大 <code>Infinity</code>、負無限大 <code>-Infinity</code> 和 <code>Not a Number NaN</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="literal">Infinity</span> + <span class="literal">Infinity</span>      <span class="comment">// Infinity</span></span><br><span class="line">-<span class="literal">Infinity</span> + -<span class="literal">Infinity</span>    <span class="comment">// -Infinity</span></span><br><span class="line">-<span class="literal">Infinity</span> + <span class="literal">Infinity</span>     <span class="comment">// NaN</span></span><br></pre></td></tr></table></figure>

<p>而只要加號 <code>+</code> 兩側其中一個是 <code>NaN</code>，結果就會是 <code>NaN</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="number">10</span> + <span class="literal">NaN</span>            <span class="comment">// NaN</span></span><br><span class="line"><span class="literal">Infinity</span> + <span class="literal">NaN</span>      <span class="comment">// NaN</span></span><br><span class="line">-<span class="literal">Infinity</span> + <span class="literal">NaN</span>     <span class="comment">// NaN</span></span><br></pre></td></tr></table></figure></li>
<li><p>如果加號 <code>+</code> 兩側其中一個是 字串 <code>string</code>，另一端就會 “自動轉型” 為 “字串”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 數字 + 字串 </span></span><br><span class="line"><span class="number">100</span> + <span class="string">&quot;100&quot;</span>      <span class="comment">// &quot;100100&quot;</span></span><br><span class="line"><span class="number">100</span> + <span class="string">&quot;ABC&quot;</span>      <span class="comment">// &quot;100ABC&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 字串 + 字串</span></span><br><span class="line"><span class="string">&quot;ABC&quot;</span> + <span class="string">&quot;XYZ&quot;</span>    <span class="comment">// &quot;ABCXYZ&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 布林 + 字串</span></span><br><span class="line"><span class="literal">true</span> + <span class="string">&quot;abc&quot;</span>     <span class="comment">// &quot;trueabc&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 數字 + 物件</span></span><br><span class="line"><span class="number">100</span> + &#123;&#125;         <span class="comment">// &quot;100[object Object]&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 數字 + undefined，undefined 會被轉成數字 NaN</span></span><br><span class="line"><span class="number">123</span> + <span class="literal">undefined</span>  <span class="comment">// NaN</span></span><br><span class="line"><span class="string">&quot;abc&quot;</span> + <span class="literal">undefined</span>    <span class="comment">// &quot;abcundefined&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 數字 + null，null 會被轉成數字 0</span></span><br><span class="line"><span class="number">123</span> + <span class="literal">null</span>  <span class="comment">// 123</span></span><br><span class="line"><span class="string">&quot;abc&quot;</span> + <span class="literal">null</span>    <span class="comment">// &quot;abcnull&quot;</span></span><br></pre></td></tr></table></figure></li>
<li><p>由於運算式是 “由左而右” 且 “先乘除後加減” 做運算，所以如果要讓數字先做運算，可以使用小括號 <code>()</code> 將數字運算包起來</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str1 = <span class="string">&quot;10 加 100 的結果是 &quot;</span> + <span class="number">10</span> + <span class="number">100</span>;   <span class="comment">// &quot;10 加 100 的結果是 10100&quot;</span></span><br><span class="line">    </span><br><span class="line"><span class="keyword">var</span> str2 = <span class="string">&quot;10 加 100 的結果是 &quot;</span> + (<span class="number">10</span> + <span class="number">100</span>);   <span class="comment">// &quot;10 加 100 的結果是 110&quot;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="減號"><a href="#減號" class="headerlink" title="減號 -"></a>減號 <code>-</code></h3><ul>
<li><p>單純數字相減</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span> - <span class="number">3</span>;</span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 7</span></span><br></pre></td></tr></table></figure></li>
<li><p>特殊的數字，如無限大 <code>Infinity</code>、負無限大 <code>-Infinity</code> 和 <code>Not a Number NaN</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="literal">Infinity</span> - <span class="literal">Infinity</span>   <span class="comment">// NaN</span></span><br><span class="line">-<span class="literal">Infinity</span> - -<span class="literal">Infinity</span>   <span class="comment">// NaN</span></span><br><span class="line"></span><br><span class="line">-<span class="literal">Infinity</span> - <span class="literal">Infinity</span>    <span class="comment">// -Infinity</span></span><br><span class="line"><span class="literal">Infinity</span> - -<span class="literal">Infinity</span>    <span class="comment">// Infinity</span></span><br></pre></td></tr></table></figure>

<p>如果減號 <code>-</code> 兩側其中之一是 <code>NaN</code>，那結果就會是 <code>NaN</code></p>
</li>
<li><p>與加號 <code>+</code> 不同的地方是如果減號 <code>-</code> 兩側其中之一不是數字，會透過 <code>Number()</code> 嘗試將數值轉為 “數字”</p>
<ul>
<li><p>不含數字的字串、<code>undefined</code> 和 物件 <code>object</code> 會被轉型成 <code>NaN</code></p>
</li>
<li><p>含數字的字串 會被轉型為 數字</p>
</li>
<li><p><code>true</code>、會被轉型為 <code>1</code></p>
</li>
<li><p><code>false</code>、<code>null</code> 會被轉型為 <code>0</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 數字 - 字串</span></span><br><span class="line"><span class="number">100</span> - <span class="string">&quot;50&quot;</span>    <span class="comment">// 50</span></span><br><span class="line"><span class="number">100</span> - <span class="string">&quot;abc&quot;</span>   <span class="comment">// NaN</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 數字 - 布林</span></span><br><span class="line"><span class="number">100</span> - <span class="literal">false</span>   <span class="comment">// 100</span></span><br><span class="line"><span class="number">100</span> - <span class="literal">true</span>    <span class="comment">// 99</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 數字 - undefined</span></span><br><span class="line"><span class="number">100</span> - <span class="literal">undefined</span>   <span class="comment">// NaN</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 數字 - null</span></span><br><span class="line"><span class="number">100</span> - <span class="literal">null</span>   <span class="comment">// 100</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 數字 - 物件</span></span><br><span class="line"><span class="number">100</span> - &#123;&#125;   <span class="comment">// NaN</span></span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<h3 id="乘號"><a href="#乘號" class="headerlink" title="乘號 *"></a>乘號 <code>*</code></h3><ul>
<li><p>單純數字的運算</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span> * <span class="number">10</span>;</span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 100</span></span><br></pre></td></tr></table></figure></li>
<li><p>超出數字的範圍由就看結果是正數或負數來決定是 <code>Infinity</code> 或 <code>-Infinity</code></p>
</li>
<li><p>如果乘號兩側其中之一出現 <code>NaN</code>，那結果就會是 <code>NaN</code>， <code>Infinity * 0</code> 也是 <code>NaN</code></p>
</li>
<li><p>如果乘號兩側其中之一出現非數字的型別，會和減號 <code>-</code> 一樣使用 <code>Number()</code> 嘗試轉型為 “數字”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 數字 * 字串</span></span><br><span class="line"><span class="number">100</span> * <span class="string">&quot;10&quot;</span>   <span class="comment">// 1000</span></span><br><span class="line"><span class="number">100</span> * <span class="string">&quot;abc&quot;</span>   <span class="comment">// NaN</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 數字 * 布林</span></span><br><span class="line"><span class="number">100</span> * <span class="literal">true</span>   <span class="comment">// 100</span></span><br><span class="line"><span class="number">100</span> * <span class="literal">false</span>  <span class="comment">// 0</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 數字 * undefined</span></span><br><span class="line"><span class="number">100</span> * <span class="literal">undefined</span>   <span class="comment">// NaN</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//數字 * null</span></span><br><span class="line"><span class="number">100</span> * <span class="literal">null</span>   <span class="comment">// 0</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 數字 * 物件</span></span><br><span class="line"><span class="number">100</span> * &#123;&#125;   <span class="comment">// NaN</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="除號"><a href="#除號" class="headerlink" title="除號 /"></a>除號 <code>/</code></h3><ul>
<li>單純數字的運算</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">100</span> / <span class="number">10</span></span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 10</span></span><br></pre></td></tr></table></figure>

<ul>
<li><p>“被除數” 為 <code>0</code> 的情況下</p>
<ul>
<li>除數為 “正數”，結果為 <code>Infinity</code></li>
<li>除數為 “負數”，結果為 <code>-Infinity</code></li>
<li>除數為 <code>0</code>，結果為 <code>NaN</code></li>
</ul>
</li>
<li><p>和乘號 <code>*</code> 相同，如果除號 <code>/</code> 兩側其中之一為數字以外的型別，會透過 <code>Number()</code> 嘗試轉型為數字</p>
</li>
</ul>
<h3 id="取餘數"><a href="#取餘數" class="headerlink" title="取餘數 %"></a>取餘數 <code>%</code></h3><ul>
<li><p>單純數字的運算</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">100</span> % <span class="number">33</span></span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 1 </span></span><br></pre></td></tr></table></figure></li>
<li><p>如果 “被除數” 為 <code>Infinity</code> 或 <code>-Infinity</code>，取餘數的結果為 <code>NaN</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="literal">Infinity</span> % <span class="number">0</span>   <span class="comment">// NaN</span></span><br><span class="line"><span class="literal">Infinity</span> % <span class="number">100</span>   <span class="comment">// NaN</span></span><br><span class="line"><span class="literal">Infinity</span> % <span class="literal">Infinity</span>   <span class="comment">// NaN</span></span><br><span class="line"><span class="literal">Infinity</span> % -<span class="literal">Infinity</span>   <span class="comment">// NaN</span></span><br></pre></td></tr></table></figure></li>
<li><p>如果 “被除數” 為 “一般值”， “除數” 為 <code>Infinity</code>，結果為 “被除數”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="number">100</span> % <span class="literal">Infinity</span>   <span class="comment">// 100</span></span><br><span class="line"><span class="number">0</span> % <span class="literal">Infinity</span>   <span class="comment">// 0</span></span><br></pre></td></tr></table></figure></li>
<li><p>如果 “被除數” 為 “一般值”， “除數” 為 <code>0</code>，結果為 <code>NaN</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="number">100</span> % <span class="number">0</span>   <span class="comment">// NaN</span></span><br><span class="line"><span class="number">0</span> % <span class="number">0</span>   <span class="comment">// NaN</span></span><br></pre></td></tr></table></figure></li>
<li><p>和除號 <code>/</code> 相同，如果取餘 <code>%</code> 兩側其中之一為數字以外的型別，會透過 <code>Number()</code> 嘗試轉型為數字</p>
</li>
</ul>
<h3 id="遞增-與-遞減"><a href="#遞增-與-遞減" class="headerlink" title="遞增 ++ 與 遞減 --"></a>遞增 <code>++</code> 與 遞減 <code>--</code></h3><ul>
<li><p>如同字面上的意思，遇到 遞增 <code>++</code> 就會加一，可以看成是 <code>a = a + 1</code> ，遇到 遞減 <code>--</code> 就會減一，可以看成是 <code>a = a - 1</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line">    </span><br><span class="line">a++;</span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 11</span></span><br><span class="line"></span><br><span class="line">a--;</span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 10</span></span><br></pre></td></tr></table></figure></li>
<li><p>位置放在變數前後是有差別的，將 遞增 <code>++</code> 放在 前面，回傳的會是 “原始數值” ，放在 後面，回傳的會是 <code>+1</code> 後的結果，但事後再將兩者印出，都會出現 <code>+1</code> 後的結果，同理 遞減 <code>--</code> 也是</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line">    </span><br><span class="line">a++;</span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 11</span></span><br><span class="line"></span><br><span class="line">a--;</span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 10</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="賦值運算子-Assignment-Operator"><a href="#賦值運算子-Assignment-Operator" class="headerlink" title="賦值運算子 (Assignment Operator)"></a>賦值運算子 (Assignment Operator)</h2><ul>
<li><p>賦值運算子透過 等號 <code>=</code> 來將左側的值指派到右側，如下將右側的運算結果指派到左側的變數 <code>a</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span> * <span class="number">10</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>除了單純等號 <code>=</code>，還可以結合算術運算子</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">a += b;   <span class="comment">// a = a + b</span></span><br><span class="line">a -= b;   <span class="comment">// a = a - b</span></span><br><span class="line">a *= b;   <span class="comment">// a = a * b</span></span><br><span class="line">a /= b;   <span class="comment">// a = a / b</span></span><br><span class="line">a %= b;   <span class="comment">// a = a % b</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="比較運算子-Comparison-Operators"><a href="#比較運算子-Comparison-Operators" class="headerlink" title="比較運算子 (Comparison Operators)"></a>比較運算子 (Comparison Operators)</h2><p>比較運算子是用來比較運算子兩側的值，可能是 <code>純值</code>、<code>物件</code>、<code>運算式</code> 或 <code>函式回傳</code> 的結果，比較後會得到 <code>true</code> 或 <code>false</code></p>
<ul>
<li><h3 id="賦值-一個等號"><a href="#賦值-一個等號" class="headerlink" title="賦值 : 一個等號 ="></a>賦值 : 一個等號 <code>=</code></h3><p>一個等號 <code>=</code> 的意思是將等號 <code>=</code> 右側的值 指定、賦值 到 等號 <code>=</code> 左側，為 賦值運算子</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br></pre></td></tr></table></figure></li>
<li><h3 id="相等-兩個等號"><a href="#相等-兩個等號" class="headerlink" title="相等 : 兩個等號 =="></a>相等 : 兩個等號 <code>==</code></h3><p>兩個等號 <code>==</code> 的意思是為左右兩側的資料進行比較，但 不同型別的值會 “自動轉型” 後再做比較</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">var</span> b = <span class="string">&quot;10&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( a == <span class="number">10</span> );   <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log( a == b );   <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log( a == <span class="number">100</span>  );   <span class="comment">// false </span></span><br></pre></td></tr></table></figure></li>
<li><h3 id="全等-三個等號"><a href="#全等-三個等號" class="headerlink" title="全等 : 三個等號 ==="></a>全等 : 三個等號 <code>===</code></h3><p>三個等號 <code>===</code> 也是為左右兩側的值進行比較，但 “不同型別的值” 不會自動轉型</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">var</span> b = <span class="string">&quot;10&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( a == b );   <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log( a === b );   <span class="comment">// false</span></span><br></pre></td></tr></table></figure></li>
<li><h3 id="不等於-與"><a href="#不等於-與" class="headerlink" title="不等於 != 與 !=="></a>不等於 <code>!=</code> 與 <code>!==</code></h3><p>等號 <code>=</code> 前面加上 驚嘆號 <code>!</code> 即為 “不等於”，而不等於分成兩種，一個等號 <code>!=</code> “不會自動轉型” ，兩個等號 <code>!==</code> “會自動轉型”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br></pre></td></tr></table></figure></li>
<li><h3 id="大於-gt-與-小於-lt"><a href="#大於-gt-與-小於-lt" class="headerlink" title="大於 &gt; 與 小於 &lt;"></a>大於 <code>&gt;</code> 與 小於 <code>&lt;</code></h3><p>除了判斷是否相等，還可以比較大小，在後方加上 等號 <code>=</code> 就會包含等於，大於等於 <code>&gt;=</code> 和 小於等於 <code>&lt;=</code> ，如遇到不同型別就會 “自動轉型”</p>
<ul>
<li><p>如果兩者都是 “數字” 則單純就字面大小比</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">var</span> b = <span class="number">11</span>;</span><br><span class="line"><span class="keyword">var</span> c = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( a &gt; b );   <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log( b &gt; a );   <span class="comment">// false</span></span><br><span class="line"><span class="built_in">console</span>.log( c &gt; a );   <span class="comment">// false</span></span><br><span class="line"><span class="built_in">console</span>.log( a &gt;= c );  <span class="comment">// true</span></span><br></pre></td></tr></table></figure></li>
<li><p>如果其中之一不是數字，則會嘗試將其轉型成 “數字” 再做比較</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;   <span class="comment">// number</span></span><br><span class="line"><span class="keyword">var</span> b = <span class="string">&quot;9&quot;</span>;   <span class="comment">// string</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( a &gt; b );   <span class="comment">// true</span></span><br></pre></td></tr></table></figure></li>
<li><p>如果兩者都是字串，則會依照字母的順序來做比較，如果有多個字母，則會比較到第一個不同的字母為止</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log( <span class="string">&quot;a&quot;</span> &gt; <span class="string">&quot;b&quot;</span> );   <span class="comment">// false</span></span><br><span class="line"><span class="built_in">console</span>.log( <span class="string">&quot;b&quot;</span> &gt; <span class="string">&quot;a&quot;</span> );   <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log( <span class="string">&quot;abd&quot;</span> &gt; <span class="string">&quot;abc&quot;</span> );   <span class="comment">// true</span></span><br></pre></td></tr></table></figure></li>
<li><p>如果其中之一是 布林 <code>boolean</code>，<code>true</code> 會被轉型為 <code>1</code>，<code>false</code> 會被轉型為 <code>0</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log( <span class="number">10</span> &gt; <span class="literal">true</span> );   <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log( <span class="literal">true</span> &gt; <span class="literal">false</span> );   <span class="comment">// true</span></span><br></pre></td></tr></table></figure></li>
<li><p>如果其中之一是物件 <code>object</code>，會先透過 <code>valueOf()</code> 求得對應的數值，如果沒有 <code>valueOf()</code> 的方法，就會透過 <code>toString()</code> 轉型再做比較</p>
</li>
</ul>
</li>
</ul>
<h2 id="邏輯運算子-Logical-Operator"><a href="#邏輯運算子-Logical-Operator" class="headerlink" title="邏輯運算子 (Logical Operator)"></a>邏輯運算子 (Logical Operator)</h2><p>邏輯運算式包含三種，and <code>&amp;&amp;</code>、or <code>||</code>、not <code>!</code>，通常會在 <code>if 判斷式 </code>和布林 <code>boolean</code> 搭配使用</p>
<ul>
<li><h3 id="And-amp-amp"><a href="#And-amp-amp" class="headerlink" title="And &amp;&amp;"></a>And <code>&amp;&amp;</code></h3><p>當 and <code>&amp;&amp;</code> 的兩側都是 <code>true</code> 結果才會是 <code>true</code>，否則就是 <code>false</code>，即 全部條件都必須成立才算成立</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">var</span> b = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( a &gt; <span class="number">0</span> &amp;&amp; b = <span class="literal">true</span> );   <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log( a &lt; <span class="number">0</span> &amp;&amp; b = <span class="literal">true</span> );   <span class="comment">// false</span></span><br></pre></td></tr></table></figure></li>
<li><h3 id="Or"><a href="#Or" class="headerlink" title="Or ||"></a>Or <code>||</code></h3><p>當 or <code>||</code> 的兩側其中之一是 <code>true</code> 結果就會是 <code>true</code>，除非兩側都是 <code>false</code> 才會是 <code>false</code>，即 滿足其中一個條件就會成立</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">var</span> b = <span class="literal">true</span>;</span><br><span class="line">                  </span><br><span class="line"><span class="built_in">console</span>.log( a &gt; <span class="number">0</span> || b = <span class="literal">true</span> );   <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log( a &lt; <span class="number">0</span> || b = <span class="literal">true</span> );   <span class="comment">// true</span></span><br></pre></td></tr></table></figure></li>
<li><h3 id="Not"><a href="#Not" class="headerlink" title="Not !"></a>Not <code>!</code></h3><p>not <code>!</code> 代表否定、反向的意思，如果是 <code>! true</code>，就是 <code>false</code> 的意思，兩個驚嘆號 <code>!!</code> ，利用雙重否定可以取代 <code>Boolean()</code> 來判斷某數值 <code>Boolean</code> 轉換後的結果</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( a = <span class="number">0</span> );   <span class="comment">// false</span></span><br><span class="line"><span class="built_in">console</span>.log( a != <span class="number">0</span> );   <span class="comment">// true</span></span><br></pre></td></tr></table></figure></li>
<li><h3 id="布林-Boolean-的型別轉換"><a href="#布林-Boolean-的型別轉換" class="headerlink" title="布林 Boolean 的型別轉換"></a>布林 Boolean 的型別轉換</h3><ul>
<li><p>透過 Boolean() 會轉換成 false 的值</p>
<ul>
<li>undefined</li>
<li>null</li>
<li>0</li>
<li>NaN</li>
<li>空字串 “” 、’’</li>
</ul>
</li>
<li><p>其他的值透過 Boolean 轉型後都會是 true</p>
</li>
</ul>
</li>
<li><h3 id="邏輯運算子的判斷"><a href="#邏輯運算子的判斷" class="headerlink" title="邏輯運算子的判斷"></a>邏輯運算子的判斷</h3><p>透過邏輯運算子 <code>&amp;&amp;</code>、<code>||</code> 做判斷時，會先對左側進行驗證</p>
<ul>
<li><p>如果是 <code>Boolean</code> 類型就會做後續判斷，如果不是就會透過 <code>Boolean()</code> 轉型</p>
</li>
<li><p>以 and <code>&amp;&amp;</code> 來說，如果第一個值為 <code>true</code>，則回傳第二個值，否則回傳第一個</p>
</li>
<li><p>以 or <code>||</code> 來說，如果第一個值為 <code>true</code>，則回傳第一個值，否則回傳第二個</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">123</span>;</span><br><span class="line"><span class="keyword">var</span> b = <span class="string">&quot;abc&quot;</span>;</span><br><span class="line"><span class="keyword">var</span> c = <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( a &amp;&amp; b );   <span class="comment">// &quot;abc&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log( a || b );   <span class="comment">// 123</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( c &amp;&amp; a );   <span class="comment">// null</span></span><br><span class="line"><span class="built_in">console</span>.log( c || b );   <span class="comment">// &quot;abc&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log( c || a );   <span class="comment">// 123</span></span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<h2 id="逗號運算子-Comma-Operator"><a href="#逗號運算子-Comma-Operator" class="headerlink" title="逗號運算子 (Comma Operator)"></a>逗號運算子 (Comma Operator)</h2><p>逗號運算子 <code>,</code> 主要是分隔運算式中的多組運算式，常使用在 <code>for 迴圈</code> 中</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>, j = <span class="number">10</span>; i &lt; <span class="number">10</span>; i++, j++)&#123;</span><br><span class="line">  k = i + j;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>也可以使用在 “變數的宣告”，但要注意的是，以下兩種宣告是 “不相同” 的</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>, b = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">var</span> a = b = <span class="number">10</span>;</span><br></pre></td></tr></table></figure>

<p>因為變數 <code>b</code>，其實沒有宣告，只有賦值，所以是全域變數</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = b;</span><br><span class="line">b = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> a = b = <span class="number">10</span>;</span><br><span class="line">  <span class="built_in">console</span>.log( a, b );   <span class="comment">// 10, 10</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 離開作用範圍後</span></span><br><span class="line"><span class="built_in">console</span>.log(a, b);   <span class="comment">// undefined, 10</span></span><br></pre></td></tr></table></figure>

<h2 id="三元運算子"><a href="#三元運算子" class="headerlink" title="三元運算子"></a>三元運算子</h2><p>三元運算子以 問號 <code>?</code> 和 冒號 <code>:</code> 所組成，如果條件為 <code>true</code>，回傳數值 <code>1</code>，否則回傳數值 <code>2</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;(條件)&quot;</span> ? <span class="string">&quot;數值1&quot;</span> : <span class="string">&quot;數值2&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果年紀大於等於 &quot;18&quot;，為&quot;成人&quot;，否則為&quot;小孩&quot;</span></span><br><span class="line"><span class="keyword">var</span> status = (age &gt;= <span class="number">18</span>) ? <span class="string">&quot;成人&quot;</span> : <span class="string">&quot;小孩&quot;</span>;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>滾動視差-滾動套用CSS效果</title>
    <url>/jquery/20190927/1d3a5dfe/</url>
    <content><![CDATA[<p>在這篇文章中，會透過 jQuery 來達到滾動網頁到指定位置時，套用特定的 CSS 效果</p>
<span id="more"></span>

<h2 id="建立版型"><a href="#建立版型" class="headerlink" title="建立版型"></a>建立版型</h2><p>首先，建立要動態套用 CSS 的版型，在這裡會做出讓 “三大重點” 中的項目在滾到到該區域時，由下而上浮現<br><strong><a href="https://codepen.io/Jerry-Yeh/pen/vYBgeVv?editors=1010">範例版型</a></strong></p>
<h2 id="隱藏內容"><a href="#隱藏內容" class="headerlink" title="隱藏內容"></a>隱藏內容</h2><p>要做出動態浮現的效果時，我們會預設內容是隱藏的，但在這裡使用的是透明化 <code>opacity: 0</code>，而不是直接未顯示 <code>display: none</code>，因為在透明化的情況下才會 “保留元素空間”，避免版型走位</p>
<p>在這裡設定一個 <code>className</code> 並帶入元素中</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.animated</span> &#123;</span><br><span class="line">  <span class="attribute">opacity</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#125;</span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;col-three-one animated&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;text-center m-y-2&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">&quot;fa fa-mobile fa-5x&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">h3</span> <span class="attr">class</span>=<span class="string">&quot;text-center m-y-1&quot;</span>&gt;</span></span><br><span class="line">    Fully Responsive</span><br><span class="line">  <span class="tag">&lt;/<span class="name">h3</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">p</span>&gt;</span>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Natus aut dolorem in autem veniam, nisi repellendus amet a eum laboriosam veritatis doloremque consectetur.<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="效果製作"><a href="#效果製作" class="headerlink" title="效果製作"></a>效果製作</h2><p>接著再新增一個 <code>className</code> 來製作呈現的效果，在這裡先增加不透明的效果 <code>opacity: 1</code></p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.fadeIn</span> &#123;</span><br><span class="line">  <span class="attribute">opacity</span>: <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="jQuery-滾動"><a href="#jQuery-滾動" class="headerlink" title="jQuery 滾動"></a>jQuery 滾動</h2><h3 id="滾動方法"><a href="#滾動方法" class="headerlink" title="滾動方法"></a>滾動方法</h3><p>在這裡透過 <code>jQuery</code> 的滾動方法 <code>scroll</code> 來觸發滾動事件，並綁定在頂層物件 <code>window</code> 上</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="built_in">document</span>).ready(<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  $(<span class="built_in">window</span>).scroll(<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;&#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="取得位置"><a href="#取得位置" class="headerlink" title="取得位置"></a>取得位置</h2><p>如果要做出這個效果，就必須取得元素的座標位置，才得以在滾動到該位置時觸發效果</p>
<h3 id="取得元素位置-offset-與-position"><a href="#取得元素位置-offset-與-position" class="headerlink" title="取得元素位置 offset 與 position"></a>取得元素位置 offset 與 position</h3><p>在 <code>jQuery</code> ，這兩種方法都可以取得元素的座標位置，可以先透過 <code>consle.log</code> 來查看取得的座標</p>
<ul>
<li><code>top</code>: 取得指定元素的 <code>y</code> 軸 座標</li>
<li><code>left</code>: 取得指定元素的 <code>x</code> 軸 座標</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> thisPos = $(<span class="built_in">this</span>).offset().top;</span><br><span class="line"><span class="keyword">var</span> thisPos = $(<span class="built_in">this</span>).position().left;</span><br></pre></td></tr></table></figure>

<p><strong><code>offset</code> 與 <code>position</code> 的差異</strong><br>這兩種方法的差異在於 “相對位置”</p>
<ul>
<li><code>offset</code>: 以最高級元素 <code>document</code> 也就是整個網站來做為相對位置</li>
<li><code>position</code>: 會以指定元素網上尋找，直到找到第一個含 <code>position: relative</code> 屬性的元素來取得相對座標</li>
</ul>
<h3 id="取得網頁高度"><a href="#取得網頁高度" class="headerlink" title="取得網頁高度"></a>取得網頁高度</h3><p>取得網頁高度有助於在觸發事件的位置上更靈活，而不會在滾動到該區域的最上方才會觸發</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> windowHeight = $(<span class="built_in">window</span>).height();</span><br></pre></td></tr></table></figure>

<h2 id="套用座標位置"><a href="#套用座標位置" class="headerlink" title="套用座標位置"></a>套用座標位置</h2><p>在了解滾動方法並取得座標位置後，就可以開始設定條件來呈現 CSS 效果</p>
<p><strong>取得元素</strong><br>透過方法 <code>each()</code> 來取得所有包含該 <code>className</code> 的元素</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">&#x27;.animated&#x27;</span>).each(<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;&#125;);</span><br></pre></td></tr></table></figure>

<p><strong>取得元素座標</strong><br>取得元素後，再透過上述提到的方法來取的座標</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">&#x27;.animated&#x27;</span>).each(<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> thisPos = $(<span class="built_in">this</span>).offset().top;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p><strong>設定條件</strong><br>在這裡設定如果 “網頁座標” 加上 “網頁高度” 大於等於 “元素座標”，就加上要套用的 CSS 效果</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="built_in">window</span>).scroll(<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> scrollPos = $(<span class="built_in">window</span>).scrollTop();</span><br><span class="line">  <span class="keyword">var</span> windowHeight = $(<span class="built_in">window</span>).height();</span><br><span class="line"></span><br><span class="line">  $(<span class="string">&#x27;.animated&#x27;</span>).each(<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> thisPos = $(<span class="built_in">this</span>).offset().top;</span><br><span class="line">    <span class="keyword">if</span> ((windowHeight + scrollPos) &gt;= thisPos)&#123;</span><br><span class="line">      $(<span class="built_in">this</span>).addClass(<span class="string">&#x27;fadeIn&#x27;</span>);</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>如此一來，就可以在滾動到特定元素時，套用指定的 CSS 效果囉</p>
<h2 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h2><p><a href="https://www.udemy.com/course/responsive/">六角學院課程 - 一變應萬變的響應式網頁設計</a></p>
]]></content>
      <categories>
        <category>jQuery</category>
      </categories>
      <tags>
        <tag>jQuery</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript - AJAX</title>
    <url>/javascript/20191004/50136/</url>
    <content><![CDATA[<p>透過 AJAX，可與伺服器進行非同步更新，不需要重新載入網頁就可以更新內容</p>
<span id="more"></span>

<h2 id="什麼是-AJAX"><a href="#什麼是-AJAX" class="headerlink" title="什麼是 AJAX ?"></a>什麼是 AJAX ?</h2><p>AJAX 全名為 非同步 JavaScript 與 <code>XML(Asynchronous JavaScript and XML)</code>，其實並不能稱為一種 “新技術”，而是結合許多現有技術的 “新方法”，這些技術包括 HTML、XHTML、JavaScript、DOM 與最重要的 XMLHttpRequest 物件等</p>
<h3 id="為什麼需要-AJAX"><a href="#為什麼需要-AJAX" class="headerlink" title="為什麼需要 AJAX ?"></a>為什麼需要 AJAX ?</h3><p>透過 AJAX 我們可以不需要重新讀取整個網頁就能及時更動網頁介面與內容，只會對改變的地方進行動態重新渲染，提高網頁效能並更快回復使用者的操作，常見於用戶註冊、驗證，不需要等到資料填完就會顯示是否重複或錯誤</p>
<h3 id="AJAX-應用"><a href="#AJAX-應用" class="headerlink" title="AJAX 應用"></a>AJAX 應用</h3><ul>
<li>使用 JavaScript 的 <code>XMLHttpRequest</code> 物件向網頁伺服器發送 <code>HTTP</code> 請求</li>
<li>使用 JavaScript 操作 <code>DOM</code> 來執行動態效果</li>
<li><code>XMLHttpRequest</code> 與 “網頁伺服器” 進行 “非同步” 資料交換</li>
</ul>
<h2 id="AJAX-操作"><a href="#AJAX-操作" class="headerlink" title="AJAX 操作"></a>AJAX 操作</h2><h3 id="XMLHttpRequest-物件"><a href="#XMLHttpRequest-物件" class="headerlink" title="XMLHttpRequest 物件"></a><code>XMLHttpRequest</code> 物件</h3><ol>
<li>宣告一個 <code>XMLHttpRequest</code> 物件<br>當宣告一個 <code>XMLHttpRequest</code> 物件後，我們可以透過 <code>console.log</code> 來查看這個物件的屬性，並透過這些屬性來操作資料與確認狀態<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line"><span class="built_in">console</span>.log(xhr);</span><br></pre></td></tr></table></figure></li>
</ol>
<p>  <img src="XMLHttpRequest.png"></p>
<ol start="2">
<li><p><code>.open(&quot;格式&quot;, &quot;網址&quot;, &quot;同步/非同步&quot;)</code> 定義連線方式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">xhr.open(<span class="string">&quot;get&quot;</span>, <span class="string">&quot;https://hexschool.github.io/ajaxHomework/data.json&quot;</span>, <span class="literal">true</span>);</span><br></pre></td></tr></table></figure>
<ul>
<li>格式<ul>
<li><code>get</code> : 讀取資料</li>
<li><code>post</code> : 傳資料到伺服器確認</li>
</ul>
</li>
<li>網址<br>要讀取的資料位置</li>
<li>同步與非同步<br>通常都使用 <code>true</code> 非同步，否則當資料太大時，會等到資料傳送完畢才會繼續渲染網頁<ul>
<li><code>true</code> 非同步: 不會等資料傳回來，就會讓程式碼繼續往下跑</li>
<li><code>false</code> 同步: 會等資料傳回來，才讓程式碼繼續往下跑</li>
</ul>
</li>
</ul>
</li>
<li><p><code>send()</code> 送出請求<br>在定義完連線方式後，接著就會送出請求，小括號 <code>()</code> 內放入要送出的資料，如果只是要取得資料，就會填入 <code>null</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">xhr.send(<span class="literal">null</span>);</span><br></pre></td></tr></table></figure></li>
<li><p><code>responseText</code> 資料內容<br>送出請求後我們可以透過 <code>responseText</code> 來查看資料內容<br><img src="xhr.responseText.png"><br>但如果在送出請求後，接著使用 <code>console.log</code> 來查看資料，會發現取得 空值，因為 <code>AJAX</code> 是 “非同步” 取得資料，所以在取得資料前網頁就會渲染完畢</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(xhr.responseText);   <span class="comment">// 空值</span></span><br></pre></td></tr></table></figure></li>
<li><p><code>onload</code> 成功取得資料後執行<br>要解決上述問題，只要使用 <code>onload</code> 在成功取得資料後再印出結果，要注意的是，從 <code>.json</code> 檔案取得的資料型態是 “字串”，所以要使用 <code>JSON.parse</code> 還原資料型態</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">&quot;message&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- after js --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">&quot;message&quot;</span>&gt;</span>王小明<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; in json</span><br><span class="line">[</span><br><span class="line">  &#123;name: &quot;王小名&quot;&#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">xhr.onload = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(xhr.responseText);   <span class="comment">// &quot;[&#123;&quot;name&quot;:&quot;王小名&quot;&#125;]&quot;</span></span><br><span class="line">  <span class="keyword">var</span> str = JOSN.parse(xhr.responseText);</span><br><span class="line">  <span class="built_in">document</span>.querySelector(<span class="string">&#x27;.message&#x27;</span>).textContent = str[<span class="number">0</span>].name;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
<li><p><code>onerror</code> 取得資料失敗後執行<br>除了設定成功取得資料後執行的內容，當然也可以設定取得資料失敗後執行的內容</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">xhr.onerror = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;...&#125;;</span><br></pre></td></tr></table></figure></li>
</ol>
<h3 id="readyState-狀態碼"><a href="#readyState-狀態碼" class="headerlink" title="readyState 狀態碼"></a><code>readyState</code> 狀態碼</h3><p>在操作 <code>XMLHttpRequest</code> 物件的每個步驟都會有專屬的狀態碼以辨別當下進度</p>
<p>通常 <code>2</code>、<code>3</code> 會在一瞬間執行完畢，所以在取得資料後查看會是 <code>4</code></p>
<ul>
<li><code>0</code> : 已經宣告一個 <code>XMLHttpRequest</code> 物件，但是還沒有連結要取得的資料</li>
<li><code>1</code> : 使用 <code>open()</code> 撈資料，但還沒有傳送資料</li>
<li><code>2</code> : 偵測到使用 <code>send</code></li>
<li><code>3</code> : <code>loading</code> 資料中</li>
<li><code>4</code> : 已經取得資料，且完全接收數據</li>
</ul>
<h3 id="傳統表單輸入"><a href="#傳統表單輸入" class="headerlink" title="傳統表單輸入"></a>傳統表單輸入</h3><p>傳統的表單如下，輸入資料後，會傳送到 action 所指定的位置</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">form</span> <span class="attr">action</span>=<span class="string">&quot;index.html&quot;</span>&gt;</span></span><br><span class="line">  帳號 : </span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;text&quot;</span> <span class="attr">name</span>=<span class="string">&quot;email&quot;</span>&gt;</span>   // abc@gmail.com</span><br><span class="line">  <span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">  密碼 : </span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;text&quot;</span> <span class="attr">name</span>=<span class="string">&quot;password&quot;</span>&gt;</span>   // abcde</span><br><span class="line">  <span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;submit&quot;</span> <span class="attr">value</span>=<span class="string">&quot;send&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>傳送的資料以問號 <code>?</code> 隔開 “位置” 內容，如果有多筆內容，使用 <code>&amp;</code> 隔開</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;指定的網址&quot;</span> + <span class="string">&quot;?&quot;</span> + <span class="string">&quot;內容&quot;</span> + <span class="string">&quot;內容&quot;</span></span><br><span class="line">index.html?email=abc%40gmail.com&amp;password=abcde</span><br></pre></td></tr></table></figure>

<h3 id="post-傳送表單資料"><a href="#post-傳送表單資料" class="headerlink" title="post 傳送表單資料"></a><code>post</code> 傳送表單資料</h3><ul>
<li><p>如果要發送資料到資料庫，在 <code>open()</code> 中的第一個參數為 <code>post</code></p>
</li>
<li><p>因為要發送的資料會直接寫在 <code>send()</code> 中，所以需要先使用 <code>setRequestHeader</code> 來設定資料類型，如果發送的資料類型是表單，分別設立兩個參數為 <code>Content-Type</code> 和 <code>application/x-www-form-urlencoded</code></p>
</li>
<li><p>如上 “傳統表單” 送出一樣的資料</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">xhr.open(<span class="string">&#x27;post&#x27;</span>, <span class="string">&#x27;https://hexschool-tutorial.herokuapp.com/api/signup&#x27;</span>, <span class="literal">true</span>);</span><br><span class="line">xhr.setRequestHeader(<span class="string">&#x27;Content-type&#x27;</span>, <span class="string">&#x27;application/x-www-form-urlencoded&#x27;</span>);</span><br><span class="line">xhr.send(<span class="string">&#x27;email=abc@gmail.com&amp;password=abcde&#x27;</span>);</span><br></pre></td></tr></table></figure></li>
<li><p>傳送資料過去後，可以使用 console.log 查詢回傳的資料</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(xhr.responseText);   <span class="comment">// &#123;&quot;success&quot;:true,&quot;result&quot;:&#123;&#125;,&quot;message&quot;:&quot;帳號註冊成功&quot;&#125;</span></span><br></pre></td></tr></table></figure></li>
<li><p>如果帳號已經被註冊。就會出現以下結果</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(xhr.responseText);   <span class="comment">// &#123;&quot;success&quot;:false,&quot;result&quot;:&#123;&#125;,&quot;message&quot;:&quot;此帳號已被使用&quot;&#125;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="post-JSON"><a href="#post-JSON" class="headerlink" title="post JSON"></a>post JSON</h3><p>除了使用 <code>post</code> 傳送表單格式資料外，也可以直接傳送 <code>json</code> 格式，只要把 <code>setRequestHeader()</code> 中第二個參數改成 <code>application/json</code>， 但要注意的是，和 <code>localStorage</code> 一樣，都是使用 “字串” 來傳送，所以要使用 <code>JSON.stringify</code> 和 <code>JSON.parse</code> 來轉換型態</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> account = &#123;</span><br><span class="line">  email: <span class="string">&#x27;abc@gmail.com&#x27;</span>,</span><br><span class="line">  password: <span class="string">&#x27;abcde&#x27;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> xhr = XMLHttpRequest();</span><br><span class="line">xhr.open(<span class="string">&#x27;post&#x27;</span>, <span class="string">&#x27;https://hexschool-tutorial.herokuapp.com/api/signup&#x27;</span>, <span class="literal">true</span>);</span><br><span class="line">xhr.setRequestHeader(<span class="string">&#x27;Content-type&#x27;</span>, <span class="string">&#x27;application/json&#x27;</span>);</span><br><span class="line"><span class="keyword">var</span> data = <span class="built_in">JSON</span>.stringify(account);</span><br><span class="line">xhr.send(data);</span><br></pre></td></tr></table></figure>

<h2 id="CORS-Corss-Origin-Resourse-Sharing-跨來源資源共享"><a href="#CORS-Corss-Origin-Resourse-Sharing-跨來源資源共享" class="headerlink" title="CORS (Corss-Origin Resourse Sharing) 跨來源資源共享"></a>CORS (Corss-Origin Resourse Sharing) 跨來源資源共享</h2><p>在開發網頁時，常常需要串接外部的資料、API，這時候我們就會對伺服器送出一個 <code>HTTP request</code> 請求，但是當瀏覽器收到來自伺服器的 <code>HTTP response</code> 回應時，基於安全性考量，在網站與資料 “不同源” <code>not same origin</code> 的情況下，瀏覽器並不會把收到的 <code>response</code> 回傳給 <code>JavaScript</code></p>
<p>而 <code>CORS</code> 就是一個允許不同源之間資料傳輸的規範，透過以下網站可以判定這個外接網址是否有開啟 <code>CORS</code></p>
<p><a href="http://www.test-cors.org/">test-cors.org</a></p>
<p>詳細說明可以參考以下資料</p>
<p><a href="https://blog.techbridge.cc/2017/05/20/api-ajax-cors-and-jsonp/">輕鬆理解 Ajax 與跨來源請求</a></p>
<h2 id="HTTP-狀態碼"><a href="#HTTP-狀態碼" class="headerlink" title="HTTP 狀態碼"></a><code>HTTP</code> 狀態碼</h2><p><code>HTTP</code> 狀態碼指的是伺服器回應 <code>HTTP response</code> 我們發出的請求 ，而 <code>HTTP request``HTTP</code> 狀態碼分為五大類、三個層級，而三個層級分別使用三個數字來表示，第一個數字就表示大類</p>
<ul>
<li>1xx - 參考資訊 (informational)</li>
<li>2xx - 成功 (OK)</li>
<li>3xx - 重新導向 (Redirection)</li>
<li>4xx - 用戶端錯誤 (Client Error)</li>
<li>5xx - 伺服器錯誤 (Server Error)</li>
</ul>
<p>最常見的為以下兩種</p>
<ul>
<li>200 : 用戶端請求成功</li>
<li>404 : 找不到用戶端請求</li>
</ul>
<p>詳細內容操考以下資料</p>
<p><a href="https://zh.wikipedia.org/wiki/HTTP%E7%8A%B6%E6%80%81%E7%A0%81">HTTP狀態碼 Wiki</a><br><a href="https://blog.miniasp.com/post/2009/01/16/Web-developer-should-know-about-HTTP-Status-Code">網頁開發人員應了解的 HTTP 狀態碼</a></p>
<h2 id="AJAX-實務範例"><a href="#AJAX-實務範例" class="headerlink" title="AJAX 實務範例"></a>AJAX 實務範例</h2><p><a href="https://codepen.io/Jerry-Yeh/pen/NZZrov?editors=1010">AJAX 實務範例</a></p>
<h2 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h2><p><a href="https://www.udemy.com/javascript-learning/">JavaScript 入門篇 - 學徒的試煉</a><br><a href="https://ithelp.ithome.com.tw/users/20102891/ironman/1955">從頭開始的API 和ajax</a><br><a href="https://zh.wikipedia.org/wiki/AJAX">AJAX Wiki</a></p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>AJAX</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript - localStorage</title>
    <url>/javascript/20191004/8109/</url>
    <content><![CDATA[<p><code>Web storage</code> 為 <code>HTML 5</code> 問世後提供的新技術，提供比 <code>cookie</code> (每條 <code>cookie</code> 的儲存空間為 <code>4K</code>) 更大 (<code>localStorage</code> 為 <code>5M</code>) 更安全的本地端儲存空間，也不會影響到網頁效能</p>
<span id="more"></span>

<h2 id="Web-Storage"><a href="#Web-Storage" class="headerlink" title="Web Storage"></a>Web Storage</h2><p><code>Web storage</code> 物件分為以下兩種，兩種物件的唯一差異是 “生命週期”，所以以下都已 <code>localStorage</code> 來做介紹</p>
<ul>
<li><p><code>window.localStorage</code><br>儲存到這裡的資料會 “永久” 保存於本地端，除非使用者主動使用 <code>JavaScript</code> 語法 或 “網頁瀏覽器功能” 來刪除</p>
</li>
<li><p><code>window.sessionStorage</code><br>保存資料於頁面 “尚未關閉” 或使用 “還原” 時，但在頁面關閉時清空儲存在內的所有資料</p>
</li>
</ul>
<h2 id="localStorage-基本操作"><a href="#localStorage-基本操作" class="headerlink" title="localStorage 基本操作"></a>localStorage 基本操作</h2><h3 id="locarStorage-裡的-key-與-value"><a href="#locarStorage-裡的-key-與-value" class="headerlink" title="locarStorage 裡的 key 與 value"></a><code>locarStorage</code> 裡的 <code>key</code> 與 <code>value</code></h3><p>開啟開發人員工具，我們可以從裡面的 <code>Application</code> 找到 <code>localStorage</code>，點開 <code>localStorage</code>，會看到兩個欄位 <code>Key</code> 與 <code>Value</code>，在沒有任何動作前，兩個欄位是空的</p>
<p><img src="localStorage-key&value.png"></p>
<ul>
<li><p>Key</p>
<p><code>Key</code> 是 <code>localStorage</code> 物件的屬性，也是存入這筆資料的 “名稱”</p>
</li>
<li><p>Value</p>
</li>
</ul>
<p>而 <code>Value</code> 是 <code>Key</code> 屬性的值，也是存入這筆資料的 “內容”，特別要注意的是，<code>Key</code> 的型態都是 字串 <code>string</code></p>
<h3 id="localStorage-操作方法"><a href="#localStorage-操作方法" class="headerlink" title="localStorage 操作方法"></a>localStorage 操作方法</h3><ul>
<li><p><code>localStorage.setItem(&#39;Key&#39;, &#39;Value&#39;)</code> 存入</p>
<p>透過這個方法可以將資料存入 <code>localStorage</code></p>
</li>
<li><p><code>localStorage.getItem(&#39;Value&#39;)</code> 取出</p>
<p>透過這個方法可以取出 <code>localStorage</code> 中指定屬性的值</p>
</li>
<li><p><code>localStorage.removeItem(&#39;Key&#39;)</code> 刪除</p>
<p>透過這個方法可以刪除 <code>localStorage</code> 中指定的屬性</p>
<p><a href="https://codepen.io/Jerry-Yeh/pen/RzObON?editors=1010">範例程式碼</a></p>
</li>
<li><p><code>localStorage.key()</code> 取得屬性</p>
<p>透過這個方法可以取得指定順序的屬性 <code>key</code>，此順序也是從 <code>0</code> 開始</p>
</li>
<li><p><code>localStorage.clear()</code> 清除全部屬性</p>
<p>這個方法會清除 <code>localStorage</code> 中所有屬性 <code>key</code></p>
</li>
</ul>
<h2 id="JSON-資料編譯"><a href="#JSON-資料編譯" class="headerlink" title="JSON 資料編譯"></a>JSON 資料編譯</h2><h3 id="為什麼我們需要編譯資料"><a href="#為什麼我們需要編譯資料" class="headerlink" title="為什麼我們需要編譯資料 ?"></a>為什麼我們需要編譯資料 ?</h3><p>當我們使用 <code>setItem</code> 將資料儲存在本地端資料庫時，會發現所有儲存的資料 <code>value</code> 都被轉換成 “字串”，原因是 <code>Storage</code> 只會儲存字串的格式，所以會將所有存入的資料使用 <code>toString()</code> 方法強行轉換成字串 ; 如果將物件強行轉換成字串，就會得到 <code>[object, Object]</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> string = <span class="string">&#x27;testString&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> number = <span class="number">123</span>;</span><br><span class="line"><span class="keyword">var</span> array = [<span class="string">&#x27;string&#x27;</span>, <span class="number">123</span>, <span class="literal">true</span>];</span><br><span class="line"><span class="keyword">var</span> object = &#123;<span class="string">&quot;test&quot;</span>: <span class="number">123</span>&#125;</span><br><span class="line"><span class="keyword">var</span> <span class="function"><span class="keyword">function</span> = <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;<span class="title">alert</span>(<span class="params">test</span>)&#125;</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">localStorage</span>.setItem(<span class="string">&#x27;item1&#x27;</span>, string);</span><br><span class="line"><span class="built_in">localStorage</span>.setItem(<span class="string">&#x27;item2&#x27;</span>, number);</span><br><span class="line"><span class="built_in">localStorage</span>.setItem(<span class="string">&#x27;item3&#x27;</span>, array);</span><br><span class="line"><span class="built_in">localStorage</span>.setItem(<span class="string">&#x27;item4&#x27;</span>, object);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">localStorage</span>.getItem(<span class="string">&#x27;item1&#x27;</span>));   <span class="comment">// testString   (string)</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">localStorage</span>.getItem(<span class="string">&#x27;item2&#x27;</span>));   <span class="comment">// 123   (string)</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">localStorage</span>.getItem(<span class="string">&#x27;item3&#x27;</span>));   <span class="comment">// string, 123, true   (string)</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">localStorage</span>.getItem(<span class="string">&#x27;item4&#x27;</span>));   <span class="comment">// [object, Object]</span></span><br></pre></td></tr></table></figure>

<h3 id="編譯資料的方式"><a href="#編譯資料的方式" class="headerlink" title="編譯資料的方式"></a>編譯資料的方式</h3><p>在這裡 <code>JSON</code> 提供了兩種方法來轉換資料的型別</p>
<ul>
<li><p><code>JSON.stringify()</code></p>
<p>將資料轉為 <code>JSON</code> 格式的字串</p>
</li>
<li><p><code>JSON.parse()</code></p>
<p>將資料由 <code>JSON</code> 格式字串轉回原來的內容與型別</p>
</li>
</ul>
<p>同樣的資料我們在存入時使用 <code>stringify()</code> 將資料轉成 <code>JSON</code> 格式字串，取出時使用 <code>parse()</code> 轉回原來的資料型別</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">localStorage</span>.setItem(<span class="string">&#x27;item1&#x27;</span>, <span class="built_in">JSON</span>.stringify(string));</span><br><span class="line"><span class="built_in">localStorage</span>.setItem(<span class="string">&#x27;item2&#x27;</span>, <span class="built_in">JSON</span>.stringify(number));</span><br><span class="line"><span class="built_in">localStorage</span>.setItem(<span class="string">&#x27;item3&#x27;</span>, <span class="built_in">JSON</span>.stringify(array));</span><br><span class="line"><span class="built_in">localStorage</span>.setItem(<span class="string">&#x27;item4&#x27;</span>, <span class="built_in">JSON</span>.stringify(object));</span><br><span class="line">              </span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">JSON</span>.parse(<span class="built_in">localStorage</span>.getItem(<span class="string">&#x27;item1&#x27;</span>)));   testString   (string)</span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">JSON</span>.parse(<span class="built_in">localStorage</span>.getItem(<span class="string">&#x27;item2&#x27;</span>)));   <span class="number">123</span>   (number)</span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">JSON</span>.parse(<span class="built_in">localStorage</span>.getItem(<span class="string">&#x27;item3&#x27;</span>)));   [<span class="string">&#x27;string&#x27;</span>, <span class="number">123</span>, <span class="literal">true</span>]   (array)</span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">JSON</span>.parse(<span class="built_in">localStorage</span>.getItem(<span class="string">&#x27;item4&#x27;</span>)));   &#123;<span class="string">&quot;test&quot;</span>: <span class="number">123</span>&#125;   (object)</span><br></pre></td></tr></table></figure>

<p><code>return</code> 回傳數值、中斷 <code>function</code> ，不會執行之後的程式碼</p>
<h2 id="HTML-5-中的-data-屬性"><a href="#HTML-5-中的-data-屬性" class="headerlink" title="HTML 5 中的 data-* 屬性"></a>HTML 5 中的 data-* 屬性</h2><p>在製作網頁時，如果可以自己新增屬性，為 <code>HTML tag</code> 做上記號，就能更靈活的操作網頁。但為了避免大家隨意添加、命名自己需要的屬性，<code>HTML 5</code> 就多了一個 <code>data-*</code> 屬性，制定了一個統一的規則，其中的米字號 <code>*</code> 是一個可以自定義的名稱</p>
<p>在 <code>*</code> 自定義名稱時，不能使用 英文大寫，可以是任何字串，如下範例</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">class</span>=<span class="string">&quot;list&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span> <span class="attr">data-num</span>=<span class="string">&quot;0&quot;</span> <span class="attr">data-size</span>=<span class="string">&quot;m&quot;</span> <span class="attr">class</span>=<span class="string">&quot;listLi&quot;</span>&gt;</span>卡斯伯<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="在-JS-中選取-data"><a href="#在-JS-中選取-data" class="headerlink" title="在 JS 中選取 data-*"></a>在 JS 中選取 data-*</h3><p>如果是綁定事件來尋找自定義的 <code>data-*</code> 屬性，都會儲存在 <code>target</code> 中的 <code>dataset</code> 物件，物件的屬性就是自定義的 <code>*</code>，屬性的值就是自定義時給定的值了。延續上方的範例</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> list = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;.list li&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">checkList</span>(<span class="params">e</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(e.target.dataset);        <span class="comment">// DOMStringMap &#123;num: &quot;0&quot;, size: &quot;m&quot;&#125;</span></span><br><span class="line">  <span class="built_in">console</span>.log(e.target.dataset.num)     <span class="comment">// 0</span></span><br><span class="line">  <span class="built_in">console</span>.log(e.target.dataset.size);   <span class="comment">// m</span></span><br><span class="line">&#125;;</span><br><span class="line">            </span><br><span class="line">list.addEventListener(<span class="string">&#x27;click&#x27;</span>, checkList, <span class="literal">false</span>);</span><br></pre></td></tr></table></figure>

<p><strong><code>dataset</code> 與 <code>array</code> 的運用</strong></p>
<ul>
<li><p>先將陣列 <code>array</code> 中的資料帶入 <code>HTML</code></p>
</li>
<li><p>點擊時再透過 <code>dataset</code> 取得對應的資料</p>
<p><a href="">範例程式碼</a></p>
</li>
</ul>
<h3 id="在-CSS-中選取-data"><a href="#在-CSS-中選取-data" class="headerlink" title="在 CSS 中選取 data-*"></a>在 CSS 中選取 data-*</h3><p>既然是自定義的屬性 <code>Attribute</code>，黨然也可以在 <code>CSS</code> 中被選取</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">article</span> <span class="attr">class</span>=<span class="string">&quot;article&quot;</span> <span class="attr">data-content</span>=<span class="string">&quot;Hello Everyone&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">img</span> <span class="attr">data-size</span>=<span class="string">&quot;m&quot;</span> <span class="attr">src</span>=<span class="string">&quot;&quot;</span> <span class="attr">alt</span>=<span class="string">&quot;&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">img</span> <span class="attr">data-size</span>=<span class="string">&quot;xl&quot;</span> <span class="attr">src</span>=<span class="string">&quot;&quot;</span> <span class="attr">alt</span>=<span class="string">&quot;&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">article</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>如果要在 <code>&lt;article&gt;</code> 中顯示 <code>data-content</code> 的內容</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">atricle<span class="selector-pseudo">::before</span> &#123;</span><br><span class="line">  <span class="attribute">content</span>: <span class="built_in">attr</span>(data-content);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果要選取指定的屬性或是屬性的值，也可以搭配 CSS 選擇棄來使用</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">article</span> <span class="selector-tag">img</span><span class="selector-attr">[data-size=<span class="string">&quot;m&quot;</span>]</span>&#123;...&#125;</span><br><span class="line"><span class="selector-tag">article</span> <span class="selector-tag">img</span><span class="selector-attr">[data-size=<span class="string">&quot;xl&quot;</span>]</span>&#123;...&#125;</span><br></pre></td></tr></table></figure>

<h2 id="toDoList"><a href="#toDoList" class="headerlink" title="toDoList"></a>toDoList</h2><p><a href="https://codepen.io/Jerry-Yeh/pen/jjRjJp?editors=1010">範例程式碼</a></p>
<ol>
<li>取得輸入框的內容加入到 <code>data</code> 陣列中</li>
<li>將 <code>data</code> 陣列轉換成 “字串” 儲存到 <code>localStorage</code></li>
<li>取出 <code>localStorage</code> 的資料並還原其形態，印出到網頁中</li>
<li>透過 <code>dataset</code> 刪除點選的指定資料</li>
</ol>
<h2 id="參考資料"><a href="#參考資料" class="headerlink" title="參考資料"></a>參考資料</h2><p><a href="https://ithelp.ithome.com.tw/users/20065504/ironman/1259">重新認識 JavaScript</a><br><a href="https://ithelp.ithome.com.tw/users/20083608/ironman/1354">邁向 JavaScript 勇者之路</a><br><a href="https://www.udemy.com/javascript-learning/">JavaScript 入門篇 - 學徒的試煉</a><br><a href="https://ithelp.ithome.com.tw/articles/10195522">JS30-Day15-LocalStorage</a><br><a href="https://pjchender.blogspot.com/2017/01/html-5-data-attribute.html">什麼是 HTML 5 中的資料屬性（data-* attribute )</a></p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript - ES6 - 變數</title>
    <url>/javascript/20191013/50209/</url>
    <content><![CDATA[<p>在 ES6 中多了兩種變數的宣告方式，<code>let</code> 與 <code>const</code>，其最大的目的在於盡量避免汙染到全域，原因在於載入多個模組或進行多人協作時，如果遇到相同名稱的全域變數就會發生衝突，也減少了程式的靈活性</p>
<span id="more"></span>

<h2 id="window-與-var"><a href="#window-與-var" class="headerlink" title="window 與 var"></a>window 與 var</h2><p>在 ES6 以前，都使用 <code>var</code> 來進行變數的宣告，而切分 <code>var</code> 有效範圍的最小單位是 函式 function，所以在 <code>function</code> 外使用 <code>var</code> 宣告的變數都會成為全域變數，也就是 最高級物件 <code>window</code> 的屬性</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">0</span>;</span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 0</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> b =<span class="number">0</span>; b &lt; <span class="number">2</span>; b++)&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(b);   <span class="comment">// 0, 1</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="built_in">console</span>.log(b);   <span class="comment">// 2</span></span><br><span class="line"></span><br><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> c = <span class="number">3</span></span><br><span class="line">  <span class="built_in">console</span>.log(c);   <span class="comment">// 3</span></span><br><span class="line">&#125;)();</span><br><span class="line"><span class="built_in">console</span>.log(c);   <span class="comment">// c is not defined</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.a);   <span class="comment">// 0</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.b);   <span class="comment">// 3</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.c);   <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>

<h2 id="let-與-var"><a href="#let-與-var" class="headerlink" title="let 與 var"></a>let 與 var</h2><p>與 <code>var</code> 不同的是，<code>let</code> 是一個 區塊 <code>block</code> 變數，<code>block</code> 意指 大括號 <code>&#123; &#125;</code>， 也就是說切分 let 有效範圍的最小單位是 大括號 <code>&#123; &#125;</code>，且使用 let 宣告的全域變數不會成為全域物件 <code>window</code> 的屬性</p>
<p>同上範例，我們將 <code>var</code> 都改成 <code>let</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = <span class="number">0</span>;</span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 0</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> b =<span class="number">0</span>; b &lt; <span class="number">2</span>; b++)&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(b);   <span class="comment">// 0, 1</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="built_in">console</span>.log(b);   <span class="comment">// b is not defined</span></span><br><span class="line"></span><br><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">let</span> c = <span class="number">3</span></span><br><span class="line">  <span class="built_in">console</span>.log(c);   <span class="comment">// 3</span></span><br><span class="line">&#125;)();</span><br><span class="line"><span class="built_in">console</span>.log(c);   <span class="comment">// c is not defined</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.a);   <span class="comment">// undefined</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.b);   <span class="comment">// undefined</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.c);   <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>

<h2 id="let-與-for-的用法"><a href="#let-與-for-的用法" class="headerlink" title="let 與 for 的用法"></a>let 與 for 的用法</h2><p>如下範例，我們要讓每一個 <code>li</code> 被點擊時都能顯示自己的數字</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">class</span>=<span class="string">&quot;list&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>1<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>2<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>3<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; i++)&#123;</span><br><span class="line">  <span class="built_in">document</span>.querySelectorAll(<span class="string">&#x27;.list li&#x27;</span>)[i].addEventListener(<span class="string">&#x27;click&#x27;</span>,<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(i + <span class="number">1</span>);</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但結果並不如預期，在點擊每個 <code>li</code> 都會是 <code>4</code>，因為這裡的變數 <code>i</code> 使用 <code>var</code> 來宣告，所以是全域變數，當跑完 for 迴圈的時候，所有 <code>console.log(i + 1)</code> 中的 <code>i</code> 都會被全域的 <code>i = 3</code> 取代</p>
<p>如果將上述範例的 <code>var</code> 改成 <code>let</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; i++)&#123;</span><br><span class="line">  <span class="built_in">document</span>.querySelectorAll(<span class="string">&#x27;.list li&#x27;</span>)[i].addEventListener(<span class="string">&#x27;click&#x27;</span>,<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(i + <span class="number">1</span>);</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>改成 <code>let</code> 來宣告的結果會如預期一樣，點擊每個 <code>li</code> 都是自己的數字，因為變數 <code>i</code> 並不會成為全域變數，所以 <code>console.log(i + 1)</code> 中的 <code>i</code> 都會是迴圈執行時當下取得的值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; i++)&#123;</span><br><span class="line">  <span class="built_in">document</span>.querySelectorAll(<span class="string">&#x27;.list li&#x27;</span>)[i].addEventListener(<span class="string">&#x27;click&#x27;</span>,<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(i + <span class="number">1</span>);</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>改成 let 來宣告的結果會如預期一樣，點擊每個 li 都是自己的數字，因為變數 i 並不會成為全域變數，所以 console.log(i + 1) 中的 i 都會是迴圈執行時當下取得的值</p>
<h2 id="const"><a href="#const" class="headerlink" title="const"></a>const</h2><ul>
<li><p>使用 <code>const</code> 宣告的變數又稱為 “常數” 或 “唯讀變數”</p>
</li>
<li><p>宣告完的變數無法再進行修改</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> a = <span class="number">1</span>;</span><br><span class="line">a = <span class="number">2</span>;  </span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 錯誤</span></span><br></pre></td></tr></table></figure></li>
<li><p>但使用 <code>const</code> 宣告的 物件 <code>&#123; &#125;</code> 和 陣列 <code>[]</code> 依然可以修改其內容，因為物件型別是 “傳址” <code>pass by reference</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> obj = &#123;</span><br><span class="line">  url: <span class="string">&#x27;a&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line">obj.url = <span class="string">&#x27;b&#x27;</span>;</span><br><span class="line"><span class="built_in">console</span>.log(obj.url);   <span class="comment">// b</span></span><br></pre></td></tr></table></figure></li>
<li><p>如果要使宣告的物件連屬性也無法修改，可以使用 <code>Object.freeze()</code> 來將物件凍結</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> obj = &#123;</span><br><span class="line">  url: <span class="string">&#x27;a&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="built_in">Object</span>.freeze(obj);</span><br><span class="line">obj.url = b;</span><br><span class="line"><span class="built_in">console</span>.log(obj.url);   <span class="comment">// a</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="let-與-const-的注意事項"><a href="#let-與-const-的注意事項" class="headerlink" title="let 與 const 的注意事項"></a>let 與 const 的注意事項</h2><p>ES6 變數的宣告與以往使用 <code>var</code> 有一些性質上的不同，以下一一介紹</p>
<ul>
<li><p>不會向上提升 Hoisting，從宣告的那一行開始成立</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> d;</span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// undefined</span></span><br><span class="line"><span class="built_in">console</span>.log(b);   <span class="comment">// ReferenceError</span></span><br><span class="line"><span class="built_in">console</span>.log(c);   <span class="comment">// ReferenceError</span></span><br><span class="line"><span class="built_in">console</span>.log(d);   <span class="comment">// undefined</span></span><br><span class="line"><span class="keyword">var</span> a = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">let</span> b = <span class="number">2</span>;</span><br><span class="line"><span class="keyword">const</span> c = <span class="number">3</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>同一個 “區塊” <code>block</code> 內不能重複命名</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">var</span> a = <span class="number">2</span>;</span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 2</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> b = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">let</span> b = <span class="number">2</span>;   <span class="comment">// SyntaxError</span></span><br><span class="line"><span class="keyword">const</span> c = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">const</span> c = <span class="number">2</span>;   <span class="comment">// SyntaxError</span></span><br></pre></td></tr></table></figure></li>
<li><p>在全域宣告的變數不會成為最高級物件 <code>window</code> 的屬性</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">let</span> b = <span class="number">2</span>;</span><br><span class="line"><span class="keyword">const</span> c = <span class="number">3</span>;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.a);   <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.b);   <span class="comment">// undefined </span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.c);   <span class="comment">// undefined </span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h2><ul>
<li><a href="https://www.udemy.com/javascript-learning/">JavaScript 入門篇 - 學徒的試煉</a></li>
<li><a href="https://ithelp.ithome.com.tw/users/20083608/ironman/1354">邁向 JavaScript 勇者之路</a></li>
<li><a href="http://es6.ruanyifeng.com/#docs/intro">ECMAScript 6 入門</a></li>
</ul>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript - ES6 - 箭頭函式</title>
    <url>/javascript/20191014/33154/</url>
    <content><![CDATA[<p><code>ES6</code> 開始可以使用箭頭 <code>=&gt;</code> 來定義涵式，但與原先的函式在某些情況並不完全相同，在這篇文章中會探討箭頭函式的寫法與傳統函式的差異</p>
<span id="more"></span>

<h2 id="基本寫法"><a href="#基本寫法" class="headerlink" title="基本寫法"></a>基本寫法</h2><ul>
<li><p>正常寫法，省略 <code>function</code> ，使用箭頭 <code>=&gt;</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fnc = <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">&#x27;Hello&#x27;</span> + e;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> fnc = <span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">&#x27;Hello&#x27;</span> + e ;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>如果只有內容只有一行，可以省略 “大括號” <code>&#123; &#125;</code> 和 <code>return</code>，並且自動回傳</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fnc = <span class="function">(<span class="params">e</span>) =&gt;</span> <span class="string">&#x27;Hello&#x27;</span> + e;</span><br></pre></td></tr></table></figure></li>
<li><p>如果只有 “一個參數”，可以省略 “小括號” <code>( )</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fnc = <span class="function"><span class="params">e</span> =&gt;</span> <span class="string">&#x27;Hello&#x27;</span> + e;</span><br></pre></td></tr></table></figure></li>
<li><p>如果 “沒有參數” 或 “多個參數”，就一定要有 “小括號” <code>( )</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fnc = <span class="function">() =&gt;</span> <span class="string">&#x27;Hello&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> fnc2 = <span class="function">(<span class="params">num1, num2</span>) =&gt;</span> num1 + num2;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="沒有-arguments-參數"><a href="#沒有-arguments-參數" class="headerlink" title="沒有 arguments 參數"></a>沒有 arguments 參數</h2><p>一般函式被呼叫時，都會產生一個 <code>argument</code> 物件，這個物件的內容就是我們帶入的 “參數”，而箭頭函式中是沒有 <code>arguments</code> 物件的</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fnc = <span class="function"><span class="keyword">function</span> (<span class="params">num1, num2</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log( <span class="built_in">arguments</span>.length );</span><br><span class="line">  <span class="keyword">return</span> num1 + num2;   </span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">fuc(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>);   <span class="comment">// 3</span></span><br></pre></td></tr></table></figure>

<h2 id="this-差異"><a href="#this-差異" class="headerlink" title="this 差異"></a>this 差異</h2><ul>
<li><p>傳統函式 : <code>this</code> 的值是動態的，由呼叫這個函式的擁有物件 <code>object</code> 而定</p>
</li>
<li><p>箭頭函式 : <code>this</code> 的值綁定到其定義時所在的物件而定，也就是由周邊的作用域而定</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name = <span class="string">&#x27;我是全域&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> test = &#123;</span><br><span class="line">  name: <span class="string">&#x27;我是作用域&#x27;</span>,</span><br><span class="line"></span><br><span class="line">  callName: <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&quot;1&quot;</span>, <span class="built_in">this</span>.name);     <span class="comment">// 1 我是作用域</span></span><br><span class="line">    <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">&quot;2&quot;</span>, <span class="built_in">this</span>.name);   <span class="comment">// 2 我是作用域</span></span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">&quot;3&quot;</span>, <span class="built_in">this</span>);        <span class="comment">// 3 test 這個物件</span></span><br><span class="line">    &#125;, <span class="number">100</span>);</span><br><span class="line">  &#125;,</span><br><span class="line"></span><br><span class="line">  callName2: <span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&quot;4&quot;</span>, <span class="built_in">this</span>.name);          <span class="comment">// 4 我是全域</span></span><br><span class="line">    <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">&quot;5&quot;</span>, <span class="built_in">this</span>.name);   <span class="comment">// 5 我是全域</span></span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">&quot;6&quot;</span>, <span class="built_in">this</span>);        <span class="comment">// 6 window 物件</span></span><br><span class="line">    &#125;, <span class="number">100</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">test.callName();</span><br><span class="line">test.callName2();</span><br></pre></td></tr></table></figure>

<p>由以上範例可以看到，傳統函式 <code>function</code> 的 <code>this</code> 指的是這個函式的擁有物件 <code>test</code>，而箭頭函式在全域被宣告定義，所以其 this 自然指向最高級物件 window</p>
<p>而外層的 “函式” 並不回影響箭頭函式的 <code>this</code> 原先指向的 <code>window</code>，外層的 “物件” 才會使箭頭函式的 <code>this</code> 指向這個物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> func = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> func2 = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line">    &#125;, <span class="number">10</span>);</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">var</span> func3 = &#123;</span><br><span class="line">    func4: func2</span><br><span class="line">  &#125;</span><br><span class="line">  func2();         <span class="comment">// window 物件</span></span><br><span class="line">  func3.func4();   <span class="comment">// func3 這個物件</span></span><br><span class="line">&#125;</span><br><span class="line">func();</span><br></pre></td></tr></table></figure>

<p>由上範例可以看到，如果 “不是” 建立在 “物件” 內的箭頭函式，就不會影響箭頭函式的 <code>this</code></p>
<ul>
<li>func() : 最外層的函式，不會影響內部箭頭函式的 <code>this</code></li>
<li>func2() : 第二層的函式，也不會影響內部箭頭函式的 <code>this</code></li>
<li>func3() : 為一個物件，內部箭頭函式的 <code>this</code> 就會指向這個物件</li>
</ul>
</li>
</ul>
<h2 id="特殊的情況"><a href="#特殊的情況" class="headerlink" title="特殊的情況"></a>特殊的情況</h2><p>內建函式 <code>call</code>、<code>apply</code> 和 <code>bind</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> obj = &#123; <span class="attr">a</span>: <span class="number">1</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> func1 = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> func2 = <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">func3</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">setTimeout</span>( <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line">  &#125;, <span class="number">1000</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">func4</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">setTimeout</span>( <span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>)</span><br><span class="line">  &#125;, <span class="number">1000</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> func5 = <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">setTimeout</span>( <span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>)</span><br><span class="line">  &#125;, <span class="number">1000</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func1.call(obj);   <span class="comment">// obj </span></span><br><span class="line">func2.call(obj);   <span class="comment">// window </span></span><br><span class="line">func3.call(obj);   <span class="comment">// window </span></span><br><span class="line">func4.call(obj);   <span class="comment">// obj </span></span><br><span class="line">func5.call(obj);   <span class="comment">// window</span></span><br></pre></td></tr></table></figure>

<ul>
<li><code>func1</code> : 透過 <code>call</code> 呼叫給定 <code>this</code> ，所以 <code>this</code> 自然指向 <code>obj</code> 這個物件</li>
<li><code>func2</code> : 箭頭函式沒有宣告在物件內部，所以 <code>this</code> 還是指向 <code>window</code></li>
<li><code>func3</code> : 傳統函式的 <code>this</code> 依據呼叫的方式，可以看成是 <code>window.setTimeout</code>，所以 <code>this</code> 指向 <code>window</code></li>
<li><code>func4</code> : 因為外層 <code>func4</code> 的 <code>this</code> 被指定為 <code>obj</code>，所以 <code>setTimeout</code> 內的箭頭函式會在 <code>obj</code> 中被宣告</li>
<li><code>func5</code> : 因為外層 <code>func5</code> 在全域宣告，所以 <code>this</code> 自然指向 <code>window，setTimeout</code> 內箭頭函式的 <code>this</code> 也會指向 window`</li>
</ul>
<h3 id="不能用在建構式函式"><a href="#不能用在建構式函式" class="headerlink" title="不能用在建構式函式"></a>不能用在建構式函式</h3><p>由於箭頭函式的 <code>this</code> 是在其所定義的物件下建立，所以不能像 <code>function</code> 一樣作為建構式的函式，直接使用 <code>new</code> 會出現錯誤</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> test = <span class="function">(<span class="params">num</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">this</span>.text = num;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> testAgain = <span class="keyword">new</span> test(<span class="string">&#x27;Hello World&#x27;</span>);</span><br><span class="line"><span class="comment">// Uncaught TypeError: test is not a constructor</span></span><br></pre></td></tr></table></figure>

<h3 id="DOM-事件的監聽"><a href="#DOM-事件的監聽" class="headerlink" title="DOM 事件的監聽"></a>DOM 事件的監聽</h3><p>在 <code>DOM</code> 事件監聽的情況下，函式的 <code>this</code> 應該指向這個 <code>DOM</code> 元素本身，但箭頭函式的 <code>this</code> 依然會指向定義其所在的物件 <code>window</code>，所以不適用於這類情況</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;.btn&#x27;</span>);</span><br><span class="line"><span class="keyword">var</span> addSomething = <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>);   <span class="comment">// window 物件</span></span><br><span class="line">  <span class="built_in">this</span>.innerHTML = <span class="string">&#x27;AddSomething&#x27;</span>;   <span class="comment">// Error</span></span><br><span class="line">&#125;</span><br><span class="line">            </span><br><span class="line">btn.addEventListener(<span class="string">&#x27;click&#x27;</span>, addSomething);</span><br></pre></td></tr></table></figure>

<h2 id="使用建議"><a href="#使用建議" class="headerlink" title="使用建議"></a>使用建議</h2><ul>
<li>物件內的函式可以優先使用，因為傳統函式以往的做法會先將 <code>this</code> 賦予再另一個變數上，以記錄當下的 <code>this</code></li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> test = &#123;</span><br><span class="line">  callName ()&#123;</span><br><span class="line">    <span class="keyword">var</span> that = <span class="built_in">this</span>;</span><br><span class="line">    <span class="built_in">setTimeout</span> (<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(that);   <span class="comment">// test 這個物件</span></span><br><span class="line">    &#125;, <span class="number">1000</span>);</span><br><span class="line">  &#125;,</span><br><span class="line">  callName2 ()&#123;</span><br><span class="line">    <span class="built_in">setTimeout</span> (<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="built_in">this</span>);   <span class="comment">// test 這個物件</span></span><br><span class="line">    &#125;, <span class="number">1000</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>雖然只有一個參數時可以省略 “小括號” <code>( )</code>，但還是建議都加上，避免意外錯誤</li>
</ul>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript - ES6 - 函式預設值</title>
    <url>/javascript/20191022/3401/</url>
    <content><![CDATA[<p>在 ES6 中，可以很簡單的給予函式參數預設值，只要在參數的位置加上等號 <code>=</code> 賦值即可，可以避免沒有帶入參數出現的錯誤</p>
<span id="more"></span>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">x, y</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(x + y);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">add();   <span class="comment">// NaN</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add2</span>(<span class="params">x = <span class="number">1</span>, y = <span class="number">2</span></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(x+y);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">add();   <span class="comment">// 3</span></span><br></pre></td></tr></table></figure>

<p>呼叫函式的參數都是 “由前往後”，當代入的參數數量未達到函式預設的參數數量，會優先帶入前面的參數</p>
<p>如果只帶入一個參數，而且只有預設第一個參數，還是會出現錯誤</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">x = <span class="number">1</span>, y</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(x + y);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">add(<span class="number">2</span>);   <span class="comment">// NaN</span></span><br></pre></td></tr></table></figure>

<p>如果只帶入一參數，但有預設後面的參數，就會順利完成函式呼叫</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">x, y = <span class="number">3</span></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(x + y);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">add(<span class="number">1</span>);   <span class="comment">// 4</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript - ES6 - 樣板字串</title>
    <url>/javascript/20191022/30921/</url>
    <content><![CDATA[<p>以往在組字串時，都是透過 引號 <code>&#39;</code>、<code>&quot;</code> 和 加號 <code>+</code> 來組合，換行也必須在結尾加上 反斜線 <code>\</code>，撰寫上容易混亂，閱讀也不夠自然，因此 ES6 新增了組字串的新方法，組起來也較為接近原生的 HTML</p>
<span id="more"></span>

<h2 id="ES6-字串基本寫法"><a href="#ES6-字串基本寫法" class="headerlink" title="ES6 字串基本寫法"></a>ES6 字串基本寫法</h2><p>ES6 新增的方法如下</p>
<ul>
<li><p>使用 重音符 ` 來取代引號包住字串</p>
</li>
<li><p>使用 <code>$&#123; &#125;</code> 來包住 “變數” 或 “函式”，取代加號 <code>+</code> 來連接</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">class</span>=<span class="string">&quot;list&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- after js --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">class</span>=<span class="string">&quot;list&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">h2</span>&gt;</span>我是標題<span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">&quot;logo.png&quot;</span> <span class="attr">alt</span>=<span class="string">&quot;&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">p</span>&gt;</span>我是內容<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> list = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;.list&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> title = <span class="string">&#x27;我是標題&#x27;</span>;</span><br><span class="line"><span class="keyword">const</span> imgUrl = <span class="string">&#x27;logo.png&#x27;</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">func</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">&#x27;我是內容&#x27;</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 傳統寫法</span></span><br><span class="line">list.innerHTML = <span class="string">&#x27;&lt;li&gt;\</span></span><br><span class="line"><span class="string">  &lt;h2&gt;&#x27;</span> + title + <span class="string">&#x27;&lt;/h2&gt;\</span></span><br><span class="line"><span class="string">  &lt;img src=&quot;&#x27;</span> + imgUrl + <span class="string">&#x27;&quot; alt=&quot;&quot;&gt;\</span></span><br><span class="line"><span class="string">  &lt;p&gt;&#x27;</span> + func() + <span class="string">&#x27;&lt;/p&gt;\</span></span><br><span class="line"><span class="string">&lt;/li&gt;&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ES6</span></span><br><span class="line">list.innerHTML = <span class="string">`</span></span><br><span class="line"><span class="string">&lt;li&gt;</span></span><br><span class="line"><span class="string">  &lt;h2&gt;<span class="subst">$&#123;title&#125;</span>&lt;/h2&gt;</span></span><br><span class="line"><span class="string">  &lt;img src=&quot;<span class="subst">$&#123;imgUrl&#125;</span>&quot; alt=&quot;&quot;&gt;</span></span><br><span class="line"><span class="string">  &lt;p&gt;<span class="subst">$&#123;func()&#125;</span>&lt;/p&gt;</span></span><br><span class="line"><span class="string">&lt;/li&gt;</span></span><br><span class="line"><span class="string">`</span>;</span><br></pre></td></tr></table></figure>

<ul>
<li>如果需要插入重音符 ` 或其他特殊字元，可以使用 “反斜線” \ 來進行轉義</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> text = <span class="string">`\`Hello\` World`</span>;</span><br><span class="line"><span class="built_in">console</span>.log(text);   <span class="comment">// `Hello` World</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="插入-JavaScript-原始碼"><a href="#插入-JavaScript-原始碼" class="headerlink" title="${} 插入 JavaScript 原始碼"></a>${} 插入 JavaScript 原始碼</h2><p>在 ES6 新增的 <code>$&#123;&#125;</code> 還可以直接插入 JavaScript 原始碼 ; 如下範例要將陣列的中的值帶到字串中，以下是一般寫法</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> people = [</span><br><span class="line">  &#123;</span><br><span class="line">    name: <span class="string">&#x27;魯夫&#x27;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    name: <span class="string">&#x27;索隆&#x27;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    name: <span class="string">&#x27;娜美&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> originUl = <span class="string">`</span></span><br><span class="line"><span class="string">  &lt;ul&gt;</span></span><br><span class="line"><span class="string">    &lt;li&gt; <span class="subst">$&#123;people[<span class="number">0</span>].name&#125;</span> &lt;/li&gt;</span></span><br><span class="line"><span class="string">    &lt;li&gt; <span class="subst">$&#123;people[<span class="number">1</span>].name&#125;</span> &lt;/li&gt;</span></span><br><span class="line"><span class="string">    &lt;li&gt; <span class="subst">$&#123;people[<span class="number">2</span>].name&#125;</span> &lt;/li&gt;</span></span><br><span class="line"><span class="string">  &lt;/ul&gt;</span></span><br><span class="line"><span class="string">`</span></span><br></pre></td></tr></table></figure>

<p>這裡我們透過 <code>map()</code> 將陣列做迴圈處理，並回傳處理後的結果，但處理後的結果因為原本是陣列，所以會用逗號 <code>,</code> 隔開，要再透過 <code>join()</code> 取消逗號 ,，而 <code>join()</code> 這個方法的預設是 <code>join(&#39;,&#39;)</code>，所以帶入空值 <code>join(&#39;&#39;)</code> 會取消逗號</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> newUl = <span class="string">`</span></span><br><span class="line"><span class="string">  &lt;ul&gt;</span></span><br><span class="line"><span class="string">    <span class="subst">$&#123;people.map(person =&gt; <span class="string">`&lt;li&gt; <span class="subst">$&#123;person.name&#125;</span> &lt;/li&gt;`</span>).join(<span class="string">&#x27;&#x27;</span>)&#125;</span></span></span><br><span class="line"><span class="string">  &lt;/ul&gt;</span></span><br><span class="line"><span class="string">`</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript - ES6 - 縮寫</title>
    <url>/javascript/20191022/54708/</url>
    <content><![CDATA[<p>ES6 提供了所多縮寫的方式，使程式碼更為簡潔</p>
<span id="more"></span>

<h2 id="物件縮寫"><a href="#物件縮寫" class="headerlink" title="物件縮寫"></a>物件縮寫</h2><p>以往物件要賦予到另一個物件的相同名稱屬性上時，需要寫成相同的 <code>屬性:物件</code>， ES6 開始可以不用重複撰寫，只要寫一次即可</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> name = <span class="string">&#x27;草帽黑賊團&#x27;</span>;</span><br><span class="line"><span class="keyword">const</span> team = &#123;</span><br><span class="line">  船長: <span class="string">&#x27;魯夫&#x27;</span>,</span><br><span class="line">  船醫: <span class="string">&#x27;喬巴&#x27;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 以往寫法 </span></span><br><span class="line"><span class="keyword">const</span> newTeam = &#123;</span><br><span class="line">  name: name,</span><br><span class="line">  team: team</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ES6 縮寫</span></span><br><span class="line"><span class="keyword">const</span> newTeam = &#123;</span><br><span class="line">  name,</span><br><span class="line">  team</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="物件函式縮寫"><a href="#物件函式縮寫" class="headerlink" title="物件函式縮寫"></a>物件函式縮寫</h2><p>當物件屬性的值是一個函式時，可以省略 <code>:function</code>，語意上不會有變化，並不會轉為箭頭函式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 以往寫法</span></span><br><span class="line"><span class="keyword">const</span> team = &#123;</span><br><span class="line">  slogan: <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;我要成為海賊王&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ES6 縮寫</span></span><br><span class="line"><span class="keyword">const</span> team = &#123;</span><br><span class="line">  slogan ()&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;我要成為海賊王&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="變數作為物件屬性"><a href="#變數作為物件屬性" class="headerlink" title="變數作為物件屬性"></a>變數作為物件屬性</h2><p>ES6 開始可以使用 “中括號” <code>[]</code> 來包住變數作為物件的屬性</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> name = <span class="string">&#x27;海賊團名稱&#x27;</span>;</span><br><span class="line"><span class="keyword">let</span> value = <span class="string">&#x27;草海賊團&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> team = &#123;</span><br><span class="line">  [name]: value</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 會等於以下結果</span></span><br><span class="line"><span class="keyword">let</span> team = &#123;</span><br><span class="line">  海賊團名稱: <span class="string">&#x27;草帽海賊團&#x27;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript - ES6 - class 建構式語法糖</title>
    <url>/javascript/20191023/6199/</url>
    <content><![CDATA[<p>JavaScript 與其他物件導向的語言不太一樣，是以 “原型” <code>prototype</code> 為基礎的物件導向，ES6 新增了 <code>class</code> 這個語法糖 <code>syntactical sugar</code>，使得原型的建構更簡潔好閱讀</p>
<span id="more"></span>

<p>以下列出 <code>class</code> 與原先建構原型的差異</p>
<h2 id="class-用法"><a href="#class-用法" class="headerlink" title="class 用法"></a>class 用法</h2><ul>
<li><code>function</code> 改成 <code>class</code></li>
<li>初始化的物件屬性都寫在 <code>constructor()</code> 內，一個 <code>class</code> 只能有一個 <code>constructor</code></li>
<li>原型的 “方法” 也可以一併寫在 <code>class</code> 內</li>
</ul>
<p>以下先列出 ES6 以前的寫法</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">strawHat</span>(<span class="params">name, position, devilFruit</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.name = name;</span><br><span class="line">  <span class="built_in">this</span>.position = position;</span><br><span class="line">  <span class="built_in">this</span>.devilFruit = devilFruit || <span class="literal">false</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 原型方法</span></span><br><span class="line">strawHat.prototype.capability = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">this</span>.devilFruit)&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="built_in">this</span>.name&#125;</span> 是能力者`</span>);</span><br><span class="line">  &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="built_in">this</span>.name&#125;</span> 不是能力者`</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">strawHat.prototype.defeat = <span class="function"><span class="keyword">function</span> (<span class="params">person</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="built_in">this</span>.name&#125;</span> k.o <span class="subst">$&#123;person&#125;</span>`</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> Luffy = <span class="keyword">new</span> strawHat(<span class="string">&#x27;魯夫&#x27;</span>, <span class="string">&#x27;船長&#x27;</span>, <span class="literal">true</span>);</span><br><span class="line"><span class="keyword">var</span> zoro = <span class="keyword">new</span> strawHat(<span class="string">&#x27;索隆&#x27;</span>, <span class="string">&#x27;戰鬥員&#x27;</span>, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">Luffy.capability();   <span class="comment">// 魯夫 是能力者</span></span><br><span class="line">zoro.defeat(<span class="string">&#x27;卡古&#x27;</span>);   <span class="comment">// 索隆 k.o 卡古</span></span><br></pre></td></tr></table></figure>

<p>將上述範例換成 ES6 寫法</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">strawHat</span> </span>&#123;</span><br><span class="line">  <span class="title">constructor</span> (<span class="params">name, position, devilFruit</span>)&#123;</span><br><span class="line">    <span class="built_in">this</span>.name = name;</span><br><span class="line">    <span class="built_in">this</span>.position = position;</span><br><span class="line">    <span class="built_in">this</span>.devilFruit = devilFruit || <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  capability ()&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">this</span>.devilFruit)&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="built_in">this</span>.name&#125;</span> 是能力者`</span>);</span><br><span class="line">    &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="built_in">this</span>.name&#125;</span> 不是能力者`</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  defeat (person)&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="built_in">this</span>.name&#125;</span> k.o <span class="subst">$&#123;person&#125;</span>`</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="靜態方法-static"><a href="#靜態方法-static" class="headerlink" title="靜態方法 static"></a>靜態方法 static</h2><p>靜態方法 <code>static</code> 是原型專屬的方法，不會繼承到新物件上</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">strawHat</span> </span>&#123;</span><br><span class="line">  <span class="title">constructor</span> (<span class="params">name, position, devilFruit</span>)&#123;</span><br><span class="line">    <span class="built_in">this</span>.name = name;</span><br><span class="line">    <span class="built_in">this</span>.position = position;</span><br><span class="line">    <span class="built_in">this</span>.devilFruit = devilFruit || <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  capability ()&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">this</span>.devilFruit)&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="built_in">this</span>.name&#125;</span> 是能力者`</span>);</span><br><span class="line">    &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="built_in">this</span>.name&#125;</span> 不是能力者`</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">static</span> solgan  ()&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`我要成為海賊王 !`</span>);</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">zoro.solgan();   <span class="comment">// Error</span></span><br><span class="line">strawHat.solgan();  <span class="comment">// 我要成為海賊王 !</span></span><br></pre></td></tr></table></figure>

<h2 id="屬性的-get-amp-set-方法"><a href="#屬性的-get-amp-set-方法" class="headerlink" title="屬性的 get &amp; set 方法"></a>屬性的 get &amp; set 方法</h2><p>在 ES6 的 <code>class</code> 也可以一併將屬性的 讀入 <code>set</code> 和 “傳出” <code>get</code></p>
<ul>
<li><code>set</code> : 只能傳入一個參數，這個參數就是讀入的屬性值，通常都會加上下底線 <code>_</code> 將屬性值進行 “封裝”</li>
<li><code>get</code> : 用來回傳 <code>set</code> 封裝好的屬性值</li>
</ul>
<p>所以如果只有 <code>get</code> 就無法將屬性值修改，而只有 <code>set</code> 則是修改後無法回傳</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">  <span class="title">constructor</span> (<span class="params">name, position</span>)&#123;</span><br><span class="line">    <span class="built_in">this</span>.name = name;</span><br><span class="line">    <span class="built_in">this</span>.position = position;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  get name ()&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>._name;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">set</span> <span class="title">name</span>(<span class="params">name</span>)&#123;</span><br><span class="line">    <span class="built_in">this</span>._name = <span class="string">`我是 <span class="subst">$&#123;name&#125;</span>`</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> zoro = <span class="keyword">new</span> Person;</span><br><span class="line"></span><br><span class="line">zoro.name = <span class="string">&#x27;索隆&#x27;</span>;</span><br><span class="line"><span class="built_in">console</span>.log(p.name);   <span class="comment">// 我是索隆</span></span><br></pre></td></tr></table></figure>

<h2 id="extends-擴增原型"><a href="#extends-擴增原型" class="headerlink" title="extends 擴增原型"></a>extends 擴增原型</h2><p>當我們以這個物件為原型建立了許多新物件，其中有新物件想要修改屬性或是新增方法，但又不希望影響到其他的新物件，這時候我們就可以透過 <code>extends</code> 來擴增新的原型，有點像是 <code>git</code> 建立分支的概念</p>
<ul>
<li><code>extends</code> : 用來指定要繼承的原型</li>
<li><code>super</code> : 要沿用的屬性可以放在這裡</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">slogan</span> <span class="keyword">extends</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">  <span class="title">constructor</span> (<span class="params">name, position</span>)&#123;</span><br><span class="line">    <span class="built_in">super</span>(name);</span><br><span class="line">    <span class="built_in">this</span>.position = <span class="string">`位置 : <span class="subst">$&#123;position&#125;</span>`</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  solgan ()&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;我要成為海賊王&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> zoro = <span class="keyword">new</span> person(<span class="string">&#x27;索隆&#x27;</span>);</span><br><span class="line"><span class="keyword">let</span> Luffy = <span class="keyword">new</span> slogan(<span class="string">&#x27;魯夫&#x27;</span>, <span class="string">&#x27;船長&#x27;</span>);</span><br><span class="line">Luffy.solgan();   <span class="comment">// 我要成為海賊王</span></span><br><span class="line">zoro.solgan();   <span class="comment">// Error</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript - ES6 - 展開與其餘參數</title>
    <url>/javascript/20191023/13950/</url>
    <content><![CDATA[<p>在 ES6 中增加了一個新的方法來展開陣列中的值，這個方法就是展開 <code>...</code></p>
<span id="more"></span>

<h2 id="展開-…"><a href="#展開-…" class="headerlink" title="展開 …"></a>展開 …</h2><p>如下範例，我們將一班的陣列在前方加上 <code>...</code> 後，陣列的資料內容就會被一一展示開來 </p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> groupA = [<span class="string">&#x27;魯夫&#x27;</span>, <span class="string">&#x27;索隆&#x27;</span>, <span class="string">&#x27;娜美&#x27;</span>];</span><br><span class="line"><span class="keyword">let</span> groupB = [<span class="string">&#x27;香吉士&#x27;</span>, <span class="string">&#x27;騙人布&#x27;</span>];</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(...groupA);   <span class="comment">// 魯夫 喬巴 索隆</span></span><br></pre></td></tr></table></figure>

<p>透過展開這個方法，我們還可以連接陣列 ; 順便比較 ES6 以前的方法</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> onePiece = [...groupA, ...groupB];</span><br><span class="line"><span class="keyword">const</span> onePiece2 = groupA.concat(groupB);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(onePiece);   <span class="comment">// [&quot;魯夫&quot;, &quot;索隆&quot;, &quot;娜美&quot;, &quot;香吉士&quot;, &quot;騙人布&quot;]</span></span><br><span class="line"><span class="built_in">console</span>.log(onePiece2);   <span class="comment">// [&quot;魯夫&quot;, &quot;索隆&quot;, &quot;娜美&quot;, &quot;香吉士&quot;, &quot;騙人布&quot;]</span></span><br></pre></td></tr></table></figure>

<h3 id="淺層複製-Shallow-copy"><a href="#淺層複製-Shallow-copy" class="headerlink" title="淺層複製 Shallow copy"></a>淺層複製 Shallow copy</h3><p>一般來說因為陣列也是物件，所以直接賦值會透過 “傳址” 的方式來傳遞，而展開方法是一種 “淺層複製” shallow copy，因為是將原陣列先拆開再放入新陣列中，所以是透過 “傳值” 的方式複製第一層資料</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> groupA = [<span class="string">&#x27;魯夫&#x27;</span>, <span class="string">&#x27;索隆&#x27;</span>, <span class="string">&#x27;娜美&#x27;</span>];</span><br><span class="line"><span class="keyword">let</span> groupB = groupA;</span><br><span class="line">ler groupC = [...groupA]</span><br><span class="line"></span><br><span class="line">groupA.push(<span class="string">&#x27;香吉士&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(groupA);   <span class="comment">// [&quot;魯夫&quot;, &quot;索隆&quot;, &quot;娜美&quot;, &quot;香吉士&quot;]</span></span><br><span class="line"><span class="built_in">console</span>.log(groupB);   <span class="comment">// [&quot;魯夫&quot;, &quot;索隆&quot;, &quot;娜美&quot;, &quot;香吉士&quot;]</span></span><br><span class="line"><span class="built_in">console</span>.log(groupC);   <span class="comment">// [&#x27;魯夫&#x27;, &#x27;索隆&#x27;, &#x27;娜美&#x27;]</span></span><br></pre></td></tr></table></figure>

<h3 id="類陣列-Array-like-轉陣列-Array"><a href="#類陣列-Array-like-轉陣列-Array" class="headerlink" title="類陣列 Array-like 轉陣列 Array"></a>類陣列 Array-like 轉陣列 Array</h3><p>在 JavaScript 中，有許多有著陣列外觀卻無法使用所有陣列方法的物件我們稱為 “類陣列” <code>Array-like</code>，類陣列並不與一般陣列共用同一個原型，常見的類陣列如 <code>DOM 陣列</code> 和 <code>Argument</code> </p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> doms = <span class="built_in">document</span>.querySelectorAll(<span class="string">&#x27;p&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">arg</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">arguments</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(arg(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>));   <span class="comment">// Arguments(3) [1, 2, 3]</span></span><br></pre></td></tr></table></figure>

<p>這些類陣列 <code>Array-like</code> 可以透過 “展開” <code>...</code> 轉為真正的陣列</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> spreadDoms = [...doms];</span><br><span class="line"><span class="keyword">let</span> spreadArg = [...arg(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)];</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(spreadArg);   <span class="comment">// [1, 2, 3]</span></span><br></pre></td></tr></table></figure>

<h2 id="其餘參數"><a href="#其餘參數" class="headerlink" title="其餘參數"></a>其餘參數</h2><p>如果帶入的參數數量大於預設的數量，可以透過展開 <code>...</code> 的方式將多餘的參數集合到陣列中</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">onePiece</span>(<span class="params">caption, ...crew</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(caption, crew);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">onePiece(<span class="string">&#x27;魯夫&#x27;</span>, <span class="string">&#x27;索隆&#x27;</span>, <span class="string">&#x27;娜美&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// in console</span></span><br><span class="line"><span class="string">&#x27;魯夫&#x27;</span> [<span class="string">&#x27;索隆&#x27;</span>, <span class="string">&#x27;娜美&#x27;</span>]</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript - ES6 - 解構賦值 deconstructing</title>
    <url>/javascript/20191023/13442/</url>
    <content><![CDATA[<p>過去陣列和物件內的元素在賦值時，都是透過一一給值的方式達成，在 ES6 中，可以透過解構的方式一次賦值，有效減少程式碼</p>
<span id="more"></span>

<h2 id="陣列解構賦值-Array-deconstructing"><a href="#陣列解構賦值-Array-deconstructing" class="headerlink" title="陣列解構賦值 Array deconstructing"></a>陣列解構賦值 Array deconstructing</h2><p>以往陣列賦值都是直接給值如下</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> onePiece = [<span class="string">&quot;魯夫&quot;</span>, <span class="string">&quot;索隆&quot;</span>, <span class="string">&quot;娜美&quot;</span>, <span class="string">&quot;香吉士&quot;</span>, <span class="string">&quot;騙人布&quot;</span>];</span><br><span class="line"><span class="keyword">let</span> captain = onePiece[<span class="number">0</span>];</span><br><span class="line"><span class="keyword">let</span> fighter = onePiece[<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(captain);   <span class="comment">// 魯夫</span></span><br></pre></td></tr></table></figure>

<h3 id="ES6-一般用法"><a href="#ES6-一般用法" class="headerlink" title="ES6 一般用法"></a>ES6 一般用法</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> [captain, fighter, navigator, chef, sniper] = onePiece;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(captain);   <span class="comment">// 魯夫</span></span><br></pre></td></tr></table></figure>

<h3 id="輸入的變數量-“少於”-給得值或遇到空變數"><a href="#輸入的變數量-“少於”-給得值或遇到空變數" class="headerlink" title="輸入的變數量 “少於” 給得值或遇到空變數"></a>輸入的變數量 “少於” 給得值或遇到空變數</h3><p>如果輸入的變數量 “少於” 給得值或遇到空變數，只有指定到的變數有值，其餘都會直接跳過</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> [captain, fighter, , chef] = onePiece;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(fighter);   <span class="comment">// 索隆</span></span><br><span class="line"><span class="built_in">console</span>.log(chef);   <span class="comment">// 香吉士</span></span><br></pre></td></tr></table></figure>

<h3 id="輸入的變數量-“多於”-給得值"><a href="#輸入的變數量-“多於”-給得值" class="headerlink" title="輸入的變數量 “多於” 給得值"></a>輸入的變數量 “多於” 給得值</h3><p>而如果輸入的變數量 “多於” 給得值就會賦予 <code>undefined</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> [captain, fighter, navigator, chef, sniper, doctor] = onePiece</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(doctor);   <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>

<h3 id="賦予預設值"><a href="#賦予預設值" class="headerlink" title="賦予預設值"></a>賦予預設值</h3><p>在輸入的變數中也可以賦予預設值，如果變數沒有取得對應的值，就會直接帶入預設值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> [captain = <span class="string">&#x27;從缺&#x27;</span>, fighter, navigator, chef, sniper, doctor = <span class="string">&#x27;喬巴&#x27;</span>] = onePiece</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(captain);   <span class="comment">// 魯夫</span></span><br><span class="line"><span class="built_in">console</span>.log(doctor);   <span class="comment">// 喬巴</span></span><br></pre></td></tr></table></figure>

<h3 id="互換變數"><a href="#互換變數" class="headerlink" title="互換變數"></a>互換變數</h3><p>傳值是有順序性的，且傳遞是及時的，不需要第三方變數來取代，所以可以直接透過解構賦值的方式互換變數順序以交換變數值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> captain = <span class="string">&#x27;魯夫&#x27;</span>;</span><br><span class="line"><span class="keyword">let</span> fakeCaptain = <span class="string">&#x27;迪馬爾·布拉克&#x27;</span>;</span><br><span class="line">          </span><br><span class="line">[captain, fakeCaptain] = [fakeCaptain, captain];</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(captain);   <span class="comment">// 迪馬爾·布拉克</span></span><br></pre></td></tr></table></figure>

<h3 id="賦予字串"><a href="#賦予字串" class="headerlink" title="賦予字串"></a>賦予字串</h3><p>如果解構方法所賦予的值是字串時，就會將字串一一拆解成單一字元賦予到變數上</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> promise = <span class="string">&quot;3D2Y&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> [three, day, two, year] = promise;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(three);   <span class="comment">// 3</span></span><br><span class="line"><span class="built_in">console</span>.log(year);   <span class="comment">// Y</span></span><br></pre></td></tr></table></figure>

<h2 id="物件解構賦值-Object-deconstructing"><a href="#物件解構賦值-Object-deconstructing" class="headerlink" title="物件解構賦值 Object deconstructing"></a>物件解構賦值 Object deconstructing</h2><p>物件解構和陣列解構有相同的解構概念，不同的地方在於對應的值，陣列解構是透過索引的 “順序” 對應，而物件解構是透過 “屬性名稱” 來對應</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> onePiece = &#123;</span><br><span class="line">  captain: <span class="string">&#x27;魯夫&#x27;</span>,</span><br><span class="line">  fighter: <span class="string">&#x27;索隆&#x27;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> &#123;captain, fighter&#125; = onePiece;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(captain);   <span class="comment">// 魯夫</span></span><br></pre></td></tr></table></figure>

<p>要注意的是新的物件屬性名稱必須和原物件屬性名稱相同，因為以上寫法其實是屬性和值的名稱相同時的 “縮寫”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123;<span class="attr">captain</span>: captain, <span class="attr">fighter</span>:fighter&#125;</span><br></pre></td></tr></table></figure>

<p>冒號 <code>:</code> 前的屬性名稱只是對應，真正賦值的是冒號 <code>:</code> 後的值，從改變值的名稱就可以了解其中概念</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123;<span class="attr">captain</span>: cap, <span class="attr">fighter</span>: fight&#125; = onePiece;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(captain);   <span class="comment">// Error</span></span><br><span class="line"><span class="built_in">console</span>.log(cap);   <span class="comment">// 魯夫</span></span><br></pre></td></tr></table></figure>

<h3 id="輸入的變數量與給得值-“不同”-或遇到空變數"><a href="#輸入的變數量與給得值-“不同”-或遇到空變數" class="headerlink" title="輸入的變數量與給得值 “不同” 或遇到空變數"></a>輸入的變數量與給得值 “不同” 或遇到空變數</h3><p>與陣列不同的是，因為物件是採屬性名稱對應而不是順序對應，所以遇到空變數會出現錯誤 <code>Error</code>，變數量與給得值也沒有數量對應問題，沒有對應到相同屬性就是 <code>undefined</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123;captain, , navigator&#125; = onePiece;   <span class="comment">// Error</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(navigator);   <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>

<h3 id="賦予預設值-1"><a href="#賦予預設值-1" class="headerlink" title="賦予預設值"></a>賦予預設值</h3><p>同樣的物件解構也可以賦予預設值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123;captain = <span class="string">&#x27;魯夫&#x27;</span>, fighter&#125; = &#123;<span class="attr">fighter</span>: <span class="string">&#x27;索隆&#x27;</span>&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(captain);   <span class="comment">// &#x27;魯夫&#x27;</span></span><br><span class="line"><span class="built_in">console</span>.log(fighter);   <span class="comment">// &#x27;索隆&#x27;</span></span><br></pre></td></tr></table></figure>

<h2 id="應用"><a href="#應用" class="headerlink" title="應用"></a>應用</h2><p>以下介紹幾個在 ES6 解構賦值常使用到的情境</p>
<h3 id="指定參數"><a href="#指定參數" class="headerlink" title="指定參數"></a>指定參數</h3><p>解構賦值有許多實際的應用，若將其運用到函式參數中，就能 “指定” 要帶入的參數而不是直接按照順序帶入</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">battle</span>(<span class="params">&#123;fighter1 = <span class="string">&#x27;魯夫&#x27;</span>, fighter2 &#125;</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;fighter1&#125;</span> k.o <span class="subst">$&#123;fighter2&#125;</span>`</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">battle(&#123;&#125;);   <span class="comment">// 魯夫 k.o undefined</span></span><br><span class="line">battle(&#123;<span class="attr">fighter2</span>: <span class="string">&#x27;大仔&#x27;</span>&#125;);   <span class="comment">// 魯夫 k.o 大仔</span></span><br><span class="line">battle(&#123;<span class="attr">fighter2</span>: <span class="string">&#x27;龍馬&#x27;</span>, <span class="attr">fighter1</span>: <span class="string">&#x27;索隆&#x27;</span>&#125;);    <span class="comment">// 索隆 k.o 龍馬</span></span><br></pre></td></tr></table></figure>

<h3 id="解構賦值搭配縮寫與展開"><a href="#解構賦值搭配縮寫與展開" class="headerlink" title="解構賦值搭配縮寫與展開"></a>解構賦值搭配縮寫與展開</h3><p>在前面提到，如果直接將物件賦予到新物件上，是透過 “傳址” 的方式複製，如果改變其一，兩者都會更改</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> onePiece = &#123;</span><br><span class="line">  captain: &#123;</span><br><span class="line">    name: <span class="string">&#x27;魯夫&#x27;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  fighter: &#123;</span><br><span class="line">    name: <span class="string">&#x27;索隆&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> newTeam = onePiece;</span><br><span class="line">newTeam.navigator = &#123;<span class="attr">name</span>: <span class="string">&#x27;娜美&#x27;</span>&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(onePiece.navigator.name);   <span class="comment">// &#x27;娜美&#x27;</span></span><br></pre></td></tr></table></figure>

<p>這時候就可以透過 “解構賦值” 與 “縮寫” 的方式賦予新物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> newTeam = &#123;</span><br><span class="line">  onePiece</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但這樣的結果形式和原來的不一樣，因為是將物件直接帶入，所以內容是物件 onePiece 和 變數 navigator，所以要透過展開 <code>...</code> 將物件內容釋放出來</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> newTeam = &#123;</span><br><span class="line">  ...onePiece</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其他應用可以參考以下連結</p>
<p><a href="http://es6.ruanyifeng.com/#docs/destructuring">变量的解构赋值</a></p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript - ES6 - 陣列方法</title>
    <url>/javascript/20191023/25937/</url>
    <content><![CDATA[<p>ES6 以前在處理陣列都是使用 <code>for</code> 迴圈，以陣列元素的數量來設定 <code>for 迴圈</code> 的次數，ES6 新增了許多方法來搭配 箭頭函式 <code>=&gt;</code>、樣板字串 <code>template-string</code> 使程式碼更精簡、直覺</p>
<span id="more"></span>

<p>以下是以往處理陣列中的資料時，透過 <code>for 迴圈</code> 一一取出資料的方法</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> wanted = [</span><br><span class="line">  &#123;</span><br><span class="line">    name: <span class="string">&#x27;魯夫&#x27;</span>,</span><br><span class="line">    price: <span class="number">15</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    name: <span class="string">&#x27;索隆&#x27;</span>,</span><br><span class="line">    price: <span class="number">3.2</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    name: <span class="string">&#x27;香吉士&#x27;</span>,</span><br><span class="line">    price: <span class="number">3.3</span></span><br><span class="line">  &#125;</span><br><span class="line">];</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> ( <span class="keyword">let</span> i = <span class="number">0</span>; i &lt; wanted.length; i++)&#123;</span><br><span class="line">  <span class="built_in">console</span>.log( wanted[i].name + <span class="string">&#x27; 懸賞 &#x27;</span> + wanted[i].price + <span class="string">&#x27; 億貝利&#x27;</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 魯夫 懸賞 15 億貝利</span></span><br><span class="line"><span class="comment">// 索隆 懸賞 3.2 億貝利</span></span><br><span class="line"><span class="comment">// 香吉士 懸賞 3.3 億貝利</span></span><br></pre></td></tr></table></figure>

<p>而 jQuery 當然也有對應的方法來處理陣列，就是 <code>$.each(array, function(index, item)&#123;&#125;)</code>，結果同上</p>
<ul>
<li><code>array</code> : 要帶入的陣列</li>
<li><code>index</code> : 索引</li>
<li><code>item</code> : 陣列中的元素</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$.each(wanted, <span class="function"><span class="keyword">function</span> (<span class="params">i, person</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(person.name + <span class="string">&#x27; 懸賞 &#x27;</span> + person.price + <span class="string">&#x27; 億貝利&#x27;</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>以下一一介紹 ES6 所新增的陣列方法</p>
<h2 id="forEach"><a href="#forEach" class="headerlink" title="forEach()"></a>forEach()</h2><p>ES6 的 <code>array.forEach((item, index, array) =&gt; &#123;&#125;)</code> 和 jQuery 的方法很類似，只是帶入的參數位置不太一樣，以下範例同時加入 “箭頭函式” 和 “樣板字串”</p>
<ul>
<li><code>item</code> : 陣列中的元素</li>
<li><code>index</code> : 索引</li>
<li><code>array</code> : 整個陣列，也較少用到</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">wanted.forEach(<span class="function"><span class="params">person</span> =&gt;</span> <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;person.name&#125;</span> 懸賞 <span class="subst">$&#123;person.price&#125;</span> 億貝利`</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">// in console</span></span><br><span class="line"></span><br><span class="line">魯夫 懸賞 <span class="number">15</span> 億貝利</span><br><span class="line">索隆 懸賞 <span class="number">3.2</span> 億貝利</span><br><span class="line">香吉士 懸賞 <span class="number">3.3</span> 億貝利</span><br></pre></td></tr></table></figure>

<h3 id="無法被中斷"><a href="#無法被中斷" class="headerlink" title="無法被中斷"></a>無法被中斷</h3><p><code>forEach()</code> 無法被中斷，如果加入 <code>break</code> 會出現錯誤 <code>Error</code>，所以如果需要中斷還是要使用 <code>for 迴圈</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">wanted.forEach(<span class="function"><span class="params">person</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (person.price &lt; <span class="number">10</span>)&#123;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;person.name&#125;</span> 懸賞 <span class="subst">$&#123;person.price&#125;</span> 億貝利`</span>)</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="無法使用的陣列"><a href="#無法使用的陣列" class="headerlink" title="無法使用的陣列"></a>無法使用的陣列</h3><p>在物件中，有許多物件有著陣列的外觀但不能使用陣列的所有方法稱為 “類陣列” <code>Array-like</code>，其中有部分的類陣列 “不能” 使用 <code>forEach()</code></p>
<ul>
<li><code>arguments</code> : 無法使用 <code>forEach()</code>，會跳錯</li>
<li><code>DOM 陣列</code> : 可以使用 <code>forEach()</code></li>
</ul>
<p>判斷物件是否有指定的方法可以從 <code>console</code> 中的原型 <code>__proto__</code> 來查看</p>
<p><img src="forEach.png"></p>
<h3 id="不會回傳"><a href="#不會回傳" class="headerlink" title="不會回傳"></a>不會回傳</h3><p>在 <code>forEach()</code> 中不會有回傳值，所以將其賦予一個變數會得到 <code>undefined</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> forEachReturn = wanted.forEach(<span class="function"><span class="params">person</span> =&gt;</span> <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;person.name&#125;</span> 懸賞 <span class="subst">$&#123;person.price&#125;</span> 億貝利`</span>));</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(forEachReturn);   <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>

<h2 id="filter"><a href="#filter" class="headerlink" title="filter()"></a>filter()</h2><p>顧名思義 <code>filter()</code> 可以用來過濾陣列中符合條件的元素，但並不會對元素進行修改，將條件寫在 <code>return</code> 中就會回傳，如果沒有設定條件就會回傳空陣列</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> filterEmpty = wanted.filter(<span class="function">(<span class="params">item, index, array</span>) =&gt;</span> &#123;&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(filterEmpty);   <span class="comment">// []</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> filter = wanted.filter(<span class="function">(<span class="params">item, index, array</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> item.price &gt; <span class="number">10</span>;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(filter);   <span class="comment">// [&#123;name: &#x27;魯夫&#x27;, price: 15]</span></span><br></pre></td></tr></table></figure>

<h2 id="find"><a href="#find" class="headerlink" title="find()"></a>find()</h2><p>用來搜尋陣列中符合條件的物件，但 “只能有一個”，如果符合條件的物件大於一個，就只會回傳第一個 ; 同樣的，如果沒有設定條件就會回傳 <code>undefined</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> findEmpty = wanted.find(<span class="function">(<span class="params">item, index, array</span>) =&gt;</span> &#123;&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(findEmpty);   <span class="comment">// []</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> find = wanted.find(<span class="function">(<span class="params">item, index, array</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> item.price &lt; <span class="number">10</span></span><br><span class="line">&#125;);</span><br><span class="line">          </span><br><span class="line"><span class="built_in">console</span>.log(find);   <span class="comment">// [&#123;name: &#x27;索隆&#x27;, price: 3.2&#125;]</span></span><br></pre></td></tr></table></figure>

<h2 id="map"><a href="#map" class="headerlink" title="map()"></a>map()</h2><p><code>map()</code> 與 <code>forEach</code> 最大的不同就是可以回傳新陣列，並透過新的變數來接收，如下範例因為是透過展開 <code>...</code> 賦值，所以兩個陣列並沒有任何關聯</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> newWanted = people.map(<span class="function">(<span class="params">item, index, array</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    ...item,</span><br><span class="line">    newPrice: item.price + <span class="number">1</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(newPeople);</span><br><span class="line">[</span><br><span class="line">  &#123;<span class="attr">name</span>: <span class="string">&quot;魯夫&quot;</span>, <span class="attr">price</span>: <span class="number">15</span>, <span class="attr">newPrice</span>: <span class="number">16</span>&#125;, </span><br><span class="line">  &#123;<span class="attr">name</span>: <span class="string">&quot;索隆&quot;</span>, <span class="attr">price</span>: <span class="number">3.2</span>, <span class="attr">newPrice</span>: <span class="number">4.2</span>&#125;,</span><br><span class="line">  &#123;<span class="attr">name</span>: <span class="string">&quot;香吉士&quot;</span>, <span class="attr">price</span>: <span class="number">3.3</span>, <span class="attr">newPrice</span>: <span class="number">4.3</span>&#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<p>回傳的新陣列與原陣列長度相同，不符合的物件會出現 <code>undeined</code>，如果沒有寫 <code>return</code> 還是全部都會回傳 <code>undefined</code>，所以如果要過濾元素，就不適合使用 <code>map</code>，<code>map</code> 適合單純調整元素的內容</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> mapEmpty = wanted.map(<span class="function">(<span class="params">item, index, array</span>) =&gt;</span> &#123;&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(mapEmpty);   <span class="comment">// []</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> mapAdd = wanted.map(<span class="function">(<span class="params">item, index, array</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> item.price = item.price + <span class="number">1</span>;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(mapAdd);   [<span class="number">16</span>, <span class="number">4.2</span>, <span class="number">4.3</span>]</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> mapTenUp = wanted.map(<span class="function">(<span class="params">item, index, array</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (item.price &gt; <span class="number">10</span>)&#123;</span><br><span class="line">    <span class="keyword">return</span> item</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;);</span><br><span class="line">          </span><br><span class="line"><span class="built_in">console</span>.lgo(mapTenUp);   <span class="comment">// [&#123;name: &#x27;魯夫&#x27;, price: 15&#125;, undefined, undefined]</span></span><br></pre></td></tr></table></figure>

<h2 id="every"><a href="#every" class="headerlink" title="every()"></a>every()</h2><p>用來驗證全部的結果</p>
<ul>
<li>如果全部為 <code>true</code>，回傳 <code>true</code></li>
<li>如果有一為 <code>false</code>，回傳 <code>false</code></li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> tenUp = wanted.every(<span class="function">(<span class="params">item, index, array</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> item.price &gt; <span class="number">10</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(tenUp);   <span class="comment">// false</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> threeUp = wanted.every(<span class="function">(<span class="params">item, index, array</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> item.price &gt; <span class="number">3</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(threeUp);   <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<h2 id="some"><a href="#some" class="headerlink" title="some()"></a>some()</h2><p>同樣用來驗證全部結果，但條件與 <code>every()</code> 不一樣</p>
<ul>
<li>只要有一為 <code>true</code>，回傳 <code>true</code></li>
<li>如果全部為 <code>false</code>，回傳 <code>false</code></li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> tenUp = wanted.some(<span class="function">(<span class="params">item, index, array</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> item.price &gt; <span class="number">10</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(tenUp);   <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> threeDown = wanted.some(<span class="function">(<span class="params">item, index, array</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> item.price &lt; <span class="number">3</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(threeDown);   <span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<h2 id="reduce"><a href="#reduce" class="headerlink" title="reduce()"></a>reduce()</h2><p><code>reduce()</code> 是 ES6 陣列方法當中最特別的一個，其參數函先前的方法不一樣，他會將前一個回傳的值和下一個物件再做計算，很適合用在累加上，該方法可以拆解成以下方式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">[x1, x2, x3, x4].reduce(f) = f(f(f(x1, x2), x3), x4);</span><br></pre></td></tr></table></figure>

<ul>
<li><code>previousValue</code> : 前一個參數，如果是第一個元素，那這個值就是另外傳入或初始化的值</li>
<li><code>currentValue</code> : 當下這個參數</li>
<li><code>index</code> : 當下這個索引</li>
<li><code>array</code> : 整個陣列</li>
<li><code>initialValue</code> : 初始化的值</li>
</ul>
<p>同樣的，如果沒有回傳結果會是 <code>undefined</code>，其中 <code>0</code> 就是 <code>initialValue</code></p>
<h3 id="加總"><a href="#加總" class="headerlink" title="加總"></a>加總</h3><p>加總是 <code>reduce</code> 很常用到的技巧，只要將前一個值與當下的值相加，就會得到加總</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> wanted = [</span><br><span class="line">  &#123;</span><br><span class="line">    name: <span class="string">&#x27;魯夫&#x27;</span>,</span><br><span class="line">    price: <span class="number">15</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    name: <span class="string">&#x27;索隆&#x27;</span>,</span><br><span class="line">    price: <span class="number">3.2</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    name: <span class="string">&#x27;香吉士&#x27;</span>,</span><br><span class="line">    price: <span class="number">3.3</span></span><br><span class="line">  &#125;</span><br><span class="line">];</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> reduceEmpty = wanted.reduce(<span class="function">(<span class="params">previousValue, currentValue, currentIndex, array</span>) =&gt;</span> &#123;&#125;, initialValue);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(reduceEmpty);   <span class="comment">// undefined</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> reduce = wanted.reduce(<span class="function">(<span class="params">previousValue, currentValue, currentIndex, array</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(previousValue, currentValue.price, currentIndex);</span><br><span class="line">  <span class="keyword">return</span> previousValue + currentValue.price;   <span class="comment">// 21.5</span></span><br><span class="line">&#125;, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(reduce);   </span><br><span class="line"><span class="comment">// 0 15 0 </span></span><br><span class="line"><span class="comment">// 15 3.2 1 </span></span><br><span class="line"><span class="comment">// 18.2 3.3 2</span></span><br></pre></td></tr></table></figure>

<h3 id="取最大值"><a href="#取最大值" class="headerlink" title="取最大值"></a>取最大值</h3><p><code>reduce</code> 是前一個值與當下的值不斷循環，所以透過 <code>max</code> 取較大值就可以取得最大值，同理最小值 <code>min</code> 也可以</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> maxPrice = wanted.reduce(<span class="function">(<span class="params">pre, cur, i</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Math</span>.max(pre, cur.price);</span><br><span class="line">&#125;, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(maxPrice);   <span class="comment">// 15</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript - 閉包 Closure</title>
    <url>/javascript/20191024/33578/</url>
    <content><![CDATA[<p>閉包 <code>Closure</code> 是 JavaScript 中一種透過函式包住函式的技巧，透過這種技巧可以避免函式中的變數汙染到全域，而出現相同變數名稱互相干擾的情況</p>
<span id="more"></span>

<h2 id="範圍鏈-Scope-Chain"><a href="#範圍鏈-Scope-Chain" class="headerlink" title="範圍鏈 Scope Chain"></a>範圍鏈 Scope Chain</h2><p>了解閉包前，先來了解範圍鏈的概念 ; 切分變數有效範圍的最小單位是 <code>function</code>，所以當 <code>function</code> 在自己的層級找不到變數時，就會一層一層往外找，直到全域範圍 <code>Global Environment</code>，這種行為就稱為 “範圍鏈” <code>Scope Chain</code></p>
<p>從以下範例可以看到在當個層級中找不到變數，所以一層一層往外找，直到全域範圍</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span>;</span><br><span class="line">funcion outer ()&#123;</span><br><span class="line">  <span class="keyword">var</span> b = a * <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">inner</span> (<span class="params">c</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(a, b, c);   <span class="comment">// 1 2 6</span></span><br><span class="line">  &#125;</span><br><span class="line">  inner(b * <span class="number">3</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">outer();</span><br></pre></td></tr></table></figure>

<h2 id="閉包-Closure"><a href="#閉包-Closure" class="headerlink" title="閉包 Closure"></a>閉包 Closure</h2><p>先了解範圍鏈的原因在於充分理解作用域之後，就能理解為什麼要使用閉包的概念</p>
<h3 id="什麼是閉包"><a href="#什麼是閉包" class="headerlink" title="什麼是閉包 ?"></a>什麼是閉包 ?</h3><ul>
<li>呼叫函式中的函式</li>
<li>內層函式中的變數不會汙染到全域</li>
<li>內層函式的變數可以在內部重複使用，不被釋放到全域</li>
<li>內層函式可以記錄當時的變數值</li>
</ul>
<h3 id="沒有使用閉包的情況"><a href="#沒有使用閉包的情況" class="headerlink" title="沒有使用閉包的情況"></a>沒有使用閉包的情況</h3><p>以下透過常見的 “計數器” 來了解閉包</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> count = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">count</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> ++count;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( count() );   <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log( count() );   <span class="comment">// 2</span></span><br><span class="line"><span class="built_in">console</span>.log( count() );   <span class="comment">// 3</span></span><br></pre></td></tr></table></figure>

<p>一個全域變數的宣告似乎沒什麼問題，但是當多人協作或是自己在多行程式碼後又建立一個計數器來記錄時，就容易發生變數名稱相同的衝突</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> count = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">countDog</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> ++count;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 多行程式碼後</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> count = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">countCat</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> ++count;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( countDog() );   <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log( countDog() );   <span class="comment">// 2</span></span><br><span class="line"><span class="built_in">console</span>.log( countCat() );   <span class="comment">// 3   預期是 1 </span></span><br></pre></td></tr></table></figure>

<h3 id="閉包寫法"><a href="#閉包寫法" class="headerlink" title="閉包寫法"></a>閉包寫法</h3><p>透過閉包的概念，使上述範例中各自的變數 <code>count</code> 成為區域變數，並將真正要執行的函式放到內層，藉由 <code>return</code> 回傳後呼叫外層函式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">dogsHouse</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> count = <span class="number">0</span>;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">countDogs</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    count += <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">console</span>.log( count + <span class="string">&#x27;dogs&#x27;</span> );</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> countDogs</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">catsHouse</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> count = <span class="number">0</span>;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">countCats</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    count += <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">console</span>.log( count + <span class="string">&#x27;cats&#x27;</span> );</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> countCats</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> dogCounter = dogsHouse();</span><br><span class="line"><span class="keyword">const</span> catCounter = catsHouse();</span><br><span class="line">dogCounter();   <span class="comment">// 1 dogs</span></span><br><span class="line">dogCounter();   <span class="comment">// 2 dogs</span></span><br><span class="line">catCounter();   <span class="comment">// 1 cats</span></span><br><span class="line">catCounter();   <span class="comment">// 2 cats</span></span><br></pre></td></tr></table></figure>

<h3 id="進一步使用閉包"><a href="#進一步使用閉包" class="headerlink" title="進一步使用閉包"></a>進一步使用閉包</h3><p>即使將函式賦予不同的宣告變數，彼此都是獨立執行不會互相干擾</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">dogsHouse</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> count = <span class="number">0</span>;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">countDogs</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    count += <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">console</span>.log( count + <span class="string">&#x27;dogs&#x27;</span> );</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> countDogs</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> l = dogsHouse();</span><br><span class="line"><span class="keyword">const</span> m = dogsHouse();</span><br><span class="line"><span class="keyword">const</span> s = dogsHouse();</span><br><span class="line"></span><br><span class="line">l();   <span class="comment">// 1dogs</span></span><br><span class="line">l();   <span class="comment">// 2dogs</span></span><br><span class="line">m();   <span class="comment">// 1dogs</span></span><br><span class="line">m();   <span class="comment">// 2dogs</span></span><br><span class="line">s();   <span class="comment">// 1dogs</span></span><br><span class="line">s();   <span class="comment">// 2dogs</span></span><br></pre></td></tr></table></figure>

<h3 id="將參數帶入閉包"><a href="#將參數帶入閉包" class="headerlink" title="將參數帶入閉包"></a>將參數帶入閉包</h3><p>還可以將參數帶入閉包，使每個變數回傳不同結果</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">dogsHouse</span> (<span class="params">s</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> count = <span class="number">0</span>;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">countDogs</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    count += <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">console</span>.log( count + <span class="string">&#x27; &#x27;</span> + s );</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> countDogs</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> l = dogsHouse(<span class="string">&#x27;l&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> m = dogsHouse(<span class="string">&#x27;m&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> s = dogsHouse(<span class="string">&#x27;s&#x27;</span>);</span><br><span class="line"></span><br><span class="line">l();   <span class="comment">// 1 l</span></span><br><span class="line">l();   <span class="comment">// 2 l</span></span><br><span class="line">m();   <span class="comment">// 1 m</span></span><br><span class="line">m();   <span class="comment">// 2 m</span></span><br><span class="line">s();   <span class="comment">// 1 s</span></span><br><span class="line">s();   <span class="comment">// 2 s</span></span><br></pre></td></tr></table></figure>

<h3 id="簡化程式碼"><a href="#簡化程式碼" class="headerlink" title="簡化程式碼"></a>簡化程式碼</h3><p>在閉包運用中，都會回傳 <code>return</code> 內層的函式，這時就可以簡化成 <code>return</code> 後面直接接上 “匿名函式”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">dogsHouse</span> (<span class="params">s</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> count = <span class="number">0</span>;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    count += <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">console</span>.log( count + <span class="string">&#x27; &#x27;</span> + s );</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">catsHouse</span> (<span class="params">s</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> count = <span class="number">0</span>;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    count += <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">console</span>.log( count + <span class="string">&#x27; &#x27;</span> + s );</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>而透過上方的 “進一步使用閉包”，將函式賦予到不同變數上，再透過不同參數加以區分</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">counter</span>(<span class="params">s</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> count = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    count += <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">console</span>.log( count + <span class="string">&#x27;&#x27;</span> + s );</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> dogCounter = counter(<span class="string">&#x27;dogs&#x27;</span>);</span><br><span class="line"><span class="keyword">var</span> catCounter = counter(<span class="string">&#x27;cats&#x27;</span>);</span><br><span class="line">dogCounter();   <span class="comment">// 1 dogs</span></span><br><span class="line">catCounter();   <span class="comment">// 1 cats</span></span><br><span class="line">dogCounter();   <span class="comment">// 2 dogs</span></span><br><span class="line">catCounter();   <span class="comment">// 2 cats</span></span><br></pre></td></tr></table></figure>

<h3 id="ES6-寫法"><a href="#ES6-寫法" class="headerlink" title="ES6 寫法"></a>ES6 寫法</h3><p>透過 ES6 使函式更精簡</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> counter =  <span class="function">(<span class="params">s</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> count = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="function">() =&gt;</span> &#123;</span><br><span class="line">    count += <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">console</span>.log( count + <span class="string">&quot; &quot;</span> + s );</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="參考資料"><a href="#參考資料" class="headerlink" title="參考資料"></a>參考資料</h2><p><a href="https://ithelp.ithome.com.tw/articles/10193009">重新認識 JavaScript: Day 19 閉包 Closure</a><br><a href="https://ithelp.ithome.com.tw/articles/10191515">另一種方式介紹 JavaScript 閉包</a><br><a href="https://pjchender.blogspot.com/2017/05/javascript-closure.html">深入淺出瞭解 JavaScript 閉包（closure）</a></p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 中的 this</title>
    <url>/javascript/20191024/42922/</url>
    <content><![CDATA[<ul>
<li><code>this</code> 是 JavaScript 中的一個關鍵字</li>
<li><code>this</code> 是 <code>function</code> 執行時，自動生成的一個內部物件</li>
<li>隨著執行的場合不同， <code>this</code> 所指向的值也會有所不同</li>
<li>影響 <code>this</code> 的是 “函式的呼叫方法”，而非函式宣告的時機</li>
</ul>
<span id="more"></span>

<p>大多數情況 <code>this</code> 就代表 “呼叫函式 <code>function</code> 的物件”</p>
<h2 id="直接呼叫"><a href="#直接呼叫" class="headerlink" title="直接呼叫"></a>直接呼叫</h2><p>如果是直接呼叫函式，此函式中的 <code>this</code> 都會指向 <code>window</code>，即使是在 <code>function</code> 中的 <code>function</code> 也是</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">window</span>.name = <span class="string">&#x27;我是全域&#x27;</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">callName</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>.name);   <span class="comment">// &#x27;我是全域&#x27;</span></span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">callName2</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>.name);   <span class="comment">// 我是全域</span></span><br><span class="line">  &#125;</span><br><span class="line">  callName2();</span><br><span class="line">&#125;</span><br><span class="line">callName();</span><br></pre></td></tr></table></figure>

<h2 id="物件呼叫"><a href="#物件呼叫" class="headerlink" title="物件呼叫"></a>物件呼叫</h2><p>如果函式 <code>function</code> 是在物件下呼叫，函式中的 <code>this</code> 就會指向該物件，無論 <code>function</code> 是在那裡宣告</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name = <span class="string">&#x27;我是全域&#x27;</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">callName</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>.name);   <span class="comment">// &#x27;&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> test = &#123;</span><br><span class="line">  name: <span class="string">&#x27;我是區域&#x27;</span>,</span><br><span class="line">  callName2: callName</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">callName();         <span class="comment">// &#x27;我是全域&#x27; </span></span><br><span class="line">test.callName2();   <span class="comment">// &#x27;我是區域</span></span><br></pre></td></tr></table></figure>

<p>如果將物件內的 <code>function</code> 賦予到變數上並呼叫，這個 <code>this</code> 就會指向全域</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">callName3 = test.callName2;</span><br><span class="line">callName3();    <span class="comment">// &#x27;我是全域&#x27;</span></span><br></pre></td></tr></table></figure>

<h2 id="DOM-元素呼叫"><a href="#DOM-元素呼叫" class="headerlink" title="DOM 元素呼叫"></a>DOM 元素呼叫</h2><p>透過 DOM 元素的事件監聽來呼叫 <code>function</code>，此 <code>function</code> 中的 <code>this</code> 就會指向該 DOM 元素</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">&quot;test&quot;</span>&gt;</span>我是 DOM 元素<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> list = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;.list&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">test</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>);   <span class="comment">// &lt;p class=&quot;test&quot;&gt;我是 DOM 元素&lt;/p&gt;</span></span><br><span class="line">&#125;;</span><br><span class="line">              </span><br><span class="line">list.addEventListener(<span class="string">&#x27;click&#x27;</span>, test);</span><br></pre></td></tr></table></figure>

<h2 id="建構式呼叫"><a href="#建構式呼叫" class="headerlink" title="建構式呼叫"></a>建構式呼叫</h2><p>在建構式下會使用 <code>new</code> 來生成一個新物件( 物件被建構出來 )，而 <code>function</code> 中的 <code>this</code> 會指向這個物件，而且可以在生成的物件上重新定義</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name = <span class="string">&#x27;我是全域&#x27;</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">callName</span> (<span class="params">newName</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.name = newName || <span class="string">&#x27;我是區域&#x27;</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> callName2 = <span class="keyword">new</span> callName(<span class="string">&#x27;我是新的區域&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> callName3 = <span class="keyword">new</span> callName();</span><br><span class="line"><span class="built_in">console</span>.log(callName2.name);   <span class="comment">// 我是新的區域</span></span><br><span class="line"><span class="built_in">console</span>.log(callName3.name);   <span class="comment">// 我是區域</span></span><br></pre></td></tr></table></figure>

<h2 id="內建函式來呼叫"><a href="#內建函式來呼叫" class="headerlink" title="內建函式來呼叫"></a>內建函式來呼叫</h2><p>透過內建函式 <code>call</code>、<code>bind</code>、<code>apply</code> 來呼叫函式，第一個參數會成為這個函式的 <code>this</code>，只是這三個內建函式在使用時機上有所不同</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name = <span class="string">&#x27;我是全域&#x27;</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">callName</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>.name);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">callName();   <span class="comment">// &#x27;我是全域&#x27;</span></span><br><span class="line">callName.call(&#123;<span class="attr">name</span>: <span class="string">&#x27;我是區域&#x27;</span>&#125;);   <span class="comment">// &#x27;我是區域&#x27;</span></span><br></pre></td></tr></table></figure>

<h2 id="重新指向-this"><a href="#重新指向-this" class="headerlink" title="重新指向 this"></a>重新指向 this</h2><p>在實際操作時，遇到 “立即函式” IIFE 或 “非同步事件” 如 <code>setTimeout</code>，<code>this</code> 大多會指向 全域 <code>window</code>，如果要調用的是物件本身，可以先設定一個變數指向這個物件，再將這個變數帶入函式中</p>
<p>常見的變數名稱有 <code>that</code>、<code>vm</code>、<code>self</code> 等</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name = <span class="string">&#x27;我是全域&#x27;</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">callName</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>.name);     <span class="comment">// 我是區域</span></span><br><span class="line">  <span class="keyword">var</span> that = <span class="built_in">this</span>;</span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>.name);   <span class="comment">// 我是全域</span></span><br><span class="line">    <span class="built_in">console</span>.log(that.name);   <span class="comment">// 我是區域</span></span><br><span class="line">  &#125;, <span class="number">1000</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> test = &#123;</span><br><span class="line">  name: <span class="string">&#x27;我是區域&#x27;</span>,</span><br><span class="line">  callName2: callName</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">test.callName2();</span><br></pre></td></tr></table></figure>

<h2 id="結論"><a href="#結論" class="headerlink" title="結論"></a>結論</h2><ul>
<li>如果 <code>function</code> 是透過物件來呼叫，那麼其中的 <code>this</code> 就會指向該物件</li>
<li>如果 <code>function</code> 是透過 DOM 元素的事件監聽來呼叫，那麼其中的 <code>this</code> 就會指向該 DOM 元素</li>
<li>如果 <code>function</code> 是透過建構式 <code>new</code> 生成的物件來呼叫，那麼其中的 <code>this</code> 就會指向該物件</li>
<li>如果 <code>function</code> 是透過內建函式 <code>call</code>、<code>apply</code>、<code>bind</code> 來呼叫，那麼其中的 <code>this</code> 就會指向內建函式的第一個參數</li>
<li>如果沒有滿足以上條件，那麼 <code>function</code> 的 <code>this</code> 就會指向全域物件 <code>window</code></li>
</ul>
<h2 id="參考資料"><a href="#參考資料" class="headerlink" title="參考資料"></a>參考資料</h2><p><a href="https://ithelp.ithome.com.tw/articles/10193193">What’s “THIS” in JavaScript</a><br><a href="https://ithelp.ithome.com.tw/articles/10191393">JavaScript 的 this 到底是誰？</a></p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Typescript 環境下使用未更新的 library</title>
    <url>/uncategorized/20191219/13747/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>JavaScript 中的傳值與傳址</title>
    <url>/javascript/20191105/30734/</url>
    <content><![CDATA[<p>JavaScript 的變數再賦予不同型別的時候，會有同的傳遞方式，以 “基本型別” <code>Primitive</code> 和 “物件型別” <code>Object</code> 作為區分 </p>
<span id="more"></span>

<h2 id="型別-Types"><a href="#型別-Types" class="headerlink" title="型別 Types"></a>型別 Types</h2><h3 id="基本型別-Primitive-type"><a href="#基本型別-Primitive-type" class="headerlink" title="基本型別 Primitive type"></a>基本型別 Primitive type</h3><p>當宣告兩個變數並賦予相同的基本型別時，兩個變數的會是 “相等” 的，這似乎很直觀</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">var</span> b = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">var</span> c = <span class="string">&quot;String&quot;</span>;</span><br><span class="line"><span class="keyword">var</span> d = <span class="string">&quot;String&quot;</span>;</span><br><span class="line"><span class="built_in">console</span>.log( a === b );   <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log( c === d );   <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<h3 id="物件型別-Object-type"><a href="#物件型別-Object-type" class="headerlink" title="物件型別 Object type"></a>物件型別 Object type</h3><p>但是當宣告兩個變數並賦予相同屬性與值的物件時，兩個變數卻是 “不相等” 的，因為 javascript 中的物件可以看成是個別的 “實體”，兩個物件雖然可以看成 “等值”，但卻不是同一個 “實體”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj1 = &#123; <span class="attr">value</span>: <span class="number">10</span> &#125;</span><br><span class="line"><span class="keyword">var</span> obj2 = &#123; <span class="attr">value</span>: <span class="number">10</span> &#125;</span><br><span class="line"><span class="built_in">console</span>.log( obj1 === obj2 );   <span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<h2 id="傳值與傳址"><a href="#傳值與傳址" class="headerlink" title="傳值與傳址"></a>傳值與傳址</h2><ul>
<li>傳值 <code>pass by value</code> </li>
<li>傳址 <code>pass by reference</code></li>
</ul>
<h3 id="傳值-pass-by-value"><a href="#傳值-pass-by-value" class="headerlink" title="傳值 pass by value"></a>傳值 pass by value</h3><p>在基本型別的變數中，當我們複製變數時，取得的是原先變數的 “值”，而不是這個變數本身</p>
<p>因為當指定 <code>b</code> 的值等於 <code>a</code> 時，會建立一個新的記憶體來存放相同的值，彼此不會互相干擾，這種情況稱為 “傳值” <code>pass by value</code> </p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">var</span> b = a;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 10</span></span><br><span class="line"><span class="built_in">console</span>.log(b);   <span class="comment">// 10</span></span><br></pre></td></tr></table></figure>

<p><img src="%E5%82%B3%E5%80%BC-1.png"></p>
<p>所以我們更改原先變數 <code>a</code> 的值時，並不會影響新變數 <code>b</code> 的值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">a = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 100</span></span><br><span class="line">consolg.log(b);   <span class="comment">// 10</span></span><br></pre></td></tr></table></figure>

<p><img src="%E5%82%B3%E5%80%BC-2.png"></p>
<h3 id="傳址-pass-by-reference"><a href="#傳址-pass-by-reference" class="headerlink" title="傳址 pass by reference"></a>傳址 pass by reference</h3><p>當我們建立一個物件型別的變數 a 時，一樣會建立一個位置在記憶體中;但是當我們另建立一個變數 b 並賦予他的值等於 a，這時和基本型別不一樣的是，並不會建立一個新的位置在記憶體中，而是讓變數 b 指向和 a 同一個記憶體中的位置，稱為 “傳址” <code>pass by reference</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = &#123; <span class="attr">value</span>: <span class="number">10</span> &#125;;</span><br><span class="line"><span class="keyword">var</span> b = a;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// &#123;value: 10&#125;</span></span><br><span class="line"><span class="built_in">console</span>.log(b);   <span class="comment">// &#123;value: 10&#125;</span></span><br></pre></td></tr></table></figure>

<p><img src="%E5%82%B3%E5%9D%80-1.png"></p>
<p>所以當我們更改 <code>a</code> 的內容時，<code>b</code> 自然會跟著更改</p>
<p><img src="%E5%82%B3%E5%9D%80-2.png"></p>
<h3 id="例外情況"><a href="#例外情況" class="headerlink" title="例外情況"></a>例外情況</h3><p>有一些例外的情況透過傳值的方式複製物件，而複製又分為 “深度複製” <code>Deep copy</code> 和 “淺度複製” <code>Shallow copy</code>，”深度複製” 全部透過 “傳值” 的方式複製，而 “淺度複製” 只有 “第一層傳值”，內層還是 “傳址”</p>
<ul>
<li><p>重新賦值</p>
<p>凡事都有例外，當物件型別的變數被重新賦值時，就會在記憶體中產生一個新的位置來存放這個內容</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = &#123;<span class="attr">value</span>: <span class="number">10</span>&#125;</span><br><span class="line"><span class="keyword">var</span> b = a;</span><br><span class="line"></span><br><span class="line">a = &#123;<span class="attr">value</span>: <span class="number">100</span>&#125;</span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// &#123;value: 100&#125;</span></span><br><span class="line"><span class="built_in">console</span>.log(b);   <span class="comment">// &#123;value: 10&#125;</span></span><br></pre></td></tr></table></figure>

<p><img src="%E5%82%B3%E5%80%BC%E8%88%87%E5%82%B3%E5%9D%80%E7%9A%84%E4%BE%8B%E5%A4%96.png"></p>
</li>
<li><p>手動賦值 (淺度複製)</p>
<p>手動賦值很直接的把 “物件傳址” 改用 “傳值” 的方式傳遞，傳值的部分就會完全獨立 ; 但要注意的是，”內層物件” 還是 “傳址”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Person = &#123;</span><br><span class="line">  name: <span class="string">&#x27;Jerry&#x27;</span>,</span><br><span class="line">  bwh: &#123;</span><br><span class="line">    strength: <span class="number">34</span>,</span><br><span class="line">    agility: <span class="number">25</span>,</span><br><span class="line">    intelligence: <span class="number">96</span></span><br><span class="line">  &#125;,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Person2 = &#123;</span><br><span class="line">  name: Person.name, </span><br><span class="line">  bwh: Person.bwh,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Person.name = <span class="string">&#x27;Tom&#x27;</span>;</span><br><span class="line">Person.bwh.strength = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(Person2.name);   <span class="comment">// 還是 &quot;Jerry&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log(Person2.bwh.strength);    <span class="comment">// 一起改成 0  </span></span><br></pre></td></tr></table></figure>

<ul>
<li>for…in (淺度複製)</li>
</ul>
<p>透過 for…in 將原本的物件屬性的值傳到新的物件</p>
<ul>
<li>轉字串</li>
</ul>
<p>將物件轉型成字串，再將字串轉回物件，兩個物件會 “完全無關”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str = <span class="built_in">JSON</span>.stringify(Person);</span><br><span class="line"><span class="keyword">var</span> P = <span class="built_in">JSON</span>.parse(Person);</span><br><span class="line"></span><br><span class="line">Person.name = <span class="string">&#x27;Tom&#x27;</span>;</span><br><span class="line">Person.bwh.strength = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(p.name);   <span class="comment">// 還是 &quot;Jerry&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log(p.bwh.strength);    <span class="comment">// 還是 34</span></span><br></pre></td></tr></table></figure>

<ul>
<li>jQuery</li>
</ul>
<p>jQuery 中可以透過 <code>extend()</code> 來複製物件，可以透過更改第一個參數來調整深度或淺度複製</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p1 = jQuery.extend(&#123;&#125;, Person);       <span class="comment">// 淺度複製</span></span><br><span class="line"><span class="keyword">var</span> p2 = jQuery.extend(<span class="literal">true</span>, &#123;&#125;, Person); <span class="comment">// 深度複製</span></span><br><span class="line"></span><br><span class="line">Person.name = <span class="string">&#x27;Tom&#x27;</span>;</span><br><span class="line">Person.bwh.strength = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(p1.name);   <span class="comment">// 還是 &quot;Jerry&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log(p1.bwh.strength);    <span class="comment">// 改成 0</span></span><br><span class="line"><span class="built_in">console</span>.log(p2.name);   <span class="comment">// 還是 &quot;Jerry&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log(p2.bwh.strength);    <span class="comment">// 還是 34</span></span><br></pre></td></tr></table></figure>

<ul>
<li>ES6</li>
</ul>
<p>在原生 JavaScript 中 ES6 也推出 <code>Object.assign()</code> 新的方法，但只適用於 “淺度複製”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> p = <span class="built_in">Object</span>.assign(&#123;&#125;, Person);</span><br><span class="line"></span><br><span class="line">Person.name = <span class="string">&#x27;Tom&#x27;</span>;</span><br><span class="line">Person.bwh.strength = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(p.name);   <span class="comment">// 還是 &quot;Jerry&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log(p.bwh.strength);    <span class="comment">// 改成 0</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h2><p><a href="https://pjchender.blogspot.com/2016/03/javascriptby-referenceby-value.html">[筆記] 談談JavaScript中by reference和by value的重要觀念</a><br><a href="https://ithelp.ithome.com.tw/articles/10191057">JavaScript 是「傳值」或「傳址」？</a></p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 同步與非同步</title>
    <url>/javascript/20191105/37805/</url>
    <content><![CDATA[<p><code>synchronous</code> 和 <code>asynchronous</code> 如果將其翻譯成中文的 “同步” 與 “非同步” 反而很容易誤解其含義，同步 <code>synchronous</code> 指的是 <code>one at a time</code>，一次執行一列程式碼並依序執行，非同步 <code>asynchronous</code> 指的是 <code>more than one at a time</code>，同時執行不只一列程式碼</p>
<span id="more"></span>

<h2 id="同步與非同步簡介"><a href="#同步與非同步簡介" class="headerlink" title="同步與非同步簡介"></a>同步與非同步簡介</h2><p>JavaScript 執行的方式 “同步” <code>synchronous</code> 的，也就是從上到下一次執行一列程式碼並依序執行，但 JavaScript 是一個 “非同步” <code>asynchronous</code> 的語言，因為其含有許多非同步的事件，避免因為其中一列程式碼卡住就影響其他程式碼和渲染</p>
<h2 id="堆疊-stack-與-佇列-queue"><a href="#堆疊-stack-與-佇列-queue" class="headerlink" title="堆疊 stack 與 佇列 queue"></a>堆疊 stack 與 佇列 queue</h2><p>JavaScript 是一個單線程 <code>single threaded</code> 的程式語言，所有程式碼都會依序在堆疊 <code>stack</code> 中執行，而且一次只會執行一列程式碼</p>
<h3 id="堆疊-stack"><a href="#堆疊-stack" class="headerlink" title="堆疊 stack"></a>堆疊 stack</h3><p>堆疊 <code>stack</code> 是 “後進先出” 的方式，在 JavaScript 中會記錄當下執行到哪段程式碼，如果進入一個函式，就會將該函式放到堆疊的最上方，在執行到 <code>return</code> 或結尾時將該函式從堆疊最上方抽離</p>
<h3 id="佇列-queue"><a href="#佇列-queue" class="headerlink" title="佇列 queue"></a>佇列 queue</h3><p>佇列 <code>queue</code> 是先進先出的方式，也是 javascript 能進行非同步的關鍵，所有的 “事件”、<code>Http request</code> 都會依照觸發順序被加入到 “佇列” <code>queue</code> 中，等到 “堆疊” <code>stack</code> 內是空的時，就會將佇列中的第一個物件加入到堆疊中開始依序執行</p>
<h3 id="JavaScript-執行非同步事件-asynchronous-callback"><a href="#JavaScript-執行非同步事件-asynchronous-callback" class="headerlink" title="JavaScript 執行非同步事件 asynchronous callback"></a>JavaScript 執行非同步事件 asynchronous callback</h3><ol>
<li>首先建立了一個函式 <code>waitThreeSeconds</code> ，這個函式透過 <code>while</code> 來延遲 3 秒出現 <code>finished function</code> 字串</li>
<li>接著添加了一個事件監聽器 <code>EventListener</code>，當點擊滑鼠時會出現 <code>click event</code> 字串</li>
<li>在程式執行的一開始我們就透過點擊來觸發事件</li>
<li>結果是 <code>started execution</code> 最先出現</li>
<li>3 秒後出現接著 <code>finished function 和 finished execution</code></li>
<li>最後才是我們點擊所觸發的 <code>click event</code></li>
</ol>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">waitThreeSeconds</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> ms = <span class="number">3000</span> + <span class="keyword">new</span> <span class="built_in">Date</span>().getTime();</span><br><span class="line">  <span class="keyword">while</span>(<span class="keyword">new</span> <span class="built_in">Date</span>() &lt; ms)&#123;&#125;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&quot;finished function&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">clickHandler</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&quot;click event!&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">document</span>.addEventListener(<span class="string">&#x27;click&#x27;</span>, clickHandler);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&quot;started execution&quot;</span>);</span><br><span class="line">waitThreeSeconds();</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&quot;finished execution&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// started execution</span></span><br><span class="line"><span class="comment">// finished function </span></span><br><span class="line"><span class="comment">// finished execution</span></span><br><span class="line"><span class="comment">// click event</span></span><br></pre></td></tr></table></figure>

<p>從以上範例可以驗證 JavaScript 中會先依序執行完堆疊 <code>stack</code> 中的程式碼，才會執行 <code>queue</code> 中的點擊事件</p>
<h2 id="Promise"><a href="#Promise" class="headerlink" title="Promise"></a>Promise</h2><h3 id="callback-hell"><a href="#callback-hell" class="headerlink" title="callback hell"></a>callback hell</h3><p>當有一個以上的非同步事件，我們希望他們是透過安排好的順序依序執行時，最直覺的做法就是在事件執行完後 <code>callback</code> 下一個事件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;a&#x27;</span>);</span><br><span class="line">    b();</span><br><span class="line">  &#125;, <span class="number">1000</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">b</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;b&#x27;</span>);</span><br><span class="line">  &#125;, <span class="number">1000</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">a();</span><br></pre></td></tr></table></figure>

<p>看似沒有什麼問題，但是當使用大量的非同步事件又希望依照固定的順序執行時，就會出現以下經典圖，<code>callback hell</code></p>
<p><img src="callback-hell.png"></p>
<h3 id="Promise-1"><a href="#Promise-1" class="headerlink" title="Promise"></a>Promise</h3><p>為了解決以上同步與非同步的問題，ES6 開始新增了 promise 物件</p>
<p><code>promise</code> 顧名思義是承諾、約定的意思，其內函式的兩個參數分別為 <code>resolve</code> 傳遞成功訊息，<code>reject</code> 傳遞失敗訊息，以下為 <code>promise</code> 的三種狀態</p>
<ul>
<li><code>pending</code>: 一進入 promise 的初始等待狀態</li>
<li><code>fulfilled</code> : 表示操作成功的狀態</li>
<li><code>rejected</code> : 表示操作失敗的狀態</li>
</ul>
<p>以下圖片表示整個 promise 流程</p>
<p><img src="promise.png"></p>
<p>如果要解決上方的 <code>callback hell</code> ，可以使用 <code>then()</code> 來串接多個非同步事件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">&#x27;a&#x27;</span>);</span><br><span class="line">      resolve();</span><br><span class="line">    &#125;, <span class="number">1000</span>);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">b</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">&#x27;b&#x27;</span>);</span><br><span class="line">      resolve();</span><br><span class="line">    &#125;, <span class="number">1000</span>);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">c</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">&#x27;c&#x27;</span>);</span><br><span class="line">      resolve();</span><br><span class="line">    &#125;, <span class="number">1000</span>);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">a().then(b).then(c);</span><br></pre></td></tr></table></figure>

<p>除此之外，<code>then()</code> 可以用來接收成功的訊息，<code>catch()</code> 可以用來接收失敗的訊息</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> onePiece = <span class="function">(<span class="params">name</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> choice = <span class="built_in">parseInt</span>(<span class="built_in">Math</span>.random() * <span class="number">2</span>);</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;起跑&#x27;</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span>(choice)&#123;</span><br><span class="line">      resolve(<span class="string">`<span class="subst">$&#123;name&#125;</span> 抵達終點`</span>);</span><br><span class="line">    &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">      reject(<span class="string">`<span class="subst">$&#123;name&#125;</span> 跌倒了...`</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">onePiece(<span class="string">&#x27;跑得快&#x27;</span>).then(<span class="function">(<span class="params">data</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(data);</span><br><span class="line">&#125;).catch(<span class="function">(<span class="params">data</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(data);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h4 id="Promise-all"><a href="#Promise-all" class="headerlink" title="Promise.all()"></a>Promise.all()</h4><p><code>Promise.all()</code> 會 “同時執行” 以下的 <code>pormise</code>，並在完成後統一回傳陣列，這個陣列內容就是 <code>promise</code> 的 <code>resolve</code> 結果，但如果 <code>all()</code> 其中有任何一個事件 <code>reject</code>，那麼整個 <code>promise</code> 就會被視為 <code>reject</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Promise</span>.all([onePiece(<span class="string">&#x27;跑得快&#x27;</span>), onePiece(<span class="string">&#x27;電話蟲&#x27;</span>)]).then(<span class="function">(<span class="params">data</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(data);</span><br><span class="line">&#125;).catch(<span class="function">(<span class="params">data</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(data);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h4 id="Promise-race"><a href="#Promise-race" class="headerlink" title="Promise.race()"></a>Promise.race()</h4><p>而 <code>Promise.race()</code> 僅會回傳第一個完成的結果</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Promise</span>.race([onePiece(<span class="string">&#x27;跑得快&#x27;</span>), onePiece(<span class="string">&#x27;電話蟲&#x27;</span>)]).then(<span class="function">(<span class="params">data</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(data);</span><br><span class="line">&#125;).catch(<span class="function">(<span class="params">data</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(data);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="asyne-與-await"><a href="#asyne-與-await" class="headerlink" title="asyne 與 await"></a>asyne 與 await</h3><p><code>asyne</code> 與 <code>await</code> 的配合也可以和 <code>promise</code> 達到相同的效果，在一個函式執行完後才會接著執行下一個</p>
<h4 id="async-非同步"><a href="#async-非同步" class="headerlink" title="async 非同步"></a><code>async</code> 非同步</h4><p><code>async</code> 又稱為非同步函式，用來包住 <code>await</code>，其用法類似 <code>Promise</code>，也可以在後方加上 <code>then()</code> 和 <code>catch()</code> 分別接收 <code>resolve</code> 和 <code>reject</code> 的結果</p>
<h4 id="await-等待"><a href="#await-等待" class="headerlink" title="await 等待"></a><code>await</code> 等待</h4><p>使用 <code>await</code> 會將後面的函式卡住，直到這個函式完成才會執行下一個函式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> onePiece = <span class="function">(<span class="params">name, timer, success = <span class="literal">true</span></span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;name&#125;</span> 開始起跑`</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span>(success)&#123;</span><br><span class="line">      <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">        resolve(<span class="string">`<span class="subst">$&#123;name&#125;</span> 10 公分花了 <span class="subst">$&#123;timer / <span class="number">1000</span>&#125;</span> 秒`</span>);</span><br><span class="line">      &#125;, timer);</span><br><span class="line">    &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">      reject(<span class="string">`<span class="subst">$&#123;name&#125;</span> 跌倒了...`</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> relay = <span class="keyword">async</span> () =&gt; &#123;</span><br><span class="line">  <span class="keyword">let</span> mushi1 = <span class="keyword">await</span> onePiece(<span class="string">&#x27;電話蟲1&#x27;</span>, <span class="number">2000</span>);</span><br><span class="line">  <span class="keyword">let</span> mushi2 = <span class="keyword">await</span> onePiece(<span class="string">&#x27;電話蟲2&#x27;</span>, <span class="number">2000</span>);</span><br><span class="line">  <span class="keyword">let</span> Karoo = <span class="keyword">await</span> onePiece(<span class="string">&#x27;跑得快&#x27;</span>, <span class="number">1</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;mushi1&#125;</span>, <span class="subst">$&#123;mushi2&#125;</span>, <span class="subst">$&#123;Karoo&#125;</span>`</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">relay().then(<span class="function">(<span class="params">data</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(data);</span><br><span class="line">&#125;).catch(<span class="function">(<span class="params">data</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(data);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 電話蟲1 開始起跑</span></span><br><span class="line"><span class="comment">// 電話蟲2 開始起跑</span></span><br><span class="line"><span class="comment">// 跑得快 開始起跑</span></span><br><span class="line"><span class="comment">// 電話蟲1 10 公分花了 2 秒, 電話蟲2 10 公分花了 2 秒, 跑得快 10 公分花了 0.001 秒</span></span><br></pre></td></tr></table></figure>

<p>同樣的，如果其中有函式執行失敗 <code>reject</code>，就會執行失敗結果</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> relay = <span class="keyword">async</span> () =&gt; &#123;</span><br><span class="line">  <span class="keyword">let</span> mushi1 = <span class="keyword">await</span> onePiece(<span class="string">&#x27;電話蟲1&#x27;</span>, <span class="number">2000</span>);</span><br><span class="line">  <span class="keyword">let</span> mushi2 = <span class="keyword">await</span> onePiece(<span class="string">&#x27;電話蟲2&#x27;</span>, <span class="number">2000</span>, <span class="literal">false</span>);</span><br><span class="line">  <span class="keyword">let</span> Karoo = <span class="keyword">await</span> onePiece(<span class="string">&#x27;跑得快&#x27;</span>, <span class="number">1</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;mushi1&#125;</span>, <span class="subst">$&#123;mushi2&#125;</span>, <span class="subst">$&#123;Karoo&#125;</span>`</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">relay().then(<span class="function">(<span class="params">data</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(data);</span><br><span class="line">&#125;).catch(<span class="function">(<span class="params">data</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(data);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 電話蟲1 開始起跑</span></span><br><span class="line"><span class="comment">// 電話蟲2 開始起跑</span></span><br><span class="line"><span class="comment">// 電話蟲2 跌倒了...</span></span><br></pre></td></tr></table></figure>

<h2 id="參考資料"><a href="#參考資料" class="headerlink" title="參考資料"></a>參考資料</h2><p><a href="https://pjchender.blogspot.com/2016/01/javascriptasynchronousevent-queue.html">談談JavaScript中的asynchronous和event queue</a><br><a href="https://ithelp.ithome.com.tw/articles/10194569">同步與非同步</a><br><a href="https://ithelp.ithome.com.tw/articles/10194622">使用 Promise 處理非同步</a><br><a href="https://blog.kdchang.cc/2016/06/24/javascript-data-structure-algorithm-stack/">用 JavaScript 學習資料結構和演算法：堆疊（Stack）篇</a></p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 物件與原型鍊</title>
    <url>/javascript/20191105/49319/</url>
    <content><![CDATA[<p>JavaScript 是一門 “物件導向” 的程式語言，但物件彼此是透過 “原型” 來繼承屬性，在介紹原型鍊之前，會先透過 “串聯” <code>Cascade</code> 與物件來了解背後的觀念</p>
<span id="more"></span>

<h2 id="Cascade-串聯"><a href="#Cascade-串聯" class="headerlink" title="Cascade 串聯"></a>Cascade 串聯</h2><p>在 javascript 的函式 function 中，如果沒有設定 <code>return</code> 的值，那麼預設就會回傳 <code>undefined</code>，而 <code>function</code> 中的 <code>this</code> 會指向 “呼叫這個函式的物件” ，如果將 <code>return</code> 的值設為 <code>this</code>，就可以達到串聯 <code>Cascade</code> 的方式，也是 <code>jQuery</code> 可以串聯許多方法背後的觀念</p>
<h3 id="一般寫法"><a href="#一般寫法" class="headerlink" title="一般寫法"></a>一般寫法</h3><p>以下為一個運算的範例</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> count = <span class="function"><span class="keyword">function</span> (<span class="params">num</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.num = num;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">this</span>.add = <span class="function"><span class="keyword">function</span>(<span class="params">newNum</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">this</span>.num += newNum;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">this</span>.sub = <span class="function"><span class="keyword">function</span>(<span class="params">newNum</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">this</span>.num -= newNum;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>接著透過 <code>new</code> 來新增物件，要注意的是，如果沒有 <code>new</code>，該新增變數的值就會是函式 <code>count</code> 回傳的值，也就是預設的 <code>undefined</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="keyword">new</span> count(<span class="number">100</span>);</span><br><span class="line"><span class="keyword">var</span> b = count(<span class="number">100</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(typeOf(a));   <span class="comment">// object</span></span><br><span class="line"><span class="built_in">console</span>.log(typeOf(b));   <span class="comment">// undefinde</span></span><br></pre></td></tr></table></figure>

<p>建立物件後，就可以呼叫物件內的函式，又稱為方法 <code>Method</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">a.add(<span class="number">100</span>);</span><br><span class="line"><span class="built_in">console</span>.log(a.num);   <span class="comment">// 200</span></span><br><span class="line"></span><br><span class="line">a.sub(<span class="number">50</span>);</span><br><span class="line"><span class="built_in">console</span>.log(a.num);   <span class="comment">// 150</span></span><br></pre></td></tr></table></figure>

<h3 id="Cascade-的方式"><a href="#Cascade-的方式" class="headerlink" title="Cascade 的方式"></a>Cascade 的方式</h3><p>一般的寫法如上，在呼叫物件中的多個方法 <code>Method</code> 時，需要個別呼叫 ; 如果將原物件中的方法都加上 <code>return this</code>，回傳 <code>this</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> count = <span class="function"><span class="keyword">function</span> (<span class="params">num</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">this</span> num = num;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">this</span>.add = <span class="function"><span class="keyword">function</span>(<span class="params">newNum</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">this</span>.num += newNum;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">this</span>.sub = <span class="function"><span class="keyword">function</span>(<span class="params">newNum</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">this</span>.num -= newNum;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>這時候就會發現可以將多個方法 <code>Method</code> 在一行程式碼一起呼叫</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="keyword">new</span> count(<span class="number">100</span>);</span><br><span class="line"></span><br><span class="line">a.add(<span class="number">100</span>).sub(<span class="number">50</span>);</span><br><span class="line"><span class="built_in">console</span>.log(a.num);   <span class="comment">// 150</span></span><br></pre></td></tr></table></figure>

<p>拆開來看的話，<code>a.add(100)</code> 在運算完後會 <code>return this</code>，其中 this 就是屬性 <code>num</code> 變成 <code>200</code> 的 <code>a</code> 物件，而不是預設的 <code>undefined</code>，所以就可以接著呼叫下一個方法 <code>Method</code></p>
<p>這就是 <code>jQuery</code> 常見的方法鍊</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">&#x27;div&#x27;</span>).addClass(<span class="string">&#x27;is-active&#x27;</span>).removeClass(<span class="string">&#x27;is-hide&#x27;</span>);</span><br></pre></td></tr></table></figure>

<h2 id="物件-Object"><a href="#物件-Object" class="headerlink" title="物件 Object"></a>物件 Object</h2><h3 id="型別-Type"><a href="#型別-Type" class="headerlink" title="型別 Type"></a>型別 Type</h3><p>在 javascript 中，主要區分為兩大型別，基本型別 <code>Primitives</code> 與 物件型別 <code>Object</code>，除了基本型別外，都可以歸類為物件型別</p>
<h4 id="基本型別"><a href="#基本型別" class="headerlink" title="基本型別"></a>基本型別</h4><ul>
<li>字串 <code>string</code></li>
<li>數字 <code>number</code></li>
<li>布林 <code>boolean</code></li>
<li>空值 <code>null</code></li>
<li>未定義 <code>undefined</code></li>
<li>ES6 新增的 <code>stymbol</code></li>
</ul>
<h3 id="自訂物件"><a href="#自訂物件" class="headerlink" title="自訂物件"></a>自訂物件</h3><p>在 javascript 中，有兩種方式可以自訂物件</p>
<ul>
<li><p>使用 <code>new</code> 關鍵字來建立</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = <span class="keyword">new</span> <span class="built_in">Object</span>();</span><br><span class="line">preson.name = <span class="string">&#x27;Jerry&#x27;</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>使用大括號 <code>&#123; &#125;</code> 直接建立新物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  name: <span class="string">&quot;Jerry&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="建構式-Constructor"><a href="#建構式-Constructor" class="headerlink" title="建構式 Constructor"></a>建構式 Constructor</h3><p>javascript 不像其他物件導向的語言具有 <code>class</code> 語法，但可以透過建構式來建立各種對應的物件，要注意的是建構式是使用 “函式” <code>function</code> 來建立物件樣板，不能使用 <code>ES6</code> 的 “箭頭函式”</p>
<ul>
<li><p><code>function</code> 中的 <code>this</code> 代表物件的屬性</p>
</li>
<li><p>可以透過參數來傳入數值</p>
</li>
<li><p>使用關鍵字 <code>new</code> 來建立新物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params"> name, age, gender </span>)</span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.name = name;</span><br><span class="line">  <span class="built_in">this</span>.age = age;</span><br><span class="line">  <span class="built_in">this</span>.gender = gender;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">this</span>.greeting = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;Hello!&#x27;</span> + <span class="built_in">this</span>.name);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> Jerry = <span class="keyword">new</span> Person( <span class="string">&#x27;Jerry&#x27;</span>, <span class="number">24</span>, <span class="string">&#x27;male&#x27;</span> );</span><br><span class="line">Jerry.greeting();   <span class="comment">// &#x27;Hello! Jerry&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> Tom = <span class="keyword">new</span> Person(<span class="string">&#x27;Tom&#x27;</span>, <span class="number">18</span>, <span class="string">&#x27;female&#x27;</span>);</span><br><span class="line">Tom.greeting();   <span class="comment">// &#x27;Hello! Tom&#x27;</span></span><br></pre></td></tr></table></figure>

<p>將建構式拆開來看，其實是先建立一個空物件 <code>&#123; &#125;</code>，再透過 <code>call</code> 方法將 <code>this</code> 指向這個物件，並傳入參數</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Jerry = &#123;&#125;;</span><br><span class="line">Person.call(Jerry, <span class="string">&#x27;Jerry&#x27;</span>, <span class="number">24</span>, <span class="string">&#x27;male&#x27;</span>);</span><br></pre></td></tr></table></figure>

<p>透過建構式建立的物件，還是可以透過 <code>.</code> 來存取屬性</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(Jerry.age);   <span class="comment">// 24</span></span><br><span class="line">Jerry.age = <span class="number">18</span>;</span><br><span class="line"><span class="built_in">console</span>.log(Jerry.age);   <span class="comment">// 18</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="屬性描述器-Property-descriptor"><a href="#屬性描述器-Property-descriptor" class="headerlink" title="屬性描述器 Property descriptor"></a>屬性描述器 Property descriptor</h3><p>從 ES5 開始，我們可以透過新的物件模型來靠至物件屬性的存取、刪除、列舉等。這些特殊的屬性就稱為 “屬性描述器” <code>Property descriptor</code></p>
<p>屬性描述器分為六種 :</p>
<ul>
<li><code>value</code> : 屬性的值</li>
<li><code>writable</code> : 定義屬性是否可以改變，如果是 <code>false</code> 就是唯讀屬性</li>
<li><code>enumerable</code> : 定義屬性是否可以透過 <code>for-in</code> 語法來迭代</li>
<li><code>configurable</code> : 定義屬性是否可以被刪除、或修改屬性的 <code>writable</code>、<code>enumerable</code> 和 <code>configurable</code> 設定</li>
<li><code>get</code> : 屬性的 <code>getter function</code>，預設為 <code>undefined</code></li>
<li><code>set</code> : 屬性的 <code>setter function</code>，預設為 <code>undefined</code></li>
</ul>
<p>這些 “屬性描述器” 都必須透過 ES5的 <code>Object.defineProperty</code> 來處理</p>
<h4 id="定義屬性-Object-defineProperty-與-查詢屬性描述-Object-getOwnPropertyDescriptor"><a href="#定義屬性-Object-defineProperty-與-查詢屬性描述-Object-getOwnPropertyDescriptor" class="headerlink" title="定義屬性 Object.defineProperty 與 查詢屬性描述 Object.getOwnPropertyDescriptor"></a>定義屬性 <code>Object.defineProperty</code> 與 查詢屬性描述 <code>Object.getOwnPropertyDescriptor</code></h4><ul>
<li><p><code>Object.defineProperty(object, property, descriptor)</code> : 用來定義物件屬性的描述</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Person = &#123;&#125;;</span><br><span class="line"><span class="built_in">Object</span>.defineProperty(Person, <span class="string">&#x27;name&#x27;</span>, &#123;<span class="attr">value</span>: <span class="string">&#x27;Jerry&#x27;</span>&#125;);</span><br><span class="line">              </span><br><span class="line"><span class="comment">// resault</span></span><br><span class="line"><span class="keyword">var</span> Person = &#123;</span><br><span class="line">  name: <span class="string">&#x27;Jerry&#x27;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>當然也可以一次定義多個屬性描述</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.definedProperty(person, <span class="string">&#x27;name&#x27;</span>, &#123;</span><br><span class="line">  value: <span class="string">&#x27;Jerry&#x27;</span>,</span><br><span class="line">  writable: <span class="literal">true</span>,</span><br><span class="line">  enumerable: <span class="literal">true</span>,</span><br><span class="line">  configurable: <span class="literal">true</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li>
<li><p><code>Object.getOwnPropertyDescriptor(Object, Property)</code> : 用來檢查物件屬性描述器的狀態</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(Person, <span class="string">&#x27;name&#x27;</span>);</span><br><span class="line"><span class="comment">// &#123;value: &#x27;Jerry&#x27;, writable: false, enumerable: false, configuarable: false&#125;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<p>要注意的是，使用兩種物件屬性的定義方式在 <code>writable</code>、<code>enumerable</code> 和 <code>configurable</code> 這三種屬性描述器上會有不同的預設值，使用 <code>Object.definedProperty</code> 定義的物件屬性的預設值會是 <code>false</code>，直接定義物件的預設值則會是 <code>true</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;&#125;;</span><br><span class="line"><span class="built_in">Object</span>.defineProperty(person, <span class="string">&#x27;name&#x27;</span>, &#123;</span><br><span class="line">  value: <span class="string">&#x27;Jerry&#x27;</span></span><br><span class="line">&#125;);</span><br><span class="line">person.name = <span class="string">&quot;Tom&quot;</span>;</span><br><span class="line"><span class="built_in">console</span>.log(person.name);   <span class="comment">// Jerry</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.getOwnPropertyDescriptor(person, <span class="string">&#x27;name&#x27;</span>));   </span><br><span class="line"><span class="comment">// &#123;value: &quot;Jerry&quot;, writable: false, enumerable: false, configurable: false&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> Person = &#123;</span><br><span class="line">  name: <span class="string">&#x27;Jerry&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line">Person.name = <span class="string">&quot;Tom&quot;</span>;</span><br><span class="line"><span class="built_in">console</span>.log(Person.name);   <span class="comment">// Tom</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.getOwnPropertyDescriptor(Person, <span class="string">&#x27;name&#x27;</span>))</span><br><span class="line"><span class="comment">// &#123;value: &quot;Jerry&quot;, writable: true, enumerable: true, configurable: true&#125;</span></span><br></pre></td></tr></table></figure>

<h4 id="屬性描述器-get-與-set"><a href="#屬性描述器-get-與-set" class="headerlink" title="屬性描述器 get 與 set"></a>屬性描述器 get 與 set</h4><p>我們可以透過 屬性描述器 <code>get</code> 與 <code>set</code> 來追蹤物件屬性的更新，以下範例透過屬性 <code>_name_</code> 來作為 <code>name</code> 屬性的封裝，物件屬性更新時會執行 <code>set</code>，讀取物件屬性則會執行 <code>get</code>，且在自定義 <code>get</code> 和 <code>set</code> 後，就無法再透過 <code>Object.defineProperty</code> 來更改屬性描述器，並將其他屬性描述器設定為 <code>false</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(person, <span class="string">&#x27;name&#x27;</span>, &#123;</span><br><span class="line">  get: <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;get&#x27;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>._name_;</span><br><span class="line">  &#125;,</span><br><span class="line">  set: <span class="function"><span class="keyword">function</span>(<span class="params">name</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;set&#x27;</span>);</span><br><span class="line">    <span class="built_in">this</span>._name_ = name;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br><span class="line">person.name = <span class="string">&quot;Jerry&quot;</span>;   <span class="comment">// set</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Uncaught TypeError</span></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(person, <span class="string">&#x27;name&#x27;</span>, &#123;</span><br><span class="line">  value: <span class="string">&#x27;Tom&#x27;</span>,</span><br><span class="line">  writable: <span class="literal">false</span>,</span><br><span class="line">  enumerable: <span class="literal">true</span>,</span><br><span class="line">  configurable: <span class="literal">true</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(person.name); </span><br><span class="line"><span class="comment">// get  </span></span><br><span class="line"><span class="comment">// Jerry</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.getOwnPropertyDescriptor(person, <span class="string">&#x27;name&#x27;</span>));</span><br><span class="line"><span class="comment">// &#123;get: ƒ, set: ƒ, enumerable: false, configurable: false&#125;</span></span><br></pre></td></tr></table></figure>

<h2 id="基本型別包裹器-Primitive-Wrapper"><a href="#基本型別包裹器-Primitive-Wrapper" class="headerlink" title="基本型別包裹器 Primitive Wrapper"></a>基本型別包裹器 Primitive Wrapper</h2><h3 id="物件型別"><a href="#物件型別" class="headerlink" title="物件型別"></a>物件型別</h3><p>上文提到，除了基本型別以外都可以歸類在物件型別，而在物件型別中，又可以細分為以下幾種 “建構器” <code>Constructor</code></p>
<ul>
<li>String()</li>
<li>Number()</li>
<li>Boolean()</li>
<li>Array()</li>
<li>Object</li>
<li>Function</li>
<li>RegExp</li>
<li>Date</li>
<li>Error</li>
<li>Symbol</li>
</ul>
<p>以上這些建構器都可以透過關鍵字 <code>new</code> 來建立對應的物件</p>
<h3 id="基本型別包裹器-Primitive-Wrapper-1"><a href="#基本型別包裹器-Primitive-Wrapper-1" class="headerlink" title="基本型別包裹器 Primitive Wrapper"></a>基本型別包裹器 Primitive Wrapper</h3><p>物件中的 “變數” 稱為 “屬性”，而物件中的 “函式” 稱為 “方法”，但在基本型別中卻有 “屬性” 和 “方法” 可以呼叫</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str = <span class="string">&#x27;Hello&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(str.length);   <span class="comment">// 5</span></span><br><span class="line"><span class="built_in">console</span>.log(str.toUpperCase());   <span class="comment">// &#x27;HELLO&#x27;</span></span><br></pre></td></tr></table></figure>

<p>因為在存取字串 <code>String</code>、數字 <code>Number</code> 和布林 <code>Boolean</code> 的那一刻會被 “自動轉型” 為該類別的物件，這就是基本型別包裹器 <code>Primitive Wrapper</code></p>
<p>當我們在呼叫基本型別的屬性和方法時，背後的原理是透過對應的物件建構器包裝成 “物件”，再回傳對應的屬性後銷毀並回復成基本型別</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str = <span class="keyword">new</span> <span class="built_in">String</span>(<span class="string">&#x27;Hello&#x27;</span>);</span><br><span class="line">str.length;</span><br><span class="line"></span><br><span class="line">str = <span class="literal">null</span>;</span><br><span class="line">str = <span class="string">&#x27;Hello&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>基本型別並沒有 “屬性” 和 “方法”，在設定完後仍會回傳 <code>undefined</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str = <span class="string">&#x27;Hello&#x27;</span>;</span><br><span class="line"><span class="built_in">console</span>.log(typeOf(str));   <span class="comment">// &quot;string&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> strObj = <span class="keyword">new</span> <span class="built_in">String</span>(<span class="string">&#x27;Hello&#x27;</span>);</span><br><span class="line"><span class="built_in">console</span>.log(typeOf(strObj));   <span class="comment">// object</span></span><br><span class="line"></span><br><span class="line">str.color = <span class="string">&#x27;red&#x27;</span>;</span><br><span class="line">strObj.color = <span class="string">&#x27;red&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(str.color);   <span class="comment">// undefined</span></span><br><span class="line"><span class="built_in">console</span>.log(strObj.color);   <span class="comment">// &#x27;red&#x27;</span></span><br></pre></td></tr></table></figure>

<h2 id="物件與原型鍊-Object-amp-Prototype-Chain"><a href="#物件與原型鍊-Object-amp-Prototype-Chain" class="headerlink" title="物件與原型鍊 Object &amp; Prototype Chain"></a>物件與原型鍊 Object &amp; Prototype Chain</h2><p>JavaScript 是一門物件導向的程式語言，它的繼承方法是透過 “原型” <code>Prototype</code> 來操作</p>
<h3 id="原型鍊-Prototype-Chain"><a href="#原型鍊-Prototype-Chain" class="headerlink" title="原型鍊 Prototype Chain"></a>原型鍊 Prototype Chain</h3><p>原型繼承的基本概念就是讓沒有該屬性的物件去存取其他物件的屬性，原型物件是物件的內部屬性，無法直接存取，通常直接標示為 <code>[[prototype]]</code></p>
<h4 id="Object-setPrototypeOf-“繼承者”-“原型”"><a href="#Object-setPrototypeOf-“繼承者”-“原型”" class="headerlink" title="Object.setPrototypeOf(“繼承者”, “原型”)"></a>Object.setPrototypeOf(“繼承者”, “原型”)</h4><p>透過 <code>Object.setPrototypeOf()</code> 可以指定物件之間的原型關係，第一個參數為繼承者物件，第二個為要繼承的原型物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;&#125;;</span><br><span class="line"><span class="keyword">var</span> person2 = &#123;</span><br><span class="line">  name: <span class="string">&#x27;Jerry&#x27;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(person.name);   <span class="comment">// undefined</span></span><br><span class="line"><span class="built_in">console</span>.log(person2.name);   <span class="comment">// &#x27;Jerry&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.setPrototypeOf(person, person2);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(person.name);   <span class="comment">// &#x27;Jerry&#x27;</span></span><br><span class="line"><span class="built_in">console</span>.log(person2.name);   <span class="comment">// &#x27;Jerry&#x27;</span></span><br></pre></td></tr></table></figure>

<p>延續上方範例，同一物件無法指定兩個原型物件，如果繼承超過一個原型物件，只會繼承最後一個</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> age = &#123;</span><br><span class="line">  age: <span class="number">18</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">Object</span>.setPrototypeOf(person, age);</span><br><span class="line"><span class="built_in">console</span>.log(person.age);   <span class="comment">// 18</span></span><br><span class="line"><span class="built_in">console</span>.log(person.name);  <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>

<p>這時候就需要 “原型鍊” 的概念了，延續上方範例，先讓 <code>person2</code> 去繼承 <code>age</code>，再讓 <code>person</code> 繼承 <code>person2</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> person2 = &#123;</span><br><span class="line">  name: <span class="string">&#x27;Jerry&#x27;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> age = &#123;</span><br><span class="line">  age: <span class="number">18</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.setPrototypeOf(person2, age);</span><br><span class="line"><span class="built_in">Object</span>.setPrototypeOf(person, person2);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(person.age);   <span class="comment">// 18</span></span><br><span class="line"><span class="built_in">console</span>.log(person.name);   <span class="comment">// &#x27;Jerry&#x27;</span></span><br></pre></td></tr></table></figure>

<h3 id="最頂層原型物件-Object-prototype"><a href="#最頂層原型物件-Object-prototype" class="headerlink" title="最頂層原型物件 Object.prototype"></a>最頂層原型物件 Object.prototype</h3><p>當我們嘗試存取不存在的物件屬性時，就會自動往該物件的 “原型物件” <code>[[prototype]]</code> 去尋找，順著原型鍊一直找，直到 “最頂層原型物件” <code>Object.prototype</code></p>
<p>最頂層原型物件 <code>Object.prototype</code> 是所有物件的起源，我們以往呼叫卻未定義的 “方法” <code>Method</code> 就是透過 “原型鍊” 繼承自這個 “最頂層原型物件” <code>Object.prototype</code> 的方法</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.prototype.hasOwnPrototype()</span><br><span class="line"><span class="built_in">Object</span>.prototype.toString()</span><br><span class="line"><span class="built_in">Object</span>.prototype.valueOf()</span><br></pre></td></tr></table></figure>

<h3 id="建構式與原型"><a href="#建構式與原型" class="headerlink" title="建構式與原型"></a>建構式與原型</h3><p>因為函式也是物件的一種，所以也可以透過 <code>prototype</code> 來擴充每個透過這個函式所建構出來的物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Person = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;&#125;;</span><br><span class="line">    </span><br><span class="line">Person.prototype.sayHello = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">&quot;Hi !&quot;</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> p1 = Person();</span><br><span class="line"><span class="keyword">var</span> p2 = <span class="keyword">new</span> Person();</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(p1);   <span class="comment">// undefined</span></span><br><span class="line"><span class="built_in">console</span>.log(p2);   <span class="comment">// Person &#123;sayHello: f&#125;</span></span><br><span class="line"><span class="built_in">console</span>.log(p2.sayHello());   <span class="comment">// &quot;Hi !&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log(Person.sayHello());   <span class="comment">// Error</span></span><br></pre></td></tr></table></figure>

<p>這裡一一解析以上的結果以釐清觀念</p>
<ul>
<li><code>p1</code>: 直接呼叫 <code>Person</code> 這個函式，所以就會是這個函式回傳的結果，因為沒有設定 return ，自然也就是預設的 undefined</li>
<li><code>p2</code>: 透過 <code>Person</code> 這個函式物件所建構出來的 “物件”</li>
<li><code>p2.sayHello()</code>: 函式被建立時，都會有原型物件，當我們透過這個函式建構一個新物件時，這個函式的原型物件就會被當作新物件的 “原型”</li>
<li><code>Person.sayHello()</code>: 因為 <code>Person</code> 本身是一個函式，所以並不會存取物件屬性</li>
</ul>
<p>如果新建構出來的物件實體和原型具有相同的 “屬性” 和 “方法” 時，會優先存取自己的</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Person = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.sayHello = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;Hello !&quot;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Person.prototype.sayHello = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">&quot;Hi !&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> p = <span class="keyword">new</span> Person();</span><br><span class="line"><span class="built_in">console</span>.log(p);   <span class="comment">// Person &#123;sayHello: function ()&#123;return &quot;Hello !&quot;&#125;&#125;</span></span><br><span class="line"><span class="built_in">console</span>.log(p.sayHello);   <span class="comment">// &quot;Hello !&quot;</span></span><br></pre></td></tr></table></figure>

<h3 id="繼承判斷"><a href="#繼承判斷" class="headerlink" title="繼承判斷"></a>繼承判斷</h3><p>透過 <code>hasOwnProperty()</code> 可以判斷該屬性是物件本身所有還是繼承而來的</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Person = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.name = <span class="string">&#x27;Jerry&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Person.prototype.age = <span class="number">18</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> p = <span class="keyword">new</span> Person;</span><br><span class="line"><span class="built_in">console</span>.log(p.name);   <span class="comment">// &quot;Jerry&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log(p.age);   <span class="comment">// 18</span></span><br><span class="line"><span class="built_in">console</span>.log(p.hasOwnProperty(<span class="string">&#x27;name&#x27;</span>));   <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log(p.hasOwnProperty(<span class="string">&#x27;age&#x27;</span>));   <span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<h3 id="物件的-proto-屬性"><a href="#物件的-proto-屬性" class="headerlink" title="物件的 __proto__ 屬性"></a>物件的 <code>__proto__</code> 屬性</h3><p>從上方的 <a href="#%E5%BB%BA%E6%A7%8B%E5%BC%8F%E8%88%87%E5%8E%9F%E5%9E%8B">建構式與原型</a> 中可以知道，當透過函式建構出一個新的物件時，這個新物件的原型會自動指向函式的原型物件 <code>prototype</code></p>
<p>在 JavaScript 中，所有物件都有 <code>__proto__</code> 這個特殊屬性，這個屬性會指向這個物件的原型物件 ; 在 ES5 中，也可以透過 <code>Object.getPropertyOf()</code> 這個方法來取得物件的原型物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Person = <span class="function"><span class="keyword">function</span>(<span class="params">name</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.name = name;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> p = <span class="keyword">new</span> Person(<span class="string">&#x27;Jerry&#x27;</span>);</span><br><span class="line"></span><br><span class="line">Person.prototype.sayHello = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">&quot;Hi! &quot;</span> + <span class="built_in">this</span>.name;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(p.sayHello());</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.getPrototypeOf(p) === Person.prototype);   <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.getPrototypeOf(p) === <span class="built_in">Function</span>.prototype);   <span class="comment">// false</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.getPrototypeOf(Person) === <span class="built_in">Function</span>.prototype);   <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(p.__proto__ === Person.prototype);   <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log(p.__proto__ === <span class="built_in">Function</span>.prototype);   <span class="comment">// false</span></span><br><span class="line"><span class="built_in">console</span>.log(Person.__proto__ === <span class="built_in">Function</span>.prototype);   <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>要注意的是，當一個函是被建立的時候，會產生一個 <code>prototype</code> 屬性，但 <code>prototype</code> “並非” 這個函式的原型物件，而是透過這個函式所建構的新物件才會將原型指向函式的 <code>prototype</code> 屬性</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Person = <span class="function"><span class="keyword">function</span> (<span class="params">sayHello</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">this</span>.sayHello = sayHello;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> p = <span class="keyword">new</span> Person(<span class="string">&#x27;Hello&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(p.sayHello);   <span class="comment">// Hello</span></span><br><span class="line"><span class="built_in">console</span>.log(Person.sayHello);   <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>

<h3 id="物件與原型繼承"><a href="#物件與原型繼承" class="headerlink" title="物件與原型繼承"></a>物件與原型繼承</h3><p>前面都是透過建構式來介紹，但直接透過物件來繼承是比較常見的做法</p>
<ul>
<li><p>第一種是上方所提到的 Object.setPropertyOf()</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.setPropertyOf(p, Person);</span><br></pre></td></tr></table></figure></li>
<li><p>第二種是透過 <code>Object.create()</code> 來建立一個新物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Person = &#123;</span><br><span class="line">  name: <span class="string">&#x27;Jerry&#x27;</span>,</span><br><span class="line">  sayHello: <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;Hi! &quot;</span> + <span class="built_in">this</span>.name</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> p = <span class="built_in">Object</span>.create(Person);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(p.sayHello());   <span class="comment">// &quot;Hi! Jerry&quot;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<p>而 <code>Object.create()</code> 背後的原理是將物件作為參數帶入，先建立一個新函式，再將這個函式的原型指向這個物件，最後回傳一個 <code>new</code> 的新物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.create = <span class="function"><span class="keyword">function</span>(<span class="params">proto</span>)</span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">F</span> (<span class="params"></span>)</span>&#123;&#125;;</span><br><span class="line">  F.prototype = proto;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> F();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="proto-amp-prototype"><a href="#proto-amp-prototype" class="headerlink" title="proto &amp; prototype"></a><strong>proto</strong> &amp; prototype</h3><p>總結一下<code> __proto__</code> &amp; <code>prototype</code> 的關係，JavaScript 的內建物件 <code>Array``、Function</code> 等，這些物件的 <code>prototype</code> 屬性本身也是一個物件，而這些屬性實際上都是繼承自最頂層物件 <code>Object.prototype</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.getPrototypeOf(<span class="built_in">Function</span>.prototype) === <span class="built_in">Object</span>.prototype);   <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Function</span>.prototype.__proto__ === <span class="built_in">Object</span>.prototype);   <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>由此可知下層物件的 <code>__proto__</code> 屬性都會指向上層物件的 <code>prototype</code> 屬性，這就是 JavaScript 的繼承關係</p>
<h2 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h2><p><a href="https://ithelp.ithome.com.tw/articles/10194154">物件與原型鏈</a><br><a href="https://chupainotebook.blogspot.com/2019/01/js.html">【克服 JS 】第四章（上）物件與函式</a><br><a href="https://developer.mozilla.org/zh-TW/docs/Web/JavaScript/Reference/Global_Objects/Object/defineProperty">Object.defineProperty()</a></p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>payload 傳遞參數</title>
    <url>/vue/20191113/32048/</url>
    <content><![CDATA[<p>當我們要從元件傳遞參數到 <code>actions</code> 時，如果原先在元件中的事件就有一個以上的參數，則第二個以後的參數就會出現 <code>undefined</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in component</span></span><br><span class="line"><span class="function"><span class="title">addtoCart</span>(<span class="params">id, qty = <span class="number">1</span></span>)</span> &#123;</span><br><span class="line">  <span class="built_in">this</span>.$store.dispatch(<span class="string">&#x27;addtoCart&#x27;</span>, id, qty);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// in store</span></span><br><span class="line">actions: &#123;</span><br><span class="line">  <span class="function"><span class="title">addtoCart</span>(<span class="params">context, id, qty</span>)</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(context, id, qty);   <span class="comment">// &#x27;預設 context 內容&#x27; &#x27;id&#x27; undefined</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="物件形式傳遞多個參數"><a href="#物件形式傳遞多個參數" class="headerlink" title="物件形式傳遞多個參數"></a>物件形式傳遞多個參數</h2><p>如果原先事件就有一個以上的參數，就可以透過物件 <code>&#123;&#125;</code> 的形式將所有參數包起來傳遞，再透過 “解構” 的方式取出參數</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in component</span></span><br><span class="line"><span class="function"><span class="title">addtoCart</span>(<span class="params">id, qty = <span class="number">1</span></span>)</span> &#123;</span><br><span class="line">  <span class="built_in">this</span>.$store.dispatch(<span class="string">&#x27;addtoCart&#x27;</span>, &#123; id, qty &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// in store</span></span><br><span class="line">actions: &#123;</span><br><span class="line">  <span class="function"><span class="title">addtoCart</span>(<span class="params">context, &#123; id, qty &#125;</span>)</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(context, id, qty);   <span class="comment">// &#x27;預設 context 內容&#x27; &#x27;id&#x27; &#x27;qty&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Vue</tag>
        <tag>Vuex</tag>
      </tags>
  </entry>
  <entry>
    <title>Actions 與 Mutations 改變資料狀態</title>
    <url>/vue/20191113/2212/</url>
    <content><![CDATA[<p>在第一個章節中有提到，在 <code>vuex</code> 中，<code>actions</code> 處理非同步的事件或取得遠端的資料，但不處理資料的狀態，而 <code>mutation</code> 則是改變資料內容的方法，所以上一章節直接更改變數的方式在 <code>vuex</code> 中並不完全正確</p>
<span id="more"></span>

<p>在這裡可以透過開發者工具中的 <code>Vue</code> 並在其中找到 <code>Vuex</code> 的選項，可以看到裡面有我們剛才在 <code>state</code> 中新增的變數 <code>isLoading</code></p>
<p><img src="Vuex-devtool.png"></p>
<h2 id="新增-Actions-與-Mutation"><a href="#新增-Actions-與-Mutation" class="headerlink" title="新增 Actions 與 Mutation"></a>新增 Actions 與 Mutation</h2><p>在官方資料中我們可以得到以下資訊 :</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">new</span> Vuex.Store(&#123;</span><br><span class="line">  state: &#123;</span><br><span class="line">    isLoading: <span class="literal">false</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  actions: &#123;</span><br><span class="line">    <span class="function"><span class="title">updateLoading</span>(<span class="params">context, payload</span>)</span> &#123;&#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  mutations: &#123;</span><br><span class="line">    <span class="function"><span class="title">LOADING</span>(<span class="params">state, payload</span>)</span> &#123;&#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="actions"><a href="#actions" class="headerlink" title="actions"></a>actions</h3><p>處理非同步的事件或取得遠端的資料，但不處理資料的狀態</p>
<ul>
<li><p>第一個參數 <code>context</code>: 為預設值，包含以下屬性</p>
<ul>
<li>state</li>
<li>rootState</li>
<li>commit</li>
<li>dispatch</li>
<li>getters</li>
<li>rootGetters</li>
</ul>
</li>
<li><p>第二個參數 <code>payload</code> : 從外部傳進來的數值，由使用者所自訂</p>
</li>
</ul>
<h3 id="mutations"><a href="#mutations" class="headerlink" title="mutations"></a>mutations</h3><p>改變資料內容的方法，並建議使用 “常數” 的變數名稱來命名，也就是全部大寫以區分其他變數，但並沒有強制性</p>
<p>而其中的方法同樣具有兩個參數</p>
<ul>
<li>第一個參數 <code>state</code>: 即為設定參數的 <code>state</code></li>
<li>第二個參數 <code>payload</code>: 同為從外部傳進來的數值，由使用者所自訂，所以可以直接透過 <code>action</code> 中的 <code>payload</code> 傳入</li>
</ul>
<h2 id="設定方法"><a href="#設定方法" class="headerlink" title="設定方法"></a>設定方法</h2><p>首先透過 <code>mutations</code> 來設定更改變數的 “方法”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">mutations: &#123;</span><br><span class="line">  <span class="function"><span class="title">LOADING</span>(<span class="params">state, payload</span>)</span> &#123;</span><br><span class="line">    state.isLoading = payload;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="觸發方法"><a href="#觸發方法" class="headerlink" title="觸發方法"></a>觸發方法</h2><p>設定方法後，透過 <code>actions</code> 來觸發 <code>mutations</code> 中的方法，觸發的方式會使用第一個參數 <code>context</code> 中的 <code>commit</code>，而其第一個參數就是要觸發的 “方法名稱”，第二個參數就是要帶入的載荷 <code>status</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">actions: &#123;</span><br><span class="line">  <span class="function"><span class="title">updateLoading</span>(<span class="params">context, payload</span>)</span> &#123;</span><br><span class="line">    context.commit(<span class="string">&#x27;LOADING&#x27;</span>, payload);</span><br><span class="line">  &#125;</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>

<h2 id="更改變數"><a href="#更改變數" class="headerlink" title="更改變數"></a>更改變數</h2><p>上一章節中，我們直接透過 <code>this.$store.state</code> 來改變變數的值並不完全正確，正確的方式是透過 <code>this.$store.dispatch</code> 來呼叫 <code>actions</code> 中的方法，第一個參數就是 “方法名稱”，第二個參數是要 “帶入的值”，再進一步呼叫 <code>mutations</code> 來更改變數的值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 錯誤方式</span></span><br><span class="line"><span class="built_in">this</span>.$store.state.isLoading = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 正確方式</span></span><br><span class="line"><span class="built_in">this</span>.$store.dispatch(<span class="string">&#x27;updateLoading&#x27;</span>, <span class="literal">true</span>);</span><br></pre></td></tr></table></figure>

<p>在這裡要注意的是，<code>dispatch</code> 是用來 “賦予值” 的，所以 <code>computed</code> 中還是直接 <code>return state</code> 中的變數 <code>isLoading</code> 就可以了</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">computed: &#123;</span><br><span class="line">  <span class="function"><span class="title">isLoading</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>.$store.state.isLoading;</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure>

<h2 id="開發者工具"><a href="#開發者工具" class="headerlink" title="開發者工具"></a>開發者工具</h2><p>將 <code>vuex</code> 改成正確的方式後，打開開發者工具中的 <code>Vue/Vuex</code>，每次呼叫 <code>mutations</code> 中的方法都會保有紀錄，將游標移到上方就會顯示幾個按鈕，按下 <code>Time Travel to This State</code> 會可回到該狀態檢視</p>
<p><img src="Vuex-devtool-timeTravel.png"></p>
<h2 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h2><p><a href="https://vuex.vuejs.org/zh/api/#mutations">官方資料</a></p>
]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Vue</tag>
        <tag>Vuex</tag>
      </tags>
  </entry>
  <entry>
    <title>Vuex 是什麼 ?</title>
    <url>/vue/20191113/2839/</url>
    <content><![CDATA[<p>Vuex 是用來管理前端網站的資料狀態，適用於規模較大的網站，如果網站規模不夠大，反而是多了限制</p>
<span id="more"></span>

<h2 id="以往的資料傳遞方式"><a href="#以往的資料傳遞方式" class="headerlink" title="以往的資料傳遞方式"></a>以往的資料傳遞方式</h2><p>以往的資料傳遞方式分別是透過以下方法來達成</p>
<ul>
<li><code>emit</code>: 內層元件傳遞至外層元件</li>
<li><code>props</code>: 外層元件傳遞至內層元件</li>
<li><code>$eventbus</code>: 同層元件或跨層元件間的傳遞，但僅適用於資料量較小的情況，如果資料量過大，容易造成管理上的混亂</li>
<li><code>全域變數</code>: 無法發揮 Vue 雙向綁定的特色</li>
</ul>
<h2 id="Vuex-的特色"><a href="#Vuex-的特色" class="headerlink" title="Vuex 的特色"></a>Vuex 的特色</h2><p>將所有資料和方法都拉進來統一管理，每個階層的元件都可以呼叫 <code>Vuex</code> 內的方法，並維持 <code>Vue</code> 雙向綁定的特性，而存在於其中的方法稱為 <code>store</code></p>
<h2 id="Vuex-的方法"><a href="#Vuex-的方法" class="headerlink" title="Vuex 的方法"></a>Vuex 的方法</h2><p>Vuex 的方法都有其相對的元件方法，但又不完全相同</p>
<table>
  <thead>
    <tr>
      <th>元件方法</th>
      <th>Vuex 方法</th>
      <th>功能</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>data</td>
      <td>state</td>
      <td>管理資料的位置與資料的狀態</td>
    </tr>
    <tr>
      <td>methods</td>
      <td>action</td>
      <td>處理非同步的事件或取得遠端的資料，但不處理資料的狀態</td>
    </tr>
    <tr>
      <td>computed</td>
      <td>getter</td>
      <td>在資料呈現於畫面前過濾或是進行運算</td>
    </tr>
    <tr>
      <td></td>
      <td>mutation</td>
      <td>改變資料內容的方法</td>
    </tr>
  </tbody>
</table>

<h2 id="Vuex-流程"><a href="#Vuex-流程" class="headerlink" title="Vuex 流程"></a>Vuex 流程</h2><ul>
<li>首先透過 <code>Dispatch</code> 這個方法來觸發 <code>Actions</code> 事件</li>
<li>接著 <code>Actions</code> 就會取得遠端的資料或處理非同步的行為，但並不會改變資料的狀態</li>
<li>再來 <code>Actions</code> 會透過 <code>Commit</code> 這個方法來呼叫 <code>Mutations</code>，並透過 <code>Mutations</code> 來改變資料的狀態</li>
<li>最後回傳給 <code>Vue</code> 元件來做畫面的繪製，其中 <code>Mutations</code> 與開法者工具 <code>Vue Devtools</code> 有很大的關係，所以在 <code>Mutations</code> 不會再取得資料，而在 <code>Actinos</code> 不會改變資料內容</li>
</ul>
<p><img src="Vuex-process.png"></p>
<h2 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h2><p><a href="https://vuex.vuejs.org/zh/guide/">官方文件</a></p>
]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Vue</tag>
        <tag>Vuex</tag>
      </tags>
  </entry>
  <entry>
    <title>Vuex 嚴謹模式</title>
    <url>/vue/20191113/36215/</url>
    <content><![CDATA[<p>在 <code>Vuex</code> 中，一直重複提到一個很重要的觀念就是 <code>mutations</code> 只負責同步的行為，非同步的行為如 <code>AJAX</code> 與 <code>setTimeout</code> 等都是由 <code>actions</code> 來負責</p>
<span id="more"></span>

<h2 id="錯誤的開發方式"><a href="#錯誤的開發方式" class="headerlink" title="錯誤的開發方式"></a>錯誤的開發方式</h2><p>以下分別示範兩種 <code>Vuex</code> 中 錯誤 的開發方式</p>
<h3 id="mutations-中設定-“非同步事件”"><a href="#mutations-中設定-“非同步事件”" class="headerlink" title="mutations 中設定 “非同步事件”"></a>mutations 中設定 “非同步事件”</h3><p>如果在 <code>mutations</code> 中建立非同步事件，透過開發者工具會發現 <code>mutations</code> 與更改的變數 <code>state</code> 會無法對上，進而造成除錯上的困難</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">mutations: &#123;</span><br><span class="line">  <span class="function"><span class="title">LOADING</span>(<span class="params">state, payload</span>)</span> &#123;</span><br><span class="line">    <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      state.isLoading = status;</span><br><span class="line">    &#125;, <span class="number">1000</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="Vuex-actions-error.png"></p>
<p>如果有非同步的行為應該直接加在 <code>actions</code> 中</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">actions: &#123;</span><br><span class="line">  <span class="function"><span class="title">updateLoading</span>(<span class="params">context, payload</span>)</span> &#123;</span><br><span class="line">    <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      context.commit(<span class="string">&#x27;LOADING&#x27;</span>, payload);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="mutations-外修改變數狀態"><a href="#mutations-外修改變數狀態" class="headerlink" title="mutations 外修改變數狀態"></a>mutations 外修改變數狀態</h3><p>如果在 <code>mutations</code> 以外修改變數狀態也會造成結果無法呈現，透過開發者工具察看也會發現沒有偵測到狀態</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">actions: &#123;</span><br><span class="line">  <span class="function"><span class="title">updateLoading</span>(<span class="params">context, payload</span>)</span> &#123;</span><br><span class="line">    <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      context.state.isLoading = status;</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="Vuex-mutations-error.png"></p>
<h2 id="嚴謹模式"><a href="#嚴謹模式" class="headerlink" title="嚴謹模式"></a>嚴謹模式</h2><p>在使用 <code>Vuex</code> 開發時，加上以下指令就會在不符合規範時跳出錯誤提示</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">new</span> Vuex.Store(&#123;</span><br><span class="line">  strict: ture,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Vue</tag>
        <tag>Vuex</tag>
      </tags>
  </entry>
  <entry>
    <title>新增一個 store 來管理網站資料狀態</title>
    <url>/vue/20191113/33419/</url>
    <content><![CDATA[<p>這章節會將兩個各自透過變數 <code>Loading</code> 管理 <code>vue-loading-overlay</code> 的元件統一由 <code>store</code> 來管理</p>
<span id="more"></span>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">template</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">Loading</span> <span class="attr">:active.sync</span>=<span class="string">&quot;isLoading&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">Loading</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">template</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">  <span class="keyword">export</span> <span class="keyword">default</span> &#123;  </span></span><br><span class="line">    data ()&#123;    </span><br><span class="line"><span class="javascript">      <span class="keyword">return</span> &#123;      </span></span><br><span class="line"><span class="javascript">        isLoading: <span class="literal">false</span>,    </span></span><br><span class="line">      &#125;  </span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="安裝-Vuex"><a href="#安裝-Vuex" class="headerlink" title="安裝 Vuex"></a>安裝 Vuex</h2><p>首先透過以下指令安裝 <code>Vuex</code></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install --save vuex</span><br></pre></td></tr></table></figure>

<h2 id="在進入點-main-js-啟用"><a href="#在進入點-main-js-啟用" class="headerlink" title="在進入點 main.js 啟用"></a>在進入點 main.js 啟用</h2><p>下載後在進入點 <code>main.js</code> 啟用</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in main.js</span></span><br><span class="line">            </span><br><span class="line"><span class="keyword">import</span> Vuex <span class="keyword">from</span> <span class="string">&#x27;vuex&#x27;</span></span><br><span class="line">Vue.use(Vuex)</span><br></pre></td></tr></table></figure>

<h2 id="新增-store-管理檔案"><a href="#新增-store-管理檔案" class="headerlink" title="新增 store 管理檔案"></a>新增 store 管理檔案</h2><p>可以透過新增一個 <code>store</code> 資料夾，並在其內新增一個 <code>index.js</code> 檔案來彙整所有 <code>vuex</code> 的行為，同樣的，需要先載入 <code>vue &amp; vuex</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in store/index.js</span></span><br><span class="line">            </span><br><span class="line"><span class="keyword">import</span> Vue <span class="keyword">from</span> <span class="string">&#x27;vue&#x27;</span></span><br><span class="line"><span class="keyword">import</span> Vuex <span class="keyword">from</span> <span class="string">&#x27;vuex&#x27;</span></span><br><span class="line"></span><br><span class="line">Vue.use(Vuex)</span><br></pre></td></tr></table></figure>

<p>載入 <code>vue &amp; vuex</code> 後就可以透過 <code>ES6</code> 的 <code>export default</code> 來輸出資料內容</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">new</span> Vuex.Store(&#123;&#125;)</span><br></pre></td></tr></table></figure>

<h2 id="從進入點載入管理檔案"><a href="#從進入點載入管理檔案" class="headerlink" title="從進入點載入管理檔案"></a>從進入點載入管理檔案</h2><p>存檔後就可以在進入點 <code>main.js</code> 載入這支彙整 <code>vuex</code> 的檔案</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in main.js</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> store <span class="keyword">from</span> <span class="string">&#x27;./store&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> Vue(&#123;</span><br><span class="line">  store,</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h2 id="新增管理變數"><a href="#新增管理變數" class="headerlink" title="新增管理變數"></a>新增管理變數</h2><p><code>vuex</code> 會將原先由各自元件管理的變數統一由一個檔案來彙整，在這個範例中會將 <code>isLoading</code> 這個變數拉到 <code>state</code> 中管理</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in store/index.js</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">new</span> Vuex.Store(&#123;</span><br><span class="line">  state: &#123;</span><br><span class="line">    isLoading: <span class="literal">false</span>,</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h2 id="載入管理變數"><a href="#載入管理變數" class="headerlink" title="載入管理變數"></a>載入管理變數</h2><p>改由 <code>vuex</code> 管理變數後，原先在各元件中的變數就可以移除，並透過 <code>compoted</code> 來載入由 <code>vuex</code> 檔案來統一管理的變數 ; 而在這裡可以使用與原先相同的變數名稱 <code>isLoading</code> 來建立函式，就可以不用該改 <code>HTML</code> 元素中的設定</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in component</span></span><br><span class="line"></span><br><span class="line">computed: &#123;</span><br><span class="line">  isLoading ()&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>.$store.state.isLoading</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="更改變數的值"><a href="#更改變數的值" class="headerlink" title="更改變數的值"></a>更改變數的值</h2><p>過往透過更改變數的值來控制呈現狀態的方式，在這裡要更改的變數也要改成 <code>vuex</code> 檔案中的變數</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in component</span></span><br><span class="line"></span><br><span class="line">methods: &#123;</span><br><span class="line">  getProducts ()&#123;</span><br><span class="line">    <span class="keyword">const</span> vm = <span class="built_in">this</span>;</span><br><span class="line">    <span class="keyword">const</span> api = <span class="string">`test.api`</span>;</span><br><span class="line">    </span><br><span class="line">    vm.$store.state.isLoading = <span class="literal">true</span>;</span><br><span class="line">    vm.$http.get(api).then(<span class="function">(<span class="params">response</span>) =&gt;</span> &#123;</span><br><span class="line">      vm.$store.state.isLoading = <span class="literal">false</span>;</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="刪除子元件元素"><a href="#刪除子元件元素" class="headerlink" title="刪除子元件元素"></a>刪除子元件元素</h2><p>透過 <code>vuex</code> 統一管理變數的方式，子元件中的元素就可以刪除，並統一由父元件與 <code>vuex</code> 檔案來管理</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- in child component --&gt;</span> </span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">template</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">Loading</span> <span class="attr">:active.sync</span>=<span class="string">&quot;isLoading&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">Loading</span>&gt;</span>  <span class="comment">&lt;!-- delete --&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">template</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h2><p><a href="https://vuex.vuejs.org/zh/installation.html">官方資料</a></p>
]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Vue</tag>
        <tag>Vuex</tag>
      </tags>
  </entry>
  <entry>
    <title>使用 actions 取得遠端資料</title>
    <url>/vue/20191113/7550/</url>
    <content><![CDATA[<p><code>Vuex</code> 最大的特色就是可以將 “重複” 使用到的變數與方法統一放到 <code>store</code> 來管理，而其管理的流程是透過元件來呼叫 <code>actions</code> 來觸發 “非同步” 的事件，再透過 <code>commit</code> 呼叫 <code>mutations</code> 中的同步事件來改變 <code>state</code> 中的資料狀態，進而影響畫面的呈現</p>
<span id="more"></span>

<p>在這個章節中，我們將原先各自放在元件中的事件拉到 <code>store</code> 中統一管理，以下是原先在元件中的資料</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in component</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  name: <span class="string">&#x27;Home&#x27;</span>,</span><br><span class="line">  <span class="function"><span class="title">data</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      products: [],</span><br><span class="line">      categories: [],</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  methods: &#123;</span><br><span class="line">    <span class="function"><span class="title">getProducts</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">      <span class="keyword">const</span> vm = <span class="built_in">this</span>;</span><br><span class="line">      <span class="keyword">const</span> url = <span class="string">`<span class="subst">$&#123;process.env.APIPATH&#125;</span>/api/<span class="subst">$&#123;process.env.CUSTOMPATH&#125;</span>/products/all`</span>;</span><br><span class="line">      vm.$store.dispatch(<span class="string">&#x27;updateLoading&#x27;</span>, <span class="literal">true</span>);</span><br><span class="line">      <span class="built_in">this</span>.$http.get(url).then(<span class="function">(<span class="params">response</span>) =&gt;</span> &#123;</span><br><span class="line">        vm.products = response.data.products;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">&#x27;取得產品列表:&#x27;</span>, response);</span><br><span class="line">        vm.getUnique();</span><br><span class="line">        vm.$store.dispatch(<span class="string">&#x27;updateLoading&#x27;</span>, <span class="literal">false</span>);</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="function"><span class="title">getUnique</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">      <span class="keyword">const</span> vm = <span class="built_in">this</span>;</span><br><span class="line">      <span class="keyword">const</span> categories = <span class="keyword">new</span> <span class="built_in">Set</span>();</span><br><span class="line">      vm.products.forEach(<span class="function">(<span class="params">item</span>) =&gt;</span> &#123;</span><br><span class="line">        categories.add(item.category);</span><br><span class="line">      &#125;);</span><br><span class="line">      vm.categories = <span class="built_in">Array</span>.from(categories);</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function"><span class="title">created</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.getProducts();</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>首先將 <code>getProducts</code> 事件整個拉到 <code>actions</code> 中，並帶入第一個參數 <code>context</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">actions: &#123;</span><br><span class="line">  <span class="function"><span class="title">getProducts</span>(<span class="params">context</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> vm = <span class="built_in">this</span>;</span><br><span class="line">    <span class="keyword">const</span> url = <span class="string">`<span class="subst">$&#123;process.env.APIPATH&#125;</span>/api/<span class="subst">$&#123;process.env.CUSTOMPATH&#125;</span>/products/all`</span>;</span><br><span class="line">    vm.$store.dispatch(<span class="string">&#x27;updateLoading&#x27;</span>, <span class="literal">true</span>);</span><br><span class="line">    <span class="built_in">this</span>.$http.get(url).then(<span class="function">(<span class="params">response</span>) =&gt;</span> &#123;</span><br><span class="line">      vm.products = response.data.products;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">&#x27;取得產品列表:&#x27;</span>, response);</span><br><span class="line">      vm.getUnique();</span><br><span class="line">      vm.$store.dispatch(<span class="string">&#x27;updateLoading&#x27;</span>, <span class="literal">false</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>接著修改以下項目 :</p>
<h2 id="this"><a href="#this" class="headerlink" title="this"></a>this</h2><p>原先在元件中的 <code>this</code> 指的是元件本身，在這裡並不指向元件，所以先將其刪除</p>
<h2 id="this-store-dispatch-方法"><a href="#this-store-dispatch-方法" class="headerlink" title="this.$store.dispatch 方法"></a>this.$store.dispatch 方法</h2><p>原先透過 <code>dispatch</code> 這個方法來觸發 <code>actions</code> 中方法的方式，可以改成直接呼叫</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 原先方法</span></span><br><span class="line"><span class="built_in">this</span>.$store.dispatch(<span class="string">&#x27;updateLoading&#x27;</span>, <span class="literal">true</span>);   </span><br><span class="line">vm.getUnique();</span><br><span class="line">vm.products = response.data.products;</span><br><span class="line"><span class="built_in">this</span>.$store.dispatch(<span class="string">&#x27;updateLoading&#x27;</span>, <span class="literal">false</span>);   </span><br><span class="line"></span><br><span class="line"><span class="comment">// 直接呼叫</span></span><br><span class="line">context.commit(<span class="string">&#x27;LOADING&#x27;</span>, <span class="literal">true</span>);</span><br><span class="line">context.commit(<span class="string">&#x27;CATEGORITES&#x27;</span>, response.data.products);</span><br><span class="line">context.commit(<span class="string">&#x27;PRODUCTS&#x27;</span>, response.data.products);</span><br><span class="line">context.commit(<span class="string">&#x27;LOADING&#x27;</span>, <span class="literal">false</span>);</span><br></pre></td></tr></table></figure>

<h2 id="儲存資料"><a href="#儲存資料" class="headerlink" title="儲存資料"></a>儲存資料</h2><p>而原先儲存資料的行為則放到 <code>mutations</code> 中管理，在這裡透過 “常數” 命名，所以名稱上不會有衝突</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">matations: &#123;</span><br><span class="line">  <span class="function"><span class="title">PRODUCTS</span>(<span class="params">state, payload</span>)</span> &#123;</span><br><span class="line">    state.products = payload;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function"><span class="title">CATEGORITES</span>(<span class="params">state, payload</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> categories = <span class="keyword">new</span> <span class="built_in">Set</span>();</span><br><span class="line">    payload.forEach(<span class="function">(<span class="params">item</span>) =&gt;</span> &#123;</span><br><span class="line">      categories.add(item.categories);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="從元件呼叫-store-事件"><a href="#從元件呼叫-store-事件" class="headerlink" title="從元件呼叫 store 事件"></a>從元件呼叫 store 事件</h2><p>原先在元件中的事件因為其中的非同步語同步都各自搬到 <code>actions</code> 與 <code>mutations</code> 中統一管理，所以在這裡只要呼叫 <code>store</code> 中的事件即可</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">methods: &#123;</span><br><span class="line">  <span class="function"><span class="title">getProducts</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.$store.dispatch(<span class="string">&#x27;getProducts&#x27;</span>);</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="axios-取得遠端資料"><a href="#axios-取得遠端資料" class="headerlink" title="axios 取得遠端資料"></a>axios 取得遠端資料</h2><p>在原先的元件中，我們透過 <code>axios</code> 這個套件中的方法來取得遠端資料，在 <code>Vue</code> 元件中，透過 <code>vue-axios</code> 的方法 <code>this.$http</code> 來操做，但是在 <code>store</code> 中只能透過 <code>axios</code> 原始的方法來操作，所以在這裡要先載入 <code>axios</code> 這個套件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> axios <span class="keyword">from</span> <span class="string">&#x27;axios&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">this</span>.$http.get(api).then();   <span class="comment">// 元件中方法</span></span><br><span class="line">axios.get(api).then();   <span class="comment">// 直接使用 axios 方法</span></span><br></pre></td></tr></table></figure>

<h2 id="將資料呈現畫面上"><a href="#將資料呈現畫面上" class="headerlink" title="將資料呈現畫面上"></a>將資料呈現畫面上</h2><p>最後再透過 <code>computed</code> 監控資料，在資料改變時同步改變畫面呈現，並刪除原先在元件中的變數</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in component</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="function"><span class="title">data</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      products: [],   <span class="comment">// delete</span></span><br><span class="line">      categories: [],   <span class="comment">// delete</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  computed: &#123;</span><br><span class="line">    <span class="function"><span class="title">categories</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="built_in">this</span>.$store.state.categories;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="function"><span class="title">products</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="built_in">this</span>.$store.state.products;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Vue</tag>
        <tag>Vuex</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTP 前後端傳輸流程</title>
    <url>/network/20200517/49993/</url>
    <content><![CDATA[<p>紀錄前端與後端之間的基本傳輸原理</p>
<span id="more"></span>

<h2 id="什麼是伺服器"><a href="#什麼是伺服器" class="headerlink" title="什麼是伺服器 ?"></a>什麼是伺服器 ?</h2><ul>
<li><p>提供服務的電腦</p>
<p>  ex: 網頁伺服器、檔案伺服器、郵件伺服器…</p>
</li>
<li><p>和一般電腦有什麼不一樣</p>
<ul>
<li>一般家用電腦重顯示效果，遊戲電玩或 3D 繪圖都需要強大的顯示卡運算能力</li>
<li>伺服器重運算及資料安全，CPU 需要非常強大, RAM 插好插滿, 磁碟陣列保護資料, 24 小時不關機</li>
</ul>
</li>
<li><p>網頁伺服器軟體</p>
<p>  ex: Apache、Micorsoft IIS、NGINX…</p>
</li>
<li><p>建立網頁伺服器</p>
<p>  安裝 node 後可以透過 express 來建立簡易網頁伺服器，首先檢查是否已安裝 node 並確認版本</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">node -v</span><br></pre></td></tr></table></figure>

<p>  安裝 express</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm i express-generator -g</span><br></pre></td></tr></table></figure>

<p>  接著可以在桌面新建立一個放置檔案的新資料夾 demo ，並進入到資料輸入以下指令來查找 express 指令</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">express -h</span><br></pre></td></tr></table></figure>

<p>  接著建立 ejs 樣板引擎</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">express -e</span><br></pre></td></tr></table></figure>

<p>  依照指示的步驟執行</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm i</span><br><span class="line">npm start</span><br></pre></td></tr></table></figure>

<p>  最後在 <a href="http://localhost:3000/">http://localhost:3000</a> 開啟伺服器網頁</p>
</li>
<li><p>網址是如何產生畫面的 ?</p>
<p>  因為 HTTP 通訊協定，通訊協定就好比人溝通的語言</p>
</li>
</ul>
<h2 id="什麼是-HTTPS"><a href="#什麼是-HTTPS" class="headerlink" title="什麼是 HTTPS ?"></a>什麼是 HTTPS ?</h2><ul>
<li>http 是沒有加密的，只要攔截到封包就可以看到傳送的內容，來路不明的 wifi 就有可能藉此攔截訊息</li>
</ul>
<h2 id="localhost-是什麼"><a href="#localhost-是什麼" class="headerlink" title="localhost 是什麼 ?"></a>localhost 是什麼 ?</h2><ul>
<li>指向自己電腦的名稱，網址會先透過 DNS 伺服器將網址解析成 IP，但 localhost 預設指向 127.0.0.1 的 IP，網址是給人看並具有意義的，而電腦則是夠過 IP 來傳遞資料的</li>
</ul>
<p><img src="DNS.png"></p>
<h2 id="127-0-0-1"><a href="#127-0-0-1" class="headerlink" title="127.0.0.1"></a>127.0.0.1</h2><p>表示自己電腦的 IP 位置</p>
<h2 id="IPv4-v-s-IPv6"><a href="#IPv4-v-s-IPv6" class="headerlink" title="IPv4 v.s IPv6"></a>IPv4 v.s IPv6</h2><ul>
<li><p>以往都是使用 IPv4 的規則來產生 IP，但隨著 IP 不斷增加，IPv4 開始遇到 IP 不夠用的問題，因此就出現了 IPv6 來解決 IP 不夠用的問題</p>
<ul>
<li>IPv4 : 0.0.0.0 ~ 255.255.255.255 共有 2**32 組</li>
<li>IPv6 : 0000:0000:0000:0000:0000:0000:0000:0000 ~ FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF 共有 2n**128n 組 (n 可以突破 javascipt 位數限制)</li>
</ul>
</li>
</ul>
<h2 id="通訊阜號"><a href="#通訊阜號" class="headerlink" title="通訊阜號"></a>通訊阜號</h2><ul>
<li>http 預設是 80, https 預設是 443, 預設的 port 可以不加, 每個 port 只能給一支應用程式監聽, 另外 FTP 檔案傳輸協定是 21, SMTP 簡單郵件傳輸協定為 25</li>
</ul>
<h2 id="怎麼查-PORT-被誰使用"><a href="#怎麼查-PORT-被誰使用" class="headerlink" title="怎麼查 PORT 被誰使用 ?"></a>怎麼查 PORT 被誰使用 ?</h2><ul>
<li>透過以下指令可以查詢現在正被使用的 port</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">netstat -ano | findstr LISTENING</span><br></pre></td></tr></table></figure>

<ul>
<li>接著可以查詢指定的 port</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tasklist | findstr &#123;&#123;port&#125;&#125;</span><br></pre></td></tr></table></figure>

<h2 id="何謂-TCP-IP"><a href="#何謂-TCP-IP" class="headerlink" title="何謂 TCP/IP ?"></a>何謂 TCP/IP ?</h2><ul>
<li>TCP : 如同寄送大型包裹時，會拆分成許多小件以方便寄送，TCP 會將傳輸物件拆分成許多封包來運送</li>
<li>IP : 可以則如同寄送包裹的地址，唯有標註 IP 才會知道要傳輸的位置</li>
</ul>
<h2 id="產生-Request-封包"><a href="#產生-Request-封包" class="headerlink" title="產生 Request 封包"></a>產生 Request 封包</h2><ul>
<li>瀏覽器 (client) 產生 Request 封包傳送給伺服器 (server), 等待伺服器回傳 response 封包解讀裡面的內容</li>
</ul>
<h2 id="Method-請求方法"><a href="#Method-請求方法" class="headerlink" title="Method 請求方法"></a>Method 請求方法</h2><ul>
<li>Get : 如同寄信片，大小限制約 2 KB，</li>
<li>Post : 如同將大物件拆分成許多包裹來寄送，傳輸內容沒有上限</li>
</ul>
<h2 id="常見的-Request-Header"><a href="#常見的-Request-Header" class="headerlink" title="常見的 Request Header"></a>常見的 Request Header</h2><h3 id="Host"><a href="#Host" class="headerlink" title="Host"></a>Host</h3><ul>
<li>瀏覽器存取的 Host 名稱，因為一個 IP 可以綁定多個 Domain ，此時就須要 Host 知道要對應哪一個伺服器</li>
</ul>
<h3 id="User-Agent"><a href="#User-Agent" class="headerlink" title="User-Agent"></a>User-Agent</h3><ul>
<li>瀏覽器名稱和版本 &amp; 作業系統名稱和版本 (讓伺服器知道)</li>
</ul>
<h3 id="Accept"><a href="#Accept" class="headerlink" title="Accept"></a>Accept</h3><ul>
<li>瀏覽器接受的類型 ex: text/plain、text/html</li>
</ul>
<h3 id="Accept-Language"><a href="#Accept-Language" class="headerlink" title="Accept-Language"></a>Accept-Language</h3><ul>
<li>瀏覽器接受的語言，q 值表示語言優先的權重 ex:zh-TW,zh;q=0.9,en-US;q=0.8,en;q=0.7</li>
</ul>
<h3 id="Accept-Encoding"><a href="#Accept-Encoding" class="headerlink" title="Accept-Encoding"></a>Accept-Encoding</h3><ul>
<li>瀏覽器支援的壓縮格式，大部分瀏覽器都支援 gzip 壓縮，伺服器則會壓縮內容後傳給瀏覽器，節省下載時間和頻寬</li>
</ul>
<h3 id="Date"><a href="#Date" class="headerlink" title="Date"></a>Date</h3><ul>
<li>傳送瀏覽器時間給伺服器，讓伺服器知道瀏覽器目前的時間</li>
</ul>
<h3 id="Expect"><a href="#Expect" class="headerlink" title="Expect"></a>Expect</h3><ul>
<li>要求伺服器回應特定的結果，期望可以給想要的結果，但最終結果還是由伺服器決定 ex: 100-continue</li>
</ul>
<h3 id="If-Modified-Since"><a href="#If-Modified-Since" class="headerlink" title="If-Modified-Since"></a>If-Modified-Since</h3><ul>
<li>瀏覽器 cache 檔案，下一次 request 則會發送此 header，伺服器使用這個時間來判斷是否修改過檔案，沒有則回傳 <ul>
<li>If-Modified-Since (時間)</li>
<li>If-None-Match (Hash Code)</li>
</ul>
</li>
</ul>
<h3 id="Cookie"><a href="#Cookie" class="headerlink" title="Cookie"></a>Cookie</h3><ul>
<li>把瀏覽器的 cookie 傳給伺服器，”每一個” request 都會包含 cookie，所以盡可能減少 cookie 的使用，避免過度增加傳輸頻寬 </li>
</ul>
<h3 id="Referer"><a href="#Referer" class="headerlink" title="Referer"></a>Referer</h3><ul>
<li>瀏覽器前一個瀏覽頁面的網址，可用此 header 判斷 request 的來源，但是不可靠，因為可能遭到串改 (原應為 referrer 因為拼錯了只好將錯就錯)</li>
</ul>
<h3 id="Connection"><a href="#Connection" class="headerlink" title="Connection"></a>Connection</h3><ul>
<li>可以設定 Keep-Alive 保持連線時間，減少重新連接 TCP 連線次數，提高傳輸效率</li>
</ul>
<h3 id="Content-type"><a href="#Content-type" class="headerlink" title="Content-type"></a>Content-type</h3><ul>
<li>指傳送內容的格式，常見格式為 :  <ul>
<li>application/x-www-form-urlencoded</li>
<li>multipart/form-data;boundary=–xxx</li>
<li>text/plain</li>
</ul>
</li>
</ul>
<h3 id="Authorizationpn"><a href="#Authorizationpn" class="headerlink" title="Authorizationpn"></a>Authorizationpn</h3><ul>
<li>HTTP 連線的身分驗證</li>
</ul>
<h3 id="Upgrade"><a href="#Upgrade" class="headerlink" title="Upgrade"></a>Upgrade</h3><ul>
<li>升級到另外一個協定 ex: websocket</li>
</ul>
<h2 id="伺服器回應什麼"><a href="#伺服器回應什麼" class="headerlink" title="伺服器回應什麼 ?"></a>伺服器回應什麼 ?</h2><p><img src="Http_Request&Response.png"></p>
<ul>
<li><p>Header 一定放在內容前面，傳送除了內容以外的資訊</p>
<ul>
<li>內容格式 (圖檔、文字檔、HTML、JSON)</li>
<li>內容大小 (KB…)</li>
<li>內容多久後過期</li>
<li>內容從哪來等…</li>
</ul>
</li>
<li><p>內容不一定是 HTML，但 HTML 一定是內容之一 (內容還包括圖片、字型、CSS、Javascript、JSON…)</p>
</li>
</ul>
<h2 id="狀態碼"><a href="#狀態碼" class="headerlink" title="狀態碼"></a>狀態碼</h2><ul>
<li><p>1XX: 訊息類 (收到請求，請求者既徐執行操作)</p>
<ul>
<li>100 continue : server 期待收到更多訊息，以進行完整處理，多半是 Request 的 header 有待 Expect 要求回應 100</li>
<li>101 Switching Protocols : 用在 websocket 初始化，進行雙向資料傳輸</li>
</ul>
</li>
<li><p>2XX: 成功類 (操作被成功接受並處理)</p>
<ul>
<li>200 OK : 最常見的狀態碼，表示請求正常</li>
</ul>
</li>
<li><p>3XX: 重定向類 (需進一步操最才能完成)</p>
<ul>
<li>301 Moved Permanently : 永久移動到新的網址，同時需要有 Lfocation 的 header，SEO 會被更新</li>
<li>302 Found : 找到網址，同時需要有 Location 的 header，SEO 不會被更新</li>
<li>304 Not Modefied : 未修改，回應不包含 Content</li>
<li>307 Temporary Redirect : 暫時重新導向，與 302 類似，但 302 如果從 POST 進來會給從 GET 導向網址，307 則不會變更請求方法</li>
<li>308 Permanent Redirect : 永久重新導向，類似於 301，就好比 302 &amp; 307 之間的關係，不會變更請求方法</li>
</ul>
</li>
<li><p>4XX: 客戶端錯誤類 (請求語法錯誤或無法完成請求)</p>
<ul>
<li>401 Unauthorized : 未驗證拒絕存取</li>
<li>403 Forbidden : 伺服器接受請求，但被拒絕處理</li>
<li>404 Not Found : 伺服器找不到要求的網頁</li>
<li>413 Request Entity Too Large : 通常是 GET 傳送超過 URL 上限大小，伺服器無法處理</li>
</ul>
</li>
<li><p>5XX: 伺服器錯誤類 (後端問題)</p>
<ul>
<li>500 Internal Server Error : 伺服器發生錯誤，通常是後端的問題</li>
<li>503 Service Unavailable : 伺服器維護或者過載，伺服器當前無法處理請求</li>
</ul>
</li>
</ul>
<h2 id="常見的-Response-Header"><a href="#常見的-Response-Header" class="headerlink" title="常見的 Response Header"></a>常見的 Response Header</h2><h3 id="Etag"><a href="#Etag" class="headerlink" title="Etag"></a>Etag</h3><ul>
<li>產生檔案的 Hash Code 給瀏覽器，下一次瀏覽器可以傳送 If-None-Match 給伺服器判斷是否有修改過檔案，沒有則回傳 304 且不會包含 Content</li>
</ul>
<h3 id="Lash-Modified"><a href="#Lash-Modified" class="headerlink" title="Lash-Modified"></a>Lash-Modified</h3><ul>
<li>回傳檔案最後的修改時間，下一次瀏覽器可以傳送 If-Modified-Since 給伺服器判斷是否有修改過檔案，沒有則回傳 304 且不會包含 Content</li>
</ul>
<h3 id="Catch-Control"><a href="#Catch-Control" class="headerlink" title="Catch-Control"></a>Catch-Control</h3><ul>
<li>快取檔案的策略，max-afe 則表示該檔案有效的 cache 秒數，no-cache 則表示不使用 cache</li>
</ul>
<h3 id="HTTP-1-1-快取機制"><a href="#HTTP-1-1-快取機制" class="headerlink" title="HTTP 1.1 快取機制"></a>HTTP 1.1 快取機制</h3><ul>
<li><a href="https://blog.toright.com/posts/3414/%E5%88%9D%E6%8E%A2-http-1-1-cache-%E6%A9%9F%E5%88%B6.htmlype">參考資料</a></li>
</ul>
<h3 id="Content-type-1"><a href="#Content-type-1" class="headerlink" title="Content-type"></a>Content-type</h3><ul>
<li>回傳 Content 的 mime 類型，當 content-type 設為 text/html 和 text/plain 可同時設定 charset 文字編碼格式，若無 content-type 瀏覽器自動判斷</li>
</ul>
<h3 id="Content-Disposition"><a href="#Content-Disposition" class="headerlink" title="Content-Disposition"></a>Content-Disposition</h3><ul>
<li>瀏覽器可以打開下載視窗並且指定檔案名稱，Ex: Content-Disposition: attachment; filename=”download.zip”</li>
</ul>
<h3 id="Content-Length"><a href="#Content-Length" class="headerlink" title="Content-Length"></a>Content-Length</h3><ul>
<li>取得傳送內容的大小，讓瀏覽器可以判斷下載的進度</li>
</ul>
<h3 id="Date-1"><a href="#Date-1" class="headerlink" title="Date"></a>Date</h3><ul>
<li>傳送伺服器時間給瀏覽器，讓瀏覽器知道伺服器目前的時間，可以藉此計算兩者之間的時差</li>
</ul>
<h3 id="Location"><a href="#Location" class="headerlink" title="Location"></a>Location</h3><ul>
<li>當狀態碼為 301 或 302 時，瀏覽器會重新導向到 Location 這個位置</li>
</ul>
<h3 id="Set-Cookie"><a href="#Set-Cookie" class="headerlink" title="Set-Cookie"></a>Set-Cookie</h3><ul>
<li>設定瀏覽器 Cookie，當瀏覽器收到此 header 則會建立 cookie 在瀏覽器中</li>
</ul>
<h3 id="Content-Encoding"><a href="#Content-Encoding" class="headerlink" title="Content-Encoding"></a>Content-Encoding</h3><ul>
<li>回傳內容的壓縮格式，讓瀏覽器知道該如何解壓縮內容</li>
</ul>
<h3 id="Access-Control-Allow-Origin"><a href="#Access-Control-Allow-Origin" class="headerlink" title="Access-Control-Allow-Origin"></a>Access-Control-Allow-Origin</h3><ul>
<li>允許跨網域存取的 Domain，全部網域使用 * 字號表示，script 和 link 標籤不再此限</li>
</ul>
<h3 id="X-Frame-Options"><a href="#X-Frame-Options" class="headerlink" title="X-Frame-Options"></a>X-Frame-Options</h3><ul>
<li>是否能夠在 <frame> , <frame> 以及 <object> 載入，網站可以利用此 header 來確保本身內容不會遭受惡意嵌入到其他網站，避免 clickjacking 攻擊</li>
</ul>
<h3 id="Upgrade-1"><a href="#Upgrade-1" class="headerlink" title="Upgrade"></a>Upgrade</h3><ul>
<li>確認升級到新的協定，此時 connection 也會同時回應 Upgrade ex: Connection: Upgrade; Upgrade: websocket</li>
</ul>
]]></content>
      <categories>
        <category>network</category>
      </categories>
      <tags>
        <tag>network</tag>
        <tag>HTTP</tag>
      </tags>
  </entry>
  <entry>
    <title>VSCode Extensions</title>
    <url>/vscode/20191114/7433/</url>
    <content><![CDATA[<p>這篇文章記錄一下目前在 VSCode 中使用到的擴充插件</p>
<span id="more"></span>

<h2 id="Chinese-Traditional-Language-Pack-for-Visual-Studio-Code"><a href="#Chinese-Traditional-Language-Pack-for-Visual-Studio-Code" class="headerlink" title="Chinese (Traditional) Language Pack for Visual Studio Code"></a>Chinese (Traditional) Language Pack for Visual Studio Code</h2><p>VSCode 中提供了許多符合本地化語言的擴充插件，這個插件可以將整個 VSCode 環境改成繁體中文，只需要按照以下官方說明的使用方式即可更改，更改後只要重啟 VSCode 就能完成</p>
<h3 id="使用方式"><a href="#使用方式" class="headerlink" title="使用方式"></a>使用方式</h3><p>安裝好之後，在 <code>locale.json</code> 裡面設定 “locale”: “zh-tw” 即可載入中文 (繁體) 的語言套件。要修改 <code>locale.json</code>，可以按下 <code>Ctrl+Shift+P</code> 來呼叫 命令選擇區，輸入 “config” 以從其他可用命令裡面篩選出 <code>Configure Language</code> 命令。詳細步驟請參考文件 。</p>
<h2 id="favorites"><a href="#favorites" class="headerlink" title="favorites"></a>favorites</h2><p>這個插件提供開發者一個獨立的區域將指定檔案加到其中，就可以快速的找到指定檔案，只要對目標檔案案右鍵選取 <code>add to favorites (添加到收藏)</code></p>
<h2 id="indent-rainbow"><a href="#indent-rainbow" class="headerlink" title="indent-rainbow"></a>indent-rainbow</h2><p>這個插件會將程式碼前的空白透過顏色來區分，如此一來就可以快速的對齊飛相鄰的程式碼</p>
<h2 id="Live-Sass-Compiler"><a href="#Live-Sass-Compiler" class="headerlink" title="Live Sass Compiler"></a>Live Sass Compiler</h2><p>當我們使用 CSS 預處理器 <code>Sass</code> 時，這個插件可以即時將 <code>.scss</code> 與 <code>.sass</code> 編譯成瀏覽器可以讀取得 <code>.css</code> 檔案格式</p>
<h2 id="Live-Server"><a href="#Live-Server" class="headerlink" title="Live Server"></a>Live Server</h2><p>安裝完此插件後，會在 VSCode 下方出現 Go Live 的選項，點選後我們在編輯網頁時就會及時在瀏覽器上改變，使我們快速的看到效果更動，不需要再次重新整理整個網頁</p>
<h2 id="Path-Intellisense"><a href="#Path-Intellisense" class="headerlink" title="Path Intellisense"></a>Path Intellisense</h2><p>這個插件提供我們再輸入 “相對路徑” 時，出現對應的路徑選項，不需要將整個路徑從頭到尾完整輸入</p>
]]></content>
      <categories>
        <category>VSCode</category>
      </categories>
      <tags>
        <tag>VSCode</tag>
      </tags>
  </entry>
  <entry>
    <title>Vuex 中的 Getters、mapGetters 與 mapActions</title>
    <url>/vue/20191114/26866/</url>
    <content><![CDATA[<p>在先前有提到，<code>store</code> 中有一個類似原件中的 <code>computed</code> 的方法叫做 <code>getters</code>，當有多個原件要用到這個屬性時，可以將其放到 <code>store</code> 中供其他原件使用，同樣會在其依賴的值發生變化時重新計算</p>
<span id="more"></span>

<h2 id="getters-與-mapGetters"><a href="#getters-與-mapGetters" class="headerlink" title="getters 與 mapGetters"></a>getters 與 mapGetters</h2><p>延續上一章節，我們將原先在原件 <code>computed</code> 中的屬性移到 <code>store</code> 中，<code>getters</code> 中的方法同樣在一個個參數可以帶入 <code>state</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in component</span></span><br><span class="line">computed: &#123;</span><br><span class="line">  <span class="function"><span class="title">categories</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>.$store.state.categories;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function"><span class="title">products</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>.$store.state.products;</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;</span><br><span class="line">          </span><br><span class="line"><span class="comment">// in store</span></span><br><span class="line">getters: &#123;</span><br><span class="line">  <span class="function"><span class="title">categories</span>(<span class="params">state</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> state.categories;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function"><span class="title">products</span>(<span class="params">state</span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> state.products;</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>將 <code>computed</code> 中的方法移到 <code>store</code> 中後，透過 <code>import</code> 解構的方式從 <code>Vuex</code> 中取得 <code>mapGetters</code> 這個方法，再透過展開 <code>...</code> 的方式在 <code>computed</code> 中載入 <code>getters</code> 屬性</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in component</span></span><br><span class="line"><span class="keyword">import</span> &#123; mapGetters &#125; <span class="keyword">from</span> <span class="string">&#x27;vuex&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  computed: &#123;</span><br><span class="line">    ...mapGetters([<span class="string">&#x27;categories&#x27;</span>, <span class="string">&#x27;products&#x27;</span>]);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="actions-與-mapActions"><a href="#actions-與-mapActions" class="headerlink" title="actions 與 mapActions"></a>actions 與 mapActions</h2><p>同樣的，<code>actions</code> 也可以透過 <code>mapActions</code> 這個方法載入，但這裡要注意的是，<code>mapActions</code> 只能應用在 “沒有參數” 的方法上，如果原件中的方法有地入參數，就只能透過 <code>methods</code> 來呼叫</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in compontent</span></span><br><span class="line"><span class="keyword">import</span> &#123; mapGetters, mapActions &#125; <span class="keyword">from</span> <span class="string">&#x27;vuex&#x27;</span>;</span><br><span class="line">          </span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  methods: &#123;</span><br><span class="line">    <span class="function"><span class="title">addtoCart</span>(<span class="params">id, qty = <span class="number">1</span></span>)</span> &#123;</span><br><span class="line">      <span class="built_in">this</span>.$store.dispatch(<span class="string">&#x27;addtoCart&#x27;</span>, &#123; id, qty &#125;);</span><br><span class="line">    &#125;,</span><br><span class="line">    ...mapActions([<span class="string">&#x27;getProducts&#x27;</span>]);</span><br><span class="line">  &#125;,</span><br><span class="line">  computed: &#123;</span><br><span class="line">    ...mapGetters([<span class="string">&#x27;categories&#x27;</span>, <span class="string">&#x27;products&#x27;</span>]);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Vue</tag>
        <tag>Vuex</tag>
      </tags>
  </entry>
  <entry>
    <title>模組化資料運用</title>
    <url>/vue/20191114/12197/</url>
    <content><![CDATA[<p><code>Veux</code> 最大的好處在於可以將原先獨立於各原件中的行為統一放到 <code>store</code> 中管理，並提供其他原件取用，但當專案變大時，如果所有的行為都放到同一支檔案中管理，這隻檔案將會變得非常龐大，因此 <code>Vuex</code> 提供了模組化的方式來拆分其中的行為</p>
<span id="more"></span>

<p>假設專案中有別有產品與購物車兩大類，在這裡我們將產品類拆分出來，首先在 <code>store</code> 中新增一支 <code>product.js</code> ，並將所有與產品有關的行為都放到這之檔案中，同理在 <code>index.js</code> 中移除與產品相關的行為，如有有使用到 <code>AJAX axios</code> 也要在這裡載入</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in product.js</span></span><br><span class="line"><span class="keyword">import</span> axios <span class="keyword">from</span> <span class="string">&#x27;axios&#x27;</span>;</span><br><span class="line">            </span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  state: &#123;</span><br><span class="line">    products: [],</span><br><span class="line">    categories: [],</span><br><span class="line">  &#125;,</span><br><span class="line">  actions: &#123;</span><br><span class="line">    <span class="function"><span class="title">getProducts</span>(<span class="params"></span>)</span> &#123;&#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">  mutations: &#123;</span><br><span class="line">    <span class="function"><span class="title">PRODUCTS</span>(<span class="params">state, payload</span>)</span> &#123;</span><br><span class="line">      state.products = payload;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="function"><span class="title">CATEGORIES</span>(<span class="params">state, payload</span>)</span> &#123;</span><br><span class="line">      state.categories = payload;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  getters: &#123;</span><br><span class="line">    products: <span class="function"><span class="params">state</span> =&gt;</span> state.products,</span><br><span class="line">    categories: <span class="function"><span class="params">state</span> =&gt;</span> state.categories,</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="載入拆分出去的檔案"><a href="#載入拆分出去的檔案" class="headerlink" title="載入拆分出去的檔案"></a>載入拆分出去的檔案</h2><p>拆分出去後，在 <code>index.js</code> 中透過 <code>import</code> 的方式在入拆分出去的 <code>product.js</code>，並新增一個 <code>modules</code> 的物件來取得檔案</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in index.js</span></span><br><span class="line"><span class="keyword">import</span> productsModules <span class="keyword">from</span> <span class="string">&#x27;./products&#x27;</span>;</span><br><span class="line">          </span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">new</span> Vuex.Store(&#123;</span><br><span class="line">  state: &#123;&#125;,</span><br><span class="line">  actions: &#123;&#125;,</span><br><span class="line">  mutations: &#123;&#125;,</span><br><span class="line">  getters: &#123;&#125;,</span><br><span class="line">  modules: &#123; productsModules &#125;,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="區域與全域"><a href="#區域與全域" class="headerlink" title="區域與全域"></a>區域與全域</h2><p>在這裡有一個很重要的觀念要注意，當我們拆分檔案時，模組中每個屬性的預設作用域並不相同</p>
<ul>
<li><code>state</code>: 屬於模組區域變數</li>
<li><code>actions</code>, <code>mutations</code>, <code>getters</code>: 屬於模組全域變數</li>
</ul>
<p>所以當不同模組在 <code>state</code> 以外存在相同名稱時，就可能會出現錯誤</p>
<h2 id="取得模組中變數"><a href="#取得模組中變數" class="headerlink" title="取得模組中變數"></a>取得模組中變數</h2><p>如果要取得拆分出去的模組中的 <code>state</code> 變數就要在前方指定模組名稱才能取得</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in component</span></span><br><span class="line"></span><br><span class="line">methods: &#123;</span><br><span class="line">  <span class="function"><span class="title">addtoCart</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>.$store.state.productModules.products);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="將-“全域變數”-轉換成-“區域變數”"><a href="#將-“全域變數”-轉換成-“區域變數”" class="headerlink" title="將 “全域變數” 轉換成 “區域變數”"></a>將 “全域變數” 轉換成 “區域變數”</h2><p>先前提到，<code>state</code> 以外的屬性預設都是全域變數，所以如果在不同模組出現相同的名稱就會出現錯誤，在這裡可以新增一個屬性來將 “全域變數” 轉換成模組內的”區域變數”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  namespaced: <span class="literal">true</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="指定模組行為"><a href="#指定模組行為" class="headerlink" title="指定模組行為"></a>指定模組行為</h2><p>但如果將模組內的行為轉換成區域變數，就會出現無法直接取得的情況，所以在元件取得模組中的行為時，要在前方指定模組名稱</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in component</span></span><br><span class="line"><span class="keyword">import</span> &#123; mapGetters, mapActions &#125; <span class="keyword">from</span> <span class="string">&#x27;vuex&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="function"><span class="title">data</span>(<span class="params"></span>)</span> &#123;<span class="keyword">return</span>&#125;,</span><br><span class="line">  computed: &#123;</span><br><span class="line">    ...mapGetters(<span class="string">&#x27;productsModules&#x27;</span>, [<span class="string">&#x27;categories&#x27;</span>, <span class="string">&#x27;products&#x27;</span>]),</span><br><span class="line">  &#125;,</span><br><span class="line">  methods: &#123;</span><br><span class="line">    ...mapActions(<span class="string">&#x27;productsModules&#x27;</span>, [<span class="string">&#x27;getProducts&#x27;</span>]),</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="模組間取得全域變數"><a href="#模組間取得全域變數" class="headerlink" title="模組間取得全域變數"></a>模組間取得全域變數</h2><p>將模組中 <code>state</code> 以外的屬性區域化後，如果有取得其他模組中的屬性，就要在後方標註全域 <code>&#123; root: true &#125;</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in products.js</span></span><br><span class="line"></span><br><span class="line">actions: &#123;</span><br><span class="line">  <span class="function"><span class="title">getProducts</span>(<span class="params">context</span>)</span> &#123;</span><br><span class="line">    context.commit(<span class="string">&#x27;LOADING&#x27;</span>, <span class="literal">true</span>, &#123; <span class="attr">root</span>: <span class="literal">true</span> &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="元件中透過-dispatch-呼叫模組方法"><a href="#元件中透過-dispatch-呼叫模組方法" class="headerlink" title="元件中透過 dispatch 呼叫模組方法"></a>元件中透過 dispatch 呼叫模組方法</h2><p>如果是在元件中呼叫拆分出去的模組中的方法，在指定模組名稱時，直接加在前方即可，並透過斜線 <code>/</code> 隔開</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in component</span></span><br><span class="line"></span><br><span class="line">methods: &#123;</span><br><span class="line">  <span class="function"><span class="title">removeCart</span>(<span class="params">id</span>)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.$store.dispatch(<span class="string">&#x27;cartModules/removeCart&#x27;</span>, id);</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="取得其他區域化元件中的-actions-行為"><a href="#取得其他區域化元件中的-actions-行為" class="headerlink" title="取得其他區域化元件中的 actions 行為"></a>取得其他區域化元件中的 actions 行為</h2><p>如果要取得的是其他區域化元件 <code>actions</code> 中的行為，不僅要在前方指定模組，還要在後方加上 <code>&#123; root: true &#125;</code> 代表從根節點開始查找</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in one.js</span></span><br><span class="line">actions: &#123;</span><br><span class="line">  <span class="function"><span class="title">test</span>(<span class="params">context</span>)</span> &#123;</span><br><span class="line">    context.dispatch(<span class="string">&#x27;two/event&#x27;</span>, parameter, &#123; <span class="attr">root</span>: <span class="literal">true</span> )</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Vue</tag>
        <tag>Vuex</tag>
      </tags>
  </entry>
  <entry>
    <title>全域環境與全域物件</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210321/51637/</url>
    <content><![CDATA[<p>在 JS 中，無論你寫的程式碼在何時、何處執行，都會被包在一個 JS 為你準備的執行環境之中，稱之為’全域環境’，全域顧名思義就是在任何區域都可以被取得，在其內所建立的物件也稱之為’全域物件’。</p>
<span id="more"></span>

<h2 id="何未全域"><a href="#何未全域" class="headerlink" title="何未全域"></a>何未全域</h2><p>在開發前端網頁時，我們基本會有一個 html 檔案，並載入如 app.js 的 JS 程式碼如下:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">head</span>&gt;</span><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">script</span> <span class="attr">scr</span>=<span class="string">&quot;app.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>在瀏覽器內打開這個檔案會發現，即使在 app.js 中沒有寫任何的內容也會在全域環境中看到以下兩項是 JS 幫你建立好的</p>
<ul>
<li><p>變數 this<br>  this 是 JS 執行環境自動幫你產生的全域變數，開啟瀏覽器 console 即可查看其內容<br>  <img src="this.PNG"></p>
</li>
<li><p>Global Object</p>
<ul>
<li>window<br>打開瀏覽器，在 console 內輸入 this 並點選打開內層，就會發現裡面有一個 window 的全域物件，其所代表的就是瀏覽器這個畫面本身，所以在後端 node.js 中輸入 this 就不會是 window 這個全域物件，但仍會有一個 JS 幫你產生的全域物件，因為 JS 執行環境已被產生</li>
</ul>
</li>
</ul>
<h2 id="建立變數"><a href="#建立變數" class="headerlink" title="建立變數"></a>建立變數</h2><p>接下來我們在 app.js 內寫一點程式碼如下:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// app.js</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> a = <span class="string">&#x27;Hellow World&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">b</span> = </span>&#123;&#125;;</span><br></pre></td></tr></table></figure>

<p>接著打開瀏覽器 console 可以透過輸入以下兩種方式找到我們宣告的變數</p>
<ul>
<li>a</li>
<li>window.a</li>
</ul>
<h2 id="結論"><a href="#結論" class="headerlink" title="結論"></a>結論</h2><p>只要不是在函式內宣告的變數就是全域變數</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>名稱/值配對與物件</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210321/56938/</url>
    <content><![CDATA[<p>JS 物件基本觀念</p>
<span id="more"></span>

<p>在 JS 中，物件是一個非常重要的觀念，一個名稱在執行環境中同時只會有一個值與之配對，但一個值可以是一至多組名稱與值的配對，如下基本範例:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">Address = <span class="string">&#x27;100 Main St.&#x27;</span></span><br><span class="line"></span><br><span class="line">Address = &#123;</span><br><span class="line">    Street: <span class="string">&#x27;Main&#x27;</span>,</span><br><span class="line">    <span class="built_in">Number</span>: <span class="number">100</span>,</span><br><span class="line">    Apartment: &#123;</span><br><span class="line">        Floor: <span class="number">3</span>,</span><br><span class="line">        <span class="built_in">Number</span>: <span class="number">301</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>函數、環境與變數環境</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210325/37130/</url>
    <content><![CDATA[<p>這章節針對變數的環境最一個簡單的介紹</p>
<span id="more"></span>

<h3 id="變數環境"><a href="#變數環境" class="headerlink" title="變數環境"></a>變數環境</h3><p>變數環境指的是變數創造時所在的環境，每個執行環境中的變數彼此式獨立的，並不會彼此影響，一段程式碼如下</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">b</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> myVar;</span><br><span class="line">  <span class="built_in">console</span>.log(myVar);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> myVar = <span class="number">2</span>;</span><br><span class="line">  <span class="built_in">console</span>.log(myVar);</span><br><span class="line">  b();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> myVar = <span class="number">1</span>;</span><br><span class="line"><span class="built_in">console</span>.log(myVar);</span><br><span class="line">a();</span><br><span class="line"><span class="built_in">console</span>.log(myVar);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 1</span></span><br><span class="line"><span class="comment">// 2</span></span><br><span class="line"><span class="comment">// undefined</span></span><br><span class="line"><span class="comment">// 1</span></span><br></pre></td></tr></table></figure>

<p>首先全域環境中的 myVar 變數被宣告，所以先取的 1，接著呼叫 a 函數加到執行堆上層並宣告該執行環境中的 myVar 變數取得 2 後同樣執行 b 函數加到執行堆上層並宣告該執行環境中的 myVar ，因為沒有賦值所以為 undefined ，最後 b 與 a 執行結束後離開執行堆，取得最後一個全域環境中的 1</p>
<p><img src="%E8%AE%8A%E6%95%B8%E7%92%B0%E5%A2%83.PNG"></p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>語法解析器、執行環境與詞彙環境</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210321/38004/</url>
    <content><![CDATA[<p>觀念小叮嚀</p>
<span id="more"></span>

<h3 id="語法解析器"><a href="#語法解析器" class="headerlink" title="語法解析器"></a>語法解析器</h3><p>當你寫完一段 JS 程式碼之後，並非直接交由電腦讀取，因為 JS 程式碼無法直接由電腦執行，所以中間會由程式設計師進行編譯，將你所寫的 JS 編譯成電腦可以讀取的格式，稱之為’語法解析器’</p>
<p><img src="%E8%AA%9E%E6%B3%95%E8%A7%A3%E6%9E%90%E5%99%A8.PNG"></p>
<h3 id="詞彙環境"><a href="#詞彙環境" class="headerlink" title="詞彙環境"></a>詞彙環境</h3><p>前面提到所寫的 JS 程式碼會被編譯成電腦看得懂的指令，而詞彙環境指的是這段程式碼被寫在哪與其環境為何，另外並非所有語言都在意其詞彙環境</p>
<h3 id="執行環境"><a href="#執行環境" class="headerlink" title="執行環境"></a>執行環境</h3><p>在一段程式碼中會有多個詞彙環境，而管理它們執行的就是執行環境。JS 是會被編譯的程式語言，所以在執行環境中所執行的被不是只有你所寫的 JS 程式碼而已，還包括程式設計師所寫的語法編譯器</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>JavaScript and undefined</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210324/27183/</url>
    <content><![CDATA[<p>這章節會進一步套討變數的的預設值 undefined 與 not undefined 的差異</p>
<span id="more"></span>

<p>一段基本的變數宣告如下</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&#x27;a&#x27;</span>, a);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&#x27;b&#x27;</span>, b);</span><br><span class="line"></span><br><span class="line"><span class="comment">// a undefined</span></span><br><span class="line"><span class="comment">// b not defined</span></span><br></pre></td></tr></table></figure>
<p>從以上結果來套討 undefined 與 not defined 的差異</p>
<h3 id="undefined"><a href="#undefined" class="headerlink" title="undefined"></a>undefined</h3><p>在 JavaScript 中值為 undefined 的情況並非沒有值，而是在創造階段已被宣告且在記憶體中佔有空間，只是還未被主動賦予一個值，所以 JavaScript 預設都會賦予 undefined 這個特殊關鍵字</p>
<h3 id="not-defined"><a href="#not-defined" class="headerlink" title="not defined"></a>not defined</h3><p>而 not defined 就不同了，由上面的結果可以知道 b 完全沒有宣告，所以在執行階段找殂道者個值所以出現 not defined 的提示</p>
<h3 id="主動賦予-undefined"><a href="#主動賦予-undefined" class="headerlink" title="主動賦予 undefined"></a>主動賦予 undefined</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="literal">undefined</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(a === <span class="literal">undefined</span>);   <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>由上面的結果可以得知如果主動賦予變數 undefined 在 JavaScript 中是成立的，但這麼做是危險且不建議的，原因是 undefined 這個值為 JavaScript 在變數已宣告但為賦值時的預設值，所以如果這麼做就會難以區分這個變數為 undefined 的原因</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>函數呼叫與執行堆</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210324/82/</url>
    <content><![CDATA[<p>函數呼叫 (Functoin Invocation) 與執行堆 (Exection Stack) 介紹</p>
<span id="more"></span>

<p>以下為一段最基礎的函示呼叫程式碼</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">b</span>(<span class="params"></span>) </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    b();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">a();</span><br></pre></td></tr></table></figure>

<p>首先，在創造階段的時，函式 a 和 b 會被創造並記錄在記憶體中，接著到了執行階段程式程式碼會由上而下依序執行，a 被呼叫，再藉由 a 來呼叫 b</p>
<h3 id="執行堆-Execution-Stack"><a href="#執行堆-Execution-Stack" class="headerlink" title="執行堆 (Execution Stack)"></a>執行堆 (Execution Stack)</h3><p>當函式被呼叫時，會創造一個獨立的執行環境放到執行堆的最上層，所以當 a 被呼叫時，就會基於全域環境放在其上層，如下圖所示</p>
<p><img src="%E5%9F%B7%E8%A1%8C%E5%A0%86a.PNG"></p>
<p>而當 a 被執行後呼叫內層的 b 又會再創造一個新的執行環境</p>
<p><img src="%E5%9F%B7%E8%A1%8C%E5%A0%86b.PNG"></p>
<p>直到 b 執行完畢離開執行堆後，繼續執行 a 尚未執行的任務，最後直到 a 也執行完畢離開執行堆，繼續由上而下依序執行全域環境的程式碼</p>
<p><img src="%E5%9F%B7%E8%A1%8C%E5%A0%86.PNG"></p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>單執行緒與同步執行</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210324/46242/</url>
    <content><![CDATA[<p>基礎介紹單執行緒 (Single threaded) 和同步執行 (Synchronous exection)</p>
<span id="more"></span>

<h3 id="單執行緒-Single-threaded"><a href="#單執行緒-Single-threaded" class="headerlink" title="單執行緒 (Single threaded)"></a>單執行緒 (Single threaded)</h3><p>這個名詞指的是 JavaScript 一次只執行一件事，但並非瀏覽器一次只做一件事，而是以我們的角度看程式的執行</p>
<h3 id="同步執行-Synchronous-exection"><a href="#同步執行-Synchronous-exection" class="headerlink" title="同步執行 (Synchronous exection)"></a>同步執行 (Synchronous exection)</h3><p>和單執行緒有點類似，但是以程式的說法指一次只執行一件事，且由上而下依序執行</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>程式執行</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210324/48119/</url>
    <content><![CDATA[<p>延續前面章節所提到的，JavaScript 分成兩個階段，第一階段為創造階段 (Creation Context)，這個章節將介紹第二階段，執行階段 (Execution Context)</p>
<span id="more"></span>

<h3 id="執行階段"><a href="#執行階段" class="headerlink" title="執行階段"></a>執行階段</h3><p>在這個階段中，JavaScript 在創造變數與函式後，會 ‘逐行’ 執行你所寫的程式碼，執行範例結果如下:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">b</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;Called b!&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">b();</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(a);</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> a = <span class="string">&#x27;Hellow World!&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(a);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Called b!</span></span><br><span class="line"><span class="comment">// undefined</span></span><br><span class="line"><span class="comment">// Hellow World!</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>第一次呼叫的 a 在宣告前呼叫，所以因為創造階段的 hoisting 而賦予一個預設值 undefined，第二次呼叫的 a 就如大家預期在執行階段被主動賦值為 ‘Hellow World!’</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>創造與提升 (Creation and Hoisting)</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210323/16932/</url>
    <content><![CDATA[<p>這個章節將延續前面的執行環境進一步探討 JS 在創造執行環境時做了什麼</p>
<span id="more"></span>

<p>一段簡單的變數與函式宣告如下:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="string">&#x27;Hellow World&#x27;</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">b</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;Called b!&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">b();</span><br><span class="line"><span class="built_in">console</span>.log(a);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Called b!</span></span><br><span class="line"><span class="comment">// Hellow World</span></span><br></pre></td></tr></table></figure>

<p>結果可想而知會依序出現，但如果將呼叫的順序對調，會出現神奇的結果如下:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">b();</span><br><span class="line"><span class="built_in">console</span>.log(a);</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> a = <span class="string">&#x27;Hellow World&#x27;</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">b</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;Called b!&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Called b!</span></span><br><span class="line"><span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>

<p>原因是 JS 執行環境被分成以下兩階段創造</p>
<h3 id="Creation-Phase"><a href="#Creation-Phase" class="headerlink" title="Creation Phase"></a>Creation Phase</h3><p>在這個階段以下的東西會被創造</p>
<ul>
<li><p>Global Object</p>
</li>
<li><p>this</p>
</li>
<li><p>Outer Environment</p>
</li>
<li><p>Hoisting : Setup memory space for variables and functions<br>  在這個階段變數與函式會被定義且已經在記憶體中，函式的程式碼會被執行，但變數只會在記憶體中宣告但並不知道 ‘=’ 右側所賦予的質為何，所以預設賦予他一個 ‘undefined’ 的值，而不是未宣告的 ‘not defined’</p>
</li>
</ul>
<h3 id="依序由上而下執行"><a href="#依序由上而下執行" class="headerlink" title="依序由上而下執行"></a>依序由上而下執行</h3><p>而在這個階段變數與函式都已經存在於記憶體中，接著 JS 會由上而下依序執行，直到出現 = 等號賦值才會出現我們期待的結果</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>Scope、ES6 &amp; let</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210403/41213/</url>
    <content><![CDATA[<p>前面章節所提到變數環境、詞彙環境定義了 ‘範圍’，’範圍’是變數可以被取用的區域</p>
<span id="more"></span>

<p>下一版本的 JavaScript 稱作 ‘ECMAScript 6’ 、 ‘ECMAScript 2015’ 或 ES6，這個版本提供了新的變數宣告方式 ‘let’</p>
<h3 id="let"><a href="#let" class="headerlink" title="let"></a>let</h3><p>let 並非完全取代 var，var 還是存在，但 let 讓 JavaScript 引擎使用一種叫做區塊範圍 (block scoping) 的東西，所謂區塊的定義包括 if &amp; for 的 ‘大括號 {}’，所以如在執行 for loop 時每次變數在記憶體都是指向不同的位置</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (a &gt; b) &#123;</span><br><span class="line">    <span class="keyword">let</span> c = <span class="literal">true</span>;</span><br><span class="line">&#125; </span><br><span class="line"><span class="keyword">for</span> () &#123;</span><br><span class="line">    <span class="keyword">let</span> d = <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>範圍鏈 (Scope Chain)</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210403/9017/</url>
    <content><![CDATA[<p>延續上一章節的執行堆 (Excution Stack)，進一步介紹執行堆中的範圍鏈 (Scope Chain)，’範圍’代表我能取到變數的地方，而’鏈’則是外部環境參照的連結</p>
<span id="more"></span>

<p>在 JavaScript 中每個執行環境都有一個參照的外部環境，如果在函數執行時沒有找到該變數，JavaScript 就會往外向外部環境尋找，和這個函數的程式碼在哪裡被寫出來無關，只與執行環境被創造和你呼叫的時間點有關</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">b</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(myVar);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> myVar = <span class="number">2</span></span><br><span class="line">    b();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> myVar = <span class="number">1</span>;</span><br><span class="line">a();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 1</span></span><br></pre></td></tr></table></figure>

<p>如下圖所示，這整個執行堆就稱為範圍鏈 (Scope Chain)，’範圍’代表我能取到變數的地方，而’鏈’則是外部環境參照的連結，外部環境不一定是正下方的環境，正下方的環境只代表程式碼的物理位置，稱之為 ‘詞彙環境’，換個說法，外部參照會找到被函數創造的執行環境</p>
<p><img src="%E5%A4%96%E9%83%A8%E7%92%B0%E5%A2%83.PNG"></p>
<p>黨我們將函數 b 換個環境如下</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">b</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(myVar);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> myVar = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">    b();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> myVar = <span class="number">1</span>;</span><br><span class="line">a();   <span class="comment">// 2</span></span><br><span class="line">b();   <span class="comment">// b is not defined</span></span><br></pre></td></tr></table></figure>

<p>b 會出現 not defined，原因是當程式碼執行時，a 在創造階段被創造後，並不會執行內部指令，而是會直接跳到該函數最後一行，所以 b 在全域環境中並不存在，直到 a 被呼叫後，才會在該執行環境創造 b 函數</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>關於非同步回呼</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210403/45306/</url>
    <content><![CDATA[<p>在章節的最前面有提到 JavaScript 會同步執行程式碼，而非同步指的就是在同一個時間不只執行一段程式碼</p>
<span id="more"></span>

<p>在瀏覽器中，JavaScript 並非唯一執行的引擎，還包括渲染引擎 (Rendering Engine) 和 HTTP Request ，彼此是非同步執行，不會因為一個卡住而使其他引擎停止運作，其中只有 JavaScript 使同步執行的</p>
<p><img src="%E5%BC%95%E6%93%8E.PNG"></p>
<p>JavaScript 的非同步其實是瀏覽器將事件放到事件佇列 (Event Queue) 中，原本的程式碼仍然一行行執行，當每個執行堆執行完清空後，才會繼續執行下一個事件，並創造其執行環境</p>
<p><img src="event-queue.PNG"></p>
<p>一段程式碼如下，首先執行一段 3 秒的 callback function，並宣告一個點擊事件，最後印出完成，如果在 3 秒內點擊會依照下方順序印出，原因是非同步只在於 JavaScript 引擎外，所以非同步只是將事件加入事件佇列，等到執行環境清空後才會依序執行事件佇列中的事件 </p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// long runuing function</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">waitThreeSeconds</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> ms = <span class="number">3000</span> = <span class="keyword">new</span> <span class="built_in">Date</span>().getTime();</span><br><span class="line">    <span class="keyword">while</span> (<span class="keyword">new</span> <span class="built_in">Date</span>() &lt; ms)&#123;&#125;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;finished function&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">clickHandler</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;click event !&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// listen for the click event</span></span><br><span class="line"><span class="built_in">document</span>.addEventListener(<span class="string">&#x27;click&#x27;</span>, clickHandler);</span><br><span class="line"></span><br><span class="line">waitThreeSeconds();</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&#x27;finished execution&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// finished function</span></span><br><span class="line"><span class="comment">// finished execution</span></span><br><span class="line"><span class="comment">// click event</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>型別與純值</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210413/11502/</url>
    <content><![CDATA[<p>JavaScript 型別基礎</p>
<span id="more"></span>

<h3 id="型別"><a href="#型別" class="headerlink" title="型別"></a>型別</h3><p>JavaScript 是一個動態型別的語言，也就是在賦值時才會知道型別</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// c#</span></span><br><span class="line">bool isNew = <span class="string">&#x27;hello&#x27;</span>;   <span class="comment">// error</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// js</span></span><br><span class="line"><span class="keyword">var</span> isNew = <span class="literal">true</span>;   <span class="comment">// boolean and no errors</span></span><br><span class="line">isNew = <span class="string">&#x27;yup&#x27;</span>;</span><br><span class="line">isNew = <span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<h3 id="純值-基本型別"><a href="#純值-基本型別" class="headerlink" title="純值 (基本型別)"></a>純值 (基本型別)</h3><p>純值是一種資料的型別，表示一個值，換句話說不是物件，因為物件是名稱/值的配對，JS 有以下六種值</p>
<h4 id="undefined"><a href="#undefined" class="headerlink" title="undefined"></a>undefined</h4><p>JS 給所有變數的初始值，直到你主動賦值，這表還未設定值，所以不應該使用它來設定值</p>
<h4 id="null"><a href="#null" class="headerlink" title="null"></a>null</h4><p>這也表示沒有值，但與 undefined 的差異在於可使用它來定義變數沒有值</p>
<h4 id="boolean"><a href="#boolean" class="headerlink" title="boolean"></a>boolean</h4><p>true 或 false 表示</p>
<h4 id="number"><a href="#number" class="headerlink" title="number"></a>number</h4><p>表示一個浮點數 float，所以永遠有小數點跟在後面，和其他程式語言分成整數和福點數不同，JS 只有一種</p>
<h4 id="string"><a href="#string" class="headerlink" title="string"></a>string</h4><p>可使用單引號 ‘’ 或雙引號 “” 來括住任何字符</p>
<h4 id="symbol"><a href="#symbol" class="headerlink" title="symbol"></a>symbol</h4><p>symbol 代表一個唯一值，可透過 symbol() 函數來生成</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> s1 = symbol();</span><br><span class="line"></span><br><span class="line"><span class="keyword">typeof</span> s1;   <span class="comment">// symbol</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> s2 = sybol();</span><br><span class="line"></span><br><span class="line">s1 === s2;   <span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<p>因為 symbol 為一個函數不是 constructor，所以使用關鍵字 new 會出錯</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// TypeError: Symbol is not a constructor</span></span><br><span class="line"><span class="keyword">let</span> s1 = <span class="keyword">new</span> symbol();</span><br></pre></td></tr></table></figure>

<p>symbol 函數可以給定一個字串參數來命名，藉此區別不同的 symbol 參數，但因為唯一特性，即使給定同一參數也是不同的值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> s1 = symbol(<span class="string">&#x27;foo&#x27;</span>);</span><br><span class="line"><span class="keyword">let</span> s2 = symbol(<span class="string">&#x27;bar&#x27;</span>);</span><br><span class="line"></span><br><span class="line">s1.toString();   <span class="comment">// &#x27;Symbol(foo)&#x27;</span></span><br><span class="line">s2.toString();   <span class="comment">// &#x27;Symbol(bar)&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> s3 = symbol(<span class="string">&#x27;foo&#x27;</span>);</span><br><span class="line"></span><br><span class="line">s1 === s2;   <span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<p>由於 symbol 的唯一特性，所以 symbol 被拿來當物件屬性名稱時可以確保不會出現相同的名稱，也確保不會被覆蓋，物件的賦值有以下 3 種方式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1</span></span><br><span class="line"><span class="keyword">let</span> a = &#123;&#125;;</span><br><span class="line">a[mySymbol] = <span class="string">&#x27;Hello&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2</span></span><br><span class="line"><span class="keyword">let</span> a = &#123;</span><br><span class="line">    [mySymbol]: <span class="string">&#x27;Hello&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3</span></span><br><span class="line"><span class="keyword">let</span> a = &#123;&#125;;</span><br><span class="line"><span class="built_in">Object</span>.defineProperty(a, mySymbol, &#123;<span class="attr">value</span>: <span class="string">&#x27;Hello&#x27;</span>&#125;);</span><br><span class="line"></span><br><span class="line">a[mySymbol] = <span class="string">&#x27;Hello&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>這裡要注意的是，使用 symbol 作為物件的屬性時，只能透過 <code>computed property []</code> 語法，不能使用 <code>.</code> 運算子</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;&#125;;</span><br><span class="line"></span><br><span class="line">a.mySymbol = <span class="string">&#x27;Hello&#x27;</span>;</span><br><span class="line"></span><br><span class="line">a[mySymbol];   <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>

<h5 id="Global-symbol"><a href="#Global-symbol" class="headerlink" title="Global symbol"></a>Global symbol</h5><p>如果希望共享同一個 <code>symbol</code> 可以透過 <code>Symbol.for()</code> 與 <code>Symbol.keyFor()</code> 來存取值</p>
<ul>
<li><p>Symbol.for(key)<br>取得名稱為 <code>key</code> 的值，如果該值不存在，則會建立一個新的值到 global symbol registry 再返回，另外 <code>key</code> 名稱也會被當成 <code>symbol</code> 名稱</p>
  <figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Symbol</span>.for(<span class="string">&#x27;foo&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">Symbol</span>.for(<span class="string">&#x27;foo&#x27;</span>) === <span class="built_in">Symbol</span>.for(<span class="string">&#x27;foo&#x27;</span>);   <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">Symbol</span>(<span class="string">&#x27;foo&#x27;</span>) === <span class="built_in">Symbol</span>(<span class="string">&#x27;foo&#x27;</span>);   <span class="comment">// false</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> a = <span class="built_in">Symbol</span>.for(<span class="string">&#x27;foo&#x27;</span>);</span><br><span class="line"></span><br><span class="line">a.toString();   <span class="comment">// &#x27;Symbol(foo)&#x27;</span></span><br></pre></td></tr></table></figure></li>
<li><p>Symbol.keyFor()<br>用來取得 <code>global symbol key</code> 的名稱，如果沒有該名稱則返回 <code>undefined</code></p>
  <figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = <span class="built_in">Symbol</span>.for(<span class="string">&#x27;foo&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">Symbol</span>.keyFor(a);   <span class="comment">// foo</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> b = <span class="built_in">Symbol</span>();</span><br><span class="line"></span><br><span class="line"><span class="built_in">Symbol</span>.keyFor(b);   <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="Object-getOwnPropertySymbols-Symbol-遍歷"><a href="#Object-getOwnPropertySymbols-Symbol-遍歷" class="headerlink" title="Object.getOwnPropertySymbols() : Symbol 遍歷"></a>Object.getOwnPropertySymbols() : Symbol 遍歷</h5><p>物件的 symbol 屬性名稱無法被遍歷，如 <code>for....in</code>、<code>for...of</code>、<code>Object.keys()</code>、<code>Object.getOwnPropertyNames()</code> 和 <code>JSON.stringify()</code> 都不會返回 Symbol 屬性名稱</p>
<p>因此如果要取得 Symbol 屬性名稱可以使用 <code>Object.getOwnPropertySymbols()</code> 方法，此方法會返回一個陣列</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> obj = &#123;&#125;;</span><br><span class="line"></span><br><span class="line">obj[<span class="built_in">Symbol</span>(<span class="string">&#x27;a&#x27;</span>)] = <span class="string">&#x27;a&#x27;</span>;</span><br><span class="line">obj[<span class="built_in">Symbol</span>.for(<span class="string">&#x27;b&#x27;</span>)] = <span class="string">&#x27;b&#x27;</span>;</span><br><span class="line">obj[<span class="string">&#x27;c&#x27;</span>] = <span class="string">&#x27;c&#x27;</span>;</span><br><span class="line">obj.d = <span class="string">&#x27;d&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i <span class="keyword">in</span> obj) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(i);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// &quot;c&quot;</span></span><br><span class="line"><span class="comment">// &quot;d&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> objectSymbols = <span class="built_in">Object</span>.getOwnPropertySymbols(obj);</span><br><span class="line"></span><br><span class="line">objectSymbols;   <span class="comment">// [Symbol(a), Symbol(b)]</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>[下班後讀書會]EP3 演算法</title>
    <url>/uncategorized/20210427/60460/</url>
    <content><![CDATA[<p>下班後讀書會筆記</p>
<span id="more"></span>

<h3 id="原始資料"><a href="#原始資料" class="headerlink" title="原始資料"></a>原始資料</h3><p><img src="%E5%8E%9F%E5%A7%8B%E8%B3%87%E6%96%99.PNG"></p>
<p>即為最一開始未經整理過的資料，而原始資料可透過以下兩個方法改變成為資料結構</p>
<h4 id="改變觀點"><a href="#改變觀點" class="headerlink" title="改變觀點"></a>改變觀點</h4><p>如果將上述的原始資料中的每筆資料看成每棵樹的根結點，在不改變資料順序的情況下即形成一個最基本的二元樹</p>
<p><img src="%E4%BA%8C%E5%85%83%E6%A8%B9.PNG"></p>
<h4 id="改變實際資料排序"><a href="#改變實際資料排序" class="headerlink" title="改變實際資料排序"></a>改變實際資料排序</h4><p>延續上面的方式進一步改變資料的順序，另每一層根節點的值都大於下方，形成一顆堆積二元樹</p>
<p><img src="%E5%A0%86%E7%A9%8D%E4%BA%8C%E5%85%83%E6%A8%B9.PNG"></p>
<h3 id="資料結構"><a href="#資料結構" class="headerlink" title="資料結構"></a>資料結構</h3><p>資料結構 = 資料 + 資料與資料的關係</p>
<p>常見的資料結構如下</p>
<ul>
<li>排列 Queue<br>先進的資料優先取出，並在每次資料的新增或刪除維持住這個原則</li>
</ul>
<p><img src="%E9%99%A3%E5%88%97.PNG"></p>
<ul>
<li>堆疊 Stack<br>後進的資料後出</li>
</ul>
<h3 id="演算法"><a href="#演算法" class="headerlink" title="演算法"></a>演算法</h3><p>演算法即為使用資料的策略，在這裡說的是’資料’而非’資料結構’，原因是根本的目的就是使用資料本身，但未經過處理的原始資料本身沒有任何規則，因此所能使用的演算法也很基本，所以一般都會進一步整理成資料結構，進而 ‘創造更多使用資料的策略’</p>
<p><img src="%E9%97%9C%E4%BF%82%E5%9C%96.PNG"></p>
<h4 id="常見的演算法"><a href="#常見的演算法" class="headerlink" title="常見的演算法"></a>常見的演算法</h4><p><img src="%E5%B8%B8%E8%A6%8B%E7%9A%84%E6%BC%94%E7%AE%97%E6%B3%95.PNG"></p>
<h4 id="泡沫排序法-Bubble-Sort"><a href="#泡沫排序法-Bubble-Sort" class="headerlink" title="泡沫排序法 Bubble Sort"></a>泡沫排序法 Bubble Sort</h4>]]></content>
  </entry>
  <entry>
    <title>Node.js 後端開發起手式 (一)</title>
    <url>/uncategorized/20210816/22460/</url>
    <content><![CDATA[<p>[偷米騎巴哥] Node.js 後端開發起手式紀錄，Node.js 基本介紹</p>
<span id="more"></span>

<h3 id="目前已知的-JS-運用"><a href="#目前已知的-JS-運用" class="headerlink" title="目前已知的 JS 運用"></a>目前已知的 JS 運用</h3><ul>
<li>瀏覽器 ( Chrome、Firefox、Safari、IE、etc. )</li>
<li>前端工具 ( Webpack、Gulp、Grunt、etc. ) </li>
<li>後端開發 ( Express、Koa、etc. )</li>
<li>資料庫 ( MongoDB )</li>
<li>硬體版 ( Raspberry Pi、Webduino、etc. )</li>
<li>Google 雲端服務 ( Google Apps Script )</li>
</ul>
<h3 id="安裝-Node-js"><a href="#安裝-Node-js" class="headerlink" title="安裝 Node.js"></a>安裝 Node.js</h3><p>安裝具備以下兩種方式</p>
<h4 id="透過-NVM-安裝"><a href="#透過-NVM-安裝" class="headerlink" title="透過 NVM 安裝"></a>透過 NVM 安裝</h4><ul>
<li>優點<ul>
<li>快速切換不同版本</li>
<li>方便測試</li>
</ul>
</li>
<li>缺點<ul>
<li>占用較多磁碟空間 ( 但以當今的硬體規格其實可以忽略這一點安裝空間 )</li>
</ul>
</li>
</ul>
<p>可以透過到官方的 <a href="https://github.com/coreybutler/nvm-windows/releases">Github</a> 下載，一般下載 nvm-setup.zip 版本接下來一路按確認即可安裝。</p>
<p>安裝完 NVM 後接著就是打開 CML 透過 NVM 來安裝 Node.js，輸入 nvm 就會出現許多功能介紹。</p>
<p>安裝 Node.js 特定版本</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nvm install &lt;version&gt;</span><br></pre></td></tr></table></figure>

<p>使用、切換下載後的 Node.js 版本</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nvm use</span><br></pre></td></tr></table></figure>

<h4 id="直接安裝"><a href="#直接安裝" class="headerlink" title="直接安裝"></a>直接安裝</h4><ul>
<li>優點<ul>
<li>於官網直接安裝</li>
<li>版本固定</li>
</ul>
</li>
<li>缺點<ul>
<li>切換版本需重新安裝</li>
</ul>
</li>
</ul>
<p>Node.js 官網後會看到有兩個版本可以下載，方別是 LTS ( Long Time Support ) 長期支援的版本 &amp; Current 最新版本，如果沒有對於最新版本的需求，官方建議下載 LTS 版本即可，此版本大版號下官方宣稱會持續維護三年左右。</p>
<p>基於以上幾點比較，現在的環境較為推薦使用 NVM 進行安裝。</p>
<h3 id="基本介紹"><a href="#基本介紹" class="headerlink" title="基本介紹"></a>基本介紹</h3><p>Node.js 是 JS 在 Node 上執行的方式，也因為載體不再是瀏覽器，因此也缺乏 DOM &amp; BOM 的操控。但與前端瀏覽器相同的是，都採用 Chrome 團隊 V8 引擎，號稱地表最快 JS 引擎，原因在於一般程式語言需要被電腦讀取都需要進行編譯的動作轉成中介碼，最後再轉成電腦可直接讀取的機械碼，但 V8 省去中介碼的轉換因此能有較好的效能。<br>一般來說大版號基數為開發測試版，偶數則為正式版號，因此都以偶數版為下載版本。</p>
<h4 id="REPL-Read-Eval-Print-Loop-交互式命令"><a href="#REPL-Read-Eval-Print-Loop-交互式命令" class="headerlink" title="REPL (Read Eval Print Loop) 交互式命令"></a>REPL (Read Eval Print Loop) 交互式命令</h4><p>REPL 其實就和前端瀏覽器中的 console 一樣，可藉由輸入 console 印出對應訊息，作法是在 CML 中輸入 <code>node</code> 後，在顯示的 <code>&gt;</code> 符號後輸入以往在前端輸入的 console 訊息即可，但也因為不在瀏覽器中，所以同樣無法使用 DOM &amp; BOM。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">node</span><br></pre></td></tr></table></figure>

<h5 id="執行-JS-檔案"><a href="#執行-JS-檔案" class="headerlink" title="執行 JS 檔案"></a>執行 JS 檔案</h5><p>亦可直接執行 JS 檔案，副檔名 <code>.js</code> 可以直接忽略</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 執行 demo.js</span></span><br><span class="line">node demo</span><br></pre></td></tr></table></figure>

<h5 id="跳出-REPL"><a href="#跳出-REPL" class="headerlink" title="跳出 REPL"></a>跳出 REPL</h5><p>按一下 <code>ctrl + D</code> 或兩下 <code>ctrl + C</code> 都可以跳出 REPL 狀態</p>
<h3 id="全域"><a href="#全域" class="headerlink" title="全域"></a>全域</h3><p>在瀏覽器中如果直接印出 <code>this</code> 會出現 <code>Window</code> 物件，因為此物件為瀏覽中的最高級物件。一段最簡單的程式碼如下，宣告一個變數後，透過 <code>this</code> 呼叫會發現，透過 browser console &amp; REPL 都會出現期待的結過，但直接透過 node 呼叫會出現 <code>undefined</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">run</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>.a);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">run();</span><br><span class="line"></span><br><span class="line"><span class="comment">// browser console &amp; REPL</span></span><br><span class="line"></span><br><span class="line">&#123;&#125;</span><br><span class="line"><span class="number">100</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// node</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">global</span></span><br><span class="line"><span class="literal">undefined</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h4 id="global"><a href="#global" class="headerlink" title="global"></a>global</h4><p>其原因是在 node 中，為了避免全域之間彼此互相汙染，所以檔案內宣告的全域變數並不會直接掛載到全域 <code>global</code> 上，但 REPL 因為每次開啟相互獨立，所以不會有這個問題</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// demo.js</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">this</span> === <span class="built_in">global</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// node demo</span></span><br><span class="line"><span class="literal">false</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// REPL</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">this</span> === <span class="built_in">global</span>)   <span class="comment">// true</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h5 id="global-屬性"><a href="#global-屬性" class="headerlink" title="global 屬性"></a>global 屬性</h5><p>以下幾個屬性是專屬於 node.js，無法在瀏覽器中使用</p>
<ul>
<li><p>require()</p>
<ul>
<li><p>載入模組 ( <a href="https://nodejs.org/dist/latest-v14.x/docs/api/fs.html">Node.js 內建模組</a> )</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// abc.txt</span></span><br><span class="line">abcdef</span><br><span class="line"></span><br><span class="line"><span class="comment">// demo.js</span></span><br><span class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">&#x27;fs&#x27;</span>);</span><br><span class="line"><span class="keyword">var</span> data = fs.readFilesSync(<span class="string">&#x27;abc.txt&#x27;</span>); </span><br><span class="line"><span class="built_in">console</span>.log(data);</span><br></pre></td></tr></table></figure>

<p>載入後會發現出現一段 <code>buffer</code>，需要透過 <code>toString</code> Function 來將其轉成一般字串</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// demo.js</span></span><br><span class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">&#x27;fs&#x27;</span>);</span><br><span class="line"><span class="keyword">var</span> data = fs.readFilesSync(<span class="string">&#x27;abc.txt&#x27;</span>); </span><br><span class="line"><span class="built_in">console</span>.log(data.toString());</span><br></pre></td></tr></table></figure></li>
<li><p>載入套件 ( 透過 NPM 下載 )<br>和前端一樣在專案底下執行指令即可安裝</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm i &lt;plugin name&gt;</span><br></pre></td></tr></table></figure>
<p>載入後會發現所有 npm 相關套件都會被安裝在 <code>node_modules</code> 資料夾下</p>
<p><strong>package.json</strong></p>
<p>使用 npm 套件時，可透過以下指令來建構 package.json 作為專案套件的目錄</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm init</span><br></pre></td></tr></table></figure>
<p>輸入後會詢問一些專案基本資料，如果希望跳過這些問題使用預設值，在後方加上 <code>-y</code> 即可</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm init -y</span><br></pre></td></tr></table></figure>

<p><strong>package.lock.json</strong></p>
<p>被記錄在這個檔案中的套件會使下次在下載專案時載入指定版本，避免有些套件橫跨大版號時語法不完全相容導致程式無法執行，因此這個檔案一般也會 commit 到 git 中。npm 5.0 版本開始就會自動產生這個檔案</p>
</li>
<li><p>載入自訂模組 ( 自己寫的模組 )</p>
</li>
</ul>
</li>
<li><p>exports<br>在製作一個模組時，可以透過 <code>exports</code> 供其他模組使用，模組預設為一個 ‘物件’，所以一般來說會在其之下新增屬性。需要特別注意的是，這裡的 require 需使用 ‘相對路徑’。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// module1.js</span></span><br><span class="line"><span class="built_in">exports</span>.run = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;run&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// demo.js</span></span><br><span class="line"><span class="keyword">var</span> m1 = <span class="built_in">require</span>(<span class="string">&#x27;./module1&#x27;</span>);</span><br><span class="line">m1.run();   <span class="comment">// run</span></span><br></pre></td></tr></table></figure>

<p><strong>module.exports</strong></p>
<p>然而如果不想從一個物件為起點，不能直接將 <code>exports</code> 賦予另一個物件，需使用 <code>module.exports</code> 來指向 <code>exports</code>，但 <code>require</code> 其實是指向 <code>module.exports</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// module1.js</span></span><br><span class="line"><span class="built_in">module</span>.exports = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;run&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// demo.js</span></span><br><span class="line"><span class="keyword">var</span> m1 = <span class="built_in">require</span>(<span class="string">&#x27;module1&#x27;</span>);</span><br><span class="line">m1();   <span class="comment">// run</span></span><br></pre></td></tr></table></figure></li>
<li><p>module<br>如同前面所述，每個 <code>exports</code> 的 .js 檔案都可以視為一個 module</p>
</li>
<li><p>__filename<br>目前正在執行的檔案</p>
</li>
<li><p>__dirname<br>目前正在執行的資料夾</p>
</li>
<li><p>process<br>提供全域資訊與訊息控制，以下為長裕資訊屬性</p>
<ul>
<li>process.argv<br>這個屬性會顯示執行時所代的參數，預設會有 node.js 和該檔案本身<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[</span><br><span class="line">  <span class="string">&#x27;C:\\Program Files\\nodejs\\node.exe&#x27;</span>,</span><br><span class="line">  <span class="string">&#x27;C:\\Users\\YA\\Desktop\\Node\\demo&#x27;</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure>
如果在後方帶入額外參數則會往後新增，多個參數間只要透過空格方開即可<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">node demo xxx yyy</span><br><span class="line"></span><br><span class="line">[</span><br><span class="line">  <span class="string">&#x27;C:\\Program Files\\nodejs\\node.exe&#x27;</span>,</span><br><span class="line">  <span class="string">&#x27;C:\\Users\\YA\\Desktop\\Node\\demo&#x27;</span>,</span><br><span class="line">  <span class="string">&#x27;xxx&#x27;</span>,</span><br><span class="line">  <span class="string">&#x27;yyy&#x27;</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure></li>
<li>process.env<br>為一個代表環境變數的物件，包含路徑、OS.etc</li>
<li>process.cwd<br>代表執行當下的工作目錄</li>
</ul>
</li>
</ul>
]]></content>
      <tags>
        <tag>Node</tag>
        <tag>Back-end</tag>
      </tags>
  </entry>
  <entry>
    <title>Vite + Vue-Roueter on Github Pages</title>
    <url>/vue-3/20210911/13819/</url>
    <content><![CDATA[<p>紀錄使用 Vite + Vue 3 + Vue-Router 部屬到 Github Pages 所遇到的問題</p>
<span id="more"></span>

<h3 id="部屬到-Github"><a href="#部屬到-Github" class="headerlink" title="部屬到 Github"></a>部屬到 Github</h3><p>不論是使用 Vite 或 Vue CLI 建構專案，在部屬到 Github Page 時，如果是要部屬到額外的 Repository，都需要在生成的讀取檔案路徑前加上 Repository 名稱，但只限於部屬到 Github 上 build 出後的檔案。</p>
<h4 id="設定"><a href="#設定" class="headerlink" title="設定"></a>設定</h4><p>可透過以下兩種方式區分開發模式 (dev) 與生成 (build) 的檔案</p>
<ul>
<li><p>環境設定檔<br>在專案跟目錄新增一個環境設定檔，如果是 Vite 就是 <code>vite.config.js</code>，Vue CLI 則是 <code>vue.config.js</code>， 在 <code>vite.config.js</code> 有一個參數 <code>base</code> 可以設定檔案的路徑，並透過 <code>process.env.NODE_ENV</code> 來判斷 Server 執行在開發或正式環境中，如果是正式環境則須加上 Github Respository 名稱</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vite.config.js</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> &#123; defineConfig &#125; <span class="keyword">from</span> <span class="string">&#x27;vite&#x27;</span>;</span><br><span class="line"><span class="keyword">import</span> vue <span class="keyword">from</span> <span class="string">&#x27;@vitejs/plugin-vue&#x27;</span>;</span><br><span class="line"><span class="keyword">import</span> path <span class="keyword">from</span> <span class="string">&#x27;path&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// https://vitejs.dev/config/</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> defineConfig(&#123;</span><br><span class="line">  plugins: [vue()],</span><br><span class="line">  build: &#123;</span><br><span class="line">    minify: <span class="literal">true</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  resolve: &#123;</span><br><span class="line">    alias: &#123;</span><br><span class="line">      <span class="string">&#x27;@&#x27;</span>: path.resolve(__dirname, <span class="string">&#x27;./src&#x27;</span>),</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">  base: process.env.NODE_ENV === <span class="string">&#x27;production&#x27;</span> ? <span class="string">&#x27;/&lt;REPO&gt;/&#x27;</span> : <span class="string">&#x27;./&#x27;</span>,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li>
<li><p>於生成正式檔案時加上<br>另外也可以透過在生成正式檔案時加上，如果是透過 vite 建置專案，可以在 <code>package.json/scripts/build</code> 後加上 <code>--base=/&lt;REPO&gt;/</code> 參數藉此在生成時附加 Repository 路徑</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// package.json</span></span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;name&quot;</span>: <span class="string">&quot;...&quot;</span>,</span><br><span class="line">  <span class="string">&quot;version&quot;</span>: <span class="string">&quot;0.0.0&quot;</span>,</span><br><span class="line">  <span class="string">&quot;scripts&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;dev&quot;</span>: <span class="string">&quot;vite&quot;</span>,</span><br><span class="line">    <span class="string">&quot;build&quot;</span>: <span class="string">&quot;vite build --base=/&lt;REPO&gt;/&quot;</span>,</span><br><span class="line">    <span class="string">&quot;serve&quot;</span>: <span class="string">&quot;vite preview&quot;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">&quot;dependencies&quot;</span>: &#123;</span><br><span class="line">    ...</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">&quot;devDependencies&quot;</span>: &#123;</span><br><span class="line">    ...</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="部屬"><a href="#部屬" class="headerlink" title="部屬"></a>部屬</h4><p>在 <a href="https://vitejs.dev/guide/static-deploy.html#github-pages">官方的文件</a> 中推薦使用建立一個 <code>deploy.sh</code> 的 Shell Script 檔案來一鍵生成、部屬到 Github <code>gh-pages</code> branch 上，在 Github 中，Github Pages 會優先讀取 <code>gh-pages</code> branch 中的檔案，因此可藉此區分開發與生成的檔案</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="comment">#! deploy.sh</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># abort on errors</span></span><br><span class="line"><span class="built_in">set</span> -e</span><br><span class="line"></span><br><span class="line"><span class="comment"># build</span></span><br><span class="line">npm run build</span><br><span class="line"></span><br><span class="line"><span class="comment"># navigate into the build output directory</span></span><br><span class="line"><span class="built_in">cd</span> dist</span><br><span class="line"></span><br><span class="line"><span class="comment"># if you are deploying to a custom domain</span></span><br><span class="line"><span class="comment"># echo &#x27;www.example.com&#x27; &gt; CNAME</span></span><br><span class="line"></span><br><span class="line">git init</span><br><span class="line">git add -A</span><br><span class="line">git commit -m <span class="string">&#x27;deploy&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># if you are deploying to https://&lt;USERNAME&gt;.github.io</span></span><br><span class="line"><span class="comment"># git push -f git@github.com:&lt;USERNAME&gt;/&lt;USERNAME&gt;.github.io.git master</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># if you are deploying to https://&lt;USERNAME&gt;.github.io/&lt;REPO&gt;</span></span><br><span class="line"><span class="comment"># git push -f git@github.com:&lt;USERNAME&gt;/&lt;REPO&gt;.git master:gh-pages</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">cd</span> -</span><br></pre></td></tr></table></figure>

<h3 id="Vue-Router"><a href="#Vue-Router" class="headerlink" title="Vue Router"></a>Vue Router</h3><p>在 Vue 3 所搭配使用的 <a href="https://next.router.vuejs.org/guide/essentials/history-mode.html#different-history-modes">Vue CLI 4</a> 中，提供了一個 <code>history</code> 參數來選擇前端路由的顯示模式，方別是 <code>Hash Mode</code> 和 <code>HTML5 Mode</code></p>
<ul>
<li><p>Hash Mode<br>這是在 Vue CLI 4 以前前端路由所使用的方式，會加網址後加上一個 <code>#</code> ，但也因為這樣會降低瀏覽器的 SEO ，因此官方建議如果注重 SEO 的網站可以使用第二種 <code>HTML5 Mode</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// router.js</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> &#123; createRouter, createWebHashHistory &#125; <span class="keyword">from</span> <span class="string">&#x27;vue-router&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> router = createRouter(&#123;</span><br><span class="line">  history: createWebHashHistory(),</span><br><span class="line">  routes: [</span><br><span class="line">    <span class="comment">//...</span></span><br><span class="line">  ],</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure></li>
<li><p>HTML5 Mode<br>將 history 切換到這個模式後就會在網址省略 <code>#</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// router.js</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> &#123; createRouter, createWebHistory &#125; <span class="keyword">from</span> <span class="string">&#x27;vue-router&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> router = createRouter(&#123;</span><br><span class="line">  history: createWebHistory(),</span><br><span class="line">  routes: [</span><br><span class="line">    <span class="comment">//...</span></span><br><span class="line">  ],</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="問題"><a href="#問題" class="headerlink" title="問題"></a>問題</h4><p>但在 Github page 使用 <code>HTML Mode</code> 就會發現當 Router 切換到其他路徑時，就會跳轉到 404 Error 頁面，原因是 Vue 只是切換 Component 而已，路徑並不存在，這裡有以下三個方式可以解決這個問題</p>
<ul>
<li><p>404.html<br>在 <code>index.html</code> 跟目錄位置新增一個 <code>404.html</code>，內容只要完全複製 <code>index.html</code> 即可</p>
</li>
<li><p>Hash Mode<br>將 Vue router history 切換回 <code>Hash Mode</code> 也可以解決這個問題</p>
</li>
<li><p>實作一個簡單的重新導向機制，在出現 404 頁面時指向 <code>index.html</code>，此原理和第一種解法相同</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Vue 3</category>
      </categories>
      <tags>
        <tag>Vue</tag>
        <tag>Bug</tag>
        <tag>Vite</tag>
        <tag>Vue Router</tag>
      </tags>
  </entry>
  <entry>
    <title>http-server and browser local file</title>
    <url>/browser/20210509/2556/</url>
    <content><![CDATA[<p><code>http-server</code> 基礎應用與 <code>browser</code> 開啟本地端檔案</p>
<span id="more"></span>

<h3 id="問題"><a href="#問題" class="headerlink" title="問題"></a>問題</h3><p>在工作上遇到一個暫時性需求需要將前端 build 出來的檔案在本地端開啟，但無論是 webpack 或是基礎專案，只要不是全寫在 <code>html</code> 中，就會透過 link 或 script 來載入其他 <code>js</code> 或 <code>css</code> 檔案，或是嘗試使用 <code>ajax</code> 來取得本地端檔案，會發現網頁打開一片空白，開啟 console 會出現以下 error log</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Cross origin requests are only supported for protocol schemes: http, data, chrome, chrome-extension, https, chrome-extension-resource.</span><br></pre></td></tr></table></figure>

<p>原因為該行為在 chrome 是被禁止的，是為了避免檔案內容被更改而載入惡意程式碼，網路上有些文章提及使用 firefox 可以解決，但我在這篇文章中的時間點使用 firefox 會出現相同的情況</p>
<h3 id="解法"><a href="#解法" class="headerlink" title="解法"></a>解法</h3><h4 id="解法一"><a href="#解法一" class="headerlink" title="解法一"></a>解法一</h4><p>透過 CMD 重新開啟 chrome ，但這個方法為一次性的，每次開啟 chrome 都要重新補上這段 CMD，以此來讓 chrome 忽略載入 local file 這個動作</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chrome.exe --allow-file-access-from-file </span><br></pre></td></tr></table></figure>

<h4 id="解法二"><a href="#解法二" class="headerlink" title="解法二"></a>解法二</h4><p>架一個最簡單的 <code>http-server</code>，透過 <code>npm</code> 即可安裝 <code>http-server</code> 這個套件，只要在對應資料夾路徑底下執行，就可以在該路徑開啟任何 <code>html</code> 檔案</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># install on global</span></span><br><span class="line">npm i http-server -g</span><br><span class="line"></span><br><span class="line"><span class="comment"># open http server</span></span><br><span class="line">http-server</span><br></pre></td></tr></table></figure>

<p>接著就可以在 browser 開啟，預設為 <code>8080</code> port</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http:&#x2F;&#x2F;localhost:8080&#x2F;index.html</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Browser</category>
      </categories>
      <tags>
        <tag>http-server</tag>
        <tag>browser</tag>
      </tags>
  </entry>
  <entry>
    <title>運算子 operators</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20210513/16622/</url>
    <content><![CDATA[<p>運算子 (operators) 基本介紹</p>
<span id="more"></span>

<p>運算子是 JS 中的特殊函數，需要兩個參數回傳一個值，以最簡單的加法為例，JS 其實有一個名為 ‘+’ 的函數</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">3</span> + <span class="number">4</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> +(<span class="params">a, b</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="comment">// add the two #s</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>所以如果是一般函數呼叫就會像是以下方式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">+(<span class="number">3</span>, <span class="number">4</span>);   <span class="comment">// 7</span></span><br></pre></td></tr></table></figure>

<p>但為了精簡這樣的寫法，就如同多數程式語言，使用 ‘中綴表示法’，也就是將函數名稱放在兩個變數之間，並去掉, 和 () 最終成為了我們常見的表示方式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="number">3</span> + <span class="number">4</span>;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>面試紀錄-哲煜科技-前端工程師</title>
    <url>/interview/20211005/27559/</url>
    <content><![CDATA[<p>哲煜科技接案公司前端工程師面試紀錄</p>
<span id="more"></span>

<h3 id="面試流程"><a href="#面試流程" class="headerlink" title="面試流程"></a>面試流程</h3><p>前端主管與技術長二人同時一起面試，主要針對過往工作經歷、專案內容操作與相關技術進行提問</p>
<h3 id="提問"><a href="#提問" class="headerlink" title="提問"></a>提問</h3><h4 id="Vue"><a href="#Vue" class="headerlink" title="Vue"></a>Vue</h4><ul>
<li>Q: Vue 生命週期了解程度</li>
<li>Q: 如何處理不同裝置解析度下載入不同圖片</li>
<li>Q: Vue 中如何使用 jQuery<br>A: 於 <code>mounted</code> 後 Vue 綁定到 DOM 上方可使用</li>
<li>Q: created 更新 data <code>watch()</code> 會先觸發還是 <code>mounted</code> ?</li>
</ul>
<h4 id="JavaScript"><a href="#JavaScript" class="headerlink" title="JavaScript"></a>JavaScript</h4><ul>
<li>Q: ES6 &amp; ES6+ 了解程度<br>A: 常用於變數、函式宣告與陣列方法</li>
<li>Q: 什麼是 JavaScript 中的 Event Loop ?</li>
<li>Q: JavaScript 是一個單線程還是多線程的程式語言 ?</li>
</ul>
<h4 id="Git"><a href="#Git" class="headerlink" title="Git"></a>Git</h4><ul>
<li>Q: 協作時發生 <code>conflict</code> 應該下什麼指令來解決</li>
</ul>
<h4 id="Else"><a href="#Else" class="headerlink" title="Else"></a>Else</h4><ul>
<li>Q: 是否使用過 CI/CD 進行自動部屬 ?</li>
</ul>
<h3 id="最後對公司提問"><a href="#最後對公司提問" class="headerlink" title="最後對公司提問"></a>最後對公司提問</h3><ul>
<li>Q: 接案公司開發週期<br>A: 是專案內容而定，常見半年至一年不等</li>
<li>Q: 什麼情況下會採用新技術 (Ex: Vue3)<br>A: 判定技術社群較為成熟且採用成本可以接受為主</li>
<li>Q: 如何規範 Coding Style ?<br>A: 透過 ESLint 與命名方式同步開發，並進行互相 Code Review</li>
</ul>
]]></content>
      <categories>
        <category>Interview</category>
      </categories>
      <tags>
        <tag>Interview</tag>
      </tags>
  </entry>
  <entry>
    <title>強制型轉 Coercion</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211003/50074/</url>
    <content><![CDATA[<p>JavaScript 是一個動態型別的語言，當不同型別的參數透過運算子來院算時，JavaScript 會將參數的型別轉乘相同，而不是回傳一個錯誤的訊息</p>
<span id="more"></span>

<p>上一章節中有提到透過 <code>加號 +</code> 來做數字的運算，但其實不只能用在數字，在字串使用時會將彼此連結</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="string">&#x27;Hello &#x27;</span> + <span class="string">&#x27;World&#x27;</span>;   <span class="comment">// Hello World</span></span><br></pre></td></tr></table></figure>

<p>如果是一個數字加上字串的運算，JavaScript 會將數字的參數強制型轉成字串</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> b = <span class="string">&#x27;1&#x27;</span> + <span class="number">2</span>;   <span class="comment">// 12</span></span><br></pre></td></tr></table></figure>

<p>即使這麼做不會出現錯誤，但一般來說盡量避免這樣的運算出現，原因是在開發時通常不會只有這麼簡單的運算，兩個參數之間可能有大量的程式碼，避免出現無法預期的 Bug</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>運算子的優先性與相依性</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211003/33835/</url>
    <content><![CDATA[<p>在一般的運算式中，通常不會只有一個運算子，所以當出現多個運算子時，就需要透過<code>優先性 Precedence</code> 和 <code>相依性 Associativity</code> 這兩個原則來規範運算，而這兩個原則並沒有名義上的複雜，國小數學中的先乘除後加減即是基礎規範的體現</p>
<span id="more"></span>

<h3 id="優先性-Precedence"><a href="#優先性-Precedence" class="headerlink" title="優先性 Precedence"></a>優先性 Precedence</h3><p>優先性即是當出現不同運算子時，函式執行的優先順序，如<code>乘號 *</code> 優先於<code>加號+</code> </p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">3</span> + <span class="number">4</span> * <span class="number">5</span>;   <span class="comment">// 23</span></span><br></pre></td></tr></table></figure>

<h3 id="相依性-Associativity"><a href="#相依性-Associativity" class="headerlink" title="相依性 Associativity"></a>相依性 Associativity</h3><p>相依性即運算函式執行的方向為左到右或是右到左，如下<code>等號 =</code> 為右到左</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">2</span>, b = <span class="number">3</span>, c = <span class="number">4</span>;</span><br><span class="line"></span><br><span class="line">a = b = c;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 4</span></span><br><span class="line"><span class="built_in">console</span>.log(b);   <span class="comment">// 4</span></span><br><span class="line"><span class="built_in">console</span>.log(c);   <span class="comment">// 4</span></span><br></pre></td></tr></table></figure>

<h3 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h3><p><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Operator_Precedence">MDN Operator Precedence &amp; Associativity</a></p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>比較運算子 Comparison Operators</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211010/9237/</url>
    <content><![CDATA[<p>結合上一章的強制轉型與運算子介紹比較運算子的應用範例</p>
<span id="more"></span>

<p>我們直接從一個最簡單的例子來理解 JavaScrit 中比較運算子的規則，下方的例子我們可以很直覺的反應答案是 <code>true</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="number">1</span> &lt; <span class="number">2</span> &lt; <span class="number">3</span>);   <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>接著看到第二個例子</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="number">3</span> &lt; <span class="number">2</span> &lt; <span class="number">1</span>);   <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>會發現答案也是 <code>true</code> ?，這裡我們先看一下 <code>&lt;</code> 這個比較運算子的規則</p>
<p><img src="less-than.PNG"></p>
<p>規則為由左到右，而兩個相同的運算子層級相同，所以由最左邊開始</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="literal">false</span> &lt; <span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<p>在強制轉型章節有提到，當不同型別的值進行運算時，會被強制轉型，而 <code>false</code> 經過轉型成數字後會是 <code>0</code>，<code>true</code> 則是 <code>1</code>，所以最後答案才會是 <code>true</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="number">0</span> &lt; <span class="number">1</span>);   <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>這時候我們回過頭看到一開始大家覺得理所當然的比較運算就會發現，雖然答案剛好和直覺相同，但運算的過程其實不一樣</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 初始狀態</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">1</span> &lt; <span class="number">2</span> &lt; <span class="number">3</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 第一次運算</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="literal">true</span> &lt; <span class="number">3</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 強制轉型後</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">1</span> &lt; <span class="number">3</span>);   <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<h3 id="常見的特別轉型結果"><a href="#常見的特別轉型結果" class="headerlink" title="常見的特別轉型結果"></a>常見的特別轉型結果</h3><p>以下列出 JavaScript 特別的轉型結果</p>
<h4 id="數字轉型"><a href="#數字轉型" class="headerlink" title="數字轉型"></a>數字轉型</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Number</span>(<span class="literal">undefined</span>);   <span class="comment">// NaN</span></span><br><span class="line"><span class="built_in">Number</span>(<span class="literal">null</span>);   <span class="comment">// 0</span></span><br></pre></td></tr></table></figure>

<h4 id="運算子"><a href="#運算子" class="headerlink" title="== 運算子"></a>== 運算子</h4><p>雙等號在不同型別比較時也會進行強制轉型</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span> == <span class="string">&#x27;1&#x27;</span>;     <span class="comment">// true</span></span><br><span class="line"><span class="literal">false</span> == <span class="number">0</span>;   <span class="comment">// true</span></span><br><span class="line"><span class="string">&quot;&quot;</span> == <span class="number">0</span>;      <span class="comment">// true</span></span><br><span class="line"><span class="string">&quot;&quot;</span> == <span class="literal">false</span>;  <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>但這裡有一個很特別的案例，在上方 <code>null</code> 轉型成數字時是 <code>0</code>，但透過 <code>==</code> 比較又是 <code>false</code>，這也被視為 JavaScript 的缺陷，因此開發時建議使用三個等號 <code>===</code> 避免將值強制轉型</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="literal">null</span> == <span class="number">0</span>;   <span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<h4 id="運算子-1"><a href="#運算子-1" class="headerlink" title="=== 運算子"></a>=== 運算子</h4><p>這也是一般開發在進行值的比較時建議使用的方式，避免值被強制傳型出現無法預期的錯誤</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span> === <span class="number">1</span>;     <span class="comment">// true</span></span><br><span class="line"><span class="number">1</span> === <span class="string">&#x27;1&#x27;</span>;   <span class="comment">// false </span></span><br></pre></td></tr></table></figure>

<h4 id="amp-運算子"><a href="#amp-運算子" class="headerlink" title="!= &amp; !== 運算子"></a>!= &amp; !== 運算子</h4><p>同理 <code>!=</code> 在比較是否不相等時會強制傳型，<code>!==</code> 則不會</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="number">0</span> != <span class="literal">false</span>;   <span class="comment">// false</span></span><br><span class="line"><span class="number">0</span> !== <span class="literal">false</span>;  <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<h3 id="參考資料"><a href="#參考資料" class="headerlink" title="參考資料"></a>參考資料</h3><p><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators#Less_than_operator">MDN Expressions and operators</a><br><a href="Operator+Precedence+In+Javascript">Operator+Precedence+In+Javascript</a><br><a href="Equalty+Comparison+And+Sameness+">Equalty+Comparison+And+Sameness</a></p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>存在與布林 Existence and Booleans</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211016/61081/</url>
    <content><![CDATA[<p>在 JavaScript 中，我們可以透過 <code>Boolean</code> 來判斷一個值是否存在，某些值被轉型為布林後會被判斷為 <code>false</code>，如下:</p>
<span id="more"></span>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Boolean</span>(<span class="literal">undefined</span>);   <span class="comment">// false</span></span><br><span class="line"><span class="built_in">Boolean</span>(<span class="literal">null</span>);        <span class="comment">// false</span></span><br><span class="line"><span class="built_in">Boolean</span>(<span class="string">&#x27;&#x27;</span>);          <span class="comment">// false</span></span><br><span class="line"><span class="built_in">Boolean</span>(<span class="number">0</span>);           <span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<p>表示這些值都不存在，但其實一般開發並不太會使用 <code>Boolean</code> 這個函式直接強制轉型，而是會透過 <code>if-else</code> 來判斷<br>如下範例，如果沒有為一個參數主動賦值，預設值就是 <code>undefined</code>，因此會被強制轉型為 <code>false</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a;</span><br><span class="line"></span><br><span class="line"><span class="comment">// goes to internet and looks for a value</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (a) &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Something is there.&#x27;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>接著看到如果賦值為 <code>0</code>，也會被強制轉型為 <code>false</code>，但如果再加上一個 <code>||</code> 使值為 <code>0</code> 的時候也通過，因為 <code>===</code> 優先於 <code>||</code>，在 <code>false</code> 或 <code>true</code> 的結果，結果就會是 <code>true</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 初始條件</span></span><br><span class="line"><span class="keyword">if</span> (a || a === <span class="number">0</span>) &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// === 運算子優先運算</span></span><br><span class="line"><span class="keyword">if</span> (a || <span class="literal">true</span>) &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// || 運算子由左到右，0 被轉型為 false</span></span><br><span class="line"><span class="keyword">if</span> (<span class="literal">false</span> || <span class="literal">true</span>) &#123;&#125;</span><br></pre></td></tr></table></figure>





]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>框架小叮嚀:預設值 Framework Aside</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211016/19790/</url>
    <content><![CDATA[<p>在開發 JavaScript 時，我們常會透過 <code>&lt;script&gt;</code> 來導入不同的套件，這章節來進一步了解透件被引入後的執行方式</p>
<span id="more"></span>

<p>一段常見的用法如下，我們透過 <code>&lt;script&gt;</code> 導入 <code>lib1.js</code> 和 <code>lib2.js</code> 這兩個框架，最後導入 <code>app.js</code> 來執行</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;lib1.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;lib2.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;app.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>各自的程式碼如下</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lib1.js</span></span><br><span class="line"><span class="keyword">var</span> libraryName = <span class="string">&#x27;Lib 1&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// lib2.js</span></span><br><span class="line"><span class="keyword">var</span> libraryName = <span class="string">&#x27;Lib 2&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// app.js</span></span><br><span class="line"><span class="built_in">console</span>.log(libraryName);   <span class="comment">// Lib 2</span></span><br></pre></td></tr></table></figure>

<p>結果會是 <code>Lib 2</code>，原因是 JavaScript 在執行這三段程式碼的時候，並不會為它們各自創造一個執行環境，直接地說是將彼此程式碼進行堆疊，所以實際上這兩個 Library 宣告了一個相同的全域變數，結果自然會以後者來取代。</p>
<p>所以如果在看許多套件的原始碼會看到類似以下的方式來宣告變數，避免影響到已存在的程式碼。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lib2.js</span></span><br><span class="line"><span class="keyword">var</span> libraryName = libraryName || <span class="string">&#x27;Lib 2&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 其實也就是在 window 宣告一個全域變數</span></span><br><span class="line"><span class="built_in">window</span>.libraryName = <span class="built_in">window</span>.libraryName || <span class="string">&#x27;Lib 2&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>這麼一來後續導入的套件就不會影響到前者了</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> libraryName = libraryName || <span class="string">&#x27;Lib 1&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> libraryName = libraryName || <span class="string">&#x27;Lib 2&#x27;</span>;</span><br><span class="line"><span class="built_in">console</span>.log(libraryName);   <span class="comment">// Lib 1</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>預設值 Default Value</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211016/58810/</url>
    <content><![CDATA[<p>這章節會透過其他角度來看 JavaScript 強制轉型的特性</p>
<span id="more"></span>

<p>首先先宣告一個擁有一個參數的函式，我們都知道 JavaScript 的變數如果沒有主動賦值，都會被給予一個 <code>undefined</code>，所以城市並不會出錯</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Hello &#x27;</span> + name);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">greet();        <span class="comment">// Hello undefined</span></span><br><span class="line">greet(Jerry);   <span class="comment">// Hello Jerry  </span></span><br></pre></td></tr></table></figure>

<p>接著將變數改成如果沒有賦值，我們透過 <code>||</code> 給予一個預設值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  name = name || <span class="string">&#x27;&lt;Your name here&gt;&#x27;</span>;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Hello &#x27;</span> + name);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">greet();   <span class="comment">// Hello &lt;Your name here&gt;</span></span><br></pre></td></tr></table></figure>

<p>原因是 <code>||</code> 優先於 <code>=</code>，而 <code>||</code> 會回傳優先為 <code>true</code> 的值，所以如果沒有賦與參數，會回傳透過 <code>Boolean</code> 轉型為 <code>true</code> 的值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  name = <span class="literal">false</span> || <span class="literal">true</span>;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Hello &#x27;</span> + name);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">greet();   <span class="comment">// // Hello &lt;Your name here&gt;</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>Debug 神器: Debugger</title>
    <url>/browser/20211017/20624/</url>
    <content><![CDATA[<p>紀錄一下透過胡立大在 Lidemy 平台上 <code>先別急著寫 leetcode</code> 這堂課中所學到的 Chrome Debug 技巧</p>
<span id="more"></span>

<p>首先建立一個 html 檔案，並在 <code>&lt;script&gt;</code> 中加入一個 <code>debugger</code> 關鍵字，接著下方可以隨意方進一段範例程式碼</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">// test.html</span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">  <span class="keyword">debugger</span></span></span><br><span class="line"><span class="javascript">  <span class="keyword">let</span> arr = [<span class="number">2</span>, <span class="number">7</span>, <span class="number">5</span>];</span></span><br><span class="line"><span class="javascript">  <span class="keyword">let</span> max = arr[<span class="number">0</span>];</span></span><br><span class="line"><span class="javascript">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; arr.length; i++) &#123;</span></span><br><span class="line">    if (arr[i] &gt; max) max = arr[i];</span><br><span class="line">  &#125;</span><br><span class="line"><span class="javascript">  <span class="built_in">console</span>.log(max);</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>如果是透過 VSCode 可以透過 <code>Live Server</code> 這個 Extension 開啟，或者也可以直接點擊 html 檔案於瀏覽器開啟，開啟後按 <code>F12</code> 開啟開發者工具會自動跳轉到 <code>Sources</code>，並顯示 <code>html</code> 檔案中的程式碼，且會停留在 <code>debugger</code> 這個關鍵字上</p>
<p><img src="Sources.PNG"></p>
<p>接著看到右邊會有一排按鈕，每個按鈕代表一種接下來程式碼執行的方式，最常用的 <code>Step F9</code>，按一下執行一步程式碼，依序一行一行執行</p>
<p><img src="Step.png"></p>
<p>點選 <code>Step</code> 會發現右側 <code>Scope</code> 中會根據當下執行的狀態顯示變數的值，我們就可以藉此為程式碼 Debug 囉</p>
<p><img src="Scope.png"></p>
]]></content>
      <categories>
        <category>Browser</category>
      </categories>
      <tags>
        <tag>Chrome</tag>
        <tag>Debug</tag>
      </tags>
  </entry>
  <entry>
    <title>.jsp 中的 ES6+</title>
    <url>/es6/20211124/45063/</url>
    <content><![CDATA[<p>因公司現行專案架構為 JAVA Base 的前後端整合架構，因此前端開發都會寫在 <code>.jsp</code> 檔案內，而該檔案類型本來就有 JAVA 相關語法可以使用，所以在此紀錄與純前端開發語法上的不同之處</p>
<span id="more"></span>

<h2 id="Literals-Template"><a href="#Literals-Template" class="headerlink" title="Literals Template"></a>Literals Template</h2><p>在 ES6 的模板字串中，我們可以使用 ` 包住 <code>$&#123;&#125;</code> 將變數帶入字串中，但剛好 <code>$&#123;&#125;</code> 在 <code>.jsp</code> 檔案中也是帶入參數的保留符號，所以在 <code>.jsp</code> 中要使用模板字串時要在前方加上反斜線 <code>\</code> 以做區別</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line"><span class="comment">// *.jsp</span></span><br><span class="line"></span><br><span class="line">&lt;script&gt;</span><br><span class="line">  <span class="keyword">const</span> test = <span class="string">&quot;123&quot;</span>;</span><br><span class="line">  console.log(`\$&#123;test&#125;`);   <span class="comment">// 123</span></span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>ES6+</category>
      </categories>
      <tags>
        <tag>JSP</tag>
        <tag>JAVA</tag>
      </tags>
  </entry>
  <entry>
    <title>Log 雖可恥但有用</title>
    <url>/browser/20211020/34642/</url>
    <content><![CDATA[<p>紀錄一下前端開發時常使用到的 <code>console.log</code> 所遇到的小陷阱</p>
<span id="more"></span>

<p>在前端開發時，我們常會使用到 <code>console.log</code> 來印出資料內容以確保程式碼實行的結果與我們預期的一致</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="number">2.</span> <span class="number">7</span>, <span class="number">5</span>];</span><br><span class="line"><span class="built_in">console</span>.log(arr);   <span class="comment">// [2, 7, 5]</span></span><br></pre></td></tr></table></figure>

<p>但當我們執行一對程式碼後，但這個 Array 進行修改後，會發現即使在修改前使用 <code>console.log</code> 印出資料，點擊後查看結果仍會是修改後的資料</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="number">2.</span> <span class="number">7</span>, <span class="number">5</span>];</span><br><span class="line"><span class="built_in">console</span>.log(arr);</span><br><span class="line"><span class="comment">// 一堆程式碼...</span></span><br><span class="line">arr.push(<span class="number">3</span>);</span><br></pre></td></tr></table></figure>

<p><img src="log.PNG"></p>
<p>原因是瀏覽器在印出 <code>log</code> 時，會印出最終資料，這裡我們可以透過將資料轉成字串 <code>JSON.stringify()</code> 來呈現資料當下的狀況</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="number">2.</span> <span class="number">7</span>, <span class="number">5</span>];</span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">JSON</span>.stringify(arr));   <span class="comment">// [2, 7, 5]</span></span><br><span class="line"><span class="comment">// 一堆程式碼...</span></span><br><span class="line">arr.push(<span class="number">3</span>);</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Browser</category>
      </categories>
      <tags>
        <tag>Debug</tag>
      </tags>
  </entry>
  <entry>
    <title>物件與物件實體 Object and Object Literals</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211020/30806/</url>
    <content><![CDATA[<p>延續上一章節最後提到使用 <code>new</code> 來建立一個物件並不是很好的方法，這章節會針對這點做說明，並介紹何為物件實體 (Object Literals)</p>
<span id="more"></span>

<h2 id="使用大括號-建立物件"><a href="#使用大括號-建立物件" class="headerlink" title="使用大括號 {} 建立物件"></a>使用大括號 {} 建立物件</h2><p>上一章節透過 <code>new</code> 建立物件，並透過點. (Dot) 或中括號 [] (Bracket) 來擴增物件屬性</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = <span class="keyword">new</span> <span class="built_in">Object</span>();</span><br><span class="line"></span><br><span class="line">person.firstname = <span class="string">&#x27;Tony&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>但其實在建立物件時，有更快的作法，我們可以透過大括號 {} 直接建立一個物件，這裡要注意的是，大括號 {} 並不是一個運算子，在 JavaScirpt 中使用大括號 {} 就會預設你建立了一個物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;&#125;;</span><br></pre></td></tr></table></figure>

<p>當然，我們可以做到更多，包含物件屬性 (Property) 和方法 (Method) 的建立，這樣的建立方式比起透過 <code>new</code> 來建立物件再一個一個增加屬性快上許多</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  firstname: <span class="string">&#x27;Tony&#x27;</span>,</span><br><span class="line">  lastname: <span class="string">&#x27;Alicea&#x27;</span>,</span><br><span class="line">  address: &#123;</span><br><span class="line">    street: <span class="string">&#x27;111 Main St.&#x27;</span>,</span><br><span class="line">    city: <span class="string">&#x27;New York&#x27;</span>,</span><br><span class="line">    state: <span class="string">&#x27;NY&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">person = <span class="keyword">new</span> <span class="built_in">Object</span>();</span><br><span class="line">person.firstname = <span class="string">&#x27;Tony&#x27;</span>;</span><br><span class="line">person.lastname = <span class="string">&#x27;Alicea&#x27;</span>;</span><br><span class="line">person.address.street = <span class="string">&#x27;111 Main St.&#x27;</span>;</span><br><span class="line">person.address.city = <span class="string">&#x27;New York&#x27;</span>;</span><br><span class="line">person.address.state = <span class="string">&#x27;NY&#x27;</span>;</span><br></pre></td></tr></table></figure>

<h2 id="搭配函式使用物件"><a href="#搭配函式使用物件" class="headerlink" title="搭配函式使用物件"></a>搭配函式使用物件</h2><p>延續上方當我們建立好函式後，可以搭配函式來使用建立好的物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Tony = &#123;</span><br><span class="line">  firstname: <span class="string">&#x27;Tony&#x27;</span>,</span><br><span class="line">  lastname: <span class="string">&#x27;Alicea&#x27;</span>,</span><br><span class="line">  address: &#123;</span><br><span class="line">    street: <span class="string">&#x27;111 Main St.&#x27;</span>,</span><br><span class="line">    city: <span class="string">&#x27;New York&#x27;</span>,</span><br><span class="line">    state: <span class="string">&#x27;NY&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params">person</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Hi&#x27;</span> + person.firstname);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">greet(Tony);   <span class="comment">// Hi Tony</span></span><br></pre></td></tr></table></figure>

<p>我們也可以透過呼叫函式時建立物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">greet(&#123;</span><br><span class="line">  firstname: <span class="string">&#x27;Mary&#x27;</span>,</span><br><span class="line">  lastname: <span class="string">&#x27;Doe&#x27;</span>&#125;</span><br><span class="line">);   <span class="comment">// Hi Mary</span></span><br></pre></td></tr></table></figure>

<h2 id="補充"><a href="#補充" class="headerlink" title="補充"></a>補充</h2><p>無論是使用 <code>new</code> 關鍵字或大括號 {} 來建立物件，對於 JavaScript 底層來說都是一樣的，本質上都是建立物件到記憶體中，並建立該物件的屬性 (Property) 和方法 (Method) 在記憶體中。</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
      <tags>
        <tag>物件與函式</tag>
      </tags>
  </entry>
  <entry>
    <title>物件與點 Objects and the Dot</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211020/22552/</url>
    <content><![CDATA[<p>這篇文章中我們將會探討什麼是 JavaScript 中的<code>物件 (Object)</code></p>
<span id="more"></span>

<h2 id="什麼是物件-Object"><a href="#什麼是物件-Object" class="headerlink" title="什麼是物件 Object ?"></a>什麼是物件 Object ?</h2><p>在 JavaScript 中，物件其實就是一對<code>名稱/值的配對 (key value pairs)</code>，而直又可以是另一組名稱/值的配對</p>
<p><img src="key-value-pairs.PNG"></p>
<p>物件的值又可以方為以下三種:</p>
<ul>
<li>純值 Primitive<br>第一種是原生的值，像是布林值 (Boolean)、數值 (Number) 和字串 (String)，在物件當中，我們把這類的職稱為物件的屬性 (Property)</li>
<li>物件 Object<br>第二種是物件 (Object)，沒有錯，就像一開始說的物件中也可以再放入物件，也就是另一組名稱/值的配對，就像是孩子一樣，這一類的值我們也稱它會為物件的屬性 (Property</li>
<li>函數 Function<br>第三種是值是函數，在物件中的函數我們會將它稱為物件的方法 (Method)</li>
</ul>
<p>當 JavaScript 宣告一個物件時，這個物件會在記憶體中佔有一個位置，物件中的屬性 (Property) 和方法 (Method) 也會，而物件會記錄其屬性和方法的位置使其可以參考到，這些屬性和方法的位置彼此可能有關也可能無關</p>
<h2 id="建立物件與屬性"><a href="#建立物件與屬性" class="headerlink" title="建立物件與屬性"></a>建立物件與屬性</h2><h3 id="方法一-中括號-Bracket"><a href="#方法一-中括號-Bracket" class="headerlink" title="方法一: 中括號 [] (Bracket)"></a>方法一: 中括號 [] (Bracket)</h3><p>首先透過 <code>new</code> 這個關鍵字來建立一個物件，並透過中括號 [] (Bracket) 來新增物件的屬性，而中括號其實也是一個運算子，我們可以透過它使物件參考到對應屬性的值</p>
<p><img src="Bracket.PNG"></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = <span class="keyword">new</span> <span class="built_in">Object</span>();</span><br><span class="line"></span><br><span class="line">person[<span class="string">&#x27;firstname&#x27;</span>] = <span class="string">&#x27;Tony&#x27;</span>;</span><br><span class="line">person[<span class="string">&#x27;lastname&#x27;</span>] = <span class="string">&#x27;Alicea&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>這個運算子好用的地方是也可透過變數來儲存物件屬性的名稱</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> firstNameProperty = <span class="string">&#x27;firstname&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(person[firstNameProperty]);   <span class="comment">// Tony</span></span><br></pre></td></tr></table></figure>

<h3 id="方法二-點-Dot"><a href="#方法二-點-Dot" class="headerlink" title="方法二: 點 . (Dot)"></a>方法二: 點 . (Dot)</h3><p>第二種方法更常見於開發，當我們透過點 .(Dot) 來指定物件的屬性時，可以省略引號直接當成字串搜尋物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(person.firstname);   <span class="comment">// Tony</span></span><br><span class="line"><span class="built_in">console</span>.log(person.lastname);    <span class="comment">// Alicea</span></span><br></pre></td></tr></table></figure>

<p>當然我們也可以透過點 . 來新增一個值為物件的屬性，且可以不斷新增其子屬性</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">person.address = <span class="keyword">new</span> <span class="built_in">Object</span>();</span><br><span class="line">person.address.street = <span class="string">&#x27;111 Main St.&#x27;</span>;</span><br><span class="line">person.address.city = <span class="string">&#x27;New York&#x27;</span>;</span><br><span class="line">person.address.state = <span class="string">&#x27;NY&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(person.address.street);   <span class="comment">// 111 Main St.</span></span><br><span class="line"><span class="built_in">console</span>.log(person.address.city);     <span class="comment">// New York</span></span><br><span class="line"><span class="built_in">console</span>.log(person.address.state);    <span class="comment">// NY</span></span><br></pre></td></tr></table></figure>

<h2 id="補充"><a href="#補充" class="headerlink" title="補充"></a>補充</h2><p><a href="https://www.udemy.com/user/anthonypalicea/">作者</a> 最後也提到，雖然中括號 [] (Bracket) 和點 . (Dot) 都可以建立、讀取物件屬性，但一般來說會建議使用點 . 來建立，它可以使程式碼更簡潔、更好除錯，除非需要使用動態字串來取得屬性。<br>另外<a href="https://www.udemy.com/user/anthonypalicea/">作者</a> 也提到，使用 <code>new</code> 關鍵字來建立物件並不是一個好方法，這在之後的課程會再介紹。</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
      <tags>
        <tag>物件與函式</tag>
      </tags>
  </entry>
  <entry>
    <title>函數就是物件</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211207/50804/</url>
    <content><![CDATA[<p>在 JavaScript 中，函數就是物件的一種，以下針對函數先有一個基礎的介紹</p>
<span id="more"></span>

<h3 id="函數就是物件"><a href="#函數就是物件" class="headerlink" title="函數就是物件"></a>函數就是物件</h3><p>函數就是物件的一種，所以所有可以對物件做的事都可以對函數做</p>
<ul>
<li>將函數指派為一個變數的值</li>
<li>將函數當作參數傳入另外一個函數</li>
<li>函數也可以有屬性和方法</li>
</ul>
<h3 id="函數特有屬性"><a href="#函數特有屬性" class="headerlink" title="函數特有屬性"></a>函數特有屬性</h3><p>另外有幾個特性是函數特有的</p>
<ul>
<li>Name<br>函數不一定要有名稱，沒有名稱的函數就稱為匿名函數 Anonymous Function</li>
<li>Code<br>函數中你所寫的程式是函數的屬性之一，但函數還有其他屬性</li>
</ul>
<h3 id="函數範例"><a href="#函數範例" class="headerlink" title="函數範例"></a>函數範例</h3><p>黨我們建立一個函數且追加一個屬性，並透過 <code>console.log</code> 印出時，只會印出函數內容，但呼叫屬性時還是可以取得先前建立的值，因為函數就是物件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;hi&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">greet.language = <span class="string">&#x27;english&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(greet);   <span class="comment">// function greet() &#123;</span></span><br><span class="line">                        <span class="comment">// console.log(&#x27;hi&#x27;);</span></span><br><span class="line">                      <span class="comment">//&#125;</span></span><br><span class="line"><span class="built_in">console</span>.log(greet.language);   <span class="comment">// english</span></span><br></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>傳值與傳參考</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211212/27832/</url>
    <content><![CDATA[<!-- @format -->

<p>這堂課將介紹在 JavaScript 一個很重要的觀念，傳值 <code>pass by value</code> 和傳參考 (傳址) <code>pass by referece</code></p>
<span id="more"></span>

<h3 id="傳值-By-Value"><a href="#傳值-By-Value" class="headerlink" title="傳值 By Value"></a>傳值 By Value</h3><p>一段簡單的程式碼如下，首先，先宣告一個變數 <code>a</code> 並給它一個 <code> primitive value</code> 3，這時候 <code>a</code> 會存在記憶體中的一個位置 <code>0x001</code> 。接著宣告一個 <code>b = a</code>，<code>b</code> 實際上會建立另一個記憶體位置 <code>0x002</code>，兩者的值並不會互相干擾，這種情況我們就稱為傳值 <code>pass by value</code>，這種情況只會發生在值為 <code>primitive type</code> 的變數上。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">3</span>;</span><br><span class="line"><span class="keyword">var</span> b;</span><br><span class="line"></span><br><span class="line">b = a;</span><br><span class="line">a = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(a); <span class="comment">// 2</span></span><br><span class="line"><span class="built_in">console</span>.log(b); <span class="comment">// 3</span></span><br></pre></td></tr></table></figure>

<p><img src="By-Value.PNG"></p>
<h3 id="傳址-傳參考-By-Reference"><a href="#傳址-傳參考-By-Reference" class="headerlink" title="傳址(傳參考) By Reference"></a>傳址(傳參考) By Reference</h3><p>相對的，如果宣告一個變數 <code>a</code> 並給它一個型別為物件 (Function 也是) 的值，同樣會在記憶體中給它一個位置 <code>0x001</code> ;但當我們建立另一個變數 <code>b</code> ，並把 <code>b</code> 的值等同於 <code>a</code>，實際上不會在記憶體中再給它一個位置，變數 <code>a</code> 和 <code>b</code> 都會指向相同的位置 <code>0x001</code> ，所以當 <code>a</code> 的值給便時也會影響到 <code>b</code>，這種情況我們就稱為傳址 (傳參考) <code>pass by reference</code>。</p>
<p><img src="By-Reference.PNG"></p>
<p>一段簡單的程式碼如下，當我們建立一個變數 <code>c</code> 並給它一個物件 <code>object</code> 的值，再建立一個 <code>d</code> 並讓它等於 <code>c</code>，接著更改 <code>mutate</code> <code>c</code> 的值時，<code>d</code> 也會跟著改變; 甚至我們都過一個函式來更改 <code>c</code> 的值時，<code>d</code> 也會跟著改變</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> c = &#123; <span class="attr">greeting</span>: <span class="string">&quot;Hello&quot;</span> &#125;;</span><br><span class="line"><span class="keyword">var</span> d;</span><br><span class="line"></span><br><span class="line">d = c;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(c); <span class="comment">// &#123; greeting: &quot;Hello&quot; &#125;</span></span><br><span class="line"><span class="built_in">console</span>.log(d); <span class="comment">// &#123; greeting: &quot;Hello&quot; &#125;</span></span><br><span class="line"></span><br><span class="line">c.greeting = <span class="string">&quot;Hola&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(c); <span class="comment">// &#123; greeting: &quot;Hola&quot; &#125;</span></span><br><span class="line"><span class="built_in">console</span>.log(d); <span class="comment">// &#123; greeting: &quot;Hola&quot; &#125;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">changeGreeting</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  obj.greeting = <span class="string">&quot;Hi&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">changeGreeting(c);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(c); <span class="comment">// &#123; greeting: &quot;Hi&quot; &#125;</span></span><br><span class="line"><span class="built_in">console</span>.log(d); <span class="comment">// &#123; greeting: &quot;Hi&quot; &#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="例外"><a href="#例外" class="headerlink" title="例外"></a>例外</h3><p>前面提到如果建立變數 <code>c</code> 並給予一個 <code>object</code> 的值，並新增另一個變數<code>d</code> 等於 <code>c</code>， 就會透過 <code>pass by reference</code> 的方式指向記憶體中的這個位置，但當我們透過 <code>object literal</code> 的方式重新賦予這個變數另外一個 <code>object</code> 的值時，JavaScript 會在記憶體中新增一個位置來存放這個新的物件，這時候 <code>c</code> 和 <code>d</code> 就會分別指向記憶體中不同的 <code>object</code>。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> c = &#123; <span class="attr">greeting</span>: <span class="string">&quot;Hello&quot;</span> &#125;;</span><br><span class="line"><span class="keyword">var</span> d;</span><br><span class="line"></span><br><span class="line">d = c;</span><br><span class="line"></span><br><span class="line">c.greeting = &#123; <span class="attr">gretting</span>: <span class="string">&quot;Hi&quot;</span> &#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(c); <span class="comment">// &#123; greeting: &quot;Hi&quot; &#125;</span></span><br><span class="line"><span class="built_in">console</span>.log(d); <span class="comment">// &#123; greeting: &quot;Hello&quot; &#125;</span></span><br></pre></td></tr></table></figure>

<h2 id="資料來源"><a href="#資料來源" class="headerlink" title="資料來源"></a>資料來源</h2><p><a href="https://www.udemy.com/course/understand-javascript/">Udemy-JavaScript: Understanding the Weird Parts</a></p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>函數的陳述句與表達式的用法</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211212/33122/</url>
    <content><![CDATA[<!-- @format -->

<p>在這堂課中，介紹了函數陳述式 <code>Function Statement</code> 和函數表達式 <code>Function Expressions</code> 兩種函數的建立方式</p>
<span id="more"></span>

<h2 id="表達式-Expressions-和-陳述句-Statement-的差異"><a href="#表達式-Expressions-和-陳述句-Statement-的差異" class="headerlink" title="表達式 Expressions 和 陳述句 Statement 的差異"></a>表達式 Expressions 和 陳述句 Statement 的差異</h2><h3 id="表達式-Expressions"><a href="#表達式-Expressions" class="headerlink" title="表達式 Expressions"></a>表達式 Expressions</h3><p><code>Expressions</code> 指的是會回傳值的一串程式，這個值可以是任何型別如字串、數字或物件，而這個值可以將它存成一個變數也可以不用。</p>
<p>如下在瀏覽器的 <code>console</code> 中輸入 <code>a = 1 + 2</code> 時會回傳 <code>3</code>，直接輸入 <code>1 + 2</code> 也會回傳 <code>3</code>;輸入 <code>a = &#123; greet: &#39;Hello&#39; &#125;</code> 時就會回傳物件</p>
<p><img src="Expressions.PNG"></p>
<h3 id="陳述句-Statement"><a href="#陳述句-Statement" class="headerlink" title="陳述句 Statement"></a>陳述句 Statement</h3><p>相對的，不會回傳值的就是一個陳述句，如下程式碼:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (a === <span class="number">3</span>) &#123;</span><br><span class="line">  <span class="comment">// do something</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>這段程式碼中因為 <code>a === 3</code> 是一個陳述句，因為會回傳 <code>true</code> 或 <code>false</code>; 而 <code>if</code> 這個指令是一個陳述句，因為不會回傳一個值，所以我們也不能將他指定給一個變數</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 錯誤寫法</span></span><br><span class="line"><span class="keyword">var</span> b = <span class="keyword">if</span> (a === <span class="number">3</span>) &#123;</span><br><span class="line">  <span class="comment">// do something</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="函數表達式-Function-Expressions-和-函數陳述句-Function-Statements"><a href="#函數表達式-Function-Expressions-和-函數陳述句-Function-Statements" class="headerlink" title="函數表達式 Function Expressions 和 函數陳述句 Function Statements"></a>函數表達式 Function Expressions 和 函數陳述句 Function Statements</h2><p>在 JavaScript 中，函數是一個物件，可以透過 Expressions 和 Statements 的方式建立</p>
<h3 id="函數表達式-Function-Expressions"><a href="#函數表達式-Function-Expressions" class="headerlink" title="函數表達式 Function Expressions"></a>函數表達式 Function Expressions</h3><p>一個最基本的 Function Expressions 如下</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&quot;Hi&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Function Statements 的特色在於，他不會回傳一個值，且在程式一開始就會透過 hoisting 先儲存在記憶體中，所以可以在這段程式碼前就呼叫它而不會出錯</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">greet();</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&quot;Hi&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在 JavaScript 中函數就是一個物件，透過物件的方式理解函數的話，這個函數會有一個屬性 <code>name</code> 的值為 <code>greet</code>和一個屬性 <code>code</code> 的值為 <code>console.log(&#39;Hi&#39;)</code></p>
<p><img src="Function-Statements.PNG"></p>
<h3 id="函數陳述句-Function-Statements"><a href="#函數陳述句-Function-Statements" class="headerlink" title="函數陳述句 Function Statements"></a>函數陳述句 Function Statements</h3><p>在 JavaScript 中，函數就是物件的一種，所以可以將它儲存在一個變數中</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> anonymousGreet = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&quot;hi&quot;</span>);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>如上程式碼，<code>function() &#123; console.log(&#39;hi&#39;) &#125;</code> 就是一個 <code>Function Expressions</code> ，我們可以將這個函數表達式存在 <code>anonymousGreet</code> 這個變數之中; 這裡可以發現，因為我們已將函數存在變數之中，所以這個函數不需要 <code>name</code>， 因為透過這個變數我們就知道這個函數在記憶體中的位置了，而這也就式匿名函數 <code>Anonymous Function </code></p>
<p><img src="Function-Expression.PNG"></p>
<p>與 <code>Function Statement</code> 不同的是，<code>Function Expressions</code> 將函數指定給一個變數，在 JavaScript 一開始執行的時候，並不會被 <code>hoisting</code>，只有變數的名稱會被存在記憶體中，而變數的預設值為 <code>undefined</code>，所以在 <code>Function Expressions</code> 前呼叫它就如同呼叫一個 <code>undefined</code></p>
<p><img src="Function-Expressions-Invoked.PNG"></p>
<h2 id="函式中的函式"><a href="#函式中的函式" class="headerlink" title="函式中的函式"></a>函式中的函式</h2><p>如下程式碼，我們將不同型別的值帶入函數中並執行</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">log</span>(<span class="params">a</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(a);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">log(<span class="number">3</span>); <span class="comment">// 3</span></span><br><span class="line">log(<span class="string">&quot;Hello&quot;</span>); <span class="comment">// Hello</span></span><br><span class="line">log(&#123; <span class="attr">Greet</span>: <span class="string">&quot;Hi&quot;</span> &#125;); <span class="comment">// &#123;Greet: &#x27;Hi&#x27;&#125;</span></span><br></pre></td></tr></table></figure>

<p>當然我們也可以將這些值預先透過變數宣告，再指定到函式當中，也會得得到一樣的結果:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">log</span>(<span class="params">a</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(a);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> number = <span class="number">3</span>;</span><br><span class="line">log(number); <span class="comment">// 3</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> string = <span class="string">&quot;Hello&quot;</span>;</span><br><span class="line">log(<span class="string">&quot;Hello&quot;</span>); <span class="comment">// Hello</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> object = &#123; <span class="attr">Greet</span>: <span class="string">&quot;Hi&quot;</span> &#125;;</span><br><span class="line">log(&#123; <span class="attr">Greet</span>: <span class="string">&quot;Hi&quot;</span> &#125;); <span class="comment">// &#123;Greet: &#x27;Hi&#x27;&#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="將函式帶入函式"><a href="#將函式帶入函式" class="headerlink" title="將函式帶入函式"></a>將函式帶入函式</h3><p>如果將一個匿名函式 <code>Anonymous Function</code> 作為變數帶入函式之中，這時就如同創造了一個函式來使用 <code>Create the function on the fly</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">log</span>(<span class="params">a</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(a);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">log(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&quot;Hi&quot;</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>這時候就會將整個函式回傳</p>
<p><img src="Function-in-Function.PNG"></p>
<p>如果要執行傳入的函式，只要將原先的 <code>log</code> 函式稍做更改，呼叫傳入的變數，呼應到 JavaScript 非同步的特性，藉此可以確保函式執行的順序，這也是開發上和許多套件中會使用到回呼函式 <code>callback function</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">log</span>(<span class="params">a</span>) </span>&#123;</span><br><span class="line">  a();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">log(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&quot;Hi&quot;</span>); <span class="comment">// Hi</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="資料來源"><a href="#資料來源" class="headerlink" title="資料來源"></a>資料來源</h2><p><a href="https://www.udemy.com/course/understand-javascript/">Udemy-JavaScript: Understanding the Weird Parts</a>`</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>物件、函數與this</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211213/23220/</url>
    <content><![CDATA[<p>這堂課將介紹時常困擾初學 JavaScript 所遇到的一個名詞 - <code>this</code></p>
<span id="more"></span>

<h2 id="Global-Object-Window-物件"><a href="#Global-Object-Window-物件" class="headerlink" title="Global Object - Window 物件"></a>Global Object - Window 物件</h2><p>首先，我們直接透過以下三個例子來印出 <code>this</code>，分別是全域環境、函式 <code>a</code> 函式陳述式 <code>Function Statement</code>和函式 <code>b</code> 函式表達式 <code>Function Expression</code>，在執行這三段程式碼時，會分別產生三個執行階段 <code>Execution Context</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="built_in">this</span>);   <span class="comment">// Window</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> b = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">a();   <span class="comment">// Window</span></span><br><span class="line">b();   <span class="comment">// Window</span></span><br></pre></td></tr></table></figure>

<p>會發現三者的結果都是全域環境中的 <code>Global Object - Window</code>，這也代表我們可以透過 <code>Function Statement</code> 和 <code>Function Expression</code> 來建立全域變數 <code>Global Variable</code>，而痊癒變數可以透過直接呼叫變數名稱來使用，不需要透過 . <code>dot-notation</code> 來指稱。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line">  <span class="built_in">this</span>.newvariable = <span class="string">&#x27;hello&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> b = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">a();</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(newvariable);</span><br></pre></td></tr></table></figure>

<h2 id="this-in-object-物件中的-this"><a href="#this-in-object-物件中的-this" class="headerlink" title="this in object 物件中的 this"></a>this in object 物件中的 this</h2><p>在 JavaScript 的物件中，如果值的型別是 <code>Primitive Type</code> (物件以外)，我們就稱為物件的屬性 <code>property</code>;而如果是一個函式 <code>Funciton</code>，我們就會稱為物件的方法 <code>method</code>，以下透過物件的 <code>method</code> 來呼叫 <code>this</code>:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> c = &#123;</span><br><span class="line">  name: <span class="string">&#x27;The c object&#x27;</span>,</span><br><span class="line">  log: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">c.log();</span><br></pre></td></tr></table></figure>

<p>會發現這次的 <code>this</code> 指向 <code>c</code> 這個 <code>object</code> 本身</p>
<p><img src="this-in-object.PNG"></p>
<h2 id="JavaScripg-中-this-的問題"><a href="#JavaScripg-中-this-的問題" class="headerlink" title="JavaScripg 中 this 的問題"></a>JavaScripg 中 this 的問題</h2><p>延續上方的範例，當我們在物件中的 <code>method</code> 再新增一個 <code>setname</code> 函式並呼叫 <code>this</code> 來更改物件的 <code>property</code>，且在這個函式的前後印出 <code>this</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> c = &#123;</span><br><span class="line">  name: <span class="string">&#x27;The c object&#x27;</span>,</span><br><span class="line">  log: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">this</span>.name = <span class="string">&#x27;Updated c object&#x27;</span>;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">var</span> setname = <span class="function"><span class="keyword">function</span>(<span class="params">newname</span>) </span>&#123;</span><br><span class="line">      <span class="built_in">this</span>.name = newname;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    setname(<span class="string">&#x27;Updated again!&#x27;</span>);</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">c.log();</span><br></pre></td></tr></table></figure>

<p>會發現即使 <code>setname</code> 函式前後的 <code>this</code> 都指向 <code>c</code> 這個物件本身</p>
<p><img src="change-property.PNG"></p>
<p>但 <code>c</code> 的 <code>property</code> 並沒有再次被改變，而是將 <code>this.name</code> 新增在全域物件 <code>Window</code> 上</p>
<p><img src="property-in-window.PNG"></p>
<p>這被許多人認為是 JavaScript 中的一個問題</p>
<h3 id="解決-this-指向-Window"><a href="#解決-this-指向-Window" class="headerlink" title="解決 this 指向 Window"></a>解決 this 指向 Window</h3><p>所以在一般開發時，為了解決這個 <code>this</code> 在物件中卻指向全域物件 <code>Window</code> 的問題，我們會透過一個變數先將 <code>this</code> 存起來，當內部函式要使用而沒有定義這個變數名稱時，就會透過 <code>scoped chain</code> 一層一層往外找這個變數名稱，就會找到前面預先存起來的 <code>this</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> c = &#123;</span><br><span class="line">  name: <span class="string">&#x27;The c object&#x27;</span>,</span><br><span class="line">  log: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">this</span>);</span><br><span class="line">    <span class="keyword">var</span> self = <span class="built_in">this</span>;</span><br><span class="line"></span><br><span class="line">    self.name = <span class="string">&#x27;Updated c object&#x27;</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">var</span> setname = <span class="function"><span class="keyword">function</span>(<span class="params">newname</span>) </span>&#123;</span><br><span class="line">      self.name = newname;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    setname(<span class="string">&#x27;Updated again!&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">c.log();</span><br></pre></td></tr></table></figure>

<h2 id="資料來源"><a href="#資料來源" class="headerlink" title="資料來源"></a>資料來源</h2><p><a href="https://www.udemy.com/course/understand-javascript/">Udemy-JavaScript: Understanding the Weird Parts</a></p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>arguments 與 spread</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211215/63221/</url>
    <content><![CDATA[<p>這堂課將會介紹在 JavaScript 中，當 <code>Function</code> 被建立時，會自動產生的關鍵字 <code>Arguemnts</code></p>
<span id="more"></span>

<p>當我們執行一個 <code>function</code> 時，執行環境 <code>Execution Context</code> 會被建立，會產生一個變數環境 <code>Variable Environment</code> 來幫我們保存變數，和一個外部外部環境 <code>Outer Evvironment</code> 來判斷 <code>Scoped Chain</code>; 接著會產生 <code>this</code> 依據函式的呼叫方式而指向不同的 <code>object</code>; 最後一個關鍵字則是這堂課的主題 <code>arguments</code></p>
<p><img src="Execution-Context-is-Created.png"></p>
<p>當我們建立一個函式並呼叫它時，在 JavaScript 中，即使位給予預期的參數，也不會出錯，只會在用到參數時回傳 <code>undefined</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params">firstname, lastname, language</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(firstname);</span><br><span class="line">  <span class="built_in">console</span>.log(lastname);</span><br><span class="line">  <span class="built_in">console</span>.log(language);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">greet();   <span class="comment">// undeined undeined undeined</span></span><br></pre></td></tr></table></figure>

<p>當然我們也可以透過修改條件判斷是否有參數傳入而給予參數一個預設值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params">firstname, lastname, language</span>) </span>&#123;</span><br><span class="line">  language = language || <span class="string">&#x27;en&#x27;</span>;</span><br><span class="line">  <span class="built_in">console</span>.log(fistname);</span><br><span class="line">  <span class="built_in">console</span>.log(lastname);</span><br><span class="line">  <span class="built_in">console</span>.log(language);</span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&#x27;John&#x27;</span>);   <span class="comment">// John undefined en</span></span><br></pre></td></tr></table></figure>

<p>除了主動設定的參數外，<code>JavaScript</code> 會自動幫我們建立一個名為 <code>arguments</code> 的 <code>array-like</code> 關鍵字來包含所有傳入的參數，它看起來像是一個 <code>Array</code>，但又不包含 <code>Array</code> 的所有特性;如下範例，我們可以和 <code>Array</code> 一樣透過 <code>length</code> 來判斷是否為空，也可以透過 [] <code>brakets operator</code> 來取得 <code>arguments</code> 中的值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params">firstname, lastname, language</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">arguments</span>.length === <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">&#x27;Missing parameters !&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">console</span>.log(fistname);</span><br><span class="line">  <span class="built_in">console</span>.log(lastname);</span><br><span class="line">  <span class="built_in">console</span>.log(language);</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>);</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>[<span class="number">0</span>]);</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;-----&#x27;</span>);</span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line">greet();</span><br><span class="line"><span class="comment">// Missing parameters !</span></span><br><span class="line"><span class="comment">// undefined</span></span><br><span class="line"><span class="comment">// undefined</span></span><br><span class="line"><span class="comment">// undefined</span></span><br><span class="line"><span class="comment">// []</span></span><br><span class="line"><span class="comment">// undefined</span></span><br><span class="line"><span class="comment">// -----</span></span><br><span class="line">greet(<span class="string">&#x27;John&#x27;</span>, <span class="string">&#x27;Doe&#x27;</span>, <span class="string">&#x27;en&#x27;</span>);</span><br><span class="line"><span class="comment">// John</span></span><br><span class="line"><span class="comment">// Doe</span></span><br><span class="line"><span class="comment">// en</span></span><br><span class="line"><span class="comment">// [&#x27;John&#x27;, &#x27;Doe&#x27;, &#x27;en&#x27;]</span></span><br><span class="line"><span class="comment">// John</span></span><br><span class="line"><span class="comment">// -----</span></span><br></pre></td></tr></table></figure>

<h2 id="展開運算子-spread-…"><a href="#展開運算子-spread-…" class="headerlink" title="展開運算子 spread (…)"></a>展開運算子 spread (…)</h2><p>在 ES6 中有一個提供了一個新的展開運算子 <code>spread</code>(…)，它可以將後續傳入的所以參數集結成一個新的變數</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params">firstname, lastname, language, ...other</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(firstname);</span><br><span class="line">  <span class="built_in">console</span>.log(lastname);</span><br><span class="line">  <span class="built_in">console</span>.log(language);</span><br><span class="line">  <span class="built_in">console</span>.log(other);</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>);</span><br><span class="line">&#125;</span><br><span class="line">greet(<span class="string">&#x27;John&#x27;</span>, <span class="string">&#x27;Doe&#x27;</span>, <span class="string">&#x27;en&#x27;</span>, <span class="string">&#x27;St.&#x27;</span>, <span class="string">&#x27;Taiwan&#x27;</span>);</span><br><span class="line"><span class="comment">// John</span></span><br><span class="line"><span class="comment">// Doe</span></span><br><span class="line"><span class="comment">// en</span></span><br><span class="line"><span class="comment">// [&#x27;St.&#x27;, &#x27;Taiwan&#x27;]</span></span><br><span class="line"><span class="comment">// [&#x27;John&#x27;, &#x27;Doe&#x27;, &#x27;en&#x27;, &#x27;St.&#x27;, &#x27;Taiwan&#x27;]</span></span><br></pre></td></tr></table></figure>

<h2 id="資料來源"><a href="#資料來源" class="headerlink" title="資料來源"></a>資料來源</h2><p><a href="https://www.udemy.com/course/understand-javascript/">Udemy-JavaScript: Understanding the Weird Parts</a></p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>陣列-任何東西的集合</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211215/29/</url>
    <content><![CDATA[<p>這堂課將介紹 Array 的建立</p>
<span id="more"></span>

<h2 id="Array-建立"><a href="#Array-建立" class="headerlink" title="Array 建立"></a>Array 建立</h2><p>在 JavaScript 建立 <code>Array</code> 最簡單得方式就是透過 <code>Array literal syntax</code> 的方式使用中括號 <code>[]</code> 來建立取得資料的索引是從 <code>0</code> 開始依序往後遞增。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">2.</span> <span class="number">3</span>];</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(arr[<span class="number">0</span>]);   <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log(arr[<span class="number">1</span>]);   <span class="comment">// 2</span></span><br></pre></td></tr></table></figure>

<p>比較特別的是，不向其他語言一樣 Array 有型別的規範，JavaScript 是一個動態型別 <code>Dynamically Typed</code>，<code>Array</code> 中的值可以為所有不同型別</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [</span><br><span class="line">  <span class="number">1.</span></span><br><span class="line">  <span class="literal">false</span>,</span><br><span class="line">  &#123;</span><br><span class="line">    name: <span class="string">&#x27;Tony&#x27;</span>,</span><br><span class="line">    address: <span class="string">&#x27;111 Main St.&#x27;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> greeting = <span class="string">&#x27;Hello&#x27;</span>;</span><br><span class="line">    <span class="built_in">console</span>.log(greeting + name);</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">&#x27;Hello&#x27;</span></span><br><span class="line">];</span><br></pre></td></tr></table></figure>

<p>因為 <code>Array</code> 可以透過索引 <code>index</code> 取得內部的值，所以在 <code>Function</code> 的建立可以直接使用匿名函式 <code>Anonymous Function</code>，甚至可以使用其中的其他值帶入函式來使用</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">arr[<span class="number">4</span>](arr[<span class="number">3</span>].name);   <span class="comment">// Hello Tony</span></span><br><span class="line"></span><br><span class="line">## 資料來源</span><br><span class="line"></span><br><span class="line">[Udemy-JavaScript: Understanding the Weird Parts](https:<span class="comment">//www.udemy.com/course/understand-javascript/)</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="Arguments"><a href="#Arguments" class="headerlink" title="Arguments"></a>Arguments</h2><p>如同 <code>this</code> 一樣，在一個 <code>Function</code> 建立時，<code>Arguments</code> 也會自動被建立，並包含傳進函式的所有變數，</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>了解閉包 Closures (二)</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211218/40201/</url>
    <content><![CDATA[<!-- @format -->

<p>有了上一堂課對於 <code>Closures</code> 的基礎概念後，這堂課將透過一個經典的例子進一步瞭解 <code>Closures</code></p>
<span id="more"></span>

<p>讓我們直看到程式碼範例:</p>
<h2 id="範例一"><a href="#範例一" class="headerlink" title="範例一"></a>範例一</h2><p>首先建立一個 <code>buildFunction</code> ，並在其中透過 <code>for</code> 迴圈把 <code>function</code> 加到 <code>array</code> 中，再透過 <code>return</code> 回傳，最後依序呼叫 <code>array</code> 中的 <code>function</code>，在這裡，我們是不是預期三個 <code>function</code> 依序回傳 <code>0~2</code> 呢 ?</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">buildFunction</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> arr = [];</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">    arr.push(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(i);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> fs = buildFunction();</span><br><span class="line"></span><br><span class="line">fs[<span class="number">0</span>](); <span class="comment">// 0 ?</span></span><br><span class="line">fs[<span class="number">1</span>](); <span class="comment">// 1 ?</span></span><br><span class="line">fs[<span class="number">2</span>](); <span class="comment">// 2 ?</span></span><br></pre></td></tr></table></figure>

<p>當我們透過 <code>console</code> 看到結果會發現，三個 <code>function</code> 的印出來的都是 <code>3</code>，讓我依序解析程式碼來了解發生了什麼事</p>
<p>首先當 <code>JavaScript Engine</code> 啟動時，會建立一個 <code>Global Execution Context</code>，接著當我們一開始建立的 <code>biildFunction</code> 被呼叫 <code>invoked</code> 時，開始執行該函式，當執行到 <code>for</code> 迴圈時，依序從 <code>i = 0~2</code> 時將 <code>function</code> 加入 <code>arr</code> 中，直到 <code>i = 3</code> ，因不符合當初設立的條件 <code>(i &lt; 3)</code>，所以跳出迴圈，最後 <code>return arr</code> 這個陣列變數</p>
<p><img src="BuildFunction-Invoked.png"></p>
<p>接著函式執行在 <code>return</code> 後執行完畢，結束這個 <code>Execution Context</code> 跳出 <code>Execution Stack</code>，根據我們上一堂課所學到的，會留下最後所建立的 <code>i = 3</code> 和 <code>arr</code> 變數在其記憶體中的位置</p>
<p><img src="buildFunction-pop-off.png"></p>
<p>在這裡我們可以先透過 <code>console.log()</code> 來看一下 <code>fs</code> 中三個 <code>function </code>的值，會看到都是一樣的 <code>function</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(fs[<span class="number">0</span>]); <span class="comment">// function() &#123;console.log(i);&#125;</span></span><br></pre></td></tr></table></figure>

<p>當我們回到 <code>Global Execution Context</code> 逐行執行程式碼時，執行到陣列中第一個函式 <code>fs[0]()</code>，新的 <code>Execution Context</code> 產生，<code>console.log(i);</code> 中的變數 <code>i</code> 會因為沒有在這個 <code>Execution Context</code> 宣告，所以透過 <code>Scoped Chain</code> 往它的 <code>Outer Environment</code> 尋找，於是在 <code>Lexical Enviroment</code> 找到 <code>i = 3</code>，因此呼叫出 <code>3</code> 這個結果，同理後續兩個函式也會按照一樣的方式執行</p>
<p><img src="First-Function-Invoked.png"></p>
<h2 id="範例二"><a href="#範例二" class="headerlink" title="範例二"></a>範例二</h2><p>那我們該如何避免範例一中，印出所有都是 <code>3</code> 的相同結果呢 ?</p>
<p>首先我們先建立一個相同內容的 <code>biildFunctions2</code> 來理解一下，在執行 <code>buildFunctions</code> 中的 <code>for</code> 迴圈時，我們只是將 <code>function</code> 加到 <code>array</code> 中，並沒有執行這個 <code>function</code>，所以才不會保留當下 <code>i</code> 的值，而是直到之後這個 <code>function</code> 被呼叫時，才會透過 <code>Scoped Chain</code> 找到被保存在記憶體中的 <code>i</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">buildFunctions2</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> arr = [];</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">    arr.push(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(i);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> fs = buildFunctions2();</span><br><span class="line">fs[<span class="number">0</span>](); <span class="comment">// 3</span></span><br><span class="line">fs[<span class="number">1</span>](); <span class="comment">// 3</span></span><br><span class="line">fs[<span class="number">2</span>](); <span class="comment">// 3</span></span><br></pre></td></tr></table></figure>

<p>為了保留當下的 <code>i</code> ，我們有兩個方法可以達到這個目的</p>
<h3 id="方法一-let"><a href="#方法一-let" class="headerlink" title="方法一: let"></a>方法一: let</h3><p>透過 <code>ES6</code> 中的關鍵字 <code>let</code>，可以讓每次跑的迴圈都建立到一個新的記憶體位致中，當函是呼叫執行時，就會指向不同的位置，所以可以得到預期 <code>0~2</code> 的結果</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">buildFunctions2</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> arr = [];</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">let</span> j = i;</span><br><span class="line">    arr.push(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(j);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> fs = buildFunctions2();</span><br><span class="line">fs[<span class="number">0</span>](); <span class="comment">// 0</span></span><br><span class="line">fs[<span class="number">1</span>](); <span class="comment">// 1</span></span><br><span class="line">fs[<span class="number">2</span>](); <span class="comment">// 2</span></span><br></pre></td></tr></table></figure>

<p>其實也可以直接將 <code>for</code> 迴圈一開始所建立的變數 <code>i</code> 透過 <code>let</code> 來建立，也會透過相同的方式建立在記憶體中不同的位置</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">buildFunctions2</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> arr = [];</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">    arr.push(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(i);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> fs = buildFunctions2();</span><br><span class="line">fs[<span class="number">0</span>](); <span class="comment">// 0</span></span><br><span class="line">fs[<span class="number">1</span>](); <span class="comment">// 1</span></span><br><span class="line">fs[<span class="number">2</span>](); <span class="comment">// 2</span></span><br></pre></td></tr></table></figure>

<h3 id="方法二-加入陣列中即執行函式"><a href="#方法二-加入陣列中即執行函式" class="headerlink" title="方法二: 加入陣列中即執行函式"></a>方法二: 加入陣列中即執行函式</h3><p>如果在不支援 <code>ES6</code> 的環境下，我們可以在把 <code>function</code> 加入到 <code>array</code> 的時候，就透過 <code>IIFE</code> 的方式執行函式來創造一個 <code>Execution Context</code>，藉此保留當下 <code>i</code> 的值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">buildFunctions2</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> arr = [];</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">    arr.push(</span><br><span class="line">      (<span class="function"><span class="keyword">function</span> (<span class="params">j</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">          <span class="built_in">console</span>.log(j);</span><br><span class="line">        &#125;;</span><br><span class="line">      &#125;(i))</span><br><span class="line">    );</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> fs = buildFunctions2();</span><br><span class="line">fs[<span class="number">0</span>](); <span class="comment">// 0</span></span><br><span class="line">fs[<span class="number">1</span>](); <span class="comment">// 1</span></span><br><span class="line">fs[<span class="number">2</span>](); <span class="comment">// 2</span></span><br></pre></td></tr></table></figure>

<h2 id="資料來源"><a href="#資料來源" class="headerlink" title="資料來源"></a>資料來源</h2><p><a href="https://www.udemy.com/course/understand-javascript/">Udemy-JavaScript: Understanding the Weird Parts</a>`</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>了解閉包 Closures (一)</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211218/44904/</url>
    <content><![CDATA[<p>這堂課將介紹一個在 <code>JavaScript</code> 中惡名昭彰的一個觀念 - 閉包 <code>Closures</code></p>
<span id="more"></span>

<h2 id="了解閉包"><a href="#了解閉包" class="headerlink" title="了解閉包"></a>了解閉包</h2><p>在過去的課程中所學到的 <code>Execution Stack</code>、<code>Execution Context</code> 等觀念都會在 <code>Closures</code> 中被應用到。</p>
<p>首先我們建立一個函式並回傳一個函式，接著，呼叫這個函式再加上一組括號呼叫回傳的函式，看起來有點奇怪，但其實就只是馬上執行回傳的函式。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params">whattosay</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(whattosay + <span class="string">&#x27; &#x27;</span> + name);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">greet(<span class="string">&#x27;Hello&#x27;</span>)(<span class="string">&#x27;Tony&#x27;</span>);   <span class="comment">// Hello Tony</span></span><br></pre></td></tr></table></figure>

<p>那如果我們透過一個變數儲存第一次函式的呼叫，再呼叫這個變數來執行回傳的函式呢?</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params">whattosay</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(whattosay + <span class="string">&#x27; &#x27;</span> + name);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> satHi = greet(<span class="string">&#x27;Hi&#x27;</span>);</span><br><span class="line">sayHi(<span class="string">&#x27;Tony&#x27;</span>);   <span class="comment">// Hi Tony</span></span><br></pre></td></tr></table></figure>

<p>還是會得到和原來一樣預期的結果，為什麼這麼做也會成立呢 ? 因為當我們執行 <code>sayHi</code> 函式的時候，<code>whattosay</code> 這個變數仍然存在，其實這就是一個閉包 <code>Closures</code>。</p>
<p>讓我們依序拆解這段程式的執行流程，當我們執行 <code>JavaScript</code> 時，會先產生一個 <code>Global Execution Context</code>，接著，執行 <code>greet</code> 函式的時候會在 <code>Context Stack</code> 上產生它的 <code>Execution Context</code>， 並回傳一個匿名函式物件 <code>creates a function on the fly</code>。</p>
<p><img src="Function-Invoked.png"></p>
<p>這裡有一個問題是，每一個函式 <code>Execution Context</code> 都有其記憶體的位置，但當一個 <code>Execution Context</code> 在 <code>return</code> 後抽離 <code>Execution Stack</code> 後，它的記憶體會發生什麼事 ?</p>
<p>一般來說，<code>JavaScript Engine</code> 會透過一個 <code>Garbage Collection</code> 的機制來清除這些內容，雖然 <code>Execution Context</code> 已經不在了，但其中的變數還是會留在該記憶體位置。</p>
<p><img src="Garbage-Collection.png"></p>
<p>接著，當程式碼執行到 <code>sayHi()</code> 時，會再建立一個匿名函式的 <code>Execution Context</code>，同時帶有一個變數 <code>name</code>。</p>
<p><img src="Next-Execution-Context.png"></p>
<p>當這個匿名函式執行到 <code>whattosay</code> 這個變數時，無法在函式內找到這個變數，因此根據 <code>Scoped Chain</code> 的概念就會往外部環境逐層搜尋，這時候就會找到這個變數。</p>
<p>在這裡，我們把這個匿名函式的 <code>Execution</code> 和外部變數關 <code>Close</code> 在一起，這種現象在 <code>JavaScript</code> 就是一個閉包 <code>Closures</code>。透過 <code>Closures</code> 這個特性，可以確保在函式執行時找對其對應的外部變數，不論某個函式是否執行完畢將 <code>Execution Context</code> 抽離 <code>Execution Stack</code>，<code>JavaScript Engine</code> 仍然可以找到對應的外部變數。  </p>
<p><img src="Closures.png"></p>
<h2 id="資料來源"><a href="#資料來源" class="headerlink" title="資料來源"></a>資料來源</h2><p><a href="https://www.udemy.com/course/understand-javascript/">Udemy-JavaScript: Understanding the Weird Parts</a>`</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>立即呼叫函式表達式 IIFE</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211217/19975/</url>
    <content><![CDATA[<p>這堂課將介紹在 <code>JavaScript</code> 中非常普遍出現在各個 <code>Framework</code> 和 <code>Library</code> 中的一個函式用法，立即呼叫函式表達式 <code>Immediately Invoked Function Expressionss</code> 簡稱 <code>IIFE</code>。</p>
<span id="more"></span>

<h2 id="Function-State-amp-Function-Expressions"><a href="#Function-State-amp-Function-Expressions" class="headerlink" title="Function State &amp; Function Expressions"></a>Function State &amp; Function Expressions</h2><p>在之前的課程中，我們已經介紹了函式陳述句 <code>Function Statement</code> 和函式表達式 <code>Function Expressions</code>，在這兩種函是被建立時，會儲存在記憶體中，但 <code>JavaScript</code> 執行時並不會執行這些函式，而是等到這些函是被呼叫 <code>invoked</code> 後才會執行</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Function Statement</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Hello&#x27;</span> + name);</span><br><span class="line">&#125;</span><br><span class="line">greet(<span class="string">&#x27;John&#x27;</span>);   <span class="comment">// Hello John</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> greetFunc = <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Hello&#x27;</span> + name);</span><br><span class="line">&#125;</span><br><span class="line">greetFunc(<span class="string">&#x27;John&#x27;</span>);   <span class="comment">// Hello John</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="立即呼叫函式-Immediately-Invoked-Function-Expressions-IIFE"><a href="#立即呼叫函式-Immediately-Invoked-Function-Expressions-IIFE" class="headerlink" title="立即呼叫函式 Immediately Invoked Function Expressions (IIFE)"></a>立即呼叫函式 Immediately Invoked Function Expressions (IIFE)</h2><p>當我們建立函式的時候就馬上使用它，把建立和呼叫的動作組合在一起，就是一個立即呼叫函式，首先建立一個 <code>Function Expressions</code> 並回傳一個值，並加它直接印出來，理所當然地會得到這個函式本身</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> greeting = <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">&#x27;Hello&#x27;</span> + name;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(greeting);   <span class="comment">// function(name) &#123;...&#125;</span></span><br></pre></td></tr></table></figure>

<p>當我們執行它時，就會得到函式回傳的值</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> greeting = <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">&#x27;Hello&#x27;</span> + name;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="built_in">console</span>.log(greeting(<span class="string">&#x27;Jonh&#x27;</span>));   <span class="comment">// Hello John</span></span><br></pre></td></tr></table></figure>

<p>那如果把呼叫的動作放到函是建立的時候，就會形成一個 <code>IIFE</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> greeting = <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">&#x27;Hello&#x27;</span> + name;</span><br><span class="line">&#125;(<span class="string">&#x27;Jonh&#x27;</span>);</span><br><span class="line"><span class="built_in">console</span>.log(greeting);   <span class="comment">// Hello John</span></span><br></pre></td></tr></table></figure>

<p>會發現得到和原先一樣的結果，但如果我們再次呼叫 <code>greeting()</code> ，就會得到以下 <code>error log</code>，因為我們呼叫的是 <code>return</code> 的 <code>string</code> </p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> greeting = <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">&#x27;Hello&#x27;</span> + name;</span><br><span class="line">&#125;(<span class="string">&#x27;Jonh&#x27;</span>);</span><br><span class="line"><span class="built_in">console</span>.log(greeting());</span><br></pre></td></tr></table></figure>

<p><img src="Invoke-IIFE.png"></p>
<h3 id="直接建立-IIFE"><a href="#直接建立-IIFE" class="headerlink" title="直接建立 IIFE"></a>直接建立 IIFE</h3><p>在 <code>JavaScript</code> 中，如果我們直接在程式碼寫上一個值，即使不透過變數宣告，也不會出錯</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="number">3</span>;   <span class="comment">// number</span></span><br><span class="line"></span><br><span class="line"><span class="string">&#x27;I am a string&#x27;</span>;   <span class="comment">// string</span></span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">  name: <span class="string">&#x27;Jonh&#x27;</span>   <span class="comment">// object</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>那如果我們直接寫了一個 <code>Anonymous Function</code> 呢 ?</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">&#x27;Hello&#x27;</span> + name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>會發現出現以下 <code>Error Log</code>，因為當 <code>JavaScript engine</code> 看到 <code>Function</code> 開頭時，它就會期待你是一個需要名稱的 <code>Function Statename</code></p>
<p><img src="Anonymous-Function.png"></p>
<p>為了不要讓 <code>JavaScript</code> 認為這是一個 <code>Statement</code>，所以我們不能以 <code>function</code> 作為程式的開頭，為了達到這個目的可以透過括號 <code>()</code> 來將整個函時包起來，而因為括號只會包含 <code>Expressions</code>，例如 <code>(3+2) * 4;</code>所以程式就不會報錯了</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(3+2) * 4;</span><br><span class="line"></span><br><span class="line">(function(name) &#123;</span><br><span class="line">  return &#39;Hello&#39; + name;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>寫到這裡就如同前面期它型別的例子，雖然這是一個合法的寫法，但其實我們也只是建立了一個函式而已什麼也沒有做，這時候我們可以函是建立時就呼叫它，看到了嗎?這就是一個 <code>IIFE</code>，</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> firstname = <span class="string">&#x27;John&#x27;</span>;</span><br><span class="line"></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log)(<span class="string">&#x27;Hello&#x27;</span> + name);</span><br><span class="line">&#125;(firstname));   <span class="comment">// Hello John</span></span><br></pre></td></tr></table></figure>

<p>這樣的寫法常見於各大 <code>Framework</code> 和 <code>Library</code> 中，我們也許會看到呼叫的括號有的放在裡面，有的放在外面，這些其實都是合法的寫法，只要選擇一種風格並保持就可以了。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// inside</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log)(<span class="string">&#x27;Hello&#x27;</span> + name);</span><br><span class="line">&#125;(firstname));   <span class="comment">// Hello John</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// outside</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log)(<span class="string">&#x27;Hello&#x27;</span> + name);</span><br><span class="line">&#125;)(firstname);   <span class="comment">// Hello John</span></span><br></pre></td></tr></table></figure>

<h3 id="安全程式碼"><a href="#安全程式碼" class="headerlink" title="安全程式碼"></a>安全程式碼</h3><p>當 <code>JavaScript Engine</code> 開始執行時，會產生一個全域的執行階段 <code>Global Execution Context</code>;接著當程式執行到我們所建立的 <code>IIFE</code> 時，這段匿名函式 <code>Anonymous Function</code> 會儲存在 <code>Global Exection Context</code> 中，而因為這是一個在最後有加上括號 <code>()</code> 的 <code>IIFE</code>，所以這個函是會立即被執行並產生一個新的 <code>Execution Context</code>，而其中所建立的變數 <code>gretting</code> 就會在這個 <code>Execution Context</code> 裡面，不會影響到 <code>Global Execution Context</code>;因此，透過 <code>IIFE</code> 我們就可以達到變數彼此不會互相影響目的了。 </p>
<p><img src="IIFE-Execution-Context.png"></p>
<h3 id="透過影響外層變數重複使用"><a href="#透過影響外層變數重複使用" class="headerlink" title="透過影響外層變數重複使用"></a>透過影響外層變數重複使用</h3><p>那如果我們希望 <code>IIFE</code> 中的變數可以重複使用的話該怎麼做呢 ?</p>
<p>雖然我們使用 <code>IIFE</code> 的目的就是希望變數彼此不會互相影響，但有時候又希望可以在全域環境中使用到這些變數，這時候就可以透過將全域物件 <code>window</code> 帶入到 <code>IIFE</code> 中，並將目標變數綁訂到 <code>window</code> 上，如此一來既可以在其他 <code>Execution Context</code> 使用到它了。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// IIFE</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"><span class="built_in">global</span>, name</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">var</span> gretting = <span class="string">&#x27;Hello&#x27;</span>;</span><br><span class="line">  <span class="built_in">global</span>.greeting = greeting;</span><br><span class="line">  <span class="built_in">console</span>.log(gretting + <span class="string">&#x27; &#x27;</span> + name);   <span class="comment">// Hello John</span></span><br><span class="line"></span><br><span class="line">&#125;(<span class="built_in">window</span>, <span class="string">&#x27;John&#x27;</span>));</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">&#x27;gretting&#x27;</span>);   <span class="comment">// Hello</span></span><br></pre></td></tr></table></figure>

<p>但在開發時，我們需要確定的定義綁定到全域的變數，不能因為一個問題或一時方便就這麼做，否則就失去 <code>IIFE</code> 將變數環境各自獨立的目的了。</p>
<h2 id="資料來源"><a href="#資料來源" class="headerlink" title="資料來源"></a>資料來源</h2><p><a href="https://www.udemy.com/course/understand-javascript/">Udemy-JavaScript: Understanding the Weird Parts</a></p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>自動插入分號</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211216/16113/</url>
    <content><![CDATA[<p>這堂課將介紹 <code>JavaScript</code> 中自動插入分號這件事</p>
<span id="more"></span>

<p>在 <code>JavaScript</code> 每段程式碼結尾的分號 <code>semicolon</code> 即使不加上也不會出錯，因為 <code>JavaScript</code> 會自動判斷在它認為需要加上的地方幫你補上分號，但其實在開發時並不建議這麼做，因為有些寫法在自動加上分號後的結果會和你預期的不太一樣</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getPerson</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> </span><br><span class="line">  &#123;</span><br><span class="line">    firstname: <span class="string">&#x27;Tony&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(getPerson());   <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>

<p>如上程式碼，<code>return</code> 的結果會是 <code>undefined</code> 而不是我們建立的 <code>literal object</code>，原因是 <code>JavaScript</code> 在判斷 <code>return</code> 後沒有程式碼後，會自動幫你加上一個分號，因此這個函式就在 <code>return</code> 這裡結束了，所以記得 <code>return</code> 後不能直接留白</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getPerson</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    firstname; <span class="string">&#x27;Tony&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(getPerson());   <span class="comment">// &#123; firstname: &#x27;Tony&#x27; &#125;</span></span><br></pre></td></tr></table></figure>

<h2 id="資料來源"><a href="#資料來源" class="headerlink" title="資料來源"></a>資料來源</h2><p><a href="https://www.udemy.com/course/understand-javascript/">Udemy-JavaScript: Understanding the Weird Parts</a></p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>重載函數</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211216/14606/</url>
    <content><![CDATA[<!-- @format -->

<p>這堂課將介紹 <code>JavaScript</code> 的 <code>Frameworks</code> 與 <code>libraries</code> 常見的函式使用方法</p>
<span id="more"></span>

<p>首先我們建立一個函式並判斷傳入的參數來執行不同的行為</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greet</span>(<span class="params">firstname, lastname, language</span>) </span>&#123;</span><br><span class="line">  language = language || <span class="string">&quot;en&quot;</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (language === <span class="string">&quot;en&quot;</span>) <span class="built_in">console</span>.log(<span class="string">&quot;Hello&quot;</span> + firstname + lastname);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (language === <span class="string">&quot;es&quot;</span>) <span class="built_in">console</span>.log(<span class="string">&quot;Hola&quot;</span> + firstname + lastname);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">greet(<span class="string">&#x27;John&#x27;</span>, <span class="string">&#x27;Doe&#x27;</span>, <span class="string">&#x27;en&#x27;</span>);   <span class="comment">// Hello John Doe</span></span><br><span class="line">greet(<span class="string">&#x27;John&#x27;</span>, <span class="string">&#x27;Doe&#x27;</span>, <span class="string">&#x27;es&#x27;</span>);   <span class="comment">// Hola John Doe</span></span><br></pre></td></tr></table></figure>

<p>可以看到上方兩次函式的呼叫方式透過判斷最後一個參數來達到不同的行為，而我們也可以將不同的判斷包裝成不同的函式來呼叫原本的函式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greetEngilsh</span>(<span class="params">firstname, lastname</span>) </span>&#123;</span><br><span class="line">  greet(firstname, lastname, <span class="string">&#x27;en&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greetSpanish</span>(<span class="params">firstname, lastname</span>) </span>&#123;</span><br><span class="line">  greet(firstname, lastname, <span class="string">&#x27;es&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">greetEnglish(<span class="string">&#x27;John&#x27;</span>, <span class="string">&#x27;Doe&#x27;</span>);   <span class="comment">// Hello John Doe</span></span><br><span class="line">greetSpanish(<span class="string">&#x27;John&#x27;</span>, <span class="string">&#x27;Doe&#x27;</span>);   <span class="comment">// Hola John Doe</span></span><br></pre></td></tr></table></figure>

<h2 id="資料來源"><a href="#資料來源" class="headerlink" title="資料來源"></a>資料來源</h2><p><a href="https://www.udemy.com/course/understand-javascript/">Udemy-JavaScript: Understanding the Weird Parts</a></p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>Framework 如何使用 Closures - Function Fatories</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211219/39540/</url>
    <content><![CDATA[<p>有了前幾堂課對於 <code>Closures</code> 的了解後，這堂課將介紹 <code>Framework</code> 如何使用 <code>Clousures</code></p>
<span id="more"></span>

<p>以下我們先來看一段程式碼</p>
<p>首先建立一個 <code>makeGreeting</code> 函式，帶入一個 <code>language</code> 變數，並透過判斷這個變數來回傳對應的函式內容，接著透過兩個變數帶入不同的參數來呼叫這函式，最後呼叫這兩個指派給變數的函式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">makeGreeting</span>(<span class="params">language</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">firstname, lastname</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (language === <span class="string">&#x27;en&#x27;</span>) &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">&#x27;Hello&#x27;</span> + firstname + lastname);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (language === <span class="string">&#x27;es&#x27;</span>) &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">&#x27;Hola&#x27;</span> + firstname + lastname);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> grettingEnglish = makeGreeting(<span class="string">&#x27;en&#x27;</span>);</span><br><span class="line"><span class="keyword">var</span> grettingSpanish = makeGreeting(<span class="string">&#x27;en&#x27;</span>);</span><br><span class="line"></span><br><span class="line">grettingEnglish(<span class="string">&#x27;John&#x27;</span>, <span class="string">&#x27;Doe&#x27;</span>);   <span class="comment">// Hello John Doe</span></span><br><span class="line">grettingSpanish(<span class="string">&#x27;John&#x27;</span>, <span class="string">&#x27;Doe&#x27;</span>);   <span class="comment">// Hola John Doe</span></span><br></pre></td></tr></table></figure>

<p>結果就是得到各自預期的字串</p>
<h2 id="解析程式碼"><a href="#解析程式碼" class="headerlink" title="解析程式碼"></a>解析程式碼</h2><p>以下我們依序解析這段程式碼</p>
<p>首先，在 <code>JavaScript Engine</code> 開始時，因為 <code>Hoisting</code> 的緣故，<code>makeGreeting()</code>、<code>greetingEnglish</code> 和 <code>greetingSpanish</code> 會被除存在記憶體中，接著才會開始執行程式碼</p>
<p><img src="Hoisting.png"></p>
<p>在執行到 <code>grettingEnglish</code> 時，<code>makeGreeting</code> 的 <code>Execution Context</code> 會被建立，同時將 <code>language = en</code> 儲存在記憶體中，最後離開 <code>Execution Stack</code>，同理 <code>greetingSpanish</code> 也會儲存 <code>language = es</code> 在記憶體的另一個位置</p>
<p><img src="greetingEnglish.png"></p>
<p>接著當執行到 <code>greetingEnglish(&#39;John&#39;, &#39;Doe&#39;)</code> 時，會產生一個新的 <code>Execution Context</code>，當執行到 <code>lanuage</code> 這個變數時，因為在函式內找不到，便會透過 <code>Scoped Chain</code> 找到外部的 <code>en</code>，這整個完竟便形成了一個 <code>Closures</code></p>
<p><img src="Closures.png"></p>
<p>程式碼往下執行到最後一行 <code>greetingSpanish</code> 時，會按照相同的流程形成第二個 <code>Closures</code></p>
<p><img src="Second-Closures.png"></p>
<p>這段程式碼指出的 <code>JavaScript</code> 特色是，每執行一次函式，都會產生一個 <code>Execution Context</code>，<code>JavaScript Engine</code> 會指向該函式對應的變數，透過函式來創造函式，可以讓我們容易使用並保留預設參數，<code>makeGreeting</code> 這個函式我們就稱它為 <code>Function Factories</code> </p>
<h2 id="資料來源"><a href="#資料來源" class="headerlink" title="資料來源"></a>資料來源</h2><p><a href="https://www.udemy.com/course/understand-javascript/">Udemy-JavaScript: Understanding the Weird Parts</a>`</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>Closures and Callback</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211220/6171/</url>
    <content><![CDATA[<p>有了先前對於 <code>Closures</code> 的認知後，這堂課將進一步介紹 <code>Closures</code> 和回呼 <code>Callback</code> </p>
<span id="more"></span>

<p>首先我看到一段程式碼如下:</p>
<p>建立一個 <code>sayHiLater</code>，並在其中設定一個 3 秒後印出 <code>greeting</code> 變數的計時器</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">sayHiLater</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">var</span> greeting = <span class="string">&#x27;Hi!&#x27;</span>;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(greeting);</span><br><span class="line">  &#125;, <span class="number">3000</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">sayHiLater();</span><br></pre></td></tr></table></figure>

<p>結果會如預期一樣，在 3 秒後印出 <code>Hi!</code>，但進一步淤考這段程式碼，為什麼 <code>setTimeout</code> 在 3 秒後還會知道 <code>greeting</code> 這個變數的值呢 ?</p>
<p>原因其實是這裡已經形成了一個 <code>Closures</code>，首先，在 <code>setTimeout</code> 函式中，我們有兩點可特別留意 ; 第一，我們之所以可以把 <code>function</code> 當成參數傳入是因為在 <code>JavaScript</code> 中，<code>function</code> 屬於 <code>First Class Function</code> ，<code>function</code> 是一個 <code>object</code> ; 第二，我們直接在參數的位置透過 <code>Function Expression</code> 的方式建立一個 <code>Anonymous Function</code></p>
<p>在函式 <code>sayHiLater</code> 執行完畢離開 <code>Execution Stack</code> 後，變數 <code>greeting</code> 仍然留在記憶體的該位置，所以當 3 秒後執行 <code>setTimeout</code> 時，會透過 <code>Scope Chain</code> 的概念取得 <code>Outer Environment</code> 的變數值，這裡便形成了一個 <code>Closures</code>。</p>
<h2 id="常用的-jQuery"><a href="#常用的-jQuery" class="headerlink" title="常用的 jQuery"></a>常用的 <code>jQuery</code></h2><p>在 <code>jQuery</code> 中，一個常見的基本寫法如下:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery uses function expressions and first-class functions !</span></span><br><span class="line">$(<span class="string">&#x27;button&#x27;</span>).click(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="comment">// do something</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>在 <code>click</code> 事件中放是 <code>function</code>，這就是運用了 <code>first class function</code> 把函式當成一般物件傳入作為參數，而把函式當作參數的也使用到了 <code>callback function</code> 的觀念，告訴這個函是當指定事件發生時，呼叫我給你的函式。</p>
<h2 id="Callback-Function"><a href="#Callback-Function" class="headerlink" title="Callback Function"></a>Callback Function</h2><p><code>Callback Function</code> 指的是當我們呼叫一個 <code>function</code> 時，傳入另一個 <code>function</code> 當作參數，呼叫的 <code>function</code> 結束時，執行我們傳入的 <code>function</code>。</p>
<p>這裡我們看到一段範例程式碼如下:</p>
<p>傳入一個 <code>function</code> 作為參數，並在呼叫的 <code>function</code> 中執行我們傳入的 <code>function</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">tellMeWhenDone</span>(<span class="params">callback</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">var</span> a = <span class="number">1000</span>;   <span class="comment">// some work</span></span><br><span class="line">  <span class="keyword">var</span> b = <span class="number">2000</span>;   <span class="comment">// some work</span></span><br><span class="line"></span><br><span class="line">  callback();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">tellMeWhenDone(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;I am done !&#x27;</span>);   <span class="comment">// I am done !</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="資料來源"><a href="#資料來源" class="headerlink" title="資料來源"></a>資料來源</h2><p><a href="https://www.udemy.com/course/understand-javascript/">Udemy-JavaScript: Understanding the Weird Parts</a>`</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>call、bind and apply</title>
    <url>/%E5%85%8B%E6%9C%8Djs%E7%9A%84%E5%A5%87%E6%80%AA%E9%83%A8%E5%88%86/20211221/22545/</url>
    <content><![CDATA[<p>這堂課將介紹 3 個很令 <code>JavaSCript</code> 初學者困擾的內建函式 <code>call()</code>、<code>apply()</code> 和 <code>bind()</code></p>
<span id="more"></span>

<p>複習一下之前學過的觀念，在執行一個 <code>funciton</code> 時，會產生一個 <code>Execution Context</code>，並自動生成 <code>Variable Environment</code>、<code>Outer Environment</code> 和關鍵字 <code>this</code>。</p>
<p><img src="Function-Execution-Context.png"></p>
<p>另外，函式被建立時還會有以下特性</p>
<ul>
<li>屬性 <code>property</code><ul>
<li>NAME: 如果是 <code>Anonymous Function</code> 就不會有</li>
<li>CODE: 在函式中可被呼叫執行的程式碼</li>
</ul>
</li>
<li>方法 <code>method</code><ul>
<li>3 個內建函式: <code>call()</code>、<code>apply()</code> 和 <code>bind()</code></li>
</ul>
</li>
</ul>
<p><img src="Function.png"></p>
<h2 id="範例程式碼"><a href="#範例程式碼" class="headerlink" title="範例程式碼"></a>範例程式碼</h2><p>接著來看一段範例程式碼如下:</p>
<p>首先，我們建立一個 <code>object</code>，包含一些 <code>property</code>，並透過 <code>method</code> 回傳自己的 <code>property</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  firstname: <span class="string">&#x27;John&#x27;</span>,</span><br><span class="line">  lastname: <span class="string">&#x27;Doe&#x27;</span>,</span><br><span class="line">  getFullName: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> fallName = <span class="built_in">this</span>.firstname + <span class="built_in">this</span>.lastname;</span><br><span class="line">    <span class="keyword">return</span> fullName;</span><br><span class="line">  </span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> logName = <span class="function"><span class="keyword">function</span>(<span class="params">log1, log2</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Logged: &#x27;</span> + <span class="built_in">this</span>.getFullName());</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">logName();</span><br></pre></td></tr></table></figure>

<p>結果當然匯出現 <code>error</code>，因為函式 <code>logName</code> 中的 <code>this</code> 會指向 <code>window</code>，而 <code>window</code> 並不是物件 <code>person</code>，所以沒有 <code>getFullName</code> 這個 <code>method</code></p>
<p><img src="error-log.png"></p>
<h2 id="bind"><a href="#bind" class="headerlink" title="bind()"></a>bind()</h2><p>那麼如果我們要讓 <code>logName</code> 函式指向物件 <code>person</code>，這時候就可以使用到 <code>bind()</code>，這裡要注意的是， <code>bind</code> 前的函式 <code>logName</code> 不能加上括號 <code>()</code> 呼叫它，否則就會直接 <code>return</code> 結果了，我們要的只是這個函式 ; 接著 <code>bind</code> 中的參數放入 <code>person</code>，這麼一來在執行新的函式 <code>logPersonName</code> 時，其時就是執行 <code>logName</code>，只是其中的 <code>this</code> 會指物件 <code>person</code>。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  firstname: <span class="string">&#x27;John&#x27;</span>,</span><br><span class="line">  lastname: <span class="string">&#x27;Doe&#x27;</span>,</span><br><span class="line">  getFullName: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> fallName = <span class="built_in">this</span>.firstname + <span class="built_in">this</span>.lastname;</span><br><span class="line">    <span class="keyword">return</span> fullName;</span><br><span class="line">  </span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> logName = <span class="function"><span class="keyword">function</span>(<span class="params">log1, log2</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Logged: &#x27;</span> + <span class="built_in">this</span>.getFullName());</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> logPersonName = logName.bind(person);</span><br><span class="line"></span><br><span class="line">logPersonName();   <span class="comment">// Logged: John Doe</span></span><br></pre></td></tr></table></figure>

<p>然而，我們其時還可以直接在建立函式 <code>creates a function on the fly</code> 時，就透過 <code>bind()</code> 來把 <code>this</code> 指向目標物件，並回傳一個 <code>function</code>，直接呼叫這個 <code>function </code> 就會得到和剛才一樣的結果。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">  firstname: <span class="string">&#x27;John&#x27;</span>,</span><br><span class="line">  lastname: <span class="string">&#x27;Doe&#x27;</span>,</span><br><span class="line">  getFullName: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> fallName = <span class="built_in">this</span>.firstname + <span class="built_in">this</span>.lastname;</span><br><span class="line">    <span class="keyword">return</span> fullName;</span><br><span class="line">  </span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> logName = <span class="function"><span class="keyword">function</span>(<span class="params">log1, log2</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Logged: &#x27;</span> + <span class="built_in">this</span>.getFullName());</span><br><span class="line"></span><br><span class="line">&#125;.bind(person);</span><br><span class="line"></span><br><span class="line">logName();   <span class="comment">// Logged: John Doe</span></span><br></pre></td></tr></table></figure>

<p>如剛才所說，新建立的函式 <code>logPerson</code> 其時就是執行 <code>logName</code> 函式，所以帶入的參數也會帶入函式 <code>logName</code> 中。 </p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  firstname: <span class="string">&#x27;John&#x27;</span>,</span><br><span class="line">  lastname: <span class="string">&#x27;Doe&#x27;</span>,</span><br><span class="line">  getFullName: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> fallName = <span class="built_in">this</span>.firstname + <span class="built_in">this</span>.lastname;</span><br><span class="line">    <span class="keyword">return</span> fullName;</span><br><span class="line">  </span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> logName = <span class="function"><span class="keyword">function</span>(<span class="params">lang1, lang2</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Arguments: &#x27;</span>, lang1, lang2);</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> logPersonName = logName.bind(person);</span><br><span class="line"></span><br><span class="line">logPersonName(<span class="string">&#x27;en&#x27;</span>);   <span class="comment">// Arguments: en undefined</span></span><br></pre></td></tr></table></figure>

<h2 id="call"><a href="#call" class="headerlink" title="call()"></a>call()</h2><p>接下來看看透過 <code>call()</code> 我們可以怎麼做。</p>
<p>首先，<code>call()</code> 其時和括號 <code>()</code> 一樣會執行這個 <code>function</code>，但不一樣的是，第一個參數是指向的 <code>this</code>，後續才是帶入的參數。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  firstname: <span class="string">&#x27;John&#x27;</span>,</span><br><span class="line">  lastname: <span class="string">&#x27;Doe&#x27;</span>,</span><br><span class="line">  getFullName: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> fallName = <span class="built_in">this</span>.firstname + <span class="built_in">this</span>.lastname;</span><br><span class="line">    <span class="keyword">return</span> fullName;</span><br><span class="line">  </span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> logName = <span class="function"><span class="keyword">function</span>(<span class="params">lang1, lang2</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Logged: &#x27;</span> + <span class="built_in">this</span>.getFullName());</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Arguments: &#x27;</span>, lang1, lang2);</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">logName.call(person, <span class="string">&#x27;en&#x27;</span>, <span class="string">&#x27;es&#x27;</span>);</span><br><span class="line"><span class="comment">// Logged: John Doe</span></span><br><span class="line"><span class="comment">// Arguments: en es</span></span><br></pre></td></tr></table></figure>

<h2 id="apply"><a href="#apply" class="headerlink" title="apply()"></a>apply()</h2><p>而 <code>apply()</code> 的用法就和 <code>call()</code> 就很像了，不同的是，第二個參數是一個 <code>array</code>，<code>array</code> 中的值就是依序帶入的參數了</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">logName.apply(person, [<span class="string">&#x27;en&#x27;</span>, <span class="string">&#x27;es&#x27;</span>]);</span><br><span class="line"><span class="comment">// Logged: John Doe</span></span><br><span class="line"><span class="comment">// Arguments: en es</span></span><br></pre></td></tr></table></figure>

<p>而我們也可以透過 <code>IIFE</code> 的方式建立函式 <code>creates a function on the fly</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">lang1, lang2</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Logged: &#x27;</span> + <span class="built_in">this</span>.getFullName());</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Arguments: &#x27;</span>, lang1, lang2);</span><br><span class="line"></span><br><span class="line">&#125;).apply(person, [<span class="string">&#x27;en&#x27;</span>, <span class="string">&#x27;es&#x27;</span>]);</span><br><span class="line"><span class="comment">// Logged: John Doe</span></span><br><span class="line"><span class="comment">// Arguments: en es</span></span><br></pre></td></tr></table></figure>

<h2 id="Function-Borrowing"><a href="#Function-Borrowing" class="headerlink" title="Function Borrowing"></a>Function Borrowing</h2><p>延續剛才的範例，我們建立第二個物件 <code>person2</code>，但它沒有 <code>getFullName</code> 這個 <code>method</code>，這時候我們就可以透過剛剛學過的 <code>bind()</code>、<code>Call()</code> 和 <code>apply()</code> 來借用 <code>person</code> 的 <code>method</code>。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  firstname: <span class="string">&#x27;John&#x27;</span>,</span><br><span class="line">  lastname: <span class="string">&#x27;Doe&#x27;</span>,</span><br><span class="line">  getFullName: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> fallName = <span class="built_in">this</span>.firstname + <span class="built_in">this</span>.lastname;</span><br><span class="line">    <span class="keyword">return</span> fullName;</span><br><span class="line">  </span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> person2 = &#123;</span><br><span class="line">  firstname: <span class="string">&#x27;Jane&#x27;</span>,</span><br><span class="line">  lastname: <span class="string">&#x27;Doe&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(person.getFullName.apply(person2));   <span class="comment">// Jane Doe</span></span><br><span class="line"><span class="built_in">console</span>.log(person.getFullName.call(person2));   <span class="comment">// Jane Doe</span></span><br><span class="line"><span class="built_in">console</span>.log(person.getFullName.bind(person2)());   <span class="comment">// Jane Doe</span></span><br></pre></td></tr></table></figure>

<h2 id="Function-Curring"><a href="#Function-Curring" class="headerlink" title="Function Curring"></a>Function Curring</h2><p>接著，我們來看一下第二個不同的範例，這個範例將使用到 <code>bind()</code> ; 前面有提到 <code>bind()</code> 只會回傳 <code>function</code>，<code>call</code> 和 <code>apply</code> 則會直接執行 <code>function</code>。</p>
<p>首先，我們建立一個函式並將傳入的兩個變數相乘，接著透過 <code>bind()</code> 來建立第二個函式，但不同的是，透過 <code>bind()</code> 建立時，我們並沒有給足兩個參數</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">multiply</span>(<span class="params">a, b</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> a * b;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> multipleByTwo = multiply.bind(<span class="built_in">this</span>, <span class="number">2</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(multipleByTwo(<span class="number">3</span>));   <span class="comment">// 2 * 3 = 6</span></span><br><span class="line"><span class="built_in">console</span>.log(multipleByTwo(<span class="number">4</span>));   <span class="comment">// 2 * 4 = 8</span></span><br></pre></td></tr></table></figure>

<p>在 <code>bind()</code> 中給的參數其時就會成為新建函式的預設值，剩餘的參數才會在呼叫時補上</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">multipleByTwo</span>(<span class="params">b</span>) </span>&#123;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">var</span> a = <span class="number">2</span>;</span><br><span class="line">  <span class="keyword">return</span> a * b;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果我想要一開始就給足參數的數量，那麼後須呼叫時帶入的參數就不會影響執行的結果了</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> multipleByTwo = multiply.bind(<span class="built_in">this</span>, <span class="number">2</span>, <span class="number">3</span>);</span><br><span class="line"></span><br><span class="line">multipleByTwo(<span class="number">5</span>);   <span class="comment">// 2 * 3 = 6</span></span><br><span class="line">multipleByTwo(<span class="number">10</span>);   <span class="comment">// 2 * 3 = 6</span></span><br></pre></td></tr></table></figure>

<p>這種先創立一個 <code>function</code> 後，再透過複製並給予預設參數的方式就稱為 <code>Function Curring</code>，實際開發很常應用在大量的算術上。</p>
<h2 id="資料來源"><a href="#資料來源" class="headerlink" title="資料來源"></a>資料來源</h2><p><a href="https://www.udemy.com/course/understand-javascript/">Udemy-JavaScript: Understanding the Weird Parts</a>`</p>
]]></content>
      <categories>
        <category>克服JS的奇怪部分</category>
      </categories>
  </entry>
  <entry>
    <title>瀏覽器裡的 JavaScript</title>
    <url>/javascript/20190928/32632/</url>
    <content><![CDATA[<p>前端開發在網頁的操作方法其實都是由 JS 的執行平台，也就是 瀏覽器 所提供，操作方法分別由 <code>BOM</code> 和 <code>DOM</code> 這兩種物件所擁有，而 JavaScript 就是透過 <code>BOM</code> 和 <code>DOM</code> 所提供的 <code>API</code> 來控制瀏覽器的行為與網頁的內容</p>
<span id="more"></span>

<h2 id="瀏覽器裡的-JavaScript-實際包含"><a href="#瀏覽器裡的-JavaScript-實際包含" class="headerlink" title="瀏覽器裡的 JavaScript 實際包含"></a>瀏覽器裡的 JavaScript 實際包含</h2><ul>
<li><code>JavaScript 核心</code> (以 ECMAScript 標準為基礎)</li>
<li><code>BOM</code> (Browser Object Model，瀏覽器物件模型)</li>
<li><code>DOM</code> (Document Object Model，文件物件模型)</li>
</ul>
<h2 id="什麼是-DOM-Document-Object-Modal"><a href="#什麼是-DOM-Document-Object-Modal" class="headerlink" title="什麼是 DOM (Document Object Modal) ?"></a>什麼是 DOM (Document Object Modal) ?</h2><ul>
<li><p><code>DOM</code> 是一個將 <code>HTML</code> 文件以樹狀結構來表示的模型，而組合起來的樹狀圖我們稱為 <code>DOM Tree</code></p>
</li>
<li><p>每個 <code>HTML</code> 檔案 都會產生一個 <code>document</code></p>
</li>
<li><p>每個 <code>HTML</code> 標籤 都會產生一個 節點，往下又可以再延伸出 文本節點 和 屬性節點</p>
</li>
<li><p>如下，我們寫了一段網頁程式碼，就會產生這樣的 <code>DOM</code></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>MyTitle<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">h1</span>&gt;</span>Header<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">&quot;#&quot;</span>&gt;</span>MyLink<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><img src="DOM.png"></p>
</li>
</ul>
<h2 id="什麼是-BOM-Browser-Object-Modal"><a href="#什麼是-BOM-Browser-Object-Modal" class="headerlink" title="什麼是 BOM (Browser Object Modal) ?"></a>什麼是 BOM (Browser Object Modal) ?</h2><ul>
<li><code>BOM</code> 是瀏覽器所有功能的核心，與網頁內容無關</li>
<li><code>BOM</code> 的核心是 <code>window</code> 物件</li>
<li><code>window</code> 物件提供的屬性為 <code>document</code>、<code>location</code>、<code>navigator</code>、<code>screen</code>、<code>history</code> 和 <code>frames</code></li>
<li><code>window</code> 中的 <code>document</code> 屬性就是 <code>DOM</code></li>
</ul>
<p><img src="BOM.png"></p>
<ul>
<li><p><code>BOM</code> 是 <code>ECMAScript</code> 標準裡的 “全域物件”，所有在 “全域作用範圍” 內宣告的變數、物件、函式等，都會自動變成 全域物件 的 屬性 ，也就是 “全域變數”，可以透過 <code>window.</code> 的方式取得</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="built_in">console</span>.log( <span class="built_in">window</span>.a );   <span class="comment">// 10</span></span><br></pre></td></tr></table></figure></li>
<li><p>在全域範圍宣告的變數無法使用 <code>delete</code> 來刪除，但透過 <code>window.</code> 來指定的物件屬性就可以</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="built_in">window</span>.b = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(a);   <span class="comment">// 10</span></span><br><span class="line"><span class="built_in">console</span>.log(b);   <span class="comment">// 10</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">delete</span> <span class="built_in">window</span>.a;   <span class="comment">// false</span></span><br><span class="line"><span class="keyword">delete</span> <span class="built_in">window</span>.b;   <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.a);   <span class="comment">// 10</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.b);   <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure></li>
<li><p><code>window</code> 物件下的屬性，是可以省略 <code>window</code> 不打的，如常見的 <code>alert</code> 完整語法是這樣的</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">window</span>.alert(<span class="string">&quot;message&quot;</span>);</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="DOM-與-BOM-的區別"><a href="#DOM-與-BOM-的區別" class="headerlink" title="DOM 與 BOM 的區別"></a>DOM 與 BOM 的區別</h2><ul>
<li><code>BOM</code> : JavaScript 與瀏覽器構通的窗口，不涉及網頁內容</li>
<li><code>DOM</code> : JavaScript 用來控制網頁的節點與內容的 標準</li>
<li><code>BOM 規範</code>: 依賴於各家瀏覽器已經實作的部分納入 <code>HTML 5</code> 標準中，也就是 <code>BOM API</code></li>
<li><code>DOM 規範</code>: 透過 <code>W3C</code> 所制定的標準來規範</li>
</ul>
<h2 id="DOM-API-節點控制"><a href="#DOM-API-節點控制" class="headerlink" title="DOM API 節點控制"></a>DOM API 節點控制</h2><h3 id="DOM-節點的選取"><a href="#DOM-節點的選取" class="headerlink" title="DOM 節點的選取"></a>DOM 節點的選取</h3><ul>
<li><p><code>document.getElementById(&#39;xxx&#39;)</code></p>
<p>可以選取 <code>DOM</code> 中 <code>ID</code> 為 “xxx” 的元素</p>
</li>
<li><p><code>document.getElementTagName(&#39;xxx&#39;)</code></p>
<p>針對給定的 <code>tag</code> 名稱，回傳所有符合的 <code>NodeList</code> 物件</p>
</li>
<li><p><code>document.getElementClassName(&#39;xxx&#39;)</code></p>
<p>針對給定的 <code>class</code> 名稱，回傳所有符合的 <code>NodeList</code> 物件</p>
</li>
<li><p><code>document.querySelector(&#39;xxx&#39;)</code></p>
<p>可以選取 <code>ID</code>、<code>class</code> 或 <code>HTML tag</code>，遇到相同名稱的元素只會選取第一個</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">&#x27;#id&#x27;</span>);</span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">&#x27;.class a&#x27;</span>);</span><br></pre></td></tr></table></figure></li>
<li><p><code>document.querySelectorAll(&#39;xxx&#39;)</code></p>
<p>可以重複選取多個元素，選取到的元素會形成一個 “陣列” ，可以透過 <code>for 迴圈</code> 給值</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">h2</span> <span class="attr">class</span>=<span class="string">&quot;titleClass&quot;</span>&gt;</span><span class="tag">&lt;<span class="name">em</span>&gt;</span><span class="tag">&lt;/<span class="name">em</span>&gt;</span><span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">h2</span> <span class="attr">class</span>=<span class="string">&quot;titleClass&quot;</span>&gt;</span><span class="tag">&lt;<span class="name">em</span>&gt;</span><span class="tag">&lt;/<span class="name">em</span>&gt;</span><span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- after js --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">h2</span> <span class="attr">class</span>=<span class="string">&quot;titleClass&quot;</span>&gt;</span><span class="tag">&lt;<span class="name">em</span>&gt;</span>0123<span class="tag">&lt;/<span class="name">em</span>&gt;</span><span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">h2</span> <span class="attr">class</span>=<span class="string">&quot;titleClass&quot;</span>&gt;</span><span class="tag">&lt;<span class="name">em</span>&gt;</span>1123<span class="tag">&lt;/<span class="name">em</span>&gt;</span><span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> el = <span class="built_in">document</span>.querySelectorAll(<span class="string">&#x27;.titleClass em&#x27;</span>);</span><br><span class="line"><span class="built_in">console</span>.log(el);   <span class="comment">// [em, em]</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> elLen = el.length;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (i = <span class="number">0</span>; el &lt; elLen; i++)&#123;</span><br><span class="line">  el[i].textContent = i + <span class="string">&#x27;123&#x27;</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="DOM-節點的新增"><a href="#DOM-節點的新增" class="headerlink" title="DOM 節點的新增"></a>DOM 節點的新增</h3><ul>
<li><p><code>innerHTML</code></p>
<p>組完字串後，傳進語法進行渲染，會直接取代原先的內容，如果外層是 單引號 <code>&#39;&#39;</code>，內層就要使用 雙引號 <code>&quot;&quot;</code> 以做區別</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;main&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">                      </span><br><span class="line"><span class="comment">&lt;!-- after js --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;main&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">h1</span> <span class="attr">class</span>=<span class="string">&quot;blue&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> el = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;main&#x27;</span>);</span><br><span class="line">el.innerHTML = <span class="string">&#x27;&lt;h1 class=&quot;blue&quot;&gt;&lt;/h1&gt;&#x27;</span>;</span><br></pre></td></tr></table></figure></li>
<li><p><code>createElement</code></p>
<p>以 <code>DOM</code> 節點來處理，每個節點都須透過 <code>JavaScript</code> 來新增</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">h1</span> <span class="attr">class</span>=<span class="string">&quot;title&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">&lt;!-- after js --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">h1</span> <span class="attr">class</span>=<span class="string">&quot;title&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">em</span> <span class="attr">class</span>=<span class="string">&quot;blue&quot;</span>&gt;</span>title<span class="tag">&lt;/<span class="name">em</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str = <span class="built_in">document</span>.createElement(<span class="string">&#x27;em&#x27;</span>);</span><br><span class="line">str.textContent = <span class="string">&#x27;1234&#x27;</span>;</span><br><span class="line">str.setAttribute(<span class="string">&#x27;class&#x27;</span>, <span class="string">&#x27;blue&#x27;</span>);</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 增加子節點</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">&#x27;.title&#x27;</span>).appendChild(str);</span><br></pre></td></tr></table></figure></li>
<li><p><code>innerHTML</code> 與 <code>createElement</code> 的差異</p>
<ul>
<li>因為 <code>innerHTML</code> 是組完字串一次帶入，所以效能較佳</li>
<li>也因為 <code>innerHTML</code> 透過組完字串帶入 <code>HTML</code>，所以在某些情況，如在輸入欄位輸入 <code>&lt;script src=&quot;&quot;&gt;&lt;/script&gt;</code> 等檔案載入形式，可能會遭到惡意植入攻擊程式碼，稱為 “XXS 跨網域指令碼 (Cross-site Scripting)”</li>
</ul>
</li>
</ul>
<h3 id="節點的修改與刪除"><a href="#節點的修改與刪除" class="headerlink" title="節點的修改與刪除"></a>節點的修改與刪除</h3><p>以上新增節點的方法只是建立節點，還未將建立的節點輸出到網頁中，透過以下方法，改變網頁中的節點</p>
<ul>
<li><code>Node.appendChild(&#39;子節點&#39;)</code></li>
</ul>
<p>透過這個方法可以將指定的 “子節點” 加入到父容器節點的末端</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">&quot;myList&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 1<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 2<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- after js --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">&quot;myList&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 1<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 2<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Hello World<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 取得容器</span></span><br><span class="line"><span class="keyword">var</span> myList = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;myList&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 建立 li 元素</span></span><br><span class="line"><span class="keyword">var</span> newList = <span class="built_in">document</span>.creatElement(<span class="string">&#x27;li&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 建立文字節點</span></span><br><span class="line"><span class="keyword">var</span> textNode = <span class="built_in">document</span>.creatTextNode(<span class="string">&#x27;Hello World&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 將文字節點加入 li </span></span><br><span class="line">newList.appendChild(textNode);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 將 li 子節點加入 #myList 父節點</span></span><br><span class="line">myList.appendList(newList);</span><br></pre></td></tr></table></figure>

<ul>
<li><code>NODE.insertBefore(&#39;新節點&#39;, &#39;指定節點&#39;)</code></li>
</ul>
<p>透過這個方法將 “新節點” 加到 “指定節點” 的 “前面”</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">&quot;myList&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 1<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 2<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br><span class="line">                </span><br><span class="line"><span class="comment">&lt;!-- after js --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">&quot;myList&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 1<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 2<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Hello World<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 取得容器</span></span><br><span class="line"><span class="keyword">var</span> myList = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;myList&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 取得 &lt;li&gt;Item 2&lt;/li&gt; 元素</span></span><br><span class="line"><span class="keyword">var</span> refNode = <span class="built_in">document</span>.querySelectorAll(<span class="string">&#x27;li&#x27;</span>)[<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 建立 li 元素節點</span></span><br><span class="line"><span class="keyword">var</span> newNode = <span class="built_in">document</span>.createElement(<span class="string">&#x27;li&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 建立文字節點</span></span><br><span class="line"><span class="keyword">var</span> textNode = <span class="built_in">document</span>.creatTextNode(<span class="string">&#x27;Hello World&#x27;</span>);</span><br><span class="line">newNode.appendChild(textNode);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 將&#x27;新節點&#x27;插入到 &#x27;指定節點&#x27; 的 前方</span></span><br><span class="line">myList.insertBefore(newNode, refNode);</span><br></pre></td></tr></table></figure>

<ul>
<li><code>NODE.replaceChild(&#39;新節點&#39;, &#39;指定節點&#39;)</code></li>
</ul>
<p>透過這個方法將 “新節點” 取代 “指定節點”</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">&quot;myList&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 1<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 2<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br><span class="line">                  </span><br><span class="line"><span class="comment">&lt;!-- after js --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">&quot;myList&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 1<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Hello World<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 取得容器</span></span><br><span class="line"><span class="keyword">var</span> myList = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;myList&#x27;</span>);</span><br><span class="line">  </span><br><span class="line"><span class="comment">// 取得 &lt;li&gt;Item 2&lt;/li&gt; 元素</span></span><br><span class="line"><span class="keyword">var</span> refNode = <span class="built_in">document</span>.querySelectorAll(<span class="string">&#x27;li&#x27;</span>)[<span class="number">1</span>];</span><br><span class="line">  </span><br><span class="line"><span class="comment">// 建立 li 元素節點</span></span><br><span class="line"><span class="keyword">var</span> newNode = <span class="built_in">document</span>.createElement(<span class="string">&#x27;li&#x27;</span>);</span><br><span class="line">  </span><br><span class="line"><span class="comment">// 建立文字節點</span></span><br><span class="line"><span class="keyword">var</span> textNode = <span class="built_in">document</span>.creatTextNode(<span class="string">&#x27;Hello World&#x27;</span>);</span><br><span class="line">newNode.appendChild(textNode);</span><br><span class="line">  </span><br><span class="line"><span class="comment">// 將&#x27;新節點&#x27; 取代 &#x27;指定節點&#x27; </span></span><br><span class="line">myList.replaceChild(newNode, refNode);</span><br></pre></td></tr></table></figure>

<ul>
<li><code>NODE.removeChild(&#39;指定節點&#39;)</code></li>
</ul>
<p>透過這個方法將 “指定節點” 移除</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">&quot;myList&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 1<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 2<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br><span class="line">                    </span><br><span class="line"><span class="comment">&lt;!-- after js --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">&quot;myList&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 1<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 取得容器</span></span><br><span class="line"><span class="keyword">var</span> myList = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;myList&#x27;</span>);</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 取得 &lt;li&gt;Item 2&lt;/li&gt; 元素</span></span><br><span class="line"><span class="keyword">var</span> removeNode = <span class="built_in">document</span>.querySelectorAll(<span class="string">&#x27;li&#x27;</span>)[<span class="number">1</span>];</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 將&#x27;#myList&#x27; 下的 &#x27;指定節點&#x27; 移除 </span></span><br><span class="line">myList.removeChild(removeNode);</span><br></pre></td></tr></table></figure>

<h3 id="新增節點屬性"><a href="#新增節點屬性" class="headerlink" title="新增節點屬性"></a>新增節點屬性</h3><p><code>setAttribute(&#39;屬性&#39;, &#39;新增的值&#39;)</code> : 動態為 html tag 增加屬性</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">h2</span> <span class="attr">class</span>=<span class="string">&quot;titleClass&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">&quot;#&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- after js --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">h2</span> <span class="attr">class</span>=<span class="string">&quot;titleClass&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">&quot;https://www.google.com/&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> el = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;.titleClass a&#x27;</span>);</span><br><span class="line">el.setAttribute(<span class="string">&#x27;href&#x27;</span>, <span class="string">&#x27;http://www.google.com&#x27;</span>);</span><br></pre></td></tr></table></figure>

<h2 id="DOM-節點的類型"><a href="#DOM-節點的類型" class="headerlink" title="DOM 節點的類型"></a>DOM 節點的類型</h2><p>DOM 節點的類型常見的有以下幾種 :</p>
<table>
  <thead>
    <tr>
      <th>節點類型常數</th>
      <th>對應數值</th>
      <th>說明</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Node.ELEMENT_NODE</td>
      <td>1</td>
      <td>HTML 元素的 Element 節點</td>
    </tr>
    <tr>
      <td>Node.TEXT_NODE</td>
      <td>3</td>
      <td>實際文字節點，包括了換行與空格</td>
    </tr>
    <tr>
      <td>Node.COMMENT_NODE</td>
      <td>8</td>
      <td>註解節點</td>
    </tr>
    <tr>
      <td>Node.DOCUMENT_NODE</td>
      <td>9</td>
      <td>根節點 (Document)</td>
    </tr>
    <tr>
      <td>Node.DOCUMENT_TYPE_NODE</td>
      <td>10</td>
      <td>文件類型的 DocumentType 節點，例如 HTML5 的 <code>&lt;!DOCTYPE html&gt;</code></td>
    </tr>
    <tr>
      <td>Node.DOCUMENT_FRAGMENT_NODE</td>
      <td>11</td>
      <td>DocumentFragment 節點</td>
    </tr>
  </tbody>
</table>

<h3 id="參考資料"><a href="#參考資料" class="headerlink" title="參考資料"></a>參考資料</h3><p><a href="https://developer.mozilla.org/en-US/docs/Web/API/Node/nodeType">Node.nodeType</a></p>
<h2 id="DOM-API-節點間的查找"><a href="#DOM-API-節點間的查找" class="headerlink" title="DOM API 節點間的查找"></a>DOM API 節點間的查找</h2><p>DOM 節點有分層的概念，節點與節點之間的關係分成兩種</p>
<ul>
<li><code>父子節點</code> : 每個節點的上層節點稱為 父節點 <code>Parent node</code> ; 相對地，下層的節點稱為子節點 <code>Child node</code>，而隔層的節點基本上沒有直接關係，也就是 不具備孫節點</li>
<li><code>兄弟節點</code> : 如果節點與節點有同一個 父節點，那彼此就是 兄弟節點 <code>Siblings node</code></li>
</ul>
<h3 id="子節點"><a href="#子節點" class="headerlink" title="子節點"></a>子節點</h3><p><code>Node.childNodes</code> 與 <code>Node.children</code> 所有子節點</p>
<ul>
<li><p>所有的 <code>DOM</code> 節點都包含 <code>childNodes</code> 屬性，此種屬性無法被修改。</p>
</li>
<li><p>可以透過 <code>Node.hasChildNodes()</code> 來檢查某個 <code>DOM</code> 節點是否有子節點，如果有會回傳 <code>true</code>，否則 <code>false</code></p>
</li>
<li><p><code>Node.childNodes</code> 回傳的是 <code>NodeList</code>，包含以下三、可能</p>
<ul>
<li>HTML 元素節點 (element nodes)</li>
<li>文字節點 (text nodes)，包含空白</li>
<li>註解節點 (comment nodes)</li>
</ul>
</li>
<li><p><code>Node.children</code> 回傳的是 <code>HTMLColllection</code> 只包含一種可能就是 “HTML 元素節點 (element nodes)”</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;test&quot;</span>&gt;</span></span><br><span class="line">  我是第一個子節點 ( 文本節點 )</span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span>我是第二個子節點 ( 第一個 元素節點 )<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  我是第三個子節點 ( 文本節點 )</span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span>我是第四個子節點 ( 第二個 元素節點 )<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 我是註解 (註解節點)--&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span>我是第五個子節點 ( 第三個 元素節點 )，但我前面有空行</span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span>&gt;</span>我是第五個子節點的子節點<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> test = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;.test&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(test.childNodes.length);   <span class="comment">// 9</span></span><br><span class="line"><span class="built_in">console</span>.log(test.childNodes);   <span class="comment">// NodeList(9) [text, div, text, div, text, comment, text, div, text]</span></span><br><span class="line"><span class="built_in">console</span>.log(test.childNodes[<span class="number">7</span>].hasChildNodes());   <span class="comment">// true</span></span><br><span class="line">              </span><br><span class="line"><span class="built_in">console</span>.log(test.children.length);   <span class="comment">// 3</span></span><br><span class="line"><span class="built_in">console</span>.log(test.children);   <span class="comment">// HTMLCollection(3) [div, div, div]</span></span><br></pre></td></tr></table></figure></li>
<li><p><code>Node.firstChild</code> 第一個子節點</p>
<p><code>Node.firstChild</code> 可以取得 <code>Node</code> 節點的 第一個子節點，沒有子節點則會回傳 <code>null</code></p>
<p>如下範例取得的第一個子節點為 <code>&lt;p&gt;</code> 與 <code>&lt;span&gt;</code> 中間的 “換行字元”</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">span</span>&gt;</span>span1<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">span</span>&gt;</span>span2<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;p&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(p.firstChild.tagName);   <span class="comment">// undefined</span></span><br><span class="line"><span class="built_in">console</span>.log(p.firstChild.textContent);   <span class="comment">// </span></span><br></pre></td></tr></table></figure>

<p>如果把中間的換行與空白移除，就會得到預期的 <code>SPAN</code></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span><span class="tag">&lt;<span class="name">span</span>&gt;</span>span1<span class="tag">&lt;/<span class="name">span</span>&gt;</span><span class="tag">&lt;<span class="name">span</span>&gt;</span>span2<span class="tag">&lt;/<span class="name">span</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;p&#x27;</span>);</span><br><span class="line">  </span><br><span class="line"><span class="built_in">console</span>.log(p.firstChild.tagName);   <span class="comment">// SPAN</span></span><br><span class="line"><span class="built_in">console</span>.log(p.firstChild.textContent);   <span class="comment">// span1</span></span><br></pre></td></tr></table></figure></li>
<li><p><code>Node.lastChild</code> 最後一個子節點</p>
</li>
</ul>
<p><code>Node.lastChild</code> 可以取得 Node 節點的 “最後一個” 子節點，沒有子節點則會回傳 <code>null</code></p>
<p>概念同 <code>Node.firstChild</code>，取得的子節點包含 “空白節點”</p>
<h3 id="父節點"><a href="#父節點" class="headerlink" title="父節點"></a>父節點</h3><p><code>Node.parentNode</code> 父節點</p>
<p><code>Node.parentNode</code> 可以用來取得 “父節點”，回傳值可能回以下三種</p>
<ul>
<li><p>HTML 元素節點 (Element node)</p>
</li>
<li><p>根節點 (Document node)</p>
</li>
<li><p><a href="https://developer.mozilla.org/zh-TW/docs/Web/API/DocumentFragment">DocumentFragment</a> 節點 (詳細介紹可以參考此連結)</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">span</span>&gt;</span>span1<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">span</span>&gt;</span>span1<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> el = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;span&#x27;</span>);</span><br><span class="line"><span class="built_in">console</span>.log(el.parentNode.nodeName);   <span class="comment">// &quot;P&quot;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="兄弟節點"><a href="#兄弟節點" class="headerlink" title="兄弟節點"></a>兄弟節點</h3><ul>
<li><p><code>Node.previousSibling</code> 和 <code>Node.nextSibling</code> 兄弟節點(包含所有節點)</p>
<p>透過 <code>Node.previousSibing</code> 和 <code>Node.nextSibling</code> 分別可以取得同層的 “前一個” 與 “下一個” 節點，如果 Node 沒有前一個或下一個節點，則會回傳 <code>null</code> ; 特別注意的是，這兩個語法包含 “所有節點”，也就是包含 “空白節點”、”文字節點” 和 “元素節點”</p>
<p><code>querySelectorAll</code> 會選取所有符合的元素形成陣列，透過 中括號 <code>[]</code> 可以選取指定元素</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span><span class="tag">&lt;<span class="name">span</span>&gt;</span>span1<span class="tag">&lt;/<span class="name">span</span>&gt;</span><span class="tag">&lt;<span class="name">span</span>&gt;</span>span2<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">span</span>&gt;</span>span3<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> el = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;span&#x27;</span>);</span><br><span class="line"><span class="keyword">var</span> el2 = <span class="built_in">document</span>.querySelectorAll(<span class="string">&#x27;span&#x27;</span>)[<span class="number">1</span>];</span><br><span class="line"><span class="keyword">var</span> el3 = <span class="built_in">document</span>.querySelectorAll(<span class="string">&#x27;span&#x27;</span>)[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( el.previousSibling );    <span class="comment">// null</span></span><br><span class="line"><span class="built_in">console</span>.log( el2.previousSibling.textContent );    <span class="comment">// &quot;span 2&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log( el3.previousSibling.textContent );    <span class="comment">// &quot;我是空白&quot;</span></span><br></pre></td></tr></table></figure></li>
<li><p><code>Node.previousElementSibling</code> 和 <code>Node.nextElementSibling</code> 兄弟節點(只包含 “元素節點”)</p>
<p>和上一組尋找兄弟節點的語法在用法上相同，唯一不同的是，搜尋時只包括 “元素節點”，會忽略空白節點和文字節點，是比較實用的方式</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">span</span>&gt;</span>span1<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">span</span>&gt;</span>span2<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">span</span>&gt;</span>span3<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> el = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;span&#x27;</span>);</span><br><span class="line"><span class="keyword">var</span> el2 = <span class="built_in">document</span>.querySelectorAll(<span class="string">&#x27;span&#x27;</span>)[<span class="number">1</span>];</span><br><span class="line"><span class="keyword">var</span> el3 = <span class="built_in">document</span>.querySelectorAll(<span class="string">&#x27;span&#x27;</span>)[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log( el.previousElementSibling );    <span class="comment">// null</span></span><br><span class="line"><span class="built_in">console</span>.log( el2.previousElementSibling.textContent );    <span class="comment">// &quot;span 1&quot;</span></span><br><span class="line"><span class="built_in">console</span>.log( el3.previousElementSibling.textContent );    <span class="comment">// &quot;span 2&quot;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="document-getElementsBy-與-document-querySelector-querySelectorAll-的差異"><a href="#document-getElementsBy-與-document-querySelector-querySelectorAll-的差異" class="headerlink" title="document.getElementsBy** 與 document.querySelector/.querySelectorAll 的差異"></a><code>document.getElementsBy**</code> 與 <code>document.querySelector/.querySelectorAll</code> 的差異</h3><p><code>document.getElementsBy**</code> 與 <code>Node.childNodes</code> 回傳的同樣是 <code>NodeList</code> 包含以下三種可能</p>
<ul>
<li>HTML 元素節點 (element nodes)</li>
<li>文字節點 (text nodes)，包含空白</li>
<li>註解節點 (comment nodes)</li>
</ul>
<p><code>document.getElementsBy**</code> 和 <code>Node.children</code> 回傳的同樣是 <code>HTMLCollection</code>，只有一種可能</p>
<ul>
<li>HTML 元素節點 (element nodes)</li>
</ul>
<p><code>HTHLCollection</code> 與 <code>NodeList</code> 都可以使用 “陣列索引” 來存取內容，兩者在大部分的情況下也都是 “即時更新” 的，但透過 <code>document.querySelector/doucment.querySelectorAll</code> 取得的 <code>NodeList</code> 是 “靜態的”</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;outer&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;inner&quot;</span>&gt;</span>inner<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> outerDiv = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;outer&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> allDivs = <span class="built_in">document</span>.getElementsByTagName(<span class="string">&#x27;div&#x27;</span>);</span><br><span class="line"><span class="keyword">var</span> allDivs2 = <span class="built_in">document</span>.querySelectorAll(<span class="string">&#x27;div&#x27;</span>);</span><br><span class="line">            </span><br><span class="line"><span class="built_in">console</span>.log(allDivs.length);   <span class="comment">// 2</span></span><br><span class="line"><span class="built_in">console</span>.log(allDivs2.length);   <span class="comment">// 2</span></span><br><span class="line">            </span><br><span class="line"><span class="comment">// 清空 &lt;div id=&quot;outer&quot;&gt; 下的節點</span></span><br><span class="line">outerDiv.innerHTML = <span class="string">&#x27;&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 因為  &lt;div id=&quot;inner&quot;&gt; 被清空，所以只剩下 &lt;div id=&quot;outer&quot;&gt;</span></span><br><span class="line"><span class="built_in">console</span>.log(allDivs.length);   <span class="comment">// 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 但是 document.querySelectorAll 回傳的是 &quot;靜態&quot; 的 NodeList ，所以不受 outerDiv 更新影響</span></span><br><span class="line"><span class="built_in">console</span>.log(allDivs2.length);   <span class="comment">// 2</span></span><br></pre></td></tr></table></figure>

<h2 id="BOM-API"><a href="#BOM-API" class="headerlink" title="BOM API"></a>BOM API</h2><h3 id="回上一頁功能實作-window-history"><a href="#回上一頁功能實作-window-history" class="headerlink" title="回上一頁功能實作 window.history"></a>回上一頁功能實作 <code>window.history</code></h3><p>透過 <code>window.history.forward()</code> 切換到下一頁，<code>window.history.back(</code>) 切換到上一頁，以此功能切換頁面</p>
<p><a href="https://codepen.io/Jerry-Yeh/pen/KjrgNW?editors=1010">範例程式碼</a></p>
<h3 id="列印功能-window-print"><a href="#列印功能-window-print" class="headerlink" title="列印功能 window.print()"></a>列印功能 window.print()</h3><p><a href="https://codepen.io/Jerry-Yeh/pen/ewQdPj">範例程式碼</a></p>
<h3 id="位置訊息-window-location"><a href="#位置訊息-window-location" class="headerlink" title="位置訊息 window.location"></a>位置訊息 window.location</h3><p><a href="https://codepen.io/Jerry-Yeh/pen/ewQdPj">範例程式碼</a></p>
<h3 id="移動位置-window-open"><a href="#移動位置-window-open" class="headerlink" title="移動位置 window.open()"></a>移動位置 window.open()</h3><p>使用此方法移動位置會 “開新分頁”</p>
<p><a href="https://codepen.io/Jerry-Yeh/pen/ewQdPj">範例程式碼</a></p>
<h3 id="動態擷取瀏覽器尺寸"><a href="#動態擷取瀏覽器尺寸" class="headerlink" title="動態擷取瀏覽器尺寸"></a>動態擷取瀏覽器尺寸</h3><ul>
<li><code>window.innerHeight</code> : 擷取瀏覽器內部高度</li>
<li><code>window.innerWidth</code> : 擷取瀏覽器內部寬度</li>
<li><code>window.outerHeight</code> : 擷取瀏覽器外部高度</li>
<li><code>window.outerWidth</code> : 擷取瀏覽器外部寬度</li>
<li><code>window.onresize</code> : 瀏覽器尺寸改變就會觸發</li>
</ul>
<p><a href="https://codepen.io/Jerry-Yeh/pen/mZQjxG">範例程式碼</a></p>
<h2 id="DOM-API"><a href="#DOM-API" class="headerlink" title="DOM API"></a>DOM API</h2><p><a href="https://codertw.com/%E7%A8%8B%E5%BC%8F%E8%AA%9E%E8%A8%80/541219/">JS 和 jquery 獲取各種螢幕的寬度和高度</a></p>
<h2 id="參考資料-1"><a href="#參考資料-1" class="headerlink" title="參考資料"></a>參考資料</h2><p><a href="https://ithelp.ithome.com.tw/users/20065504/ironman/1259">重新認識 JavaScript</a><br><a href="https://ithelp.ithome.com.tw/users/20083608/ironman/1354">邁向 JavaScript 勇者之路</a><br><a href="https://www.udemy.com/javascript-learning/">JavaScript 入門篇 - 學徒的試煉</a></p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript - 事件</title>
    <url>/javascript/20191003/52047/</url>
    <content><![CDATA[<p>JavaScript 是一個事件驅動 <code>Event-driven</code> 的程式語言，當瀏覽器載入網頁開始讀取後，會馬上讀取 JS 事件的相關程式碼，但必須等到事件被觸發後，才會進行對應程式的執行</p>
<span id="more"></span>

<h2 id="事件介紹"><a href="#事件介紹" class="headerlink" title="事件介紹"></a>事件介紹</h2><p>以下方的 Bootstrap 按鈕為例，當使用者點擊這個按鈕，並跳出另一個視窗， “點擊按鈕” 這件事就是 “事件” Event，而負責處理事件的程式被稱為 事件處理者 <code>Event Handler</code>，也就是 “啟動另一個視窗的顯示” 這個動作</p>
<p><a href="https://codepen.io/Jerry-Yeh/pen/eYOqLaK?editors=1001">bootstrap button</a></p>
<h2 id="事件流程"><a href="#事件流程" class="headerlink" title="事件流程"></a>事件流程</h2><h3 id="事件流程介紹"><a href="#事件流程介紹" class="headerlink" title="事件流程介紹"></a>事件流程介紹</h3><p>這裡有兩個 <code>div</code> 元素，外層是 <code>.outer</code>，內層是 <code>.inner</code>，當我們點擊 <code>.inner</code> 的時候，其實也同時在點擊 <code>.outer</code>，更廣義來說，其實也在點擊整個網頁</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;outer&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;inner&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><img src="event-flow.png"></p>
<p>事件流程指的就是 “網頁元素接收事件的順序”，而事件流程分成兩種機制</p>
<ul>
<li>事件冒泡 (Event Bubbling)</li>
<li>事件捕獲 (Event Capturing)</li>
</ul>
<h3 id="事件冒泡-Event-Bubbling"><a href="#事件冒泡-Event-Bubbling" class="headerlink" title="事件冒泡 (Event Bubbling)"></a>事件冒泡 (Event Bubbling)</h3><p>“事件冒泡” 指的就是從啟動的元素節點開始，逐漸 “由下而上” 傳遞，直到網頁的根節點 <code>document</code></p>
<p>如下範例，當我們點擊 <code>&lt;div&gt;click&lt;/div&gt;</code>，在 “事件冒泡” 的機制下，觸發順序為:</p>
<ol>
<li><code>&lt;div&gt;click&lt;/div&gt;</code></li>
<li><code>&lt;body&gt;</code></li>
<li><code>&lt;html&gt;</code></li>
<li><code>document</code></li>
</ol>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Document<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span>&gt;</span>click<span class="tag">&lt;/<span class="name">div</span>&gt;</span>   // 被點擊的元素</span><br><span class="line">  <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="事件捕獲-Event-Capturing"><a href="#事件捕獲-Event-Capturing" class="headerlink" title="事件捕獲 (Event Capturing)"></a>事件捕獲 (Event Capturing)</h3><p>“事件捕獲” 和 “事件冒泡” 相反，指的就是從啟動的元素節點開始，逐漸 “由上而下” 傳遞，從網頁的根節點 <code>document</code> 開始直到點擊的元素</p>
<p>同以上範例，當我們點擊 <code>&lt;div&gt;click&lt;/div&gt;</code>，在 事件捕獲的機制下，觸發順序為:</p>
<ol>
<li><code>document</code></li>
<li><code>&lt;html&gt;</code></li>
<li><code>&lt;body&gt;</code></li>
<li><code>&lt;div&gt;click&lt;/div&gt;</code></li>
</ol>
<h2 id="事件的註冊綁定"><a href="#事件的註冊綁定" class="headerlink" title="事件的註冊綁定"></a>事件的註冊綁定</h2><p>透過事件的綁定，可以指定 “事件處理器” <code>handler</code> 給事件，也就是當事件被觸發後會執行的動作</p>
<p>事件的綁定可以分為以下三種:</p>
<ul>
<li><code>on-event</code> 處理器 (HTML 屬性)</li>
<li><code>on-event</code> 處理器 (非 HTML 屬性)</li>
<li>事件監聽 <code>EventTarget.addEventListener()</code></li>
</ul>
<h3 id="on-event-處理器-HTML-屬性"><a href="#on-event-處理器-HTML-屬性" class="headerlink" title="on-event 處理器 (HTML 屬性)"></a>on-event 處理器 (HTML 屬性)</h3><p>我們可以透過加上 <code>on</code> + 事件名稱 的屬性在 HTML 元素上來註冊事件，但基於使用性與維護性，不建議使用此方法來綁定事件</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">button</span> <span class="attr">class</span>=<span class="string">&quot;btn&quot;</span> <span class="attr">onclick</span>=<span class="string">&quot;alert(&#x27;Hello World&#x27;)&quot;</span>&gt;</span> click<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="on-event-處理器-非-HTML-屬性"><a href="#on-event-處理器-非-HTML-屬性" class="headerlink" title="on-event 處理器 (非 HTML 屬性)"></a>on-event 處理器 (非 HTML 屬性)</h3><p>像是 <code>window</code> 或 <code>document</code> 這種沒有實體元素的情況，還是可以透過 <code>DOM API</code> 提供的 <code>on-event</code> 處理器 來處理事件</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">button</span> <span class="attr">id</span>=<span class="string">&quot;btn&quot;</span>&gt;</span>Click<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;btn&#x27;</span>);</span><br><span class="line">          </span><br><span class="line">btn.onclick = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Hi&#x27;</span>);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="事件監聽-EventTarget-addEventListener"><a href="#事件監聽-EventTarget-addEventListener" class="headerlink" title="事件監聽 EventTarget.addEventListener()"></a>事件監聽 <code>EventTarget.addEventListener()</code></h3><p>事件監聽 <code>EventTarget.addEventListener()</code> 包含三個參數</p>
<ul>
<li>事件名稱</li>
<li>事件的處理器 <code>event handler</code> (事件觸發時執行的 function )</li>
<li>布林值 : 如果沒有寫預設值就是 <code>false</code> 代表 事件冒泡，反之，<code>true</code> 代表 事件捕獲</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">EventTarget.addEventListener(<span class="string">&#x27;事件名稱&#x27;</span>, <span class="string">&#x27;事件的處理器(function)&#x27;</span>, <span class="literal">true</span>/<span class="literal">false</span>);</span><br></pre></td></tr></table></figure>

<p><strong>指定多個 處理器 handler</strong></p>
<p>事件監聽 .addEventListener 的好處事可以重複指定多個 事件處理器 event handler 給同一個元素的同一個事件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;btn&#x27;</span>);</span><br><span class="line"></span><br><span class="line">btn.adaEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Hello&#x27;</span>);</span><br><span class="line">&#125;, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">btn.adaEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;World&#x27;</span>);</span><br><span class="line">&#125;, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// in console</span></span><br><span class="line"><span class="string">&quot;Hello&quot;</span></span><br><span class="line"><span class="string">&quot;World&quot;</span></span><br></pre></td></tr></table></figure>

<h3 id="解除事件註冊-EventTarget-removeEventListener"><a href="#解除事件註冊-EventTarget-removeEventListener" class="headerlink" title="解除事件註冊 EventTarget.removeEventListener()"></a>解除事件註冊 <code>EventTarget.removeEventListener()</code></h3><p>解除事件註冊 <code>removeEventListener</code> 與 事件監聽 <code>addEventListener</code> 一樣包含三個參數，分別是 “事件名稱”、”事件處理器” 和 “冒泡/捕獲”</p>
<p>但要注意的是，<code>removeEventListener</code> 要移除的事件要確定是與 <code>addEventListener</code> 綁定的是同一個 實體，否則無法移除</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;btn&#x27;</span>);</span><br><span class="line"></span><br><span class="line">btn.addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Hi&#x27;</span>);</span><br><span class="line">&#125;, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 移除事件，但沒有用</span></span><br><span class="line">btn.removeEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Hi&#x27;</span>);</span><br><span class="line">&#125;, <span class="literal">false</span>);</span><br></pre></td></tr></table></figure>

<p>如果是透過變數將 事件處理 <code>event handler</code> 拉出來，兩者就會指向 “同一個實體”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;btn&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> clickHandler = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Hi&#x27;</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">btn.addEventListener(<span class="string">&#x27;click&#x27;</span>, clickHandler, <span class="literal">false</span>);</span><br><span class="line">              </span><br><span class="line"><span class="comment">// 成功移除</span></span><br><span class="line">btn.removeEventListener(<span class="string">&#x27;click&#x27;</span>, clickHandler, <span class="literal">false</span>);</span><br></pre></td></tr></table></figure>

<h2 id="事件處理器中的事件"><a href="#事件處理器中的事件" class="headerlink" title="事件處理器中的事件"></a>事件處理器中的事件</h2><p>當我們透過 <code>addEventListener</code> 來註冊事件時，會有三個參數，分別是 事件名稱、事件處理器 (Event Handler) 以及 捕獲/冒泡</p>
<h3 id="事件處理器中的事件介紹"><a href="#事件處理器中的事件介紹" class="headerlink" title="事件處理器中的事件介紹"></a>事件處理器中的事件介紹</h3><p>同時，事件監聽器 <code>EventListener</code> 會建立一個事件物件 <code>Event Object</code>，這個物件包含所有與事件有關的屬性，並以 參數 的形式回傳給 <code>Event Handler</code></p>
<p>如下範例，參數 <code>e</code> 就是 事件物件，透過 <code>console.log</code> 我們可以查看所有與事件有關的屬性，<code>e</code> 是參數，所以當然可以自定義名稱</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">button</span> <span class="attr">id</span>=<span class="string">&quot;btn&quot;</span>&gt;</span>Click<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;btn&#x27;</span>);</span><br><span class="line"></span><br><span class="line">btn.addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(e);</span><br><span class="line">&#125;, <span class="literal">false</span>);</span><br></pre></td></tr></table></figure>

<p><img src="event.png"></p>
<ul>
<li><code>typeof</code> : 事件名稱</li>
<li><code>target</code> : 觸發事件的元素</li>
<li><code>bubbles</code> : 事件是否在冒泡階段觸發 ( true/false )</li>
<li><code>pageX / page Y</code> : 事件觸發時，滑鼠座標在網頁上的相對位置</li>
</ul>
<h3 id="取消預設行為-event-preventDefault"><a href="#取消預設行為-event-preventDefault" class="headerlink" title="取消預設行為 event.preventDefault()"></a>取消預設行為 <code>event.preventDefault()</code></h3><p>如果我們需要在 <code>&lt;a&gt;</code> 或 <code>&lt;submit&gt;</code> 等上綁定事件，就需要適當的取消他們的預設行為</p>
<p>如下範例，我們想要在點擊這個連結的時候，瀏覽器執行 <code>console.log(&#39;Google&#39;)</code>，而不是直接連結到 <code>Google</code> 首頁，就可以使用 <code>preventDefault()</code> 來取消 <code>&lt;a&gt;</code> 的預設行為</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">&quot;https://www.google.com&quot;</span> <span class="attr">id</span>=<span class="string">&quot;link&quot;</span>&gt;</span>Google<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> link = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;#link&#x27;</span>);</span><br><span class="line"></span><br><span class="line">link.addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;</span><br><span class="line">  e.preventDefault();   <span class="comment">// 取消預設行為</span></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;Google&#x27;</span>);</span><br><span class="line">&#125;, <span class="literal">false</span>);</span><br></pre></td></tr></table></figure>

<h3 id="阻擋事件冒泡傳遞-enent-stopPropagation"><a href="#阻擋事件冒泡傳遞-enent-stopPropagation" class="headerlink" title="阻擋事件冒泡傳遞 enent.stopPropagation"></a>阻擋事件冒泡傳遞 <code>enent.stopPropagation</code></h3><p>當 “子元素” 和 “父元素” 有 相同的事件 ，我們希望觸發子元素的事件時不要觸發到父元素的事件，就可以使用到 <code>event object</code> 提供的另一個方法 <code>event.stopPropagation</code> 來阻擋事件向上冒泡</p>
<p>如下範例，有時候為了增加 <code>checkbox</code> 的靈活性，會使用 <code>label</code> 包住的寫法，當我們在 label 和 <code>checkbox</code> 上都註冊 <code>click</code> 事件時，會發現 <code>label</code> 上的事件執行了兩次</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">label</span> <span class="attr">id</span>=<span class="string">&quot;label&quot;</span>&gt;</span></span><br><span class="line">  Label <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;checkbox&quot;</span> <span class="attr">id</span>=<span class="string">&quot;checkbox&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> label = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;label&#x27;</span>);</span><br><span class="line"><span class="keyword">var</span> checkobx = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;checkbox&#x27;</span>);</span><br><span class="line"></span><br><span class="line">lable.addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;label click&#x27;</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">checkbox.addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;checkbox click&#x27;</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// in console</span></span><br><span class="line"><span class="string">&#x27;label click&#x27;</span></span><br><span class="line"><span class="string">&#x27;checkbox click&#x27;</span></span><br><span class="line"><span class="string">&#x27;label click&#x27;</span></span><br></pre></td></tr></table></figure>

<p>如上範例，<code>label</code> 會執行兩次是因為當我們點擊 <code>label</code> 時，瀏覽器會自動將 <code>click</code> 事件傳給 <code>checkbox</code>，而 <code>checkbox</code> 受到事件冒泡影響，又會將 <code>click</code> 傳給上層的 <code>label</code>，所以執行了兩次</p>
<p>要解決這個問題，只要在 <code>checkbox</code> 加上 <code>e.stopPropagation()</code>，就可以阻擋 <code>checkbox</code> 向上傳遞的事件冒泡</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> label = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;label&#x27;</span>);</span><br><span class="line"><span class="keyword">var</span> checkobx = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;checkbox&#x27;</span>);</span><br><span class="line">            </span><br><span class="line">lable.addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;label click&#x27;</span>);</span><br><span class="line">&#125;);</span><br><span class="line">            </span><br><span class="line">checkbox.addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;</span><br><span class="line">  e.stopPropagation();</span><br><span class="line">&#125;);</span><br><span class="line">            </span><br><span class="line"><span class="comment">// in console</span></span><br><span class="line"><span class="string">&#x27;label click&#x27;</span></span><br></pre></td></tr></table></figure>

<h3 id="事件中的自己-this-與-e-target"><a href="#事件中的自己-this-與-e-target" class="headerlink" title="事件中的自己 this 與 e.target"></a>事件中的自己 <code>this</code> 與 <code>e.target</code></h3><ul>
<li><code>this</code> : 透過 <code>this</code> 可以找到 觸發事件的目標 元素，也就是 <code>event.currentTarget</code></li>
<li><code>e.target</code> : 透過 <code>e.target</code> 可以找到 觸發事件的元素</li>
<li>大部分的情況 <code>this</code> 會和 <code>e.target</code> 指向同一個元素</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">label</span> <span class="attr">id</span>=<span class="string">&quot;label&quot;</span>&gt;</span></span><br><span class="line">  Label <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;checkbox&quot;</span> <span class="attr">id</span>=<span class="string">&quot;checkbox&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> label = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;label&#x27;</span>);</span><br><span class="line"></span><br><span class="line">label.addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span>(<span class="params">e</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(e.target.tagName);   <span class="comment">// &#x27;LABEL&#x27;</span></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>.tagName);       <span class="comment">// &#x27;LABEL&#x27;</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<ul>
<li>但如果是 “傳遞” 的情況兩者指向的元素就會有所不同</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> label = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;label&#x27;</span>);</span><br><span class="line"><span class="keyword">var</span> checkbox = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;checkbox&#x27;</span>);</span><br><span class="line"></span><br><span class="line">label.addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(e.target.tagName, <span class="number">1</span>);</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>.tagName, <span class="number">1</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">checkbox.addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(e.target.tagName, <span class="number">2</span>);</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">this</span>.tagName, <span class="number">2</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// in console</span></span><br><span class="line"><span class="string">&quot;LABEL 1&quot;</span></span><br><span class="line"><span class="string">&quot;LABEL 1&quot;</span></span><br><span class="line"><span class="string">&quot;INPUT 2&quot;</span></span><br><span class="line"><span class="string">&quot;INPUT 2&quot;</span></span><br><span class="line"><span class="string">&quot;INPUT 1&quot;</span></span><br><span class="line"><span class="string">&quot;LABEL 1&quot;</span></span><br></pre></td></tr></table></figure>

<p>前兩組結果可以輕易理解為 <code>label</code> 和 <code>checkbox</code> 各觸發一次 <code>click</code> 事件</p>
<p>但最後一組結果就不一樣了，由 <code>1</code> 可以判斷這組結果是 <code>checkbox</code> 經由 “事件冒泡” 傳遞給 <code>label</code> 所觸發的事件</p>
<p>這裡的 <code>e.target</code> 指的是 觸發事件的元素，也就是觸發事件冒泡的 <code>checkbox</code></p>
<p>而 <code>this</code> 指的是 觸發事件的目標元素，也就是事件冒泡 的目標 <code>label</code></p>
<h3 id="事件指派-Event-Delegation"><a href="#事件指派-Event-Delegation" class="headerlink" title="事件指派 Event Delegation"></a>事件指派 <code>Event Delegation</code></h3><p>事件指派 指的是透過前面介紹的 “事件流程” 和 “單一事件監聽器” 來處理多個事件目標</p>
<p>如下範例，我們為 <code>myList</code> 的 <code>li</code> 綁定 <code>click</code> 事件，就要透過 <code>for</code> 迴圈來一一綁定</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">&quot;myList&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 01<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 02<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">li</span>&gt;</span>Item 03<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 取得容器</span></span><br><span class="line"><span class="keyword">var</span> myList = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;myList&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 分別為 li 綁定事件</span></span><br><span class="line"><span class="keyword">if</span> ( myList.hasChildNodes() )&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>, i &lt; myList.childNodes.length, i++ )&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//nodeType === 1 代表為實體 HTML 元素</span></span><br><span class="line">    <span class="keyword">if</span> ( myList.childNodes[i].nodeType === <span class="number">1</span> )&#123;</span><br><span class="line">      myList.childNodes[i].addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="built_in">this</span>.textContent);</span><br><span class="line">      &#125;, <span class="literal">false</span>);</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 建立新的 li 元素</span></span><br><span class="line"><span class="keyword">var</span> newList = <span class="built_in">document</span>.createElement(<span class="string">&#x27;li&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 建立文字節點</span></span><br><span class="line"><span class="keyword">var</span> textNode = <span class="built_in">document</span>.createTextNode(<span class="string">&quot;Hello Wordl&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 將 文字結點 加入新增的 li 元素</span></span><br><span class="line">newList.appendChild(textNode);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 將新增的 li 元素加入 myList</span></span><br><span class="line">myList.appendChild(newList);</span><br></pre></td></tr></table></figure>

<p>這時會發生一個問題，新增的 <code>li</code> 並不會有 <code>click</code> 事件的註冊</p>
<p>如果每次新增新的 <code>li</code> 都要新增新的事件監聽 <code>addEventListener</code>，不僅沒完沒了，沒有移除舊的事件監聽可能會發生錯誤</p>
<p>因此透過 事件指派 <code>Event Delegation</code> 是比較好的做法</p>
<p>如下範例，我們是將 <code>click</code> 事件改由外層的 <code>myList</code> 來監聽，利用事件傳遞的原理來取得目標結點，所以後續新增的 <code>li</code> 也會有 <code>click</code> 事件的效果</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 取得容器</span></span><br><span class="line"><span class="keyword">var</span> myList = <span class="built_in">document</span>.getElementById(<span class="string">&#x27;myList&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 改由外層 myList 來監聽 click 事件</span></span><br><span class="line">myList.addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (e.target.tagName.toLowerCase() === <span class="string">&#x27;li&#x27;</span>)&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(e.target.textContent);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">/ 建立新的 li 元素</span><br><span class="line"><span class="keyword">var</span> newList = <span class="built_in">document</span>.createElement(<span class="string">&#x27;li&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 建立文字節點</span></span><br><span class="line"><span class="keyword">var</span> textNode = <span class="built_in">document</span>.createTextNode(<span class="string">&quot;Hello Wordl&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 將 文字結點 加入新增的 li 元素</span></span><br><span class="line">newList.appendChild(textNode);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 將新增的 li 元素加入 myList</span></span><br><span class="line">myList.appendChild(newList);</span><br></pre></td></tr></table></figure>

<h2 id="事件的種類"><a href="#事件的種類" class="headerlink" title="事件的種類"></a>事件的種類</h2><p>事件的種類分為以下幾種</p>
<ul>
<li>介面相關事件</li>
<li>滑鼠相關事件</li>
<li>鍵盤相關事件</li>
<li>表單相關事件</li>
<li>特殊事件</li>
<li>自訂事件</li>
</ul>
<h3 id="介面相關事件"><a href="#介面相關事件" class="headerlink" title="介面相關事件"></a>介面相關事件</h3><p>介面事件不一定與使用者對 <code>DOM</code> 的操作有關，反而大多數與 <code>window</code> 物件比較相關</p>
<h4 id="load"><a href="#load" class="headerlink" title="load"></a>load</h4><p>註冊在 <code>window</code> 物件上，指的是網頁資源 (包括 CSS、JS、圖片等)，全數載入後觸發。</p>
<p>如果是 <code>img</code> 元素的 <code>load</code> 事件，就代表是在圖片載入後觸發</p>
<h4 id="unload-與-beforeunload"><a href="#unload-與-beforeunload" class="headerlink" title="unload 與 beforeunload"></a>unload 與 beforeunload</h4><p><code>unload</code> 和 <code>beforeunload</code> 分別會在離開頁面或重新整理時觸發，<code>beforeunload</code> 會跳出對話框詢問使用者是否確定要離開</p>
<p><img src="beforeunload.png"></p>
<h4 id="error"><a href="#error" class="headerlink" title="error"></a>error</h4><p>error 事件會在 document 或圖片載入錯誤時觸發。</p>
<p>基於維護性考量，大部分的 JS 建議都使用 非侵入式 JS 的寫法，也就是另外寫 <code>&lt;script&gt;</code>，但 <code>error</code> 反而很適合以 <code>on-event handler</code> 的寫法來處理</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">&quot;image.jpg&quot;</span> <span class="attr">onerror</span>=<span class="string">&quot;this.src=&#x27;default.jpg&#x27;&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>如上範例，當 image.jpg 不存在的時候，就會觸發 error 事件，此時會透過 this.src 將 src 屬性替換成 default.jpg</p>
<p>如果是在 <code>load</code> 完成後才註冊 <code>error</code> 事件，就只會看到破圖的結果，因為 <code>error</code> 事件不會再度被觸發，所以後來掛上的 <code>handler</code> 形同虛設</p>
<h4 id="resize"><a href="#resize" class="headerlink" title="resize"></a>resize</h4><p>當瀏覽器 <code>window</code> 或指定元素 <code>element</code> 的 “尺寸變更” 時觸發</p>
<h4 id="scroll"><a href="#scroll" class="headerlink" title="scroll"></a>scroll</h4><p>當瀏覽器 <code>window</code> 或指定元素 <code>element</code> 的 “卷軸被拉動” 時觸發</p>
<h4 id="DOMContentLoaded"><a href="#DOMContentLoaded" class="headerlink" title="DOMContentLoaded"></a>DOMContentLoaded</h4><p>和 <code>load</code> 事件類似，不同的是，<code>load</code> 是在網頁的所有資源載入後才會觸發，而 <code>DOMContentLoaded</code> 是在 <code>DOM</code> 結構被完整的讀取和解析後就會被觸發</p>
<p>一般來說如果將 <code>&lt;script&gt;</code> 放在 <code>&lt;head&gt;</code> 之間，會發生讀取不到 DOM 的問題，但如果使用 <code>DOMContentLoaded</code>，就可以排除這個問題了</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">    <span class="built_in">document</span>.addEventListener(<span class="string">&quot;DOMContentLoaded&quot;</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span></span><br><span class="line"><span class="javascript">      <span class="comment">// 當 document 結構已解析完成才會執行</span></span></span><br><span class="line"><span class="javascript">      <span class="built_in">document</span>.getElementById(<span class="string">&#x27;hello&#x27;</span>).textContent = <span class="string">&#x27;Hello&#x27;</span>;</span></span><br><span class="line"><span class="javascript">    &#125;, <span class="literal">false</span>);</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="滑鼠相關事件"><a href="#滑鼠相關事件" class="headerlink" title="滑鼠相關事件"></a>滑鼠相關事件</h3><h4 id="click"><a href="#click" class="headerlink" title="click"></a>click</h4><p>當滑鼠點擊某元素時觸發</p>
<h4 id="dblclick"><a href="#dblclick" class="headerlink" title="dblclick"></a>dblclick</h4><p>當滑鼠 “連點兩次” 時觸發</p>
<h4 id="mouseenter-mousemove-mouseleave-不支援事件冒泡"><a href="#mouseenter-mousemove-mouseleave-不支援事件冒泡" class="headerlink" title="mouseenter/mousemove/mouseleave 不支援事件冒泡"></a>mouseenter/mousemove/mouseleave 不支援事件冒泡</h4><p>這三個事件並 “不支援事件冒泡”，也就是無法藉由綁定父元素來觸發子元素的事件</p>
<ol>
<li><code>mouseenter</code> : 當滑鼠 “移入” 這個元素時觸發</li>
<li><code>mousemove</code>: 當滑鼠在這個元素內 “移動” 時觸發</li>
<li><code>mouseleave</code>: 當滑鼠 “移出” 這個元素時觸發</li>
</ol>
<p><a href="https://codepen.io/Jerry-Yeh/pen/zVbKBK?editors=1010">範例程式碼 (mousemove)</a><br><a href="https://codepen.io/Jerry-Yeh/pen/zVbWaO">範例程式碼 (網頁座標)</a></p>
<h4 id="mouseover-mouseout-支援事件冒泡"><a href="#mouseover-mouseout-支援事件冒泡" class="headerlink" title="mouseover/mouseout 支援事件冒泡"></a>mouseover/mouseout 支援事件冒泡</h4><p>和 <code>mouseover/mouseout</code> 對應的就是 <code>mouseenter/mouseleave</code>，但不同的地方在於 <code>mouseover/mouseout</code> 支援冒泡事件，也就是可以藉由綁定父元素來觸發子元素事件</p>
<h3 id="鍵盤相關事件"><a href="#鍵盤相關事件" class="headerlink" title="鍵盤相關事件"></a>鍵盤相關事件</h3><p>鍵盤事件分成三種，大多數情況會註冊在 <code>input</code> 上</p>
<h4 id="keydown"><a href="#keydown" class="headerlink" title="keydown"></a>keydown</h4><p>當 “壓下” 按鍵時觸發</p>
<h4 id="keypress"><a href="#keypress" class="headerlink" title="keypress"></a>keypress</h4><p>當按著不放時會連續觸發</p>
<h4 id="keyup"><a href="#keyup" class="headerlink" title="keyup"></a>keyup</h4><p>當 “放開” 按鍵時觸發</p>
<p>如果同一個元素綁定了這三個鍵盤事件，觸發順序為 <code>keydown=&gt;keypress=&gt;keyup</code></p>
<h4 id="keycode"><a href="#keycode" class="headerlink" title="keycode"></a>keycode</h4><p>每個按鍵都有自己的代號，透過 keycode 可以知道每個按鍵的代號，進而指定按鍵來觸發</p>
<p><a href="https://keycode.info/">keycode 對照</a><br><a href="https://codepen.io/Jerry-Yeh/pen/LKaxxp">範例程式碼</a></p>
<h3 id="表單相關事件"><a href="#表單相關事件" class="headerlink" title="表單相關事件"></a>表單相關事件</h3><h4 id="input"><a href="#input" class="headerlink" title="input"></a>input</h4><p>當 input、textarea 以及帶有 contenteditable 的元素內容被改變時，就會觸發 input 事件</p>
<h4 id="change"><a href="#change" class="headerlink" title="change"></a>change</h4><p>當 input、select、textarea、radio、checkbox 等被改變時觸發。與 input 不同的是， input 事件會在輸入框輸入內容的當下觸發，而 <code>change</code> 事件則是在目前焦點離開輸入框後才觸發</p>
<p><a href="https://codepen.io/Jerry-Yeh/pen/QXoqPq?editors=1010">範例程式碼</a></p>
<h4 id="submit"><a href="#submit" class="headerlink" title="submit"></a>submit</h4><p>當表單被送出時觸發，通常表單驗證都會在這一步處理，驗證未過則 <code>return false</code></p>
<h4 id="focus"><a href="#focus" class="headerlink" title="focus"></a>focus</h4><p>元素被聚焦時觸發</p>
<h4 id="blur"><a href="#blur" class="headerlink" title="blur"></a>blur</h4><p>當元素失去焦點時觸發</p>
<h3 id="特殊事件"><a href="#特殊事件" class="headerlink" title="特殊事件"></a>特殊事件</h3><h4 id="Composition-Event-組成事件"><a href="#Composition-Event-組成事件" class="headerlink" title="Composition Event 組成事件"></a>Composition Event 組成事件</h4><p>透過 組成事件 Composition Event，可以將輸入的過程分的更細，針對每個環節綁定不同的事件。組成事件分為以下三種:</p>
<ol>
<li><code>compositionstart</code></li>
</ol>
<p>  輸入框內開啟輸入法，且 正在拼音時 觸發</p>
<ol start="2">
<li><code>compositionupdate</code></li>
</ol>
<p>  輸入框內開啟輸入法，且 正在拼音時 或 選字時 更改了內容觸發</p>
<ol start="3">
<li><code>compositionend</code></li>
</ol>
<p>  輸入框內開啟輸入法，且拼字獲選字 完成 時觸發</p>
<p><a href="https://codepen.io/Jerry-Yeh/pen/EBMEYM?editors=0010">範例程式碼</a></p>
<h3 id="自訂事件"><a href="#自訂事件" class="headerlink" title="自訂事件"></a>自訂事件</h3><ul>
<li>使用 <code>Event constructor</code> 建立</li>
<li>同樣透過 <code>addEventListener</code> 來監聽</li>
<li>由 <code>dispatchEvent</code> 決定事件觸發的時機</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 自訂事件</span></span><br><span class="line"><span class="keyword">var</span> event = <span class="keyword">new</span> Event(<span class="string">&#x27;build&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 監聽事件</span></span><br><span class="line">element.addEventListener(<span class="string">&#x27;build&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;...&#125;, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 觸發事件</span></span><br><span class="line">element.dispatchEvent(event);</span><br></pre></td></tr></table></figure>

<ul>
<li>如果要在自訂事件內增加更多資料，可以改用 CustomEvent 來自訂事件，Event Handler 就可以透過 event 來接收</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 自訂事件</span></span><br><span class="line"><span class="keyword">var</span> event = <span class="keyword">new</span> CustomEvent(<span class="string">&#x27;build&#x27;</span>, &#123;<span class="string">&#x27;detail&#x27;</span>: element.dataset.time&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 事件監聽</span></span><br><span class="line">element.addEventListener(<span class="string">&#x27;build&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">e</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">&#x27;The time is&#x27;</span> + e.detail);</span><br><span class="line">&#125;, <span class="literal">false</span>);</span><br></pre></td></tr></table></figure>

<h3 id="其他事件"><a href="#其他事件" class="headerlink" title="其他事件"></a>其他事件</h3><p>其他的事件可以參考 <code>MDN</code> 的 <a href="https://developer.mozilla.org/en-US/docs/Web/Events">Event reference</a></p>
<h2 id="資料參考"><a href="#資料參考" class="headerlink" title="資料參考"></a>資料參考</h2><p><a href="https://ithelp.ithome.com.tw/users/20065504/ironman/1259">重新認識 JavaScript</a><br><a href="https://ithelp.ithome.com.tw/users/20083608/ironman/1354">邁向 JavaScript 勇者之路</a><br><a href="https://www.udemy.com/javascript-learning/">JavaScript 入門篇 - 學徒的試煉</a></p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
</search>
